# Milestone v1.8: Combined Metrics

**Status:** ✅ SHIPPED 2026-03-15
**Phases:** 37-40
**Total Plans:** 4

## Overview

Poll groups can declare an aggregate computation (sum, subtract, absDiff, mean) over their individual SNMP GET responses. The collector computes the result and dispatches it as a named synthetic gauge through the full MediatR pipeline, appearing in Prometheus with `source="synthetic"` alongside the individual per-OID metrics.

## Phases

### Phase 37: Config and Runtime Models

**Goal**: Data types for aggregate metrics exist with backward-compatible defaults
**Plans**: 1 plan

Plans:
- [x] 37-01: AggregationKind enum, AggregatedMetricDefinition record, PollOptions + MetricPollInfo extensions + 13 unit tests

### Phase 38: DeviceWatcherService Validation

**Goal**: Combined metric definitions validated at load time in BuildPollGroups
**Plans**: 1 plan

Plans:
- [x] 38-01: BuildPollGroups 5-rule validation (co-presence, Aggregator parse, min 2 OIDs, dup name, OID map collision) + 10 unit tests

### Phase 39: Pipeline Bypass Guards

**Goal**: MediatR pipeline safely passes synthetic messages without corrupting pre-set MetricName
**Plans**: 1 plan

Plans:
- [x] 39-01: SnmpSource.Synthetic + OidResolution bypass + sentinel "0.0" + 4 tests

### Phase 40: MetricPollJob Aggregate Dispatch

**Goal**: MetricPollJob computes aggregate and dispatches as synthetic gauge
**Plans**: 1 plan

Plans:
- [x] 40-01: DispatchAggregatedMetricAsync — sum/subtract/absDiff/mean, all-or-nothing guard, snmp.aggregated.computed counter, exception isolation + 10 tests

---

## Milestone Summary

**Key Decisions:**
- AggregatedMetricName + Aggregator naming pair (lowercase in JSON: sum/subtract/absDiff/mean)
- AggregatedMetricDefinition sealed record with MetricName, Kind, SourceOids
- MetricPollInfo.AggregatedMetrics as init-only property (backward compatible)
- OidResolution bypass: Option B (Source == SnmpSource.Synthetic)
- Sentinel OID: "0.0" (passes existing ValidationBehavior regex)
- TypeCode: Subtract/AbsDiff → Integer32, Sum/Mean → Gauge32
- OID map name collision = Error + skip (real metric priority)
- Minimum 2 resolved OIDs for aggregate (not raw MetricNames count)
- All-or-nothing computation (all numeric, all responded)
- Exception isolation (no RecordFailure, no snmp_poll_unreachable_total)

**Issues Resolved:**
- None — clean milestone with no blockers

**Issues Deferred:**
- Ratio aggregation — scale factor design not settled
- Pipeline counter zero-initialization at startup

**Technical Debt Incurred:**
- Phase 37/38 SUMMARY files reference old "CombinedMetricDefinition" name (doc only, no runtime impact)

---

*For current project status, see .planning/ROADMAP.md*
