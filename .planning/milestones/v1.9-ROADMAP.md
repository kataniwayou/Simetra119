# Milestone v1.9: Metric Threshold Structure & Validation

**Status:** ✅ SHIPPED 2026-03-15
**Phases:** 41-42
**Total Plans:** 3

## Overview

Tenant metric entries can carry an optional Threshold object with Min/Max bounds that is stored on MetricSlotHolder at load time and validated by the watcher — structural foundation for future runtime evaluation.

## Phases

### Phase 41: Threshold Model & Holder Storage

**Goal**: ThresholdOptions exists, is deserializable, and stored on MetricSlotHolder
**Plans**: 1 plan

Plans:
- [x] 41-01: ThresholdOptions sealed class + MetricSlotOptions.Threshold + MetricSlotHolder storage + 3 tests

### Phase 42: Threshold Validation & Config Files

**Goal**: Invalid thresholds caught at load time, valid thresholds pass through, examples in configs
**Plans**: 2 plans

Plans:
- [x] 42-01: Min > Max validation in ValidateAndBuildTenants + 3 tests
- [x] 42-02: Example thresholds in all 3 tenant config files

## Quick Tasks

- Quick 058: GraceMultiplier on PollOptions + device-resolved IntervalSeconds/GraceMultiplier in ValidateAndBuildTenants

---

## Milestone Summary

**Key Decisions:**
- ThresholdOptions sealed class with Min (double?) and Max (double?)
- Both null = always-violated semantics (threshold gate always returns true)
- Min > Max = Error log, skip threshold (metric still loads)
- GraceMultiplier on PollOptions (default 2.0), resolved from device poll group
- IntervalSeconds resolved from device poll group (not operator-set on tenants)
- Runtime threshold evaluation deferred to future milestone

**Issues Resolved:** None — clean milestone

**Issues Deferred:**
- Runtime threshold evaluation (staleness detection)
- Heartbeat refactor (remove hardcoded tenant, use pipeline arrival staleness)

**Technical Debt Incurred:** None

---

*For current project status, see .planning/ROADMAP.md*
