---
phase: 31-human-name-device-config
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/SnmpCollector/Services/OidMapWatcherService.cs
  - src/SnmpCollector/Pipeline/OidMapService.cs
  - src/SnmpCollector/Program.cs
  - src/SnmpCollector/config/oidmaps.json
  - deploy/k8s/snmp-collector/simetra-oidmaps.yaml
  - tests/SnmpCollector.Tests/Services/OidMapWatcherValidationTests.cs
  - tests/e2e/fixtures/oid-added-configmap.yaml
  - tests/e2e/fixtures/oid-removed-configmap.yaml
  - tests/e2e/fixtures/oid-renamed-configmap.yaml
  - tests/e2e/fixtures/invalid-json-oidmaps-schema-configmap.yaml
  - src/SnmpCollector/Configuration/MetricPollOptions.cs
  - src/SnmpCollector/Configuration/DeviceOptions.cs
  - src/SnmpCollector/Configuration/Validators/DevicesOptionsValidator.cs
  - src/SnmpCollector/Extensions/ServiceCollectionExtensions.cs
  - src/SnmpCollector/Pipeline/MetricPollInfo.cs
  - src/SnmpCollector/appsettings.Development.json
  - tests/SnmpCollector.Tests/Pipeline/DeviceRegistryTests.cs
  - tests/SnmpCollector.Tests/Pipeline/PipelineIntegrationTests.cs
autonomous: true

must_haves:
  truths:
    - "OidMapWatcherService parses the new array-of-objects oidmaps.json format with EnumerateArray"
    - "3-pass duplicate detection (OID keys, metric names, clean build) works identically on new array format"
    - "Local dev Program.cs loads oidmaps.json in new array format via ValidateAndParseOidMap"
    - "K8s simetra-oidmaps.yaml has all 98+7 entries in array format including 6 previously missing entries"
    - "C# model rename: MetricPollOptions->PollOptions, Oids->MetricNames, MetricPolls->Polls throughout codebase"
    - "All validation tests pass with array-of-objects JSON strings"
    - "Solution compiles with zero errors after all renames"
  artifacts:
    - path: "src/SnmpCollector/Services/OidMapWatcherService.cs"
      provides: "ValidateAndParseOidMap with EnumerateArray + GetProperty(Oid/MetricName)"
      contains: "EnumerateArray"
    - path: "src/SnmpCollector/Configuration/MetricPollOptions.cs"
      provides: "Renamed to PollOptions with MetricNames property"
      contains: "class PollOptions"
    - path: "src/SnmpCollector/Configuration/DeviceOptions.cs"
      provides: "Polls property of type List<PollOptions>"
      contains: "List<PollOptions> Polls"
    - path: "src/SnmpCollector/config/oidmaps.json"
      provides: "Array-of-objects format with Oid and MetricName fields"
      contains: "MetricName"
    - path: "deploy/k8s/snmp-collector/simetra-oidmaps.yaml"
      provides: "K8s ConfigMap in array format with all 105 entries (98+7)"
      contains: "MetricName"
  key_links:
    - from: "src/SnmpCollector/Program.cs"
      to: "OidMapWatcherService.ValidateAndParseOidMap"
      via: "static method call for local dev oidmap loading"
      pattern: "ValidateAndParseOidMap"
    - from: "src/SnmpCollector/Configuration/DeviceOptions.cs"
      to: "src/SnmpCollector/Configuration/MetricPollOptions.cs"
      via: "List<PollOptions> Polls property"
      pattern: "List<PollOptions>"
---

<objective>
Restructure oidmaps.json from flat dictionary to array-of-objects format AND mechanically rename all C# models (MetricPollOptions->PollOptions, Oids->MetricNames, MetricPolls->Polls) throughout the codebase.

Purpose: The array format enables explicit Oid/MetricName fields (future extensibility) and the model rename future-proofs for Commands[] as a sibling field under Polls. Both changes are prerequisites for name resolution in Plan 02.

Output: Compiling codebase with new oidmap parsing, renamed models, updated config files, and passing validation tests.
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-human-name-device-config/31-CONTEXT.md
@.planning/phases/31-human-name-device-config/31-RESEARCH.md
@src/SnmpCollector/Services/OidMapWatcherService.cs
@src/SnmpCollector/Configuration/MetricPollOptions.cs
@src/SnmpCollector/Configuration/DeviceOptions.cs
@src/SnmpCollector/Configuration/Validators/DevicesOptionsValidator.cs
@src/SnmpCollector/Extensions/ServiceCollectionExtensions.cs
@src/SnmpCollector/Pipeline/DeviceRegistry.cs
@src/SnmpCollector/Program.cs
@tests/SnmpCollector.Tests/Services/OidMapWatcherValidationTests.cs
@tests/SnmpCollector.Tests/Pipeline/DeviceRegistryTests.cs
@tests/SnmpCollector.Tests/Pipeline/PipelineIntegrationTests.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure oidmaps.json parsing and config files to array-of-objects format</name>
  <files>
    src/SnmpCollector/Services/OidMapWatcherService.cs
    src/SnmpCollector/Program.cs
    src/SnmpCollector/config/oidmaps.json
    deploy/k8s/snmp-collector/simetra-oidmaps.yaml
    tests/SnmpCollector.Tests/Services/OidMapWatcherValidationTests.cs
    tests/e2e/fixtures/oid-added-configmap.yaml
    tests/e2e/fixtures/oid-removed-configmap.yaml
    tests/e2e/fixtures/oid-renamed-configmap.yaml
    tests/e2e/fixtures/invalid-json-oidmaps-schema-configmap.yaml
  </files>
  <action>
    **OidMapWatcherService.ValidateAndParseOidMap** -- rewrite parsing from EnumerateObject to EnumerateArray:
    1. After JsonDocument.Parse succeeds, check `doc.RootElement.ValueKind != JsonValueKind.Array` -- if not array, log error "OidMap JSON must be an array of objects -- skipping reload" and return null.
    2. Replace `foreach (var property in doc.RootElement.EnumerateObject())` with `foreach (var element in doc.RootElement.EnumerateArray())`.
    3. For each element: call `element.TryGetProperty("Oid", out var oidProp)` and `element.TryGetProperty("MetricName", out var nameProp)`. If either is missing, log warning "OidMap array element missing Oid or MetricName -- skipping entry" and continue.
    4. Extract: `var oid = oidProp.GetString();` and `var name = nameProp.ValueKind == JsonValueKind.Null ? null : nameProp.GetString();`.
    5. Keep all existing null/empty checks and 3-pass duplicate logic (OID dedup, name dedup, clean build) exactly as-is -- only the source of (oid, name) pairs changes.
    6. Keep existing JsonDocumentOptions (CommentHandling = JsonCommentHandling.Skip, AllowTrailingCommas = true).

    **Program.cs** -- replace local dev oidmap loading:
    - Replace `JsonSerializer.Deserialize<Dictionary<string, string>>(oidJson, jsonOptions)` with `OidMapWatcherService.ValidateAndParseOidMap(oidJson, logger)` where `logger` is the app's logger (resolve from `app.Services`). Import `SnmpCollector.Services` namespace. The method is `internal static` and Program.cs is in the same assembly.

    **config/oidmaps.json** -- rewrite from flat dict to array of objects:
    - Convert every `"OID": "name"` pair to `{ "Oid": "OID", "MetricName": "name" }`.
    - Keep section comments (e.g., `// ---- OBP OIDs ----`) between array elements -- JsonCommentHandling.Skip handles this.
    - Result: 98 entries (27 OBP + 71 NPB) in array format.

    **deploy/k8s/snmp-collector/simetra-oidmaps.yaml** -- rewrite embedded JSON:
    - Convert from flat dict to array of objects (same as local file).
    - ADD the 6 missing entries that exist in local but not K8s:
      - `{ "Oid": "1.3.6.1.4.1.47477.10.21.60.1.0", "MetricName": "obp_device_type" }`
      - `{ "Oid": "1.3.6.1.4.1.47477.10.21.60.13.0", "MetricName": "obp_sw_version" }`
      - `{ "Oid": "1.3.6.1.4.1.47477.10.21.60.15.0", "MetricName": "obp_serial" }`
      - `{ "Oid": "1.3.6.1.4.1.47477.100.1.5.0", "MetricName": "npb_model" }`
      - `{ "Oid": "1.3.6.1.4.1.47477.100.1.6.0", "MetricName": "npb_serial" }`
      - `{ "Oid": "1.3.6.1.4.1.47477.100.1.7.0", "MetricName": "npb_sw_version" }`
    - Keep the 7 E2E sim entries (e2e_gauge_test through e2e_ip_test) in array format.
    - Total: 105 entries (98 + 7 E2E).

    **OidMapWatcherValidationTests.cs** -- rewrite all 9 test JSON strings:
    - Every test currently uses flat dict JSON `{ "OID": "name" }`. Change to array format `[{ "Oid": "OID", "MetricName": "name" }]`.
    - The assertions and test behavior stay the same -- only the JSON input format changes.
    - For duplicate OID tests: use array with duplicate Oid values.
    - For duplicate name tests: use array with duplicate MetricName values.
    - For null/empty tests: use array elements with null or empty MetricName.

    **E2E oidmap fixtures** -- rewrite 3 files from flat dict to array format:
    - `oid-added-configmap.yaml`: Convert all entries to array format. The added entry (`1.3.6.1.4.1.47477.999.2.1.0` -> `e2e_unmapped_gauge`) becomes `{ "Oid": "1.3.6.1.4.1.47477.999.2.1.0", "MetricName": "e2e_unmapped_gauge" }`.
    - `oid-removed-configmap.yaml`: Same conversion, minus the removed entry.
    - `oid-renamed-configmap.yaml`: Same conversion, with the renamed metric name.
    - Add the 6 missing entries (obp_device_type, obp_sw_version, obp_serial, npb_model, npb_serial, npb_sw_version) to all 3 fixtures to match the updated simetra-oidmaps.yaml baseline.

    **invalid-json-oidmaps-schema-configmap.yaml** -- update the "wrong schema" test:
    - Current content: `["this", "is", "valid", "json", "but", "wrong", "schema"]` -- this is an array of strings, not objects.
    - This still works as a wrong-schema test because the elements lack "Oid"/"MetricName" properties. The new parsing will log "OidMap array element missing Oid or MetricName -- skipping entry" for each element and return an empty map, then log "OidMap validation produced empty map". Keep this fixture as-is OR change to `{ "not": "an array" }` to test the new ValueKind.Array check. Choose whichever is more useful. RECOMMENDED: change to `{ "not": "an array" }` to specifically test the "OidMap JSON must be an array" error path, since the old fixture (string array) would only trigger per-element warnings.
  </action>
  <verify>
    1. `dotnet build src/SnmpCollector/SnmpCollector.sln` compiles with zero errors.
    2. `dotnet test tests/SnmpCollector.Tests/ --filter "FullyQualifiedName~OidMapWatcher"` -- all 9 validation tests pass.
    3. Grep: `grep -r "EnumerateObject" src/SnmpCollector/Services/OidMapWatcherService.cs` returns no matches.
    4. Grep: `grep -c "MetricName" src/SnmpCollector/config/oidmaps.json` returns 98.
    5. Grep: `grep -c "MetricName" deploy/k8s/snmp-collector/simetra-oidmaps.yaml` returns 105.
    6. Grep: `grep "Deserialize<Dictionary" src/SnmpCollector/Program.cs` returns no matches (replaced by ValidateAndParseOidMap).
  </verify>
  <done>
    OidMapWatcherService.ValidateAndParseOidMap uses EnumerateArray with Oid/MetricName property access. All oidmaps config files (local + K8s + 3 E2E fixtures) use array-of-objects format. K8s oidmaps has all 105 entries. Program.cs reuses ValidateAndParseOidMap. All 9 validation tests pass with array JSON.
  </done>
</task>

<task type="auto">
  <name>Task 2: Mechanical C# model rename (MetricPollOptions->PollOptions, MetricPolls->Polls, Oids->MetricNames)</name>
  <files>
    src/SnmpCollector/Configuration/MetricPollOptions.cs
    src/SnmpCollector/Configuration/DeviceOptions.cs
    src/SnmpCollector/Configuration/Validators/DevicesOptionsValidator.cs
    src/SnmpCollector/Extensions/ServiceCollectionExtensions.cs
    src/SnmpCollector/Pipeline/DeviceRegistry.cs
    src/SnmpCollector/Pipeline/MetricPollInfo.cs
    src/SnmpCollector/appsettings.Development.json
    tests/SnmpCollector.Tests/Pipeline/DeviceRegistryTests.cs
    tests/SnmpCollector.Tests/Pipeline/PipelineIntegrationTests.cs
  </files>
  <action>
    Pure mechanical rename -- no behavioral changes. DeviceRegistry still builds MetricPollInfo.Oids from poll.MetricNames (treating MetricNames as raw strings for now -- name resolution is Plan 02).

    **MetricPollOptions.cs** -- rename file and class:
    - Rename class `MetricPollOptions` -> `PollOptions`.
    - Rename property `Oids` -> `MetricNames`.
    - Update XML doc: "OID strings to poll" -> "Metric names to poll in this group. Resolved to OIDs at device config load time via IOidMapService.ResolveToOid."
    - Rename the file itself to `PollOptions.cs`.

    **DeviceOptions.cs** -- rename property:
    - Change `public List<MetricPollOptions> MetricPolls` -> `public List<PollOptions> Polls`.
    - Update XML doc to reference "Poll configurations" instead of "Metric polling configurations".

    **DevicesOptionsValidator.cs** -- rename all references:
    - Method parameter type: `MetricPollOptions` -> `PollOptions`.
    - Method name: `ValidateMetricPoll` -> `ValidatePoll`.
    - Property access: `device.MetricPolls` -> `device.Polls`.
    - Error message prefix: `$"Devices[{deviceIndex}].MetricPolls[{pollIndex}]"` -> `$"Devices[{deviceIndex}].Polls[{pollIndex}]"`.
    - Validation: `poll.Oids.Count == 0` -> `poll.MetricNames.Count == 0`.
    - Error message: `"{prefix}.Oids must contain at least one entry"` -> `"{prefix}.MetricNames must contain at least one entry"`.

    **ServiceCollectionExtensions.cs** -- rename 4 occurrences:
    - Line ~438: `device.MetricPolls.Count` -> `device.Polls.Count` (in thread pool calculation).
    - Line ~485: `device.MetricPolls.Count` -> `device.Polls.Count` (in for loop).
    - Line ~487: `device.MetricPolls[pi]` -> `device.Polls[pi]`.
    - Search for any other `MetricPolls` references in this file.

    **DeviceRegistry.cs** -- rename property accesses (2 locations: constructor + ReloadAsync):
    - `d.MetricPolls` -> `d.Polls` (2 places).
    - `poll.Oids.AsReadOnly()` -> `poll.MetricNames.AsReadOnly()` (2 places).
    - NOTE: MetricPollInfo.Oids keeps its name -- it holds resolved OIDs at runtime. Only the config-side properties rename.
    - At this stage, MetricNames are passed directly to MetricPollInfo.Oids (name resolution comes in Plan 02).

    **MetricPollInfo.cs** -- XML doc comment only:
    - Update any reference to "MetricPolls" in doc comments.

    **appsettings.Development.json** -- rename JSON keys:
    - `"MetricPolls"` -> `"Polls"`.
    - `"Oids"` -> `"MetricNames"`.

    **DeviceRegistryTests.cs** -- rename all test references:
    - `MetricPolls =` -> `Polls =`.
    - `new MetricPollOptions` -> `new PollOptions`.
    - `Oids =` -> `MetricNames =`.
    - These are in test setup code only -- multiple occurrences across test methods.

    **PipelineIntegrationTests.cs** -- rename:
    - `MetricPolls = []` -> `Polls = []`.

    IMPORTANT: After all renames, run `grep -r "MetricPollOptions\|\.MetricPolls\b\|\.Oids\b" src/SnmpCollector/ --include="*.cs"` to confirm zero remaining references in source. Exclude `MetricPollInfo.Oids` (that keeps its name). Also grep tests/ for the same patterns.
  </action>
  <verify>
    1. `dotnet build src/SnmpCollector/SnmpCollector.sln` compiles with zero errors.
    2. `dotnet test tests/SnmpCollector.Tests/` -- all tests pass.
    3. Grep: `grep -r "MetricPollOptions" src/SnmpCollector/ --include="*.cs"` returns no matches.
    4. Grep: `grep -r "\.MetricPolls\b" src/SnmpCollector/ --include="*.cs"` returns no matches.
    5. File `src/SnmpCollector/Configuration/PollOptions.cs` exists (renamed from MetricPollOptions.cs).
    6. File `src/SnmpCollector/Configuration/MetricPollOptions.cs` does NOT exist (deleted).
  </verify>
  <done>
    All C# source files, test files, and appsettings use the new naming (PollOptions, Polls, MetricNames). MetricPollInfo.Oids retains its name (holds resolved OIDs). Full solution compiles and all tests pass.
  </done>
</task>

</tasks>

<verification>
1. `dotnet build src/SnmpCollector/SnmpCollector.sln` -- zero errors, zero warnings related to renamed types.
2. `dotnet test tests/SnmpCollector.Tests/` -- all tests pass (validation tests with new JSON, registry tests with renamed properties).
3. No remaining references to old names: `grep -rn "MetricPollOptions\|\"MetricPolls\"\|\"Oids\"" src/SnmpCollector/ --include="*.cs" --include="*.json"` returns zero matches (excluding MetricPollInfo.Oids which is intentional).
4. `config/oidmaps.json` parses as valid JSON array with 98 elements.
5. `simetra-oidmaps.yaml` embedded JSON parses as valid JSON array with 105 elements.
</verification>

<success_criteria>
- Codebase compiles cleanly with renamed models and array-format oidmap parsing
- All existing tests pass (validation tests rewritten for array format, registry/pipeline tests renamed)
- Local dev oidmap loading uses ValidateAndParseOidMap (single source of truth)
- K8s oidmaps ConfigMap includes all 6 previously-missing entries
- E2E oidmap fixtures (oid-added, oid-removed, oid-renamed, invalid-schema) updated to array format
- No remaining references to MetricPollOptions, MetricPolls (JSON key), or Oids (JSON key) in source
</success_criteria>

<output>
After completion, create `.planning/phases/31-human-name-device-config/31-01-SUMMARY.md`
</output>
