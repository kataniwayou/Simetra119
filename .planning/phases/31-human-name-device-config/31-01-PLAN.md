---
phase: 31-human-name-device-config
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/SnmpCollector/Configuration/MetricPollOptions.cs
  - src/SnmpCollector/Configuration/PollOptions.cs
  - src/SnmpCollector/Configuration/DeviceOptions.cs
  - src/SnmpCollector/Configuration/Validators/DevicesOptionsValidator.cs
  - src/SnmpCollector/Pipeline/MetricPollInfo.cs
  - src/SnmpCollector/Pipeline/PollInfo.cs
  - src/SnmpCollector/Pipeline/DeviceInfo.cs
  - src/SnmpCollector/Pipeline/DeviceRegistry.cs
  - src/SnmpCollector/Services/DeviceWatcherService.cs
  - src/SnmpCollector/Services/DynamicPollScheduler.cs
  - src/SnmpCollector/Jobs/MetricPollJob.cs
  - src/SnmpCollector/Extensions/ServiceCollectionExtensions.cs
  - src/SnmpCollector/Pipeline/TenantVectorRegistry.cs
  - src/SnmpCollector/Pipeline/CardinalityAuditService.cs
  - tests/SnmpCollector.Tests/Pipeline/DeviceRegistryTests.cs
  - tests/SnmpCollector.Tests/Jobs/MetricPollJobTests.cs
  - tests/SnmpCollector.Tests/Services/DynamicPollSchedulerTests.cs
  - tests/SnmpCollector.Tests/Pipeline/PipelineIntegrationTests.cs
  - tests/SnmpCollector.Tests/Pipeline/Behaviors/TenantVectorFanOutBehaviorTests.cs
  - deploy/k8s/snmp-collector/simetra-oidmaps.yaml
  - deploy/k8s/production/configmap.yaml
autonomous: true

must_haves:
  truths:
    - "All C# code compiles with zero errors after rename"
    - "MetricPollOptions class no longer exists -- replaced by PollOptions"
    - "MetricPollInfo class no longer exists -- replaced by PollInfo"
    - "K8s oidmap ConfigMaps contain all 6 previously missing entries (obp_device_type, obp_sw_version, obp_serial, npb_model, npb_serial, npb_sw_version)"
    - "All existing unit tests pass after rename (behavior unchanged)"
  artifacts:
    - path: "src/SnmpCollector/Configuration/PollOptions.cs"
      provides: "PollOptions class with Names property (List<string>)"
      contains: "public sealed class PollOptions"
    - path: "src/SnmpCollector/Pipeline/PollInfo.cs"
      provides: "PollInfo record with Oids parameter"
      contains: "public sealed record PollInfo"
    - path: "deploy/k8s/snmp-collector/simetra-oidmaps.yaml"
      provides: "Complete oidmap with all 98+ entries"
      contains: "obp_device_type"
  key_links:
    - from: "src/SnmpCollector/Configuration/DeviceOptions.cs"
      to: "PollOptions"
      via: "Polls property type"
      pattern: "List<PollOptions> Polls"
    - from: "src/SnmpCollector/Pipeline/DeviceRegistry.cs"
      to: "PollInfo"
      via: "poll group construction"
      pattern: "new PollInfo"
---

<objective>
Mechanical rename of C# model types and sync missing K8s oidmap entries as prerequisites for name resolution logic.

Purpose: Establish the new naming conventions (MetricPolls->Polls, Oids->Names, MetricPollOptions->PollOptions, MetricPollInfo->PollInfo) across all C# source and test files, and ensure all OID map entries exist in K8s ConfigMaps so name resolution can succeed for every metric.
Output: All C# files use new type names; PollOptions.Names holds metric name strings (still OIDs at runtime until Plan 02 adds resolution); K8s oidmaps are complete.
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-human-name-device-config/31-CONTEXT.md
@.planning/phases/31-human-name-device-config/31-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add missing OID entries to K8s oidmap ConfigMaps</name>
  <files>
    deploy/k8s/snmp-collector/simetra-oidmaps.yaml
    deploy/k8s/production/configmap.yaml
  </files>
  <action>
Add 6 missing OID-to-metric-name entries to BOTH K8s oidmap ConfigMaps. These entries exist in local config/oidmaps.json but are missing from K8s manifests:

```
"1.3.6.1.4.1.47477.10.21.60.1.0": "obp_device_type",
"1.3.6.1.4.1.47477.10.21.60.13.0": "obp_sw_version",
"1.3.6.1.4.1.47477.10.21.60.15.0": "obp_serial",
"1.3.6.1.4.1.47477.100.1.5.0": "npb_model",
"1.3.6.1.4.1.47477.100.1.6.0": "npb_serial",
"1.3.6.1.4.1.47477.100.1.7.0": "npb_sw_version"
```

For `simetra-oidmaps.yaml`: Add the 3 OBP 60s entries after the existing OBP entries (after obp_r4_power_L4), and add the 3 NPB 60s entries after npb_uptime (before the npb_port_* entries). Maintain the same formatting and indentation as existing entries.

For `production/configmap.yaml`: Find the oidmaps.json section within the multi-ConfigMap manifest and add the same 6 entries in the same positions.

Do NOT modify any other entries. Do NOT reorder existing entries.
  </action>
  <verify>
Grep for "obp_device_type" in both files to confirm presence. Count total entries in each oidmap to verify 6 more than before (should be ~104 entries in simetra-oidmaps.yaml including e2e entries). Run `dotnet build` to verify no YAML parse issues affect the build.
  </verify>
  <done>Both K8s oidmap ConfigMaps contain all OID entries that exist in local oidmaps.json. The 6 previously missing entries (obp_device_type, obp_sw_version, obp_serial, npb_model, npb_serial, npb_sw_version) are present.</done>
</task>

<task type="auto">
  <name>Task 2: Rename C# models and update all references</name>
  <files>
    src/SnmpCollector/Configuration/MetricPollOptions.cs
    src/SnmpCollector/Configuration/PollOptions.cs
    src/SnmpCollector/Configuration/DeviceOptions.cs
    src/SnmpCollector/Configuration/Validators/DevicesOptionsValidator.cs
    src/SnmpCollector/Pipeline/MetricPollInfo.cs
    src/SnmpCollector/Pipeline/PollInfo.cs
    src/SnmpCollector/Pipeline/DeviceInfo.cs
    src/SnmpCollector/Pipeline/DeviceRegistry.cs
    src/SnmpCollector/Services/DeviceWatcherService.cs
    src/SnmpCollector/Services/DynamicPollScheduler.cs
    src/SnmpCollector/Jobs/MetricPollJob.cs
    src/SnmpCollector/Extensions/ServiceCollectionExtensions.cs
    src/SnmpCollector/Pipeline/TenantVectorRegistry.cs
    src/SnmpCollector/Pipeline/CardinalityAuditService.cs
    tests/SnmpCollector.Tests/Pipeline/DeviceRegistryTests.cs
    tests/SnmpCollector.Tests/Jobs/MetricPollJobTests.cs
    tests/SnmpCollector.Tests/Services/DynamicPollSchedulerTests.cs
    tests/SnmpCollector.Tests/Pipeline/PipelineIntegrationTests.cs
    tests/SnmpCollector.Tests/Pipeline/Behaviors/TenantVectorFanOutBehaviorTests.cs
  </files>
  <action>
Perform the following renames systematically across ALL source and test files:

**File renames (create new, delete old):**
1. `Configuration/MetricPollOptions.cs` -> `Configuration/PollOptions.cs`
   - Rename class: `MetricPollOptions` -> `PollOptions`
   - Rename property: `Oids` -> `Names`
   - Update XML doc: "OID strings to poll" -> "Metric names to resolve to OIDs and poll in this group. Each name is resolved via IOidMapService.ResolveToOid at config load time."
   - Update XML doc on class: "One poll group for a device" (keep), update OID references to mention metric names

2. `Pipeline/MetricPollInfo.cs` -> `Pipeline/PollInfo.cs`
   - Rename record: `MetricPollInfo` -> `PollInfo`
   - Keep `Oids` parameter name (it holds actual OID strings at runtime)
   - Update XML doc param: "Zero-based index of this poll group within the device's Polls list"

**Property renames in existing files:**
3. `Configuration/DeviceOptions.cs`:
   - `List<MetricPollOptions> MetricPolls` -> `List<PollOptions> Polls`
   - Update XML doc: "Metric polling configurations" -> "Poll group configurations for this device"

4. `Configuration/Validators/DevicesOptionsValidator.cs`:
   - `device.MetricPolls` -> `device.Polls`
   - `MetricPollOptions` -> `PollOptions`
   - `poll.Oids.Count` -> `poll.Names.Count`
   - Update prefix string: `"Devices[{deviceIndex}].MetricPolls[{pollIndex}]"` -> `"Devices[{deviceIndex}].Polls[{pollIndex}]"`
   - Update error message: `".Oids must contain"` -> `".Names must contain"`
   - Update method name: `ValidateMetricPoll` -> `ValidatePoll`
   - Update class XML doc to reference Polls/Names instead of MetricPolls/Oids

5. `Pipeline/DeviceInfo.cs`: Update doc comments referencing MetricPollInfo -> PollInfo

6. `Pipeline/DeviceRegistry.cs`:
   - `d.MetricPolls` -> `d.Polls` (both constructor and ReloadAsync)
   - `new MetricPollInfo(` -> `new PollInfo(`
   - `poll.Oids.AsReadOnly()` -> `poll.Names.AsReadOnly()` (the Names list will contain resolved OIDs after Plan 02 adds resolution; for now it still works since Names holds whatever strings are in config)
   - Update XML doc references

7. `Services/DynamicPollScheduler.cs`: `MetricPollInfo` -> `PollInfo` in type references

8. `Jobs/MetricPollJob.cs`: `MetricPollInfo` -> `PollInfo` in type references (pollGroup.Oids stays as Oids -- it's PollInfo.Oids which holds actual OIDs at runtime)

9. `Extensions/ServiceCollectionExtensions.cs`:
   - `device.MetricPolls.Count` -> `device.Polls.Count`
   - `device.MetricPolls[pi]` -> `device.Polls[pi]`

10. `Pipeline/TenantVectorRegistry.cs`: `pollGroup.Oids` stays as is (PollInfo.Oids is NOT renamed). Only rename `MetricPollInfo` type references to `PollInfo`.

11. `Pipeline/CardinalityAuditService.cs`: `p.Oids` stays (PollInfo.Oids). Rename `MetricPollInfo` type refs to `PollInfo`.

**Test files -- same pattern:**
12-16. In each test file, rename all occurrences of:
   - `MetricPollOptions` -> `PollOptions`
   - `MetricPollInfo` -> `PollInfo`
   - `MetricPolls` (property) -> `Polls`
   - `Oids` (on PollOptions) -> `Names`
   - Keep `Oids` (on PollInfo) unchanged

**CRITICAL: Do NOT change PollInfo.Oids.** That parameter holds actual OID strings at runtime. Only PollOptions.Oids becomes PollOptions.Names (config-level, holds metric names).

**CRITICAL: DeviceWatcherService currently reads `d.MetricPolls` -> update to `d.Polls`. It does NOT yet do name resolution (that's Plan 02). After this task, the system still works with OID strings in the Names field -- they pass through unchanged to PollInfo.Oids via DeviceRegistry.**
  </action>
  <verify>
1. `dotnet build src/SnmpCollector/SnmpCollector.csproj` -- zero errors, zero warnings about missing types
2. `dotnet build tests/SnmpCollector.Tests/SnmpCollector.Tests.csproj` -- zero errors
3. `dotnet test tests/SnmpCollector.Tests/` -- all existing tests pass
4. Grep for "MetricPollOptions" and "MetricPollInfo" across src/ and tests/ -- zero hits (old names fully removed)
5. Verify old files deleted: MetricPollOptions.cs and MetricPollInfo.cs should not exist
  </verify>
  <done>All C# source and test files use PollOptions/PollInfo/Polls/Names. Old MetricPollOptions.cs and MetricPollInfo.cs files are deleted. Build succeeds. All existing unit tests pass unchanged (only type/property names changed, not behavior).</done>
</task>

</tasks>

<verification>
1. `dotnet build` succeeds with zero errors
2. `dotnet test` passes all existing tests
3. No references to MetricPollOptions or MetricPollInfo remain in C# code
4. K8s oidmap ConfigMaps contain all entries from local oidmaps.json
5. The system still functions (OID strings pass through Names field unchanged until Plan 02 adds resolution)
</verification>

<success_criteria>
- Build: zero errors after full rename
- Tests: all existing unit tests pass
- Grep: zero hits for "MetricPollOptions" or "MetricPollInfo" in src/ and tests/
- K8s oidmaps: 6 new entries present in both simetra-oidmaps.yaml and production/configmap.yaml
</success_criteria>

<output>
After completion, create `.planning/phases/31-human-name-device-config/31-01-SUMMARY.md`
</output>
