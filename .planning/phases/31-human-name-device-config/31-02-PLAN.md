---
phase: 31-human-name-device-config
plan: 02
type: execute
wave: 2
depends_on: ["31-01"]
files_modified:
  - src/SnmpCollector/Pipeline/DeviceRegistry.cs
  - tests/SnmpCollector.Tests/Pipeline/DeviceRegistryTests.cs
autonomous: true

must_haves:
  truths:
    - "DeviceRegistry resolves MetricNames to OIDs via IOidMapService.ResolveToOid at device load time"
    - "Unresolvable metric names log a structured warning and are excluded from the poll group"
    - "A device with zero resolved poll groups is still registered (needed for traps)"
    - "Reload diff logging includes per-name resolution detail (resolved count, unresolved names)"
    - "Both constructor (startup) and ReloadAsync (hot-reload + local dev) paths resolve names"
  artifacts:
    - path: "src/SnmpCollector/Pipeline/DeviceRegistry.cs"
      provides: "Name resolution via IOidMapService injection"
      contains: "ResolveToOid"
    - path: "tests/SnmpCollector.Tests/Pipeline/DeviceRegistryTests.cs"
      provides: "Unit tests for name resolution, unresolvable names, zero-resolved groups"
      contains: "ResolveToOid"
  key_links:
    - from: "src/SnmpCollector/Pipeline/DeviceRegistry.cs"
      to: "IOidMapService.ResolveToOid"
      via: "constructor injection of IOidMapService"
      pattern: "_oidMapService\\.ResolveToOid"
    - from: "src/SnmpCollector/Pipeline/DeviceRegistry.cs"
      to: "MetricPollInfo.Oids"
      via: "resolved OIDs populate the runtime poll list"
      pattern: "new MetricPollInfo"
---

<objective>
Add metric name resolution to DeviceRegistry so that PollOptions.MetricNames[] (human-readable names) are resolved to OIDs via IOidMapService.ResolveToOid at device config load time. Unresolvable names are warned and skipped. Add unit tests covering resolution, unresolvable names, and zero-resolved groups.

Purpose: This is the core behavioral change of Phase 31 -- transforming human-readable metric names into runtime OIDs that MetricPollJob can poll. Without this, the renamed MetricNames field would pass name strings directly into MetricPollInfo.Oids, causing SNMP GET failures.

Output: Working name resolution in DeviceRegistry with comprehensive test coverage.
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-human-name-device-config/31-CONTEXT.md
@.planning/phases/31-human-name-device-config/31-RESEARCH.md
@.planning/phases/31-human-name-device-config/31-01-SUMMARY.md
@src/SnmpCollector/Pipeline/DeviceRegistry.cs
@src/SnmpCollector/Pipeline/IOidMapService.cs
@src/SnmpCollector/Pipeline/MetricPollInfo.cs
@src/SnmpCollector/Configuration/DeviceOptions.cs
@tests/SnmpCollector.Tests/Pipeline/DeviceRegistryTests.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inject IOidMapService into DeviceRegistry, add name resolution logic, and update test CreateRegistry helper</name>
  <files>
    src/SnmpCollector/Pipeline/DeviceRegistry.cs
    tests/SnmpCollector.Tests/Pipeline/DeviceRegistryTests.cs
  </files>
  <action>
    **Inject IOidMapService:**
    - Add `private readonly IOidMapService _oidMapService;` field.
    - Add `IOidMapService oidMapService` parameter to constructor: `public DeviceRegistry(IOptions<DevicesOptions> devicesOptions, IOidMapService oidMapService, ILogger<DeviceRegistry> logger)`.
    - Assign: `_oidMapService = oidMapService ?? throw new ArgumentNullException(nameof(oidMapService));`.
    - DI registration in ServiceCollectionExtensions already registers `IDeviceRegistry, DeviceRegistry` as singleton -- the container will resolve IOidMapService automatically. No changes needed to ServiceCollectionExtensions.

    **Add name resolution in BOTH constructor and ReloadAsync** (they share identical poll-group building logic):

    Replace the poll group building block (which currently does `poll.MetricNames.AsReadOnly()` directly into `MetricPollInfo.Oids`) with resolution logic:

    ```csharp
    var pollGroups = d.Polls
        .Select((poll, index) =>
        {
            var resolvedOids = new List<string>();
            var unresolvedNames = new List<string>();

            foreach (var name in poll.MetricNames)
            {
                var oid = _oidMapService.ResolveToOid(name);
                if (oid is not null)
                {
                    resolvedOids.Add(oid);
                }
                else
                {
                    unresolvedNames.Add(name);
                    _logger.LogWarning(
                        "MetricName '{MetricName}' on device '{DeviceName}' poll {PollIndex} not found in OID map -- skipping",
                        name, d.Name, index);
                }
            }

            // Always log resolution summary for reload diff visibility
            _logger.LogInformation(
                "Device '{DeviceName}' poll {PollIndex}: resolved {ResolvedCount}/{TotalCount} metric names{UnresolvedDetail}",
                d.Name, index, resolvedOids.Count, poll.MetricNames.Count,
                unresolvedNames.Count > 0 ? $"; unresolved: [{string.Join(", ", unresolvedNames)}]" : "");

            return new MetricPollInfo(
                PollIndex: index,
                Oids: resolvedOids.AsReadOnly(),
                IntervalSeconds: poll.IntervalSeconds,
                TimeoutMultiplier: poll.TimeoutMultiplier);
        })
        .ToList()
        .AsReadOnly();
    ```

    Apply this pattern to BOTH the constructor AND ReloadAsync. The logic is identical -- consider extracting a private helper method `BuildPollGroups(List<PollOptions> polls, string deviceName)` to avoid duplication. The helper returns `ReadOnlyCollection<MetricPollInfo>`.

    **Important:** The device is ALWAYS registered even if all poll groups have zero resolved OIDs. This is correct -- devices with zero poll OIDs still receive traps. The existing code already handles this (no early return on empty poll groups).

    **Important:** MetricPollInfo.Oids now contains resolved OID strings, NOT metric name strings. Downstream consumers (MetricPollJob, CardinalityAuditService) read MetricPollInfo.Oids and pass them to SNMP GET -- they must receive real OIDs.

    **IMPORTANT -- Update DeviceRegistryTests.CreateRegistry to compile with 3-arg constructor:**
    The existing `CreateRegistry` helper calls `new DeviceRegistry(options, logger)` with only 2 args. After adding IOidMapService as the second constructor parameter, this will fail to compile. Update the helper NOW (in this task) to pass a mock IOidMapService:
    - Add `using NSubstitute;` (or whatever mocking library is already used in the test project).
    - Create a default mock: `var oidMapService = Substitute.For<IOidMapService>();`
    - For the default mock, configure it to return the metric name as-is (passthrough): `oidMapService.ResolveToOid(Arg.Any<string>()).Returns(callInfo => callInfo.Arg<string>());` -- this makes existing tests pass without change since they use OID strings as MetricNames (which will "resolve" to themselves).
    - Update signature: `private static DeviceRegistry CreateRegistry(DevicesOptions? devicesOptions = null, IOidMapService? oidMapService = null)`
    - Inside: `var svc = oidMapService ?? CreateDefaultOidMapService();` where `CreateDefaultOidMapService()` returns the passthrough mock.
    - Pass to constructor: `new DeviceRegistry(Options.Create(devicesOptions ?? TwoDeviceOptions()), svc, NullLogger<DeviceRegistry>.Instance)`.

    This ensures `dotnet build` succeeds after this task, before Task 2 adds new test methods.
  </action>
  <verify>
    1. `dotnet build src/SnmpCollector/SnmpCollector.sln` compiles with zero errors.
    2. `dotnet test tests/SnmpCollector.Tests/ --filter "FullyQualifiedName~DeviceRegistry"` -- all EXISTING tests pass (with passthrough mock).
    3. Grep: `grep "ResolveToOid" src/SnmpCollector/Pipeline/DeviceRegistry.cs` returns at least 1 match.
    4. Grep: `grep "IOidMapService" src/SnmpCollector/Pipeline/DeviceRegistry.cs` returns at least 2 matches (field + constructor param).
  </verify>
  <done>
    DeviceRegistry constructor and ReloadAsync both resolve MetricNames to OIDs via IOidMapService.ResolveToOid. Unresolvable names log structured warnings and are excluded. Resolution summary always logged per poll group for reload diff visibility. DeviceRegistryTests.CreateRegistry updated to pass IOidMapService mock -- existing tests compile and pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add unit tests for name resolution in DeviceRegistryTests</name>
  <files>
    tests/SnmpCollector.Tests/Pipeline/DeviceRegistryTests.cs
  </files>
  <action>
    **Add new test methods covering name resolution** (CreateRegistry helper already accepts IOidMapService from Task 1):

    1. **ResolvedMetricNames_PopulateOids**: Create device with `Polls = [new PollOptions { MetricNames = ["obp_channel_L1", "obp_r1_power_L1"], IntervalSeconds = 10 }]`. Configure mock: `ResolveToOid("obp_channel_L1")` returns `"1.3.6.1.4.1.47477.10.21.1.3.4.0"`, `ResolveToOid("obp_r1_power_L1")` returns `"1.3.6.1.4.1.47477.10.21.1.3.10.0"`. Assert: `device.PollGroups[0].Oids` contains both OID strings, not the metric name strings.

    2. **UnresolvableMetricName_LogsWarningAndSkipped**: Create device with `MetricNames = ["valid_name", "bad_name"]`. Configure mock: `ResolveToOid("valid_name")` returns an OID, `ResolveToOid("bad_name")` returns null. Assert: `device.PollGroups[0].Oids.Count == 1` (only the valid one). Assert: logger received a warning containing "bad_name" and "not found in OID map".

    3. **AllNamesUnresolvable_DeviceStillRegistered**: Create device with `MetricNames = ["no_match_1", "no_match_2"]`. Configure mock: both return null. Assert: device IS registered in registry (`TryGetByIpPort` returns true). Assert: `device.PollGroups[0].Oids.Count == 0`.

    4. **ReloadAsync_ResolvesNames**: Similar to test 1 but via `ReloadAsync` path. Create registry with empty initial config, then call `ReloadAsync` with devices containing MetricNames. Assert resolved OIDs are correct.

    5. **ReloadAsync_UnresolvableLogsWarning**: Via ReloadAsync path, verify unresolvable names produce warnings.

    **Test naming convention:** Follow existing file patterns (check existing test method names for naming style).

    **Mock pattern:** If existing tests use a specific mocking library, use the same. If they use `NullLogger<T>` or `Substitute.For<T>()`, follow that pattern for IOidMapService.
  </action>
  <verify>
    1. `dotnet test tests/SnmpCollector.Tests/ --filter "FullyQualifiedName~DeviceRegistry"` -- all tests pass (old + new).
    2. `dotnet test tests/SnmpCollector.Tests/` -- full test suite passes.
  </verify>
  <done>
    DeviceRegistryTests covers: metric name resolution populates OIDs correctly, unresolvable names are warned and skipped, devices with zero resolved names are still registered, both constructor and ReloadAsync paths resolve names.
  </done>
</task>

</tasks>

<verification>
1. `dotnet build src/SnmpCollector/SnmpCollector.sln` -- zero errors.
2. `dotnet test tests/SnmpCollector.Tests/` -- all tests pass including new name resolution tests.
3. DeviceRegistry has IOidMapService injected and calls ResolveToOid in both constructor and ReloadAsync.
4. At least 5 new test methods covering resolution, unresolvable names, zero-resolved groups, and reload path.
</verification>

<success_criteria>
- DeviceRegistry resolves MetricNames[] to OIDs at device load time via IOidMapService.ResolveToOid
- Unresolvable names log structured warning with device name, metric name, poll index
- Resolution summary always logged per poll group (no conditional -- enables reload diff)
- Device registered even with zero resolved OIDs
- Both constructor and ReloadAsync paths have resolution logic
- Existing DeviceRegistryTests compile and pass after constructor signature change
- Comprehensive unit tests pass for all resolution scenarios
</success_criteria>

<output>
After completion, create `.planning/phases/31-human-name-device-config/31-02-SUMMARY.md`
</output>
