---
phase: 31-human-name-device-config
plan: 03
type: execute
wave: 2
depends_on: ["31-01"]
files_modified:
  - src/SnmpCollector/config/devices.json
  - deploy/k8s/snmp-collector/simetra-devices.yaml
  - deploy/k8s/production/configmap.yaml
  - tests/e2e/fixtures/device-added-configmap.yaml
  - tests/e2e/fixtures/device-modified-interval-configmap.yaml
  - tests/e2e/fixtures/device-removed-configmap.yaml
  - tests/e2e/fixtures/e2e-sim-unmapped-configmap.yaml
  - tests/e2e/fixtures/fake-device-configmap.yaml
  - tests/e2e/fixtures/.original-devices-configmap.yaml
  - tests/e2e/scenarios/06-poll-unreachable.sh
autonomous: true

must_haves:
  truths:
    - "devices.json uses MetricNames with human-readable metric names instead of raw OID strings"
    - "K8s simetra-devices.yaml uses Polls/MetricNames with human-readable names for all 3 devices"
    - "Production configmap.yaml uses Polls/MetricNames with human-readable names and updated YAML comments"
    - "All E2E device fixture files use Polls/MetricNames instead of MetricPolls/Oids"
    - "E2E scenario 06 inline JSON uses Polls/MetricNames with e2e_gauge_test instead of raw OID"
    - "Every OID in current devices.json is correctly translated to its metric name from oidmaps.json"
    - "Unmapped fixture uses a deliberately non-existent metric name to preserve unmapped test behavior"
  artifacts:
    - path: "src/SnmpCollector/config/devices.json"
      provides: "Local device config with metric names"
      contains: "MetricNames"
    - path: "deploy/k8s/snmp-collector/simetra-devices.yaml"
      provides: "K8s device ConfigMap with metric names for OBP-01, NPB-01, E2E-SIM"
      contains: "MetricNames"
    - path: "deploy/k8s/production/configmap.yaml"
      provides: "Production multi-doc ConfigMap with oidmaps in array format and devices in Polls/MetricNames format"
      contains: "MetricNames"
    - path: "tests/e2e/fixtures/device-added-configmap.yaml"
      provides: "E2E fixture with Polls/MetricNames format"
      contains: "MetricNames"
    - path: "tests/e2e/scenarios/06-poll-unreachable.sh"
      provides: "Inline JSON using e2e_gauge_test metric name"
      contains: "e2e_gauge_test"
  key_links:
    - from: "src/SnmpCollector/config/devices.json"
      to: "src/SnmpCollector/config/oidmaps.json"
      via: "metric names in devices.json must exist in oidmaps.json for resolution"
      pattern: "obp_channel_L1|npb_cpu_util"
    - from: "deploy/k8s/snmp-collector/simetra-devices.yaml"
      to: "deploy/k8s/snmp-collector/simetra-oidmaps.yaml"
      via: "K8s metric names must exist in K8s oidmaps for resolution"
      pattern: "e2e_gauge_test|obp_channel_L1"
    - from: "deploy/k8s/production/configmap.yaml"
      to: "deploy/k8s/production/configmap.yaml"
      via: "production oidmaps and devices sections in same file must be consistent"
      pattern: "Polls|MetricNames"
---

<objective>
Rewrite all device configuration files (local, K8s, production, E2E fixtures, E2E scenarios) to use the new Polls/MetricNames format with human-readable metric names instead of raw OID strings.

Purpose: Operators can now write `"MetricNames": ["obp_channel_L1"]` instead of `"Oids": ["1.3.6.1.4.1.47477.10.21.1.3.4.0"]` -- the core UX improvement of Phase 31. All config files must be rewritten to ship a working system.

Output: All device config files using human-readable metric names with Polls/MetricNames JSON structure.
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-human-name-device-config/31-CONTEXT.md
@.planning/phases/31-human-name-device-config/31-RESEARCH.md
@.planning/phases/31-human-name-device-config/31-01-SUMMARY.md
@src/SnmpCollector/config/devices.json
@src/SnmpCollector/config/oidmaps.json
@deploy/k8s/snmp-collector/simetra-devices.yaml
@deploy/k8s/snmp-collector/simetra-oidmaps.yaml
@deploy/k8s/production/configmap.yaml
@tests/e2e/fixtures/device-added-configmap.yaml
@tests/e2e/fixtures/fake-device-configmap.yaml
@tests/e2e/fixtures/e2e-sim-unmapped-configmap.yaml
@tests/e2e/scenarios/06-poll-unreachable.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite local devices.json, K8s simetra-devices.yaml, and production configmap.yaml with metric names</name>
  <files>
    src/SnmpCollector/config/devices.json
    deploy/k8s/snmp-collector/simetra-devices.yaml
    deploy/k8s/production/configmap.yaml
  </files>
  <action>
    **config/devices.json** -- rewrite with complete OID-to-name translation:

    Read the current devices.json and the updated oidmaps.json (array format from Plan 01). For every OID in each device's poll groups, look up the corresponding MetricName from oidmaps.json. Replace `"MetricPolls"` with `"Polls"` and `"Oids"` with `"MetricNames"`.

    **OBP-01 translation (3 poll groups):**
    - Poll group 0 (IntervalSeconds: 10) -- 23 metric names:
      `obp_channel_L1, obp_r1_power_L1, obp_r2_power_L1, obp_r3_power_L1, obp_r4_power_L1, obp_link_state_L2, obp_channel_L2, obp_r1_power_L2, obp_r2_power_L2, obp_r3_power_L2, obp_r4_power_L2, obp_link_state_L3, obp_channel_L3, obp_r1_power_L3, obp_r2_power_L3, obp_r3_power_L3, obp_r4_power_L3, obp_link_state_L4, obp_channel_L4, obp_r1_power_L4, obp_r2_power_L4, obp_r3_power_L4, obp_r4_power_L4`
    - Poll group 1 (IntervalSeconds: 30) -- 1 metric name:
      `obp_link_state_L1`
    - Poll group 2 (IntervalSeconds: 60) -- 3 metric names:
      `obp_device_type, obp_sw_version, obp_serial`

    **NPB-01 translation (2 poll groups):**
    - Poll group 0 (IntervalSeconds: 10) -- 68 metric names:
      `npb_cpu_util, npb_mem_util, npb_sys_temp, npb_uptime` plus 64 port metrics:
      For each port P1-P8: `npb_port_status_P{n}, npb_port_speed_P{n}, npb_port_rx_bytes_P{n}, npb_port_tx_bytes_P{n}, npb_port_rx_errors_P{n}, npb_port_tx_errors_P{n}, npb_port_rx_drops_P{n}, npb_port_tx_drops_P{n}`
    - Poll group 1 (IntervalSeconds: 60) -- 3 metric names:
      `npb_model, npb_serial, npb_sw_version`

    Cross-reference EVERY name against the oidmaps.json to confirm it exists. All names must be present.

    **deploy/k8s/snmp-collector/simetra-devices.yaml** -- same translation plus E2E-SIM:
    - OBP-01 and NPB-01: same translations as local devices.json above.
    - E2E-SIM poll group 0 (IntervalSeconds: 10) -- 7 metric names:
      `e2e_gauge_test, e2e_integer_test, e2e_counter32_test, e2e_counter64_test, e2e_timeticks_test, e2e_info_test, e2e_ip_test`
    - Replace `"MetricPolls"` with `"Polls"` and `"Oids"` with `"MetricNames"` in the embedded JSON.
    - Keep all other device fields unchanged (Name, IpAddress, Port, CommunityString).

    **deploy/k8s/production/configmap.yaml** -- translate BOTH the oidmaps and devices sections:
    This is a multi-document YAML file containing simetra-oidmaps, simetra-devices, and simetra-tenantvector ConfigMaps.

    **Oidmaps section (simetra-oidmaps ConfigMap):**
    - Convert the embedded flat dict JSON to array-of-objects format, same as the simetra-oidmaps.yaml conversion in Plan 01.
    - Each `"OID": "name"` becomes `{ "Oid": "OID", "MetricName": "name" }`.
    - The production oidmaps currently has 88 entries (22 OBP + 66 NPB -- missing the 6 info OIDs and the 4 NPB port speed OIDs that exist in dev). ADD the 6 missing info entries to match dev:
      - `{ "Oid": "1.3.6.1.4.1.47477.10.21.60.1.0", "MetricName": "obp_device_type" }`
      - `{ "Oid": "1.3.6.1.4.1.47477.10.21.60.13.0", "MetricName": "obp_sw_version" }`
      - `{ "Oid": "1.3.6.1.4.1.47477.10.21.60.15.0", "MetricName": "obp_serial" }`
      - `{ "Oid": "1.3.6.1.4.1.47477.100.1.5.0", "MetricName": "npb_model" }`
      - `{ "Oid": "1.3.6.1.4.1.47477.100.1.6.0", "MetricName": "npb_serial" }`
      - `{ "Oid": "1.3.6.1.4.1.47477.100.1.7.0", "MetricName": "npb_sw_version" }`
    - Note: Production has NO E2E sim entries (those are dev/staging only). Do NOT add e2e_* entries.
    - Update the YAML comment block above oidmaps.json: change "Flat dictionary mapping SNMP OID strings to Prometheus metric names" to "Array of objects mapping SNMP OIDs to Prometheus metric names. Each entry: { Oid, MetricName }."

    **Devices section (simetra-devices ConfigMap):**
    - Replace `"MetricPolls"` with `"Polls"` and `"Oids"` with `"MetricNames"` in the embedded JSON.
    - Translate all OIDs to metric names using the same mapping as local devices.json.
    - Production has 3 devices: OBP-01, NPB-01, E2E-SIM (same as simetra-devices.yaml).
    - Update the YAML comment block: change `MetricPolls` to `Polls`, `Oids` to `MetricNames`, and "Flat string array of OID strings" to "Array of human-readable metric names (resolved to OIDs at load time via oidmaps)".

    **Tenantvector section:** Leave unchanged -- it already uses MetricName (singular), not Oids.
  </action>
  <verify>
    1. `python -c "import json; d=json.load(open('src/SnmpCollector/config/devices.json')); print(len(d))"` returns 2 (two devices).
    2. Grep: `grep -c "MetricNames" src/SnmpCollector/config/devices.json` returns at least 5 (one per poll group).
    3. Grep: `grep "Oids\|MetricPolls" src/SnmpCollector/config/devices.json` returns no matches.
    4. Grep: `grep -c "MetricNames" deploy/k8s/snmp-collector/simetra-devices.yaml` returns at least 6 (3 devices x 2+ poll groups).
    5. Grep: `grep "Oids\|MetricPolls" deploy/k8s/snmp-collector/simetra-devices.yaml` returns no matches.
    6. Grep: `grep -c "MetricNames" deploy/k8s/production/configmap.yaml` returns at least 6 (devices section poll groups).
    7. Grep: `grep '"Oids"\|"MetricPolls"' deploy/k8s/production/configmap.yaml` returns no matches.
    8. Grep: `grep -c '"MetricName"' deploy/k8s/production/configmap.yaml` returns at least 88 (oidmaps array entries -- may be more with added entries).
  </verify>
  <done>
    Local devices.json has 2 devices (OBP-01, NPB-01) with all OIDs translated to metric names in Polls/MetricNames format. K8s simetra-devices.yaml has 3 devices (OBP-01, NPB-01, E2E-SIM) with same translation. Production configmap.yaml has oidmaps in array format and devices in Polls/MetricNames format with updated YAML comments.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite E2E device fixtures and scenario 06 inline JSON</name>
  <files>
    tests/e2e/fixtures/device-added-configmap.yaml
    tests/e2e/fixtures/device-modified-interval-configmap.yaml
    tests/e2e/fixtures/device-removed-configmap.yaml
    tests/e2e/fixtures/e2e-sim-unmapped-configmap.yaml
    tests/e2e/fixtures/fake-device-configmap.yaml
    tests/e2e/fixtures/.original-devices-configmap.yaml
    tests/e2e/scenarios/06-poll-unreachable.sh
  </files>
  <action>
    **All 6 E2E device fixture YAML files** -- apply the same Polls/MetricNames transformation:

    Each fixture file contains embedded JSON with device configs. For each file:
    1. Replace all `"MetricPolls"` with `"Polls"`.
    2. Replace all `"Oids"` with `"MetricNames"`.
    3. Replace all raw OID strings with their corresponding metric names.

    The fixtures contain variations of the 3-device config (OBP-01, NPB-01, E2E-SIM) with modifications for each test scenario. Use the oidmaps.json translations from Task 1. The E2E-SIM OIDs map to e2e_* names.

    Specific fixture details:
    - **device-added-configmap.yaml**: Has the base 3 devices PLUS an added device. The added device's OIDs must also be translated. Check what OIDs the added device uses and translate them.
    - **device-modified-interval-configmap.yaml**: Same devices but with a modified IntervalSeconds on one poll group. OIDs->names translation still needed.
    - **device-removed-configmap.yaml**: One fewer device. Remaining devices need OID->name translation.
    - **e2e-sim-unmapped-configmap.yaml**: This fixture tests the behavior when a device references a metric name that does NOT exist in the oidmap. The current fixture has OIDs like `1.3.6.1.4.1.47477.999.2.1.0` which map to `e2e_unmapped_gauge` in the oidmaps -- but that name IS in the oidmaps, so using it would make it resolvable and BREAK the unmapped test scenario. Instead, use a deliberately non-existent metric name like `e2e_intentionally_missing` that has NO entry in oidmaps.json. This ensures the resolution-time "unresolvable name" warning path is exercised. For the other OIDs in this fixture that ARE in the oidmaps (the standard e2e_* ones), translate them normally.
    - **fake-device-configmap.yaml**: Contains a fake device for testing. Translate OIDs to names.
    - **.original-devices-configmap.yaml**: Hidden file -- saved copy of the baseline ConfigMap. Must match the updated simetra-devices.yaml format exactly.

    **IMPORTANT:** Read each fixture file fully before modifying. Understand what each fixture tests (added device? removed device? interval change?) and ensure the translation preserves the test intent.

    **E2E scenario 06-poll-unreachable.sh** -- update inline jq JSON:
    - Current line: `"MetricPolls": [{"IntervalSeconds": 10, "Oids": ["1.3.6.1.4.1.47477.999.1.1.0"]}]`
    - New line: `"Polls": [{"IntervalSeconds": 10, "MetricNames": ["e2e_gauge_test"]}]`
    - The OID `1.3.6.1.4.1.47477.999.1.1.0` maps to metric name `e2e_gauge_test` in simetra-oidmaps.yaml.
    - Find the exact line in the script and update both the JSON keys and the value.
  </action>
  <verify>
    1. Grep: `grep -r "MetricPolls\|\"Oids\"" tests/e2e/fixtures/device-*` returns no matches.
    2. Grep: `grep -r "MetricPolls\|\"Oids\"" tests/e2e/fixtures/fake-*` returns no matches.
    3. Grep: `grep -r "MetricPolls\|\"Oids\"" tests/e2e/fixtures/.original-*` returns no matches.
    4. Grep: `grep -r "MetricPolls\|\"Oids\"" tests/e2e/fixtures/e2e-sim-*` returns no matches.
    5. Grep: `grep "MetricPolls\|\"Oids\"" tests/e2e/scenarios/06-poll-unreachable.sh` returns no matches.
    6. Grep: `grep "e2e_gauge_test" tests/e2e/scenarios/06-poll-unreachable.sh` returns at least 1 match.
    7. Grep: `grep "e2e_intentionally_missing" tests/e2e/fixtures/e2e-sim-unmapped-configmap.yaml` returns at least 1 match (confirms deliberate non-existent name).
    8. All fixture YAML files are valid YAML (no syntax errors from the rewrite).
  </verify>
  <done>
    All 6 E2E device fixture files and scenario 06 use Polls/MetricNames with human-readable metric names. Unmapped fixture uses `e2e_intentionally_missing` (deliberately absent from oidmaps) to preserve unmapped test behavior. No remaining references to MetricPolls or Oids in any device fixture or device-related E2E scenario.
  </done>
</task>

</tasks>

<verification>
1. Grep across entire repo: `grep -rn "\"MetricPolls\"\|\"Oids\"" src/ deploy/ tests/ --include="*.json" --include="*.yaml" --include="*.sh"` returns ZERO matches (complete migration).
2. All JSON files parse correctly (no syntax errors from rewrite).
3. Every metric name used in devices.json exists in oidmaps.json (cross-reference check).
4. E2E scenario 06 uses `e2e_gauge_test` metric name instead of raw OID.
5. Production configmap.yaml oidmaps section uses array-of-objects format, devices section uses Polls/MetricNames.
6. Unmapped fixture uses `e2e_intentionally_missing` -- a name that does NOT exist in any oidmaps file.
</verification>

<success_criteria>
- Zero remaining references to "MetricPolls" or "Oids" (as JSON keys) in any config, fixture, or scenario file
- Local devices.json: 2 devices with 5 poll groups using metric names
- K8s simetra-devices.yaml: 3 devices including E2E-SIM with metric names
- Production configmap.yaml: oidmaps in array format, devices in Polls/MetricNames format, comments updated
- All E2E device fixtures use Polls/MetricNames format
- Unmapped fixture preserves unmapped test intent via deliberate non-existent metric name
- Scenario 06 inline JSON uses e2e_gauge_test
</success_criteria>

<output>
After completion, create `.planning/phases/31-human-name-device-config/31-03-SUMMARY.md`
</output>
