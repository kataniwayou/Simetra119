---
phase: 31-human-name-device-config
plan: 03
type: execute
wave: 2
depends_on: ["31-01"]
files_modified:
  - src/SnmpCollector/config/devices.json
  - deploy/k8s/snmp-collector/simetra-devices.yaml
  - deploy/k8s/production/configmap.yaml
  - tests/e2e/fixtures/device-added-configmap.yaml
  - tests/e2e/fixtures/device-removed-configmap.yaml
  - tests/e2e/fixtures/device-modified-interval-configmap.yaml
  - tests/e2e/fixtures/fake-device-configmap.yaml
  - tests/e2e/fixtures/e2e-sim-unmapped-configmap.yaml
  - tests/e2e/scenarios/06-poll-unreachable.sh
autonomous: true

must_haves:
  truths:
    - "devices.json uses Polls/Names with human-readable metric names instead of MetricPolls/Oids with raw OID strings"
    - "All K8s device ConfigMaps use the new Polls/Names format with metric names"
    - "All E2E fixture ConfigMaps use the new format with metric names"
    - "E2E unmapped fixture uses intentionally unresolvable names for the 2 extra entries"
    - "E2E scenario 06 inline JSON uses new format"
    - "Every metric name in config files has a corresponding entry in the OID map"
  artifacts:
    - path: "src/SnmpCollector/config/devices.json"
      provides: "Local dev device config with metric names"
      contains: "obp_channel_L1"
    - path: "deploy/k8s/snmp-collector/simetra-devices.yaml"
      provides: "K8s device ConfigMap with metric names"
      contains: "obp_channel_L1"
    - path: "tests/e2e/fixtures/e2e-sim-unmapped-configmap.yaml"
      provides: "E2E fixture with intentionally unresolvable names"
      contains: "e2e_unmapped_1"
  key_links:
    - from: "config/devices.json"
      to: "config/oidmaps.json"
      via: "every Names[] entry must exist as a value in oidmaps.json"
      pattern: "obp_channel_L1|npb_cpu_util|e2e_gauge_test"
---

<objective>
Rewrite all device configuration files to use human-readable metric names instead of raw OID strings, with the new Polls/Names schema.

Purpose: Complete the config-level migration so operators see metric names like "obp_channel_L1" instead of "1.3.6.1.4.1.47477.10.21.1.3.4.0" in all device configuration files.
Output: All JSON and YAML device configs use Polls[].Names[] with metric names; E2E fixtures updated; E2E scenario 06 inline JSON updated.
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-human-name-device-config/31-CONTEXT.md
@.planning/phases/31-human-name-device-config/31-RESEARCH.md
@.planning/phases/31-human-name-device-config/31-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite local devices.json and K8s device ConfigMaps</name>
  <files>
    src/SnmpCollector/config/devices.json
    deploy/k8s/snmp-collector/simetra-devices.yaml
    deploy/k8s/production/configmap.yaml
  </files>
  <action>
For each file, apply TWO transformations:

**Schema rename:**
- `"MetricPolls"` -> `"Polls"`
- `"Oids"` -> `"Names"`

**OID-to-name translation** (use the OID map in config/oidmaps.json as the authoritative source):

OBP-01 10s group (22 entries) -- replace OID strings with metric names:
```
"obp_channel_L1", "obp_r1_power_L1", "obp_r2_power_L1", "obp_r3_power_L1", "obp_r4_power_L1",
"obp_link_state_L2", "obp_channel_L2", "obp_r1_power_L2", "obp_r2_power_L2", "obp_r3_power_L2", "obp_r4_power_L2",
"obp_link_state_L3", "obp_channel_L3", "obp_r1_power_L3", "obp_r2_power_L3", "obp_r3_power_L3", "obp_r4_power_L3",
"obp_link_state_L4", "obp_channel_L4", "obp_r1_power_L4", "obp_r2_power_L4", "obp_r3_power_L4", "obp_r4_power_L4"
```

OBP-01 30s group (1 entry):
```
"obp_link_state_L1"
```

OBP-01 60s group (3 entries):
```
"obp_device_type", "obp_sw_version", "obp_serial"
```

NPB-01 10s group (68 entries):
```
"npb_cpu_util", "npb_mem_util", "npb_sys_temp", "npb_uptime",
"npb_port_status_P1", "npb_port_rx_octets_P1", "npb_port_tx_octets_P1", "npb_port_rx_packets_P1", "npb_port_tx_packets_P1", "npb_port_rx_errors_P1", "npb_port_tx_errors_P1", "npb_port_rx_drops_P1",
"npb_port_status_P2", "npb_port_rx_octets_P2", "npb_port_tx_octets_P2", "npb_port_rx_packets_P2", "npb_port_tx_packets_P2", "npb_port_rx_errors_P2", "npb_port_tx_errors_P2", "npb_port_rx_drops_P2",
"npb_port_status_P3", "npb_port_rx_octets_P3", "npb_port_tx_octets_P3", "npb_port_rx_packets_P3", "npb_port_tx_packets_P3", "npb_port_rx_errors_P3", "npb_port_tx_errors_P3", "npb_port_rx_drops_P3",
"npb_port_status_P4", "npb_port_rx_octets_P4", "npb_port_tx_octets_P4", "npb_port_rx_packets_P4", "npb_port_tx_packets_P4", "npb_port_rx_errors_P4", "npb_port_tx_errors_P4", "npb_port_rx_drops_P4",
"npb_port_status_P5", "npb_port_rx_octets_P5", "npb_port_tx_octets_P5", "npb_port_rx_packets_P5", "npb_port_tx_packets_P5", "npb_port_rx_errors_P5", "npb_port_tx_errors_P5", "npb_port_rx_drops_P5",
"npb_port_status_P6", "npb_port_rx_octets_P6", "npb_port_tx_octets_P6", "npb_port_rx_packets_P6", "npb_port_tx_packets_P6", "npb_port_rx_errors_P6", "npb_port_tx_errors_P6", "npb_port_rx_drops_P6",
"npb_port_status_P7", "npb_port_rx_octets_P7", "npb_port_tx_octets_P7", "npb_port_rx_packets_P7", "npb_port_tx_packets_P7", "npb_port_rx_errors_P7", "npb_port_tx_errors_P7", "npb_port_rx_drops_P7",
"npb_port_status_P8", "npb_port_rx_octets_P8", "npb_port_tx_octets_P8", "npb_port_rx_packets_P8", "npb_port_tx_packets_P8", "npb_port_rx_errors_P8", "npb_port_tx_errors_P8", "npb_port_rx_drops_P8"
```

NPB-01 60s group (3 entries):
```
"npb_model", "npb_serial", "npb_sw_version"
```

E2E-SIM 10s group (7 entries -- present in K8s simetra-devices.yaml but NOT in local devices.json):
```
"e2e_gauge_test", "e2e_integer_test", "e2e_counter32_test", "e2e_counter64_test", "e2e_timeticks_test", "e2e_info_test", "e2e_ip_test"
```

FAKE-UNREACHABLE 10s group (1 entry -- present in K8s fixtures but NOT in local devices.json):
```
"e2e_gauge_test"
```

E2E-SIM-2 10s group (1 entry -- present in simetra-devices.yaml only):
```
"e2e_gauge_test"
```

**File-specific notes:**

`config/devices.json` (local dev): Contains OBP-01 and NPB-01 only. No E2E-SIM or FAKE-UNREACHABLE.

`deploy/k8s/snmp-collector/simetra-devices.yaml`: Contains OBP-01, NPB-01, E2E-SIM, FAKE-UNREACHABLE, E2E-SIM-2.

`deploy/k8s/production/configmap.yaml`: Find the simetra-devices section and apply same changes. This file contains multiple ConfigMaps -- ONLY modify the simetra-devices section.

Maintain exact same formatting, indentation, and entry count in each file. Verify entry counts match after translation.
  </action>
  <verify>
1. Grep for `"MetricPolls"` across all modified files -- zero hits
2. Grep for `"Oids"` in the devices sections of modified files -- zero hits (note: oidmaps sections still have OIDs, that's correct)
3. Grep for `"Polls"` in modified files -- present in all
4. Grep for `"Names"` in modified files -- present in all
5. Count entries in each poll group to verify no entries were lost during translation
6. Verify `"obp_channel_L1"` appears in all device config files
7. JSON syntax validation: `python -c "import json; json.load(open('src/SnmpCollector/config/devices.json'))"` -- no parse errors
  </verify>
  <done>Local devices.json, K8s simetra-devices.yaml, and production configmap.yaml all use Polls/Names with human-readable metric names. Zero raw OID strings remain in device configuration sections. Entry counts match originals.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite E2E fixture ConfigMaps</name>
  <files>
    tests/e2e/fixtures/device-added-configmap.yaml
    tests/e2e/fixtures/device-removed-configmap.yaml
    tests/e2e/fixtures/device-modified-interval-configmap.yaml
    tests/e2e/fixtures/fake-device-configmap.yaml
    tests/e2e/fixtures/e2e-sim-unmapped-configmap.yaml
  </files>
  <action>
Apply the same MetricPolls->Polls, Oids->Names, OID-to-metric-name translation to all E2E device fixture ConfigMaps.

**device-added-configmap.yaml** (identical to simetra-devices.yaml): Full translation of OBP-01, NPB-01, E2E-SIM, FAKE-UNREACHABLE, E2E-SIM-2.

**device-removed-configmap.yaml**: Same translation but with fewer devices (OBP-01, NPB-01, FAKE-UNREACHABLE -- E2E-SIM removed).

**device-modified-interval-configmap.yaml**: Same as device-removed-configmap but with E2E-SIM interval changed to 5s. Translate all OIDs to metric names. The IntervalSeconds change stays as-is.

**fake-device-configmap.yaml**: Full translation of OBP-01, NPB-01, E2E-SIM, FAKE-UNREACHABLE.

**e2e-sim-unmapped-configmap.yaml (SPECIAL HANDLING):**
This fixture currently has E2E-SIM with 9 OIDs: 7 mapped + 2 unmapped (999.2.1.0 and 999.2.2.0).
- The 7 mapped OIDs become their metric names: "e2e_gauge_test", "e2e_integer_test", "e2e_counter32_test", "e2e_counter64_test", "e2e_timeticks_test", "e2e_info_test", "e2e_ip_test"
- The 2 unmapped OIDs become intentionally unresolvable names: "e2e_unmapped_1", "e2e_unmapped_2"
  These names do NOT exist in the OID map, so they will trigger the unresolvable-name warning logs.
  The E2E scenario that uses this fixture tests wire-level unknown OID handling -- those 2 entries will now test config-level unresolvable name handling instead (they never reach the wire).
- Final Names list for E2E-SIM in this fixture: ["e2e_gauge_test", "e2e_integer_test", "e2e_counter32_test", "e2e_counter64_test", "e2e_timeticks_test", "e2e_info_test", "e2e_ip_test", "e2e_unmapped_1", "e2e_unmapped_2"]

For all fixtures: Maintain exact YAML formatting, indentation, and structure. Only change the JSON content within the `devices.json: |` block.
  </action>
  <verify>
1. Grep for `"MetricPolls"` across all E2E fixture files -- zero hits
2. Grep for `"Oids"` across all E2E fixture files -- zero hits
3. Grep for `"e2e_unmapped_1"` in e2e-sim-unmapped-configmap.yaml -- present
4. Grep for `"e2e_gauge_test"` in device-added-configmap.yaml -- present
5. YAML syntax: `python -c "import yaml; yaml.safe_load(open('tests/e2e/fixtures/device-added-configmap.yaml'))"` for each fixture
  </verify>
  <done>All E2E fixture ConfigMaps use Polls/Names with metric names. The unmapped fixture uses intentionally unresolvable names "e2e_unmapped_1" and "e2e_unmapped_2" for the 2 extra entries.</done>
</task>

<task type="auto">
  <name>Task 3: Update E2E scenario 06 inline JSON and build verification</name>
  <files>
    tests/e2e/scenarios/06-poll-unreachable.sh
  </files>
  <action>
**E2E scenario 06:**
This script contains inline JSON for a device config with `"MetricPolls": [{"IntervalSeconds": 10, "Oids": ["1.3.6.1.4.1.47477.999.1.1.0"]}]`.

Replace with: `"Polls": [{"IntervalSeconds": 10, "Names": ["e2e_gauge_test"]}]`

Search the entire tests/e2e/scenarios/ directory for any other references to "MetricPolls" or "Oids" in .sh files and update them too.

**Build verification:**
After all config changes, run `dotnet build` to ensure the project still compiles (config files don't affect compilation, but this confirms no unintended changes).

Verify no remaining references to old format:
- Grep for "MetricPolls" across entire repo (excluding .planning/ and .git/) -- should only appear in git history, not in any active file
- Grep for `"Oids"` in JSON/YAML device config files -- should be zero (oidmap files use OIDs as keys, that's different and correct)
  </action>
  <verify>
1. `grep -r "MetricPolls" src/ tests/ deploy/ --include="*.cs" --include="*.json" --include="*.yaml" --include="*.sh"` -- zero hits
2. `grep -r '"Oids"' tests/e2e/ deploy/k8s/snmp-collector/simetra-devices.yaml src/SnmpCollector/config/devices.json` -- zero hits
3. `dotnet build` succeeds
4. Read scenario 06 and verify inline JSON uses Polls/Names
  </verify>
  <done>E2E scenario 06 inline JSON updated. Zero references to "MetricPolls" or "Oids" remain in active device configuration anywhere in the repository. Build passes.</done>
</task>

</tasks>

<verification>
1. Zero references to "MetricPolls" in any active file (source, config, fixtures, scenarios)
2. Zero references to "Oids" in device config files (JSON/YAML device sections)
3. All device configs use "Polls" and "Names" with human-readable metric names
4. Every metric name in config files exists as a value in the OID map
5. E2E unmapped fixture has intentionally unresolvable names
6. Build passes
</verification>

<success_criteria>
- Grep: zero "MetricPolls" hits in src/, tests/, deploy/
- Grep: zero "Oids" hits in device config files
- All config files syntactically valid (JSON, YAML)
- Entry counts in each poll group match original OID counts
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/31-human-name-device-config/31-03-SUMMARY.md`
</output>
