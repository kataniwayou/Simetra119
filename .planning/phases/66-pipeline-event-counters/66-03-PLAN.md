---
phase: 66-pipeline-event-counters
plan: 03
type: execute
wave: 2
depends_on: ["66-01"]
files_modified:
  - tests/e2e/scenarios/73-mcv05-rejected-unmapped-behavior.sh
  - tests/e2e/scenarios/74-mcv06-rejected-stays-zero-mapped.sh
  - tests/e2e/scenarios/75-mcv07-errors-stays-zero.sh
autonomous: true

must_haves:
  truths:
    - "snmp.event.rejected stays 0 during normal E2E operation (unmapped OIDs are not polled, and all polled OIDs pass validation)"
    - "snmp.event.rejected does NOT increment for mapped OIDs"
    - "snmp.event.errors stays 0 after a complete normal E2E run"
  artifacts:
    - path: "tests/e2e/scenarios/73-mcv05-rejected-unmapped-behavior.sh"
      provides: "MCV-05 documents that rejected only fires for validation failures, not unmapped OIDs"
      contains: "snmp_event_rejected_total"
    - path: "tests/e2e/scenarios/74-mcv06-rejected-stays-zero-mapped.sh"
      provides: "MCV-06 rejected stays 0 for mapped OIDs"
      contains: "assert_delta_eq"
    - path: "tests/e2e/scenarios/75-mcv07-errors-stays-zero.sh"
      provides: "MCV-07 errors stays 0 in normal run"
      contains: "snmp_event_errors_total"
  key_links:
    - from: "tests/e2e/scenarios/73-mcv05-rejected-unmapped-behavior.sh"
      to: "tests/e2e/lib/common.sh"
      via: "sourced assert_delta_eq"
      pattern: "assert_delta_eq"
    - from: "tests/e2e/scenarios/75-mcv07-errors-stays-zero.sh"
      to: "tests/e2e/lib/prometheus.sh"
      via: "snapshot_counter for errors metric"
      pattern: "snapshot_counter"
---

<objective>
Create E2E scenarios 73-75 verifying MCV-05 through MCV-07: rejected behavior for unmapped OIDs (document actual behavior), rejected stays 0 for mapped OIDs, and errors stays 0 in normal run.

Purpose: Complete the pipeline event counter verification suite. MCV-05 is critical: research found that `snmp.event.rejected` does NOT fire for unmapped OIDs (only for malformed OID format). The scenario documents this actual behavior. MCV-06 and MCV-07 confirm the zero-delta safety-net counters.
Output: Three new scenario files completing the Phase 66 E2E suite (scenarios 73-75).
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-pipeline-event-counters/66-RESEARCH.md
@.planning/phases/66-pipeline-event-counters/66-01-SUMMARY.md

@tests/e2e/scenarios/08-event-rejected.sh
@tests/e2e/scenarios/09-event-errors.sh
@tests/e2e/lib/common.sh
@tests/e2e/lib/prometheus.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scenario 73 (MCV-05: rejected behavior for unmapped OIDs)</name>
  <files>tests/e2e/scenarios/73-mcv05-rejected-unmapped-behavior.sh</files>
  <action>
Create `tests/e2e/scenarios/73-mcv05-rejected-unmapped-behavior.sh` (no shebang):

**Critical context from research:** `snmp.event.rejected` only fires on ValidationBehavior failures (malformed OID format or null DeviceName). Unmapped OIDs (not in oidmaps.json) are NOT rejected -- they resolve to MetricName="Unknown" and are handled normally. Furthermore, the 2 unmapped OIDs (.999.2.x) served by the simulator are NOT in the poll config, so they never enter the pipeline during normal operation.

**Test strategy:** Document and verify the ACTUAL behavior:
1. During normal E2E operation, rejected stays 0 because:
   - All polled OIDs are mapped (no Unknown resolution)
   - All OIDs have valid format (no ValidationBehavior rejection)
   - All events have DeviceName set (no null DeviceName)
2. Meanwhile, published and handled both increase (proving the pipeline is active)
3. This documents that "rejected" means "validation failure", NOT "unmapped OID"

```
# Scenario 73: MCV-05 — rejected counter behavior during normal operation
# IMPORTANT FINDING: snmp.event.rejected fires ONLY on ValidationBehavior failures:
#   1. OID string fails regex ^\d+(\.\d+){1,}$
#   2. DeviceName is null
# Unmapped OIDs (not in oidmaps.json) do NOT increment rejected -- they resolve
# to MetricName="Unknown" and are handled normally by OtelMetricHandler.
# The 2 unmapped OIDs (.999.2.x) are served by the simulator but NOT in the poll
# config, so they never enter the pipeline in normal operation.
#
# This scenario verifies: rejected stays 0 during normal operation while the
# pipeline is actively publishing and handling events.
SCENARIO_NAME="MCV-05: rejected stays 0 during normal operation (no validation failures)"
PUB_METRIC="snmp_event_published_total"
REJ_METRIC="snmp_event_rejected_total"
FILTER='device_name="E2E-SIM"'

PUB_BEFORE=$(snapshot_counter "$PUB_METRIC" "$FILTER")
REJ_BEFORE=$(snapshot_counter "$REJ_METRIC" "$FILTER")

# Wait for pipeline activity (published must move to prove pipeline is active)
poll_until 45 "$POLL_INTERVAL" "$PUB_METRIC" "$FILTER" "$PUB_BEFORE" || true
sleep 20

PUB_AFTER=$(snapshot_counter "$PUB_METRIC" "$FILTER")
REJ_AFTER=$(snapshot_counter "$REJ_METRIC" "$FILTER")

PUB_DELTA=$((PUB_AFTER - PUB_BEFORE))
REJ_DELTA=$((REJ_AFTER - REJ_BEFORE))

EVIDENCE="pub_delta=$PUB_DELTA rej_delta=$REJ_DELTA pub_before=$PUB_BEFORE pub_after=$PUB_AFTER rej_before=$REJ_BEFORE rej_after=$REJ_AFTER"

# Pipeline must be active (published moved) AND rejected must stay 0
if [ "$PUB_DELTA" -gt 0 ]; then
    assert_delta_eq "$REJ_DELTA" 0 "$SCENARIO_NAME" "$EVIDENCE"
else
    record_fail "$SCENARIO_NAME" "Pipeline inactive -- cannot verify rejected. $EVIDENCE"
fi
```
  </action>
  <verify>
- `ls tests/e2e/scenarios/73-mcv05-rejected-unmapped-behavior.sh` exists
- `head -1 tests/e2e/scenarios/73-mcv05-rejected-unmapped-behavior.sh` starts with `#` (not shebang)
- `grep 'assert_delta_eq' tests/e2e/scenarios/73-mcv05-rejected-unmapped-behavior.sh` returns match
- `grep 'ValidationBehavior' tests/e2e/scenarios/73-mcv05-rejected-unmapped-behavior.sh` returns match (documents the finding)
  </verify>
  <done>
Scenario 73 exists. It documents the research finding that rejected only fires for validation failures (not unmapped OIDs), and asserts rejected stays 0 during normal operation while the pipeline is active.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create scenarios 74-75 (MCV-06 rejected stays 0 for mapped, MCV-07 errors stays 0)</name>
  <files>
    tests/e2e/scenarios/74-mcv06-rejected-stays-zero-mapped.sh
    tests/e2e/scenarios/75-mcv07-errors-stays-zero.sh
  </files>
  <action>
**Scenario 74 — MCV-06: rejected does NOT increment for mapped OIDs**

Create `tests/e2e/scenarios/74-mcv06-rejected-stays-zero-mapped.sh` (no shebang):

Strategy: During normal operation, all polled E2E-SIM OIDs are mapped. Verify that rejected stays 0 AND that handled increases (proving mapped OIDs flow through to handler, not to rejection). This is the complement of scenario 73 -- scenario 73 proves rejected=0 globally, scenario 74 proves it specifically because all OIDs are mapped.

```
# Scenario 74: MCV-06 — rejected does NOT increment for mapped OIDs
# All E2E-SIM polled OIDs are mapped in oid_metric_map. Mapped OIDs pass
# ValidationBehavior (valid format, non-null DeviceName) and reach OtelMetricHandler.
# They should NEVER cause a rejected increment.
SCENARIO_NAME="MCV-06: rejected stays 0 while mapped OIDs are handled"
HDL_METRIC="snmp_event_handled_total"
REJ_METRIC="snmp_event_rejected_total"
FILTER='device_name="E2E-SIM"'

HDL_BEFORE=$(snapshot_counter "$HDL_METRIC" "$FILTER")
REJ_BEFORE=$(snapshot_counter "$REJ_METRIC" "$FILTER")

# Wait for handled to move (proves mapped OIDs are flowing through)
poll_until 45 "$POLL_INTERVAL" "$HDL_METRIC" "$FILTER" "$HDL_BEFORE" || true
sleep 20

HDL_AFTER=$(snapshot_counter "$HDL_METRIC" "$FILTER")
REJ_AFTER=$(snapshot_counter "$REJ_METRIC" "$FILTER")

HDL_DELTA=$((HDL_AFTER - HDL_BEFORE))
REJ_DELTA=$((REJ_AFTER - REJ_BEFORE))

EVIDENCE="hdl_delta=$HDL_DELTA rej_delta=$REJ_DELTA hdl_before=$HDL_BEFORE hdl_after=$HDL_AFTER rej_before=$REJ_BEFORE rej_after=$REJ_AFTER"

# Mapped OIDs must be handled (hdl_delta > 0) AND rejected must stay 0
if [ "$HDL_DELTA" -gt 0 ]; then
    assert_delta_eq "$REJ_DELTA" 0 "$SCENARIO_NAME" "$EVIDENCE"
else
    record_fail "$SCENARIO_NAME" "No handled events -- cannot verify rejection behavior. $EVIDENCE"
fi
```

**Scenario 75 — MCV-07: errors stays 0 during normal E2E run**

Create `tests/e2e/scenarios/75-mcv07-errors-stays-zero.sh` (no shebang):

Strategy: `snmp.event.errors` only increments on unhandled pipeline exceptions (ExceptionBehavior catch). During a normal E2E run, there should be zero exceptions. Verify the counter stays at 0 (or does not increment) while the pipeline is active.

```
# Scenario 75: MCV-07 — errors stays 0 during normal E2E run
# snmp.event.errors only increments when ExceptionBehavior catches an unhandled
# exception from downstream pipeline behaviors. In normal operation: 0 errors.
SCENARIO_NAME="MCV-07: errors stays 0 during normal E2E run"
PUB_METRIC="snmp_event_published_total"
ERR_METRIC="snmp_event_errors_total"
FILTER='device_name="E2E-SIM"'

PUB_BEFORE=$(snapshot_counter "$PUB_METRIC" "$FILTER")
ERR_BEFORE=$(snapshot_counter "$ERR_METRIC" "$FILTER")

# Wait for pipeline activity
poll_until 45 "$POLL_INTERVAL" "$PUB_METRIC" "$FILTER" "$PUB_BEFORE" || true
sleep 20

PUB_AFTER=$(snapshot_counter "$PUB_METRIC" "$FILTER")
ERR_AFTER=$(snapshot_counter "$ERR_METRIC" "$FILTER")

PUB_DELTA=$((PUB_AFTER - PUB_BEFORE))
ERR_DELTA=$((ERR_AFTER - ERR_BEFORE))

EVIDENCE="pub_delta=$PUB_DELTA err_delta=$ERR_DELTA pub_before=$PUB_BEFORE pub_after=$PUB_AFTER err_before=$ERR_BEFORE err_after=$ERR_AFTER"

# Pipeline must be active AND errors must stay 0
if [ "$PUB_DELTA" -gt 0 ]; then
    assert_delta_eq "$ERR_DELTA" 0 "$SCENARIO_NAME" "$EVIDENCE"
else
    record_fail "$SCENARIO_NAME" "Pipeline inactive -- cannot verify errors. $EVIDENCE"
fi
```

IMPORTANT: No shebang line on either file. They are sourced by run-all.sh.
  </action>
  <verify>
- `ls tests/e2e/scenarios/74-mcv06-rejected-stays-zero-mapped.sh tests/e2e/scenarios/75-mcv07-errors-stays-zero.sh` both exist
- `head -1 tests/e2e/scenarios/74-mcv06-rejected-stays-zero-mapped.sh` starts with `#` (not shebang)
- `head -1 tests/e2e/scenarios/75-mcv07-errors-stays-zero.sh` starts with `#` (not shebang)
- `grep 'assert_delta_eq' tests/e2e/scenarios/74-mcv06-rejected-stays-zero-mapped.sh` returns match
- `grep 'snmp_event_errors_total' tests/e2e/scenarios/75-mcv07-errors-stays-zero.sh` returns match
  </verify>
  <done>
Scenarios 74 and 75 exist. Scenario 74 proves rejected stays 0 while mapped OIDs are actively handled. Scenario 75 proves errors stays 0 during normal operation. No shebang. Syntax clean.
  </done>
</task>

</tasks>

<verification>
- All 3 scenario files exist in `tests/e2e/scenarios/`
- No scenario has a shebang line
- Scenario 73 documents the ValidationBehavior-only rejection finding
- Scenario 73 asserts rejected == 0 with proof of pipeline activity
- Scenario 74 asserts rejected == 0 with proof of handled activity (mapped OIDs flowing)
- Scenario 75 asserts errors == 0 with proof of pipeline activity
- All scenarios use `device_name="E2E-SIM"` filter
</verification>

<success_criteria>
- MCV-05: Scenario 73 documents and verifies that rejected stays 0 during normal operation (unmapped OIDs not polled, all polled OIDs pass validation)
- MCV-06: Scenario 74 proves rejected does NOT increment while mapped OIDs are being handled
- MCV-07: Scenario 75 proves errors stays 0 during a complete normal E2E run
</success_criteria>

<output>
After completion, create `.planning/phases/66-pipeline-event-counters/66-03-SUMMARY.md`
</output>
