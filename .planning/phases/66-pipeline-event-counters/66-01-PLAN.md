---
phase: 66-pipeline-event-counters
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/lib/common.sh
  - tests/e2e/lib/report.sh
autonomous: true

must_haves:
  truths:
    - "assert_delta_eq and assert_delta_ge helpers exist and work correctly in common.sh"
    - "New scenarios 69+ appear in the E2E Markdown report under a new category"
  artifacts:
    - path: "tests/e2e/lib/common.sh"
      provides: "assert_delta_eq and assert_delta_ge helper functions"
      contains: "assert_delta_eq"
    - path: "tests/e2e/lib/report.sh"
      provides: "Report category for Pipeline Counter Verification scenarios 69+"
      contains: "Pipeline Counter Verification"
  key_links:
    - from: "tests/e2e/lib/common.sh"
      to: "tests/e2e/scenarios/69-*.sh"
      via: "sourced functions"
      pattern: "assert_delta_(eq|ge)"
    - from: "tests/e2e/lib/report.sh"
      to: "SCENARIO_RESULTS array"
      via: "category index range covering 68+"
      pattern: "68\\|"
---

<objective>
Add assertion helpers and report category for Phase 66 pipeline counter verification scenarios.

Purpose: The existing E2E assertion library only has `assert_delta_gt`. Phase 66 scenarios need exact equality (`assert_delta_eq`) and greater-or-equal (`assert_delta_ge`) comparisons. The report system also needs a new category so scenarios 69+ appear in the Markdown report.
Output: Updated common.sh with two new assertion functions, updated report.sh with new category.
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-pipeline-event-counters/66-RESEARCH.md

@tests/e2e/lib/common.sh
@tests/e2e/lib/report.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add assert_delta_eq and assert_delta_ge to common.sh</name>
  <files>tests/e2e/lib/common.sh</files>
  <action>
Add two new assertion functions to `tests/e2e/lib/common.sh` in the Assertions section (after the existing `assert_exists` function, before the Summary section):

1. `assert_delta_eq` - Exact equality check:
   - Parameters: `delta` (int), `expected` (int), `scenario_name` (string), `evidence` (optional string)
   - Passes if `delta == expected`, fails otherwise
   - Evidence format matches existing pattern: `"delta=${delta} == expected=${expected}. ${evidence}"`

2. `assert_delta_ge` - Greater-or-equal check:
   - Parameters: `delta` (int), `minimum` (int), `scenario_name` (string), `evidence` (optional string)
   - Passes if `delta >= minimum`, fails otherwise
   - Evidence format: `"delta=${delta} >= minimum=${minimum}. ${evidence}"`

Both functions must follow the exact pattern of the existing `assert_delta_gt`:
- Use `record_pass` / `record_fail` (not echo directly)
- Use shell arithmetic `[ "$delta" -eq "$expected" ]` (not `(( ))`)
- Default evidence to empty string with `${4:-}`
  </action>
  <verify>
Grep `tests/e2e/lib/common.sh` for `assert_delta_eq` and `assert_delta_ge` -- both must exist.
Verify the functions use `record_pass`/`record_fail` (not raw echo).
Run `bash -n tests/e2e/lib/common.sh` to confirm no syntax errors.
  </verify>
  <done>
common.sh contains assert_delta_eq and assert_delta_ge following the same pattern as assert_delta_gt. No syntax errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add report category for Pipeline Counter Verification scenarios</name>
  <files>tests/e2e/lib/report.sh</files>
  <action>
Add a new category entry to the `_REPORT_CATEGORIES` array in `tests/e2e/lib/report.sh`.

Append after the last existing entry `"Progressive Snapshot Suite|52|67"`:
```
"Pipeline Counter Verification|68|75"
```

This covers scenarios 69-76 (0-indexed 68-75), giving room for the 7 new scenarios this phase adds (69 through 75).

Do NOT modify any existing category bounds -- adding at the end preserves all existing index mappings. The run-all.sh PSS hardcoded indices (52-67) remain unaffected.
  </action>
  <verify>
Grep `tests/e2e/lib/report.sh` for `Pipeline Counter Verification` -- must exist.
Verify the entry follows the existing format `"CategoryName|start|end"`.
Run `bash -n tests/e2e/lib/report.sh` to confirm no syntax errors.
  </verify>
  <done>
report.sh contains a "Pipeline Counter Verification|68|75" category entry. Syntax check passes.
  </done>
</task>

</tasks>

<verification>
- `bash -n tests/e2e/lib/common.sh` exits 0
- `bash -n tests/e2e/lib/report.sh` exits 0
- `grep -c 'assert_delta_eq\|assert_delta_ge' tests/e2e/lib/common.sh` returns 2 (one definition each)
- `grep 'Pipeline Counter Verification' tests/e2e/lib/report.sh` returns the category line
</verification>

<success_criteria>
- assert_delta_eq and assert_delta_ge exist in common.sh with correct signatures
- New report category covers scenarios 69+ (0-indexed 68+)
- No syntax errors in either file
</success_criteria>

<output>
After completion, create `.planning/phases/66-pipeline-event-counters/66-01-SUMMARY.md`
</output>
