---
phase: 89-observability-and-deployment-wiring
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - deploy/k8s/snmp-collector/deployment.yaml
autonomous: true

must_haves:
  truths:
    - "Two snmp-collector pods cannot land on the same Kubernetes node"
    - "PHYSICAL_HOSTNAME env var is injected from spec.nodeName via Downward API"
  artifacts:
    - path: "deploy/k8s/snmp-collector/deployment.yaml"
      provides: "Pod anti-affinity and Downward API env vars"
      contains: "requiredDuringSchedulingIgnoredDuringExecution"
  key_links:
    - from: "deployment.yaml pod spec"
      to: "affinity.podAntiAffinity"
      via: "requiredDuringSchedulingIgnoredDuringExecution with kubernetes.io/hostname"
      pattern: "topologyKey.*kubernetes.io/hostname"
    - from: "deployment.yaml env"
      to: "spec.nodeName"
      via: "Downward API fieldRef"
      pattern: "fieldPath.*spec.nodeName"
---

<objective>
Add pod anti-affinity to the deployment manifest so each Kubernetes node runs at most one snmp-collector pod, and verify the PHYSICAL_HOSTNAME Downward API env var is already present.

Purpose: DEP-01 requires one-pod-per-node topology for the preferred leader election to work correctly across sites. DEP-02 requires PHYSICAL_HOSTNAME from spec.nodeName — this already exists in the manifest but must be verified, not duplicated.

Output: Updated deployment.yaml with anti-affinity rule.
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89-observability-and-deployment-wiring/89-CONTEXT.md
@deploy/k8s/snmp-collector/deployment.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pod anti-affinity and verify PHYSICAL_HOSTNAME</name>
  <files>deploy/k8s/snmp-collector/deployment.yaml</files>
  <action>
1. ADD pod anti-affinity to the pod spec (under `spec.template.spec`, at the same level as `serviceAccountName` and `containers`). Place it BEFORE `containers`:

```yaml
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
          - labelSelector:
              matchLabels:
                app: snmp-collector
            topologyKey: kubernetes.io/hostname
```

This uses `requiredDuringSchedulingIgnoredDuringExecution` (hard anti-affinity, not preferred) so the scheduler REFUSES to place two snmp-collector pods on the same node. The label selector matches `app: snmp-collector` which is already in `spec.selector.matchLabels` and `spec.template.metadata.labels`.

2. VERIFY that PHYSICAL_HOSTNAME already exists in the env section with:
```yaml
        - name: PHYSICAL_HOSTNAME
          valueFrom:
            fieldRef:
              fieldPath: spec.nodeName
```
It is already present (lines 38-41 of the current manifest). Do NOT add it again. Do NOT add POD_NAMESPACE — it was explicitly dropped per CONTEXT.md.

3. Do NOT change any other part of the manifest: no image changes, no resource changes, no probe changes, no volume changes.
  </action>
  <verify>
Validate YAML syntax: `python -c "import yaml; yaml.safe_load(open('deploy/k8s/snmp-collector/deployment.yaml'))"` or equivalent YAML lint.
Grep confirms anti-affinity: `grep -A5 "podAntiAffinity" deploy/k8s/snmp-collector/deployment.yaml` shows `requiredDuringSchedulingIgnoredDuringExecution` with `kubernetes.io/hostname`.
Grep confirms PHYSICAL_HOSTNAME: `grep "PHYSICAL_HOSTNAME" deploy/k8s/snmp-collector/deployment.yaml` returns exactly one match.
Grep confirms NO POD_NAMESPACE: `grep "POD_NAMESPACE" deploy/k8s/snmp-collector/deployment.yaml` returns zero matches.
  </verify>
  <done>
Pod anti-affinity rule enforces one-pod-per-node with requiredDuringSchedulingIgnoredDuringExecution.
PHYSICAL_HOSTNAME env var confirmed present (not duplicated).
POD_NAMESPACE not present (dropped per requirements).
YAML is valid and no other manifest sections were modified.
  </done>
</task>

</tasks>

<verification>
1. YAML parses without errors
2. Anti-affinity block present with correct topology key and label selector
3. PHYSICAL_HOSTNAME present exactly once
4. POD_NAMESPACE absent
5. No other changes to the manifest beyond the affinity block
</verification>

<success_criteria>
Deployment manifest enforces one-pod-per-node topology via hard pod anti-affinity and correctly injects PHYSICAL_HOSTNAME from the Downward API. The preferred-election feature will activate correctly in a multi-node cluster.
</success_criteria>

<output>
After completion, create `.planning/phases/89-observability-and-deployment-wiring/89-02-SUMMARY.md`
</output>
