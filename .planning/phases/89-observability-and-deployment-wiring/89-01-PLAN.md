---
phase: 89-observability-and-deployment-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/SnmpCollector/Telemetry/K8sLeaseElection.cs
  - src/SnmpCollector/Jobs/PreferredHeartbeatJob.cs
autonomous: true

must_haves:
  truths:
    - "Backing off decision emits a structured INFO log with delay duration"
    - "Competing normally decision emits a structured INFO log (stamp stale or feature off)"
    - "Yielding decision emits a structured INFO log (already exists at correct level)"
    - "Heartbeat stamping started emits a structured INFO log on first write after readiness"
  artifacts:
    - path: "src/SnmpCollector/Telemetry/K8sLeaseElection.cs"
      provides: "Gate 1 decision logging at INFO level"
      contains: "LogInformation"
    - path: "src/SnmpCollector/Jobs/PreferredHeartbeatJob.cs"
      provides: "Stamping started log at INFO level"
      contains: "LogInformation"
  key_links:
    - from: "K8sLeaseElection.ExecuteAsync"
      to: "Gate 1 backoff block"
      via: "LogInformation replacing LogDebug"
      pattern: "LogInformation.*Preferred pod is alive"
    - from: "K8sLeaseElection.ExecuteAsync"
      to: "Post-Gate-1 election attempt"
      via: "LogInformation for competing normally"
      pattern: "LogInformation.*competing normally"
    - from: "PreferredHeartbeatJob.Execute"
      to: "Writer path guard"
      via: "LogInformation for stamping started"
      pattern: "LogInformation.*stamping"
---

<objective>
Upgrade preferred-election decision-point logging to structured INFO level so every election decision is visible in production logs without enabling Debug verbosity.

Purpose: OBS-01 requires operators to see backing off, competing normally, yielding, and stamping started decisions at INFO level. Currently the backoff log is Debug (invisible in production) and the "competing normally" and "stamping started" decisions have no log at all.

Output: Two modified .cs files with 4 INFO-level decision-point logs.
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89-observability-and-deployment-wiring/89-CONTEXT.md
@src/SnmpCollector/Telemetry/K8sLeaseElection.cs
@src/SnmpCollector/Jobs/PreferredHeartbeatJob.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade Gate 1 decision logs in K8sLeaseElection</name>
  <files>src/SnmpCollector/Telemetry/K8sLeaseElection.cs</files>
  <action>
In ExecuteAsync, modify the Gate 1 block (the `if (!_preferredLeaderService.IsPreferredPod && _stampReader.IsPreferredStampFresh)` block):

1. UPGRADE the existing LogDebug to LogInformation (line ~166). Keep the same message and structured property:
   `_logger.LogInformation("Preferred pod is alive — delaying election attempt for {DurationSeconds}s", _leaseOptions.DurationSeconds);`

2. ADD a "competing normally" LogInformation AFTER the Gate 1 if-block, BEFORE the `using var innerCts` line (line ~173). This fires when Gate 1 was not triggered (stamp stale, feature off, or this IS the preferred pod):
   `_logger.LogDebug("Competing normally — preferred stamp not fresh or feature not configured");`

   WAIT — per CONTEXT.md, only the 4 decision points get INFO. "Competing normally" IS one of the 4 decision points (OBS-01). But this fires every loop iteration when competing, which could be noisy. Use LogInformation but include enough context:
   `_logger.LogInformation("Competing normally for lease {LeaseName} — preferred stamp is not fresh", _leaseOptions.Name);`

   NOTE: This log only applies to non-preferred pods when stamp is stale. For the preferred pod itself OR when feature is off, the message should reflect that. To keep it simple and avoid branching log messages, log this ONLY for the non-preferred + stale case. The preferred pod path and feature-off path are already covered by the existing "Acquired leadership" log. So add this log as an else-if:

   ```csharp
   if (!_preferredLeaderService.IsPreferredPod && _stampReader.IsPreferredStampFresh)
   {
       _logger.LogInformation(
           "Preferred pod is alive — delaying election attempt for {DurationSeconds}s",
           _leaseOptions.DurationSeconds);
       await Task.Delay(TimeSpan.FromSeconds(_leaseOptions.DurationSeconds), stoppingToken);
       continue;
   }
   else if (!_preferredLeaderService.IsPreferredPod && !_stampReader.IsPreferredStampFresh)
   {
       _logger.LogInformation(
           "Competing normally for lease {LeaseName} — preferred stamp is not fresh",
           _leaseOptions.Name);
   }
   ```

   This means: preferred pods log nothing extra here (they just compete), non-preferred pods log which decision they took. The existing "Acquired leadership" log covers the outcome.

Do NOT touch the OnStartedLeading, OnStoppedLeading, or OnNewLeader handlers — those are already correct.
Do NOT touch StopAsync — that is shutdown behavior, not a decision point.
Do NOT add event IDs, prefixes, or categories — follow existing K8sLeaseElection style.
  </action>
  <verify>
Run `dotnet build src/SnmpCollector/SnmpCollector.csproj` — must compile with zero errors.
Run `dotnet test tests/SnmpCollector.Tests/SnmpCollector.Tests.csproj` — all existing tests must pass (524+).
Visually confirm: grep for LogDebug in the Gate 1 block should return zero results (all upgraded to LogInformation).
  </verify>
  <done>
Gate 1 backoff decision: LogInformation with DurationSeconds.
Competing normally decision: LogInformation with LeaseName (non-preferred + stale only).
No LogDebug remaining in the Gate 1 decision path.
All existing tests pass unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add stamping-started log in PreferredHeartbeatJob</name>
  <files>src/SnmpCollector/Jobs/PreferredHeartbeatJob.cs</files>
  <action>
Add a one-time "stamping started" INFO log that fires the FIRST time the writer path executes after readiness. This should NOT fire on every heartbeat tick — only once.

1. Add a private field to track first-stamp:
   `private bool _hasLoggedStampingStarted;`

2. Inside the writer guard in Execute (the `if (_preferredLeaderService.IsPreferredPod && _isSchedulerReady)` block, BEFORE the call to WriteHeartbeatLeaseAsync), add:
   ```csharp
   if (!_hasLoggedStampingStarted)
   {
       _logger.LogInformation(
           "Heartbeat stamping started for lease {LeaseName} — preferred pod is ready",
           $"{_leaseOptions.Name}-preferred");
       _hasLoggedStampingStarted = true;
   }
   ```

3. The yield log (line ~296) is already LogInformation with a good message: "Voluntary yield: deleted leadership lease {LeaseName} — preferred pod recovered". This satisfies the "yielding" decision point. Do NOT modify it.

Do NOT add logs to WriteHeartbeatLeaseAsync itself — that fires every tick.
Do NOT add logs to ReadAndUpdateStampFreshnessAsync — that is reader path, not a decision point.
Do NOT change the existing warning-level logs for transient errors — those are operational, not decision points.

NOTE: PreferredHeartbeatJob is a Quartz IJob, so instances may be created per-execution depending on DI scope. Check if the job is registered as singleton or transient. If transient, the `_hasLoggedStampingStarted` field would reset each tick. In that case, move the flag to PreferredLeaderService (which IS singleton) — add a `bool HasLoggedStampingStarted` property there. But FIRST check the DI registration in ServiceCollectionExtensions.cs to determine the job's lifetime.

If the job is transient (Quartz default), use a static field instead:
   `private static bool _hasLoggedStampingStarted;`
This is safe because only one preferred pod runs the writer path.
  </action>
  <verify>
Run `dotnet build src/SnmpCollector/SnmpCollector.csproj` — must compile with zero errors.
Run `dotnet test tests/SnmpCollector.Tests/SnmpCollector.Tests.csproj` — all existing tests must pass.
Grep PreferredHeartbeatJob.cs for "stamping started" — should find exactly one LogInformation call.
  </verify>
  <done>
Heartbeat stamping started: one-time LogInformation on first writer-path execution after readiness.
Yield log: already LogInformation (unchanged, verified present).
All 4 decision points now have INFO-level structured logs.
All existing tests pass unchanged.
  </done>
</task>

</tasks>

<verification>
1. `dotnet build src/SnmpCollector/SnmpCollector.csproj` compiles cleanly
2. `dotnet test tests/SnmpCollector.Tests/SnmpCollector.Tests.csproj` — all 524+ tests pass
3. Grep confirms 4 INFO-level decision-point logs exist:
   - K8sLeaseElection.cs: "Preferred pod is alive — delaying" (LogInformation)
   - K8sLeaseElection.cs: "Competing normally" (LogInformation)
   - PreferredHeartbeatJob.cs: "Heartbeat stamping started" (LogInformation)
   - PreferredHeartbeatJob.cs: "Voluntary yield: deleted leadership lease" (LogInformation, existing)
4. No LogDebug in Gate 1 decision path
</verification>

<success_criteria>
All 4 preferred-election decision points emit structured INFO logs with descriptive messages and structured {PropertyName} fields, following existing codebase style. All existing tests pass without modification.
</success_criteria>

<output>
After completion, create `.planning/phases/89-observability-and-deployment-wiring/89-01-SUMMARY.md`
</output>
