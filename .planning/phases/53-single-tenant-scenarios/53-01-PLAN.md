---
phase: 53-single-tenant-scenarios
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - simulators/e2e-sim/e2e_simulator.py
  - tests/e2e/fixtures/tenant-cfg01-suppression.yaml
  - tests/e2e/lib/report.sh
autonomous: true

must_haves:
  truths:
    - "Simulator exposes a 'healthy' scenario where resolved and evaluate OIDs are all in-range"
    - "A suppression-tuned tenant fixture exists with SuppressionWindowSeconds=30"
    - "Report categories cover the new Snapshot Evaluation scenarios 29-33"
  artifacts:
    - path: "simulators/e2e-sim/e2e_simulator.py"
      provides: "healthy scenario in SCENARIOS dict"
      contains: '"healthy"'
    - path: "tests/e2e/fixtures/tenant-cfg01-suppression.yaml"
      provides: "Tenant fixture with SuppressionWindowSeconds 30"
      contains: "SuppressionWindowSeconds"
    - path: "tests/e2e/lib/report.sh"
      provides: "Snapshot Evaluation category"
      contains: "Snapshot Evaluation"
  key_links:
    - from: "simulators/e2e-sim/e2e_simulator.py"
      to: "tests/e2e/scenarios/29-sts-01-healthy.sh"
      via: "sim_set_scenario healthy"
      pattern: "healthy"
---

<objective>
Add the "healthy" simulator scenario, create the suppression-tuned tenant fixture, and extend report.sh with the Snapshot Evaluation category.

Purpose: These are prerequisites that unblock all five STS scenario scripts. No scenario can run without the healthy simulator scenario (STS-01) or the suppression fixture (STS-04), and the report needs the new category to display results.

Output: Modified e2e_simulator.py, new tenant-cfg01-suppression.yaml, updated report.sh
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-single-tenant-scenarios/53-RESEARCH.md

@simulators/e2e-sim/e2e_simulator.py
@tests/e2e/fixtures/tenant-cfg01-single.yaml
@tests/e2e/lib/report.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add "healthy" simulator scenario and suppression fixture</name>
  <files>
    simulators/e2e-sim/e2e_simulator.py
    tests/e2e/fixtures/tenant-cfg01-suppression.yaml
  </files>
  <action>
1. In `e2e_simulator.py`, add a `"healthy"` entry to the `SCENARIOS` dict (after `"command_trigger"`):

```python
"healthy":          _make_scenario({
                        f"{E2E_PREFIX}.4.1": 5,    # e2e_port_utilization = 5 (< Max:80)
                        f"{E2E_PREFIX}.4.2": 2,    # e2e_channel_state = 2 (>= Min:1.0)
                        f"{E2E_PREFIX}.4.3": 2,    # e2e_bypass_status = 2 (>= Min:1.0)
                    }),
```

This produces: resolved NOT all violated (both >= 1.0) -> passes Tier 2 -> evaluate NOT violated (5 < 80) -> Tier 3 Healthy. Update the module docstring "Scenarios:" count if one exists.

2. Create `tests/e2e/fixtures/tenant-cfg01-suppression.yaml` as a copy of `tenant-cfg01-single.yaml` with these changes:
   - `"Id": "e2e-tenant-A-supp"` (distinct tenant ID to avoid suppression cache bleed)
   - `"SuppressionWindowSeconds": 30` (must be > SnapshotJob interval of 15s so suppression is observable)
   - Everything else identical (same OIDs, same thresholds, same commands)
  </action>
  <verify>
    - `grep '"healthy"' simulators/e2e-sim/e2e_simulator.py` finds the new scenario
    - `grep 'SuppressionWindowSeconds.*30' tests/e2e/fixtures/tenant-cfg01-suppression.yaml` matches
    - `grep 'e2e-tenant-A-supp' tests/e2e/fixtures/tenant-cfg01-suppression.yaml` matches
  </verify>
  <done>
    - SCENARIOS dict contains "healthy" with .4.1=5, .4.2=2, .4.3=2
    - tenant-cfg01-suppression.yaml exists with SuppressionWindowSeconds=30 and distinct tenant ID
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend report.sh with Snapshot Evaluation category</name>
  <files>
    tests/e2e/lib/report.sh
  </files>
  <action>
Add a new entry to the `_REPORT_CATEGORIES` array in `report.sh`:

```bash
"Snapshot Evaluation|28|32"
```

Append this AFTER the existing `"Tenant Vector|33|36"` entry. The existing Tenant Vector category uses indices 33-36 (0-based) which covers scenario files up to index 36. With 28 existing scenarios (indices 0-27), STS-01 through STS-05 will be indices 28-32.

IMPORTANT: The existing `"Tenant Vector|33|36"` range looks wrong -- there are only 28 scenarios (indices 0-27), so 33-36 are out of range. The report.sh `generate_report` function already handles this gracefully (skips if `start_idx >= total`). Add the new category entry at the end. The correct 0-based indices for scenarios 29-33 are 28-32.
  </action>
  <verify>
    - `grep 'Snapshot Evaluation' tests/e2e/lib/report.sh` matches
    - The `_REPORT_CATEGORIES` array contains exactly 7 entries (6 existing + 1 new)
  </verify>
  <done>
    - report.sh _REPORT_CATEGORIES includes "Snapshot Evaluation|28|32"
    - Running the report with scenarios 29-33 will categorize them under "Snapshot Evaluation"
  </done>
</task>

</tasks>

<verification>
- `grep -c '"healthy"\|"default"\|"threshold_breach"\|"threshold_clear"\|"bypass_active"\|"stale"\|"command_trigger"' simulators/e2e-sim/e2e_simulator.py` returns 7 (all 7 scenarios present)
- tenant-cfg01-suppression.yaml is valid YAML (same structure as tenant-cfg01-single.yaml)
- report.sh has no syntax errors: `bash -n tests/e2e/lib/report.sh`
</verification>

<success_criteria>
- Simulator has 7 scenarios (6 existing + healthy)
- Suppression fixture exists with 30s window and distinct tenant ID
- Report category covers scenario indices 28-32
</success_criteria>

<output>
After completion, create `.planning/phases/53-single-tenant-scenarios/53-01-SUMMARY.md`
</output>
