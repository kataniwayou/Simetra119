---
phase: 53-single-tenant-scenarios
plan: 02
type: execute
wave: 2
depends_on: ["53-01"]
files_modified:
  - tests/e2e/scenarios/29-sts-01-healthy.sh
  - tests/e2e/scenarios/30-sts-02-evaluate-violated.sh
  - tests/e2e/scenarios/31-sts-03-resolved-gate.sh
autonomous: true

must_haves:
  truths:
    - "STS-01: after stabilization with all OIDs in-range, pod logs contain tier=3 log and command counter delta is zero"
    - "STS-02: after switching to command_trigger, pod logs contain tier=4 log and command_sent delta >= 1"
    - "STS-03: with default scenario (all resolved violated), pod logs contain tier=2 ConfirmedBad log and command counter delta is zero"
  artifacts:
    - path: "tests/e2e/scenarios/29-sts-01-healthy.sh"
      provides: "Healthy baseline scenario script"
      contains: "tier=3"
    - path: "tests/e2e/scenarios/30-sts-02-evaluate-violated.sh"
      provides: "Evaluate violated scenario script"
      contains: "tier=4"
    - path: "tests/e2e/scenarios/31-sts-03-resolved-gate.sh"
      provides: "Resolved gate scenario script"
      contains: "tier=2"
  key_links:
    - from: "tests/e2e/scenarios/29-sts-01-healthy.sh"
      to: "simulators/e2e-sim/e2e_simulator.py"
      via: "sim_set_scenario healthy"
      pattern: "sim_set_scenario healthy"
    - from: "tests/e2e/scenarios/30-sts-02-evaluate-violated.sh"
      to: "simulators/e2e-sim/e2e_simulator.py"
      via: "sim_set_scenario command_trigger"
      pattern: "sim_set_scenario command_trigger"
    - from: "tests/e2e/scenarios/29-sts-01-healthy.sh"
      to: "tests/e2e/lib/prometheus.sh"
      via: "snapshot_counter for delta assertion"
      pattern: "snapshot_counter.*snmp_command_sent_total"
---

<objective>
Create the three straightforward single-tenant scenario scripts: STS-01 (healthy baseline), STS-02 (evaluate violated with command dispatch), and STS-03 (resolved gate block).

Purpose: These three scripts validate Tier 3 (healthy no-action), Tier 4 (command dispatch), and Tier 2 (ConfirmedBad gate) of the 4-tier evaluation tree.

Output: Three executable bash scenario scripts following established E2E patterns.
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-single-tenant-scenarios/53-RESEARCH.md

@tests/e2e/scenarios/28-tenantvector-routing.sh
@tests/e2e/lib/sim.sh
@tests/e2e/lib/prometheus.sh
@tests/e2e/lib/common.sh
@tests/e2e/lib/kubectl.sh
@tests/e2e/fixtures/tenant-cfg01-single.yaml
@src/SnmpCollector/Jobs/SnapshotJob.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: STS-01 healthy baseline script</name>
  <files>tests/e2e/scenarios/29-sts-01-healthy.sh</files>
  <action>
Create `tests/e2e/scenarios/29-sts-01-healthy.sh` following the standard scenario skeleton.

**Script structure (sourced by run-all.sh, no shebang needed):**

```
# Scenario 29: STS-01 Healthy baseline -- tier=3 no-action, zero command counters
```

**Steps:**

1. **Setup:** Save current tenants ConfigMap, apply tenant-cfg01-single.yaml, poll_until_log for "Tenant vector reload complete" or "TenantVectorWatcher initial load complete" (timeout 60s, interval 5s, since 30s). Use `FIXTURES_DIR` pattern from scenario 28.

2. **Set healthy scenario:** `sim_set_scenario healthy`

3. **Baseline counters:** Snapshot `snmp_command_sent_total` with filter `device_name="E2E-SIM"` and `snmp_command_suppressed_total` with filter `device_name="e2e-tenant-A"` BEFORE the assertion window.

4. **Wait for tier=3 log:** `poll_until_log 90 5 "tier=3 — not all evaluate metrics violated" 60`
   - The em-dash in the log is a real UTF-8 em-dash (U+2014), matching SnapshotJob.cs exactly.

5. **Sub-scenario 29a (log assertion):** If poll_until_log succeeds, record_pass for "STS-01: Healthy tier=3 log detected". If timeout, record_fail.

6. **Sub-scenario 29b (counter assertion):** Snapshot counters AFTER. Compute delta for command_sent and command_suppressed. Assert BOTH deltas == 0. Use direct arithmetic `[ "$DELTA" -eq 0 ]` with record_pass/record_fail (NOT assert_delta_gt, which tests > threshold). Include both counter values in evidence string.

7. **Cleanup:** `reset_scenario`, restore ConfigMap from saved snapshot.

**Important details:**
- Use `save_configmap "simetra-tenants" "simetra" "$FIXTURES_DIR/.original-tenants-configmap.yaml"` at the start
- Evidence strings must include "log=tier3_found" and counter values for the report
- The grep pattern for poll_until_log must use the literal em-dash from SnapshotJob.cs: `"tier=3 — not all evaluate metrics violated"`
  </action>
  <verify>
    - `bash -n tests/e2e/scenarios/29-sts-01-healthy.sh` passes (no syntax errors)
    - Script contains `sim_set_scenario healthy`
    - Script contains `poll_until_log` with tier=3 pattern
    - Script contains `snapshot_counter` for both sent and suppressed metrics
    - Script contains cleanup (reset_scenario + restore)
  </verify>
  <done>
    - STS-01 script asserts BOTH tier=3 log presence AND command counter deltas == 0
    - Script follows established save/apply/poll/assert/cleanup pattern
  </done>
</task>

<task type="auto">
  <name>Task 2: STS-02 evaluate violated and STS-03 resolved gate scripts</name>
  <files>
    tests/e2e/scenarios/30-sts-02-evaluate-violated.sh
    tests/e2e/scenarios/31-sts-03-resolved-gate.sh
  </files>
  <action>
**STS-02: `tests/e2e/scenarios/30-sts-02-evaluate-violated.sh`**

```
# Scenario 30: STS-02 Evaluate violated -- tier=4 command dispatch, sent counter increments
```

Steps:
1. Setup: save ConfigMap, apply tenant-cfg01-single.yaml, poll_until_log for reload (timeout 60s).
2. `sim_set_scenario command_trigger` (sets .4.1=90 violated, .4.2=2 and .4.3=2 resolved in-range)
3. Baseline: snapshot `snmp_command_sent_total{device_name="E2E-SIM"}` BEFORE.
4. Wait for tier=4 log: `poll_until_log 90 5 "tier=4 — commands enqueued" 60`
   - TimeSeriesSize=3 means 3 poll cycles (~30s) to fill series. The 90s timeout accommodates this.
5. Sub-scenario 30a (log assertion): record_pass/record_fail based on poll_until_log result.
6. Sub-scenario 30b (counter assertion): Snapshot AFTER. Compute delta. Use `assert_delta_gt "$DELTA" 0` for "STS-02: Command sent counter incremented".
7. Cleanup: reset_scenario, restore ConfigMap.

**STS-03: `tests/e2e/scenarios/31-sts-03-resolved-gate.sh`**

```
# Scenario 31: STS-03 Resolved gate -- tier=2 ConfirmedBad, zero command counters
```

Steps:
1. Setup: save ConfigMap, apply tenant-cfg01-single.yaml, poll_until_log for reload (timeout 60s).
2. `reset_scenario` (explicit -- default scenario has .4.1=0, .4.2=0, .4.3=0 which means ALL resolved metrics violated).
   - Actually use `sim_set_scenario default` explicitly for clarity, even though reset_scenario does the same thing.
3. Baseline: snapshot `snmp_command_sent_total{device_name="E2E-SIM"}` and `snmp_command_suppressed_total{device_name="e2e-tenant-A"}` BEFORE.
4. Wait for tier=2 ConfirmedBad log: `poll_until_log 90 5 "tier=2 — all resolved violated, device confirmed bad" 60`
5. Sub-scenario 31a (log assertion): record_pass/record_fail based on poll_until_log result.
6. Sub-scenario 31b (counter assertion): Snapshot AFTER. Assert BOTH deltas == 0 (no commands sent while device is ConfirmedBad).
7. Sub-scenario 31c (negative assertion): After tier=2 log found, check that tier=4 log does NOT appear. Use a short kubectl logs grep (NOT poll_until_log which would just timeout). Extract recent pod logs (since 60s) and grep for "tier=4"; if found, record_fail "STS-03: Unexpected tier=4 log while resolved gate active".
8. Cleanup: reset_scenario, restore ConfigMap.

**Shared pattern notes:**
- Both scripts use the same FIXTURES_DIR derivation as scenario 28
- Both scripts use `save_configmap` at start, `restore_configmap` at end
- Both scripts use UTF-8 em-dash in grep patterns (copy from SnapshotJob.cs)
- Evidence strings format: "log=tierN_found sent_delta=N suppressed_delta=N"
  </action>
  <verify>
    - `bash -n tests/e2e/scenarios/30-sts-02-evaluate-violated.sh` passes
    - `bash -n tests/e2e/scenarios/31-sts-03-resolved-gate.sh` passes
    - STS-02 contains `sim_set_scenario command_trigger` and `tier=4` assertion
    - STS-03 contains `sim_set_scenario default` and `tier=2` assertion
    - STS-03 contains negative tier=4 assertion
    - Both contain cleanup (reset_scenario + restore)
  </verify>
  <done>
    - STS-02 asserts BOTH tier=4 log AND command_sent delta > 0
    - STS-03 asserts BOTH tier=2 ConfirmedBad log AND command counter deltas == 0, plus absence of tier=4
    - Both follow established pattern with proper cleanup
  </done>
</task>

</tasks>

<verification>
- All three scripts pass `bash -n` syntax check
- Each script has both log AND counter assertions (CONTEXT.md mandate)
- Each script has proper setup (save ConfigMap, apply fixture, poll for reload) and cleanup (reset sim, restore ConfigMap)
- Scenario numbering is sequential: 29, 30, 31
- No hardcoded sleeps for waiting on logs -- all use poll_until_log
</verification>

<success_criteria>
- STS-01: asserts tier=3 log + zero command counters
- STS-02: asserts tier=4 log + command_sent delta >= 1
- STS-03: asserts tier=2 ConfirmedBad log + zero command counters + absence of tier=4
- All scripts follow save/apply/poll/assert/cleanup pattern
</success_criteria>

<output>
After completion, create `.planning/phases/53-single-tenant-scenarios/53-02-SUMMARY.md`
</output>
