---
phase: 57-deterministic-watcher-startup-order
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/SnmpCollector/Services/OidMapWatcherService.cs
  - src/SnmpCollector/Services/DeviceWatcherService.cs
  - src/SnmpCollector/Services/CommandMapWatcherService.cs
  - src/SnmpCollector/Services/TenantVectorWatcherService.cs
autonomous: true

must_haves:
  truths:
    - "Each watcher exposes a public InitialLoadAsync method that performs the ConfigMap read and applies configuration"
    - "ExecuteAsync on each watcher contains ONLY the watch loop -- no initial-load try/catch block"
    - "InitialLoadAsync lets exceptions propagate (crash-the-pod semantics, no catch)"
    - "InitialLoadAsync logs an INFO line with entry count on success"
    - "All existing unit tests pass without modification"
  artifacts:
    - path: "src/SnmpCollector/Services/OidMapWatcherService.cs"
      provides: "public InitialLoadAsync method, watch-loop-only ExecuteAsync"
      contains: "public async Task InitialLoadAsync"
    - path: "src/SnmpCollector/Services/DeviceWatcherService.cs"
      provides: "public InitialLoadAsync method, watch-loop-only ExecuteAsync"
      contains: "public async Task InitialLoadAsync"
    - path: "src/SnmpCollector/Services/CommandMapWatcherService.cs"
      provides: "public InitialLoadAsync method, watch-loop-only ExecuteAsync"
      contains: "public async Task InitialLoadAsync"
    - path: "src/SnmpCollector/Services/TenantVectorWatcherService.cs"
      provides: "public InitialLoadAsync method, watch-loop-only ExecuteAsync"
      contains: "public async Task InitialLoadAsync"
  key_links:
    - from: "InitialLoadAsync"
      to: "LoadFromConfigMapAsync"
      via: "direct call"
      pattern: "LoadFromConfigMapAsync"
---

<objective>
Extract `InitialLoadAsync` from all 4 ConfigMap watchers so Program.cs can call them sequentially during startup.

Purpose: Decouple initial config loading from the BackgroundService watch loop, enabling deterministic startup ordering in Plan 02.
Output: All 4 watcher services with public `InitialLoadAsync` methods and watch-loop-only `ExecuteAsync` methods.
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-deterministic-watcher-startup-order/57-CONTEXT.md
@.planning/phases/57-deterministic-watcher-startup-order/57-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract InitialLoadAsync from all 4 watchers</name>
  <files>
    src/SnmpCollector/Services/OidMapWatcherService.cs
    src/SnmpCollector/Services/DeviceWatcherService.cs
    src/SnmpCollector/Services/CommandMapWatcherService.cs
    src/SnmpCollector/Services/TenantVectorWatcherService.cs
  </files>
  <action>
For each of the 4 watcher services, apply the identical refactoring pattern:

1. **Add a public `InitialLoadAsync` method** that:
   - Signature: `public async Task<int> InitialLoadAsync(CancellationToken ct)`
   - Calls `await LoadFromConfigMapAsync(ct).ConfigureAwait(false)`
   - Logs an INFO line on success with the entry count
   - Returns the entry count as `int`
   - Does NOT catch exceptions (crash-the-pod semantics per CONTEXT.md)
   - Has XML doc comment explaining it's called by Program.cs during startup sequencing

2. **Strip the initial-load try/catch block from `ExecuteAsync`** so it contains ONLY the watch loop (`while (!stoppingToken.IsCancellationRequested) { ... }`). Delete the entire block from "Initial load: read current ConfigMap state" through the catch block, leaving only the watch loop.

**Per-watcher entry count details:**

- **OidMapWatcherService**: Log `"OidMapWatcher initial load complete ({EntryCount} entries)"` using `_oidMapService.EntryCount`. Return `_oidMapService.EntryCount`.
- **DeviceWatcherService**: Log `"DeviceWatcher initial load complete ({DeviceCount} devices)"` using `_deviceRegistry.AllDevices.Count`. Return `_deviceRegistry.AllDevices.Count`.
- **CommandMapWatcherService**: Log `"CommandMapWatcher initial load complete ({EntryCount} entries)"` using `_commandMapService.Count`. Return `_commandMapService.Count`.
- **TenantVectorWatcherService**: Log `"TenantVectorWatcher initial load complete ({TenantCount} tenants)"` using `_registry.TenantCount`. Return `_registry.TenantCount`.

**What NOT to do:**
- Do NOT change `LoadFromConfigMapAsync` (keep it private, unchanged)
- Do NOT change `HandleConfigMapChangedAsync` (keep it private, unchanged)
- Do NOT change the watch loop logic in `ExecuteAsync`
- Do NOT add retry logic or exception handling in `InitialLoadAsync`
- Do NOT change constructor signatures or DI registration
  </action>
  <verify>
Run `dotnet build src/SnmpCollector/SnmpCollector.csproj` -- must compile with zero errors and zero warnings related to the refactored methods.
Run `dotnet test tests/SnmpCollector.Tests/SnmpCollector.Tests.csproj` -- all existing tests must pass (watcher validation tests exercise static methods, unaffected by ExecuteAsync changes).
Grep each watcher file to confirm: (a) `public async Task<int> InitialLoadAsync` exists, (b) no `"initial load failed"` string remains in ExecuteAsync (that string was in the removed try/catch block).
  </verify>
  <done>
All 4 watchers have public `InitialLoadAsync` returning `Task<int>`, ExecuteAsync contains only the watch loop with no initial-load block, all existing tests pass.
  </done>
</task>

</tasks>

<verification>
- `dotnet build` succeeds with no errors
- `dotnet test` passes all existing tests
- Each watcher has exactly one `InitialLoadAsync` method (public, returns Task<int>)
- Each watcher's ExecuteAsync starts directly with the watch loop (no initial-load try/catch)
</verification>

<success_criteria>
All 4 watcher services expose InitialLoadAsync, ExecuteAsync is watch-loop-only, all existing tests pass unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/57-deterministic-watcher-startup-order/57-01-SUMMARY.md`
</output>
