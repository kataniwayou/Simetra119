---
phase: 55-advanced-scenarios
plan: 02
type: execute
wave: 2
depends_on: ["55-01"]
files_modified:
  - tests/e2e/scenarios/37-adv-02-depth3-allsamples.sh
autonomous: true

must_haves:
  truths:
    - "ADV-02 tier=4 fires only after all 3 time-series slots are violated (minimum ~75s wait)"
    - "ADV-02 sent counter increments when tier-4 fires (command dispatch confirmed)"
    - "ADV-02 recovery: switching one sample in-range produces tier-3 healthy log"
    - "ADV-02 recovery: counter delta == 0 during recovery window (no commands sent)"
  artifacts:
    - path: "tests/e2e/scenarios/37-adv-02-depth3-allsamples.sh"
      provides: "ADV-02 depth-3 time-series all-samples scenario"
      min_lines: 80
  key_links:
    - from: "tests/e2e/scenarios/37-adv-02-depth3-allsamples.sh"
      to: "simulators/e2e-sim/e2e_simulator.py"
      via: "sim_set_scenario agg_breach then healthy"
      pattern: "sim_set_scenario (agg_breach|healthy)"
    - from: "tests/e2e/scenarios/37-adv-02-depth3-allsamples.sh"
      to: "Prometheus"
      via: "snapshot_counter for sent counter delta"
      pattern: "snapshot_counter.*snmp_command_sent_total"
---

<objective>
Create the ADV-02 depth-3 time-series all-samples scenario script that validates the AreAllEvaluateViolated logic: tier-4 only fires after all 3 slots in the time series are violated, and recovery occurs when a single in-range sample enters the series.

Purpose: ADV-02 is the final coverage gap in the evaluation pipeline -- it proves that the depth-3 all-samples check works correctly for both violation (all 3 violated -> tier-4) and recovery (1 in-range sample -> tier-3 + zero commands). Combined with ADV-01, this completes v2.1 scenario coverage.

Output: Complete ADV-02 scenario script with 4 sub-assertions covering breach, command dispatch, recovery log, and recovery counter.
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-advanced-scenarios/55-RESEARCH.md
@.planning/phases/55-advanced-scenarios/55-CONTEXT.md
@.planning/phases/55-advanced-scenarios/55-01-SUMMARY.md

Key reference files:
@simulators/e2e-sim/e2e_simulator.py
@tests/e2e/scenarios/35-mts-02-advance-gate.sh (pattern reference — most complex prior scenario)
@tests/e2e/scenarios/30-sts-02-evaluate-violated.sh (pattern reference — tier-4 assertion)
@tests/e2e/fixtures/tenant-cfg04-aggregate.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ADV-02 depth-3 all-samples scenario script</name>
  <files>tests/e2e/scenarios/37-adv-02-depth3-allsamples.sh</files>
  <action>
Create `tests/e2e/scenarios/37-adv-02-depth3-allsamples.sh` following the 35-mts-02 pattern. This is the most timing-sensitive scenario in the suite. It has two phases (breach + recovery) with 4 sub-assertions.

**Header comment block:**
```
# Scenario 37: ADV-02 Depth-3 time-series all-samples check
# Uses tenant-cfg04-aggregate.yaml (e2e-tenant-agg, Priority 1, Evaluate=e2e_total_util, TimeSeriesSize=3, Max:80)
#
# Phase 1 (Breach): agg_breach fills 3 slots with violated values -> AreAllEvaluateViolated returns true -> tier-4
# Phase 2 (Recovery): healthy overwrites one slot with in-range value -> AreAllEvaluateViolated returns false -> tier-3
#
# Timing model:
#   T=0:    sim_set_scenario agg_breach (sum=100 > Max:80)
#   T=10s:  Poll 1 -> slot[0] = 100 (violated)
#   T=20s:  Poll 2 -> slot[1] = 100 (violated)
#   T=30s:  Poll 3 -> slot[2] = 100 (violated) — series full
#   T=~45s: SnapshotJob: all 3 violated -> tier=4 fires, commands enqueued
#   Recovery:
#   T=breach+0:  sim_set_scenario healthy (sum=0 < Max:80)
#   T=breach+10: Poll overwrites one slot with 0 (in-range)
#   T=breach+15: SnapshotJob: 1 in-range + 2 violated -> AreAllEvaluateViolated = false -> tier=3
#
# Success criteria (from CONTEXT.md):
#   - tier-4 fires only after all 3 slots violated (min 75s wait)
#   - single in-range sample recovers to Healthy (tier-3 log + counter delta == 0)
#   - partial series violation does not fire (proven by recovery assertions)
```

**Setup section (identical to ADV-01 pattern):**
- `FIXTURES_DIR` path
- `save_configmap "simetra-tenants" "simetra" "$FIXTURES_DIR/.original-tenants-configmap.yaml" || true`
- `kubectl apply -f "$FIXTURES_DIR/tenant-cfg04-aggregate.yaml" > /dev/null 2>&1 || true`
- `poll_until_log 60 5 "Tenant vector reload complete\|TenantVectorWatcher initial load complete" 30` with info/warn

**Phase 1: Breach**
- `sim_set_scenario agg_breach`
- `BEFORE_SENT=$(snapshot_counter "snmp_command_sent_total" 'device_name="E2E-SIM"')`

**Sub-scenario 37a** (tier-4 all-samples): `poll_until_log 90 5 "e2e-tenant-agg.*tier=4 — commands enqueued" 60`
- 90s timeout accommodates 3 poll cycles (30s) + SnapshotJob (15s) + jitter
- Pass: `record_pass "ADV-02: Depth-3 tier=4 after all slots violated" "log=tier4_depth3_found"`
- Fail: `record_fail "ADV-02: Depth-3 tier=4 after all slots violated" "tier4 log not found within 90s — series may not have filled"`

**Sub-scenario 37b** (sent counter): snapshot_counter after, compute delta
- Pass (delta > 0): `record_pass "ADV-02: Command sent counter incremented" "sent_delta=... $(get_evidence ...)"`
- Fail: `record_fail "ADV-02: Command sent counter incremented" "sent_delta=... expected > 0"`
- Use explicit if/else, not assert_delta_gt

**Phase 2: Recovery**
- `log_info "ADV-02: Switching to healthy scenario for recovery phase..."`
- `sim_set_scenario healthy` (sets .4.5=0, .4.6=0 -> sum=0 < 80 -> in-range)
- Capture recovery baseline: `RECOVERY_BASELINE=$(snapshot_counter "snmp_command_sent_total" 'device_name="E2E-SIM"')`

**Sub-scenario 37c** (tier-3 recovery log): `poll_until_log 90 5 "e2e-tenant-agg.*tier=3" 30`
- Use `since=30` to focus on recent logs and avoid matching any pre-breach tier-3 from other scenarios
- Pass: `record_pass "ADV-02: Recovery tier=3 after single in-range sample" "log=tier3_recovery_found"`
- Fail: `record_fail "ADV-02: Recovery tier=3 after single in-range sample" "tier3 log not found within 90s"`

**Sub-scenario 37d** (counter delta == 0): After tier-3 confirmed, check counter delta
- `RECOVERY_AFTER=$(snapshot_counter "snmp_command_sent_total" 'device_name="E2E-SIM"')`
- `RECOVERY_DELTA=$((RECOVERY_AFTER - RECOVERY_BASELINE))`
- Pass (delta == 0): `record_pass "ADV-02: No commands sent during recovery" "recovery_delta=0 baseline=... after=..."`
- Fail (delta != 0): `record_fail "ADV-02: No commands sent during recovery" "recovery_delta=... expected 0 — commands fired during recovery window"`

**Cleanup (identical pattern):**
- `reset_scenario`
- Restore original ConfigMap

Total: exactly 4 record_pass/record_fail pairs (sub-scenarios 37a, 37b, 37c, 37d).

IMPORTANT timing notes:
- The 90s timeout for tier-4 (37a) is critical -- TimeSeriesSize=3 requires ~30s of polling before the series fills, plus SnapshotJob cycle time
- Recovery baseline MUST be captured BEFORE switching to healthy, so the delta correctly isolates the recovery window
- Actually, capture recovery baseline AFTER tier-4 is confirmed (after 37b) and BEFORE switching to healthy. This way the baseline includes the breach commands but the delta measures only the recovery window.

Wait -- correction: capture the recovery baseline AFTER switching to healthy but BEFORE the tier-3 assertion. The goal is to prove no NEW commands are sent during recovery. So:
1. After breach phase completes (37a, 37b done)
2. Switch to healthy
3. Snapshot recovery baseline
4. Wait for tier-3 (37c)
5. Snapshot recovery after
6. Assert delta == 0 (37d)

This ensures the baseline is taken at the moment recovery begins, and the delta measures only the recovery observation window.
  </action>
  <verify>
- `grep -c "record_pass\|record_fail" tests/e2e/scenarios/37-adv-02-depth3-allsamples.sh` returns 8 (4 pass + 4 fail branches)
- `bash -n tests/e2e/scenarios/37-adv-02-depth3-allsamples.sh` passes (syntax check)
- Script uses `sim_set_scenario agg_breach` for breach and `sim_set_scenario healthy` for recovery
- Script has both tier-4 and tier-3 log assertions
- Script checks counter delta == 0 during recovery (not just > 0)
- Script has cleanup section with reset_scenario and restore_configmap
  </verify>
  <done>
- ADV-02 scenario script exists at tests/e2e/scenarios/37-adv-02-depth3-allsamples.sh
- Phase 1 (breach): tier-4 log + sent counter (proving all 3 slots violated)
- Phase 2 (recovery): tier-3 log + counter delta == 0 (proving partial violation does not fire)
- Follows identical setup/cleanup pattern as prior scenarios
- 90s poll timeouts accommodate TimeSeriesSize=3 fill time
  </done>
</task>

</tasks>

<verification>
- `bash -n tests/e2e/scenarios/37-adv-02-depth3-allsamples.sh` passes
- `grep -c "record_pass\|record_fail" tests/e2e/scenarios/37-adv-02-depth3-allsamples.sh` returns 8
- Script contains both `sim_set_scenario agg_breach` and `sim_set_scenario healthy`
- Script contains `recovery_delta` or equivalent zero-delta check
- `ls tests/e2e/scenarios/3[67]-adv-*.sh | wc -l` returns 2 (both ADV scripts exist)
</verification>

<success_criteria>
- ADV-02 script validates depth-3 all-samples check with breach and recovery phases
- Breach: tier-4 fires only after 3 violated samples fill the series
- Recovery: tier-3 log + zero counter delta proves single in-range sample recovers
- 4 sub-assertions with explicit record_pass/record_fail
- Complete v2.1 scenario coverage (ADV-01 + ADV-02 = requirements ADV-01 and ADV-02)
</success_criteria>

<output>
After completion, create `.planning/phases/55-advanced-scenarios/55-02-SUMMARY.md`
</output>
