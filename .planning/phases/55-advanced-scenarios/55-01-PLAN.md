---
phase: 55-advanced-scenarios
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - simulators/e2e-sim/e2e_simulator.py
  - tests/e2e/lib/report.sh
  - tests/e2e/scenarios/36-adv-01-aggregate-evaluate.sh
autonomous: true

must_haves:
  truths:
    - "sim_set_scenario agg_breach returns 200 and sets .4.5=50, .4.6=50, .4.2=2, .4.3=2"
    - "ADV-01 tier=4 log fires for e2e-tenant-agg with aggregate breach (sum 100 > Max:80)"
    - "ADV-01 sent counter increments proving command dispatch occurred"
    - "ADV-01 source=synthetic label visible in Prometheus for e2e_total_util"
    - "report.sh covers scenario indices 35-36 under Snapshot Evaluation category"
  artifacts:
    - path: "simulators/e2e-sim/e2e_simulator.py"
      provides: "agg_breach scenario in SCENARIOS dict"
      contains: "agg_breach"
    - path: "tests/e2e/lib/report.sh"
      provides: "Extended category range"
      contains: "Snapshot Evaluation|28|36"
    - path: "tests/e2e/scenarios/36-adv-01-aggregate-evaluate.sh"
      provides: "ADV-01 aggregate evaluate scenario"
      min_lines: 50
  key_links:
    - from: "tests/e2e/scenarios/36-adv-01-aggregate-evaluate.sh"
      to: "simulators/e2e-sim/e2e_simulator.py"
      via: "sim_set_scenario agg_breach"
      pattern: "sim_set_scenario agg_breach"
    - from: "tests/e2e/scenarios/36-adv-01-aggregate-evaluate.sh"
      to: "Prometheus"
      via: "query_prometheus with source=synthetic label"
      pattern: 'source="synthetic"'
---

<objective>
Add the agg_breach simulator scenario, extend report.sh category range, and create the ADV-01 aggregate evaluate scenario script.

Purpose: ADV-01 validates that the synthetic aggregate pipeline (sum of two SNMP sources) feeds the threshold evaluation correctly -- tier-4 fires when the aggregate exceeds Max:80, commands are dispatched, and the metric carries source=synthetic in Prometheus.

Output: Working simulator scenario, updated report categories, and complete ADV-01 scenario script with 4 sub-assertions.
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-advanced-scenarios/55-RESEARCH.md
@.planning/phases/55-advanced-scenarios/55-CONTEXT.md

Key reference files:
@simulators/e2e-sim/e2e_simulator.py
@tests/e2e/scenarios/30-sts-02-evaluate-violated.sh (pattern reference — ADV-01 follows same structure)
@tests/e2e/scenarios/35-mts-02-advance-gate.sh (pattern reference — setup/cleanup/log patterns)
@tests/e2e/lib/report.sh
@tests/e2e/fixtures/tenant-cfg04-aggregate.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add agg_breach scenario to simulator and extend report.sh</name>
  <files>simulators/e2e-sim/e2e_simulator.py, tests/e2e/lib/report.sh</files>
  <action>
1. In `e2e_simulator.py`, add a new entry to the `SCENARIOS` dict (after the "healthy" entry):

```python
"agg_breach": _make_scenario({
    f"{E2E_PREFIX}.4.2": 2,    # e2e_channel_state = 2 (>= Min:1.0 -> in-range)
    f"{E2E_PREFIX}.4.3": 2,    # e2e_bypass_status = 2 (>= Min:1.0 -> in-range)
    f"{E2E_PREFIX}.4.5": 50,   # e2e_agg_source_a = 50
    f"{E2E_PREFIX}.4.6": 50,   # e2e_agg_source_b = 50
    # sum(50,50) = 100 > Max:80 -> e2e_total_util violated
    # Resolved metrics in-range -> tier-2 passes -> reaches tier-4
}),
```

IMPORTANT: .4.2 and .4.3 MUST be set to 2 (in-range for Min:1.0 Resolved thresholds). If left at 0 (default), both Resolved metrics are violated -> tier-2 fires ConfirmedBad instead of reaching tier-4. This matches the command_trigger pattern.

2. In `tests/e2e/lib/report.sh`, change the Snapshot Evaluation category range from `"Snapshot Evaluation|28|34"` to `"Snapshot Evaluation|28|36"`. This extends coverage to include scenario indices 35 (ADV-01, scenario 36 1-based) and 36 (ADV-02, scenario 37 1-based). The array clamp logic handles cases where fewer scenarios ran.
  </action>
  <verify>
- `grep -c "agg_breach" simulators/e2e-sim/e2e_simulator.py` returns 1
- `grep "Snapshot Evaluation" tests/e2e/lib/report.sh` shows `|28|36|`
- `python3 -c "exec(open('simulators/e2e-sim/e2e_simulator.py').read().split('SCENARIOS')[0])"` does not error (syntax check — partial, but catches obvious issues)
  </verify>
  <done>
- agg_breach scenario exists in SCENARIOS dict with .4.2=2, .4.3=2, .4.5=50, .4.6=50
- report.sh Snapshot Evaluation range is |28|36|
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ADV-01 aggregate evaluate scenario script</name>
  <files>tests/e2e/scenarios/36-adv-01-aggregate-evaluate.sh</files>
  <action>
Create `tests/e2e/scenarios/36-adv-01-aggregate-evaluate.sh` following the exact pattern from 30-sts-02 and 35-mts-02. The script has 4 sub-assertions (record_pass/record_fail calls), proving:

1. tier=4 log fires for e2e-tenant-agg (aggregate breach)
2. sent counter increments (command dispatch)
3. source=synthetic label visible in Prometheus for e2e_total_util
4. Recovery: after switching back to healthy, tier-3 log appears (healthy evaluation)

**Script structure:**

Header comment block:
```
# Scenario 36: ADV-01 Aggregate evaluate -- synthetic pipeline feeds threshold check
# Uses tenant-cfg04-aggregate.yaml (e2e-tenant-agg, Priority 1, Evaluate=e2e_total_util, TimeSeriesSize=3, Max:80)
#
# agg_breach scenario: .4.5=50 + .4.6=50 = sum 100 > Max:80 -> violated
# Resolved metrics (.4.2=2, .4.3=2) in-range -> tier-2 passes -> tier-4 fires
#
# Timing: TimeSeriesSize=3, poll 10s -> ~30s fill + SnapshotJob 15s = ~75s min
```

**Setup section:**
- `FIXTURES_DIR` path (same pattern as 35-mts-02)
- `save_configmap "simetra-tenants" "simetra" "$FIXTURES_DIR/.original-tenants-configmap.yaml" || true`
- `kubectl apply -f "$FIXTURES_DIR/tenant-cfg04-aggregate.yaml" > /dev/null 2>&1 || true`
- `poll_until_log 60 5 "Tenant vector reload complete\|TenantVectorWatcher initial load complete" 30` with info/warn logging

**Breach phase:**
- `sim_set_scenario agg_breach`
- `BEFORE_SENT=$(snapshot_counter "snmp_command_sent_total" 'device_name="E2E-SIM"')`

**Sub-scenario 36a** (tier-4 log): `poll_until_log 90 5 "e2e-tenant-agg.*tier=4 — commands enqueued" 60`
- Pass: `record_pass "ADV-01: Aggregate tier=4 detected" "log=tier4_agg_found"`
- Fail: `record_fail "ADV-01: Aggregate tier=4 detected" "tier4 log for e2e-tenant-agg not found within 90s"`

**Sub-scenario 36b** (sent counter): snapshot_counter after tier-4, compute delta
- Pass (delta > 0): `record_pass "ADV-01: Command sent counter incremented" "sent_delta=..."`
- Fail: `record_fail "ADV-01: Command sent counter incremented" "sent_delta=... expected > 0"`
- Use explicit if/else (not assert_delta_gt) for consistent record_pass/record_fail count

**Sub-scenario 36c** (source=synthetic): Wait 30s after tier-4 confirmed for OTel export + Prometheus scrape, then:
```bash
SYNTHETIC_RESULT=$(query_prometheus 'snmp_gauge{resolved_name="e2e_total_util",source="synthetic"}')
SYNTHETIC_COUNT=$(echo "$SYNTHETIC_RESULT" | jq -r '.data.result | length')
```
- Pass (count > 0): `record_pass "ADV-01: source=synthetic in Prometheus" "count=..."`
- Fail: `record_fail "ADV-01: source=synthetic in Prometheus" "count=0 — metric not found"`

**Recovery phase (sub-scenario 36d):**
- `sim_set_scenario healthy` (sets .4.5=0, .4.6=0 -> sum=0 < 80 -> in-range)
- `poll_until_log 90 5 "e2e-tenant-agg.*tier=3" 30` for tier-3 healthy log
- Pass: `record_pass "ADV-01: Recovery tier=3 after healthy switch" "log=tier3_healthy_found"`
- Fail: `record_fail "ADV-01: Recovery tier=3 after healthy switch" "tier3 log not found within 90s"`

**Cleanup:**
- `reset_scenario`
- Restore original ConfigMap (same pattern as 35-mts-02)

Total: exactly 4 record_pass/record_fail pairs (sub-scenarios 36a, 36b, 36c, 36d).

DO NOT use `assert_delta_gt` or `assert_exists` — use explicit if/else with record_pass/record_fail for each assertion. This is consistent with Phase 54 decisions requiring literal record_pass/record_fail counts.
  </action>
  <verify>
- `grep -c "record_pass\|record_fail" tests/e2e/scenarios/36-adv-01-aggregate-evaluate.sh` returns 8 (4 pass + 4 fail branches)
- `bash -n tests/e2e/scenarios/36-adv-01-aggregate-evaluate.sh` passes (syntax check)
- Script uses `sim_set_scenario agg_breach` (not command_trigger)
- Script queries `source="synthetic"` in Prometheus
- Script has cleanup section with reset_scenario and restore_configmap
  </verify>
  <done>
- ADV-01 scenario script exists at tests/e2e/scenarios/36-adv-01-aggregate-evaluate.sh
- 4 sub-assertions: tier-4 log, sent counter, source=synthetic, recovery tier-3
- Follows identical setup/cleanup pattern as 35-mts-02
- Uses agg_breach scenario for breach and healthy scenario for recovery
  </done>
</task>

</tasks>

<verification>
- `grep "agg_breach" simulators/e2e-sim/e2e_simulator.py` shows the scenario entry
- `grep "Snapshot Evaluation|28|36" tests/e2e/lib/report.sh` confirms range extension
- `bash -n tests/e2e/scenarios/36-adv-01-aggregate-evaluate.sh` passes
- `grep -c "record_pass\|record_fail" tests/e2e/scenarios/36-adv-01-aggregate-evaluate.sh` returns 8
</verification>

<success_criteria>
- agg_breach scenario in simulator with correct OID values (.4.2=2, .4.3=2, .4.5=50, .4.6=50)
- report.sh extended to |28|36|
- ADV-01 script has 4 sub-assertions covering tier-4 log, sent counter, source=synthetic, and recovery
- All files pass syntax checks
</success_criteria>

<output>
After completion, create `.planning/phases/55-advanced-scenarios/55-01-SUMMARY.md`
</output>
