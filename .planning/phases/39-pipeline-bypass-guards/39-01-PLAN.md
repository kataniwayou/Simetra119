---
phase: 39-pipeline-bypass-guards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/SnmpCollector/Pipeline/SnmpSource.cs
  - src/SnmpCollector/Pipeline/Behaviors/OidResolutionBehavior.cs
  - tests/SnmpCollector.Tests/Pipeline/Behaviors/OidResolutionBehaviorTests.cs
  - tests/SnmpCollector.Tests/Pipeline/Behaviors/ValidationBehaviorTests.cs
autonomous: true

must_haves:
  truths:
    - "SnmpSource.Synthetic is a valid enum member -- a synthetic SnmpOidReceived with Source = SnmpSource.Synthetic compiles and can be sent through the pipeline"
    - "A synthetic message with pre-set MetricName 'obp_combined_power' exits OidResolutionBehavior with MetricName still equal to 'obp_combined_power' -- OidMapService.Resolve is never called for it"
    - "A synthetic message with Oid '0.0' passes ValidationBehavior without being rejected -- the OID regex does not block it"
    - "OidResolutionBehavior bypass uses 'return await next();' (NOT 'await next(); return;') -- method returns Task<TResponse>"
    - "A regular Poll message still goes through full OID resolution as before -- no regression"
    - "A regular Trap message still goes through full OID resolution as before -- no regression"
  artifacts:
    - path: "src/SnmpCollector/Pipeline/SnmpSource.cs"
      provides: "Synthetic enum member alongside Poll and Trap"
      contains: "Synthetic"
    - path: "src/SnmpCollector/Pipeline/Behaviors/OidResolutionBehavior.cs"
      provides: "Bypass guard for synthetic messages before OID resolution logic"
      contains: "SnmpSource.Synthetic"
    - path: "tests/SnmpCollector.Tests/Pipeline/Behaviors/OidResolutionBehaviorTests.cs"
      provides: "3 new tests for synthetic bypass guard plus regression for Poll/Trap"
      contains: "SnmpSource.Synthetic"
    - path: "tests/SnmpCollector.Tests/Pipeline/Behaviors/ValidationBehaviorTests.cs"
      provides: "1 new test confirming sentinel OID 0.0 passes validation"
      contains: "0.0"
  key_links:
    - from: "SnmpSource.Synthetic"
      to: "OidResolutionBehavior bypass guard"
      via: "msg.Source == SnmpSource.Synthetic check inside the SnmpOidReceived cast block"
      pattern: "Early return with 'return await next();' -- skips OID resolution entirely"
    - from: "Sentinel OID '0.0'"
      to: "ValidationBehavior OID regex"
      via: "Regex '^\\d+(\\.\\d+){1,}$' matches '0.0' without code changes"
      pattern: "No changes to ValidationBehavior -- sentinel passes existing regex"
---

<objective>
Add `SnmpSource.Synthetic` enum member, insert a bypass guard in `OidResolutionBehavior` that skips OID resolution for synthetic messages, and add unit tests confirming the bypass and sentinel OID passthrough. Three lines of production code, four new tests.

Purpose: Phase 40 (MetricPollJob Aggregate Dispatch) will publish synthetic messages through the MediatR pipeline. Without this bypass guard, OidResolutionBehavior would overwrite the pre-set MetricName with an OID map lookup result. This phase ensures synthetic messages pass through the pipeline with their MetricName intact.
Output: Modified SnmpSource enum, modified OidResolutionBehavior, 3 new tests in OidResolutionBehaviorTests, 1 new test in ValidationBehaviorTests.
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/39-pipeline-bypass-guards/39-CONTEXT.md
@.planning/phases/39-pipeline-bypass-guards/39-RESEARCH.md

@src/SnmpCollector/Pipeline/SnmpSource.cs
@src/SnmpCollector/Pipeline/Behaviors/OidResolutionBehavior.cs
@tests/SnmpCollector.Tests/Pipeline/Behaviors/OidResolutionBehaviorTests.cs
@tests/SnmpCollector.Tests/Pipeline/Behaviors/ValidationBehaviorTests.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SnmpSource.Synthetic enum member and OidResolutionBehavior bypass guard</name>
  <files>
    src/SnmpCollector/Pipeline/SnmpSource.cs
    src/SnmpCollector/Pipeline/Behaviors/OidResolutionBehavior.cs
  </files>
  <action>
**File 1: `src/SnmpCollector/Pipeline/SnmpSource.cs`**

Add `Synthetic` as the third enum member:

```csharp
public enum SnmpSource
{
    Poll,
    Trap,
    Synthetic
}
```

**File 2: `src/SnmpCollector/Pipeline/Behaviors/OidResolutionBehavior.cs`**

Insert the bypass guard as the FIRST line inside the `if (notification is SnmpOidReceived msg)` block, BEFORE `msg.MetricName = _oidMapService.Resolve(msg.Oid)`:

```csharp
if (notification is SnmpOidReceived msg)
{
    if (msg.Source == SnmpSource.Synthetic) { return await next(); }

    msg.MetricName = _oidMapService.Resolve(msg.Oid);

    if (msg.MetricName == OidMapService.Unknown)
        _logger.LogDebug("OID {Oid} not found in OidMap", msg.Oid);
    else
        _logger.LogDebug("OID {Oid} resolved to {MetricName}", msg.Oid, msg.MetricName);
}

return await next();
```

CRITICAL: The bypass MUST use `return await next();` (returns `TResponse`), NOT `await next(); return;` (which would be a compile error since `Handle` returns `Task<TResponse>`). The `next()` delegate returns `Task<TResponse>`, and the awaited result must be returned to satisfy the method signature.

No other files are modified. ValidationBehavior requires NO changes -- the sentinel OID "0.0" already passes the existing regex `^\d+(\.\d+){1,}$`.
  </action>
  <verify>
Run `dotnet build src/SnmpCollector/SnmpCollector.csproj` -- must compile with zero errors.
Run `dotnet test tests/SnmpCollector.Tests/SnmpCollector.Tests.csproj` -- ALL existing tests must pass (no regressions from adding the enum member or the bypass guard).
  </verify>
  <done>
`SnmpSource.Synthetic` exists as a valid enum member. `OidResolutionBehavior.Handle` has a bypass guard that returns early (via `return await next()`) for synthetic messages, skipping `_oidMapService.Resolve`. All existing tests pass unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add unit tests for synthetic bypass guard and sentinel OID passthrough</name>
  <files>
    tests/SnmpCollector.Tests/Pipeline/Behaviors/OidResolutionBehaviorTests.cs
    tests/SnmpCollector.Tests/Pipeline/Behaviors/ValidationBehaviorTests.cs
  </files>
  <action>
**File 1: `tests/SnmpCollector.Tests/Pipeline/Behaviors/OidResolutionBehaviorTests.cs`**

1. Modify the existing `MakeNotification` factory to accept an optional `source` parameter with a default of `SnmpSource.Poll` (backward compatible with all existing test calls):

```csharp
private static SnmpOidReceived MakeNotification(string oid, SnmpSource source = SnmpSource.Poll) =>
    new()
    {
        Oid = oid,
        AgentIp = IPAddress.Parse("10.0.0.1"),
        Value = new Integer32(42),
        Source = source,
        TypeCode = SnmpType.Integer32,
        DeviceName = "test-device"
    };
```

2. Add 3 new tests after the existing `ResolvesHeartbeatOid_ViaOidMapService` test (before the `StubOidMapService` class):

**Test 1: SyntheticMessage_BypassesOidResolution_MetricNamePreserved**
- Create a synthetic notification with `Oid = "0.0"` and `Source = SnmpSource.Synthetic`
- Pre-set `MetricName = "obp_combined_power"` on the notification object after construction (separate assignment statement)
- Use StubOidMapService with any knownOid (e.g., "1.3.6.1.2.1.25.3.3.1.2" / "hrProcessorLoad")
- After `behavior.Handle`, assert `notification.MetricName == "obp_combined_power"` (unchanged -- OID resolution was bypassed)

**Test 2: SyntheticMessage_StillCallsNext**
- Create a synthetic notification with `Oid = "0.0"` and `Source = SnmpSource.Synthetic`
- Track `nextCalled = false`, set to `true` inside the next delegate
- After `behavior.Handle`, assert `nextCalled == true` (pipeline continues)

**Test 3: PollMessage_StillResolvesOid_NotAffectedByBypassGuard**
- Create a Poll notification with `Oid = "1.3.6.1.2.1.25.3.3.1.2"` and `Source = SnmpSource.Poll` (uses default)
- Use StubOidMapService with knownOid "1.3.6.1.2.1.25.3.3.1.2" / "hrProcessorLoad"
- After `behavior.Handle`, assert `notification.MetricName == "hrProcessorLoad"` (OID resolution ran normally)
- This is a regression test confirming the bypass guard does not affect Poll messages

All three tests follow the exact existing pattern: create StubOidMapService, create behavior with NullLogger, call `behavior.Handle`, assert result.

**File 2: `tests/SnmpCollector.Tests/Pipeline/Behaviors/ValidationBehaviorTests.cs`**

Add 1 new test at the end of the class (after `AcceptsPreSetDeviceName`):

**Test: AcceptsSentinelOid_ZeroDotZero_WhenDeviceNameSet**
- Create notification via `MakeNotification("0.0", deviceName: "synthetic-device")`
- Track `nextCalled`, assert `nextCalled == true` after `behavior.Handle`
- This confirms the sentinel OID "0.0" passes the existing OID format regex and the DeviceName check

Follows the exact pattern of the existing `AcceptsValidOid_WhenDeviceNameSet` test.
  </action>
  <verify>
Run `dotnet test tests/SnmpCollector.Tests/SnmpCollector.Tests.csproj --filter "FullyQualifiedName~OidResolutionBehaviorTests"` -- all 8 tests pass (5 existing + 3 new).
Run `dotnet test tests/SnmpCollector.Tests/SnmpCollector.Tests.csproj --filter "FullyQualifiedName~ValidationBehaviorTests"` -- all 7 tests pass (6 existing + 1 new).
Run `dotnet test tests/SnmpCollector.Tests/SnmpCollector.Tests.csproj` -- ALL tests pass (no regressions).
  </verify>
  <done>
3 new tests in OidResolutionBehaviorTests: synthetic bypass preserves pre-set MetricName, synthetic bypass still calls next(), Poll message regression (OID resolution unaffected). 1 new test in ValidationBehaviorTests: sentinel OID "0.0" passes validation when DeviceName is set. All existing tests unaffected by the MakeNotification factory parameter addition (default value preserves backward compatibility).
  </done>
</task>

</tasks>

<verification>
1. `dotnet build src/SnmpCollector/SnmpCollector.csproj` compiles cleanly
2. `dotnet build tests/SnmpCollector.Tests/SnmpCollector.Tests.csproj` compiles cleanly
3. `dotnet test tests/SnmpCollector.Tests/SnmpCollector.Tests.csproj --filter "FullyQualifiedName~OidResolutionBehaviorTests"` -- all 8 tests pass
4. `dotnet test tests/SnmpCollector.Tests/SnmpCollector.Tests.csproj --filter "FullyQualifiedName~ValidationBehaviorTests"` -- all 7 tests pass
5. `dotnet test tests/SnmpCollector.Tests/SnmpCollector.Tests.csproj` -- ALL existing tests still pass (no regressions)
6. OidResolutionBehavior bypass guard uses `return await next();` (NOT `await next(); return;`)
7. Bypass guard is placed INSIDE the `if (notification is SnmpOidReceived msg)` block, BEFORE `msg.MetricName = _oidMapService.Resolve(msg.Oid)`
8. No changes to ValidationBehavior.cs -- sentinel OID "0.0" passes existing regex without modification
9. Synthetic messages must have DeviceName set at construction (ValidationBehavior runs before OidResolutionBehavior in pipeline order)
10. MakeNotification factory in OidResolutionBehaviorTests has optional `source` parameter with default `SnmpSource.Poll` -- all existing test calls unchanged
</verification>

<success_criteria>
- SnmpSource.Synthetic is a valid enum member
- Synthetic message with pre-set MetricName exits OidResolutionBehavior with MetricName intact
- Synthetic message with Oid "0.0" passes ValidationBehavior
- OidResolutionBehavior bypass uses `return await next();`
- Poll and Trap messages are completely unaffected by the bypass guard
- 4 new tests pass (3 in OidResolutionBehaviorTests, 1 in ValidationBehaviorTests)
- All pre-existing tests pass with zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/39-pipeline-bypass-guards/39-01-SUMMARY.md`
</output>
