---
phase: 70-label-correctness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/lib/report.sh
  - tests/e2e/scenarios/94-mlc01-source-poll.sh
  - tests/e2e/scenarios/95-mlc02-source-trap.sh
  - tests/e2e/scenarios/97-mlc04-source-synthetic.sh
  - tests/e2e/scenarios/98-mlc05-snmptype-gauge.sh
  - tests/e2e/scenarios/99-mlc06-snmptype-info.sh
  - tests/e2e/scenarios/100-mlc07-resolved-name.sh
  - tests/e2e/scenarios/101-mlc08-device-name.sh
autonomous: true

must_haves:
  truths:
    - "snmp_gauge for e2e_gauge_test carries source=poll"
    - "snmp_gauge for e2e_gauge_test carries source=trap"
    - "snmp_gauge for e2e_total_util carries source=synthetic"
    - "snmp_gauge snmp_type label matches for all 5 numeric types (gauge32, integer32, counter32, counter64, timeticks)"
    - "snmp_info snmp_type label matches for both string types (octetstring, ipaddress)"
    - "resolved_name label matches oidmaps.json mapping for a representative OID"
    - "device_name label equals E2E-SIM for community-derived device"
  artifacts:
    - path: "tests/e2e/scenarios/94-mlc01-source-poll.sh"
      provides: "MLC-01 source=poll assertion"
    - path: "tests/e2e/scenarios/95-mlc02-source-trap.sh"
      provides: "MLC-02 source=trap assertion"
    - path: "tests/e2e/scenarios/97-mlc04-source-synthetic.sh"
      provides: "MLC-04 source=synthetic assertion"
    - path: "tests/e2e/scenarios/98-mlc05-snmptype-gauge.sh"
      provides: "MLC-05 snmp_type for 5 numeric types"
    - path: "tests/e2e/scenarios/99-mlc06-snmptype-info.sh"
      provides: "MLC-06 snmp_type for 2 string types"
    - path: "tests/e2e/scenarios/100-mlc07-resolved-name.sh"
      provides: "MLC-07 resolved_name cross-reference"
    - path: "tests/e2e/scenarios/101-mlc08-device-name.sh"
      provides: "MLC-08 device_name assertion"
  key_links:
    - from: "tests/e2e/lib/report.sh"
      to: "SCENARIO_RESULTS array"
      via: "Label Correctness category at indices 96-103"
      pattern: "Label Correctness\\|96\\|103"
---

<objective>
Create 7 static label-correctness E2E scenarios (MLC-01, 02, 04-08) that verify source, snmp_type, resolved_name, and device_name labels on snmp_gauge/snmp_info metrics, plus update report.sh with the Label Correctness category.

Purpose: Prove that every metric label emitted by the pipeline matches expected values derived from the SNMP source enum, type code, OID mapping, and community string.
Output: 7 scenario files (94, 95, 97-101) and updated report.sh category.
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-label-correctness/70-RESEARCH.md
@tests/e2e/scenarios/11-gauge-labels-e2e-sim.sh
@tests/e2e/scenarios/16-trap-originated.sh
@tests/e2e/lib/report.sh
@tests/e2e/lib/common.sh
@tests/e2e/lib/prometheus.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add report category and create source-label scenarios (94, 95, 97)</name>
  <files>
    tests/e2e/lib/report.sh
    tests/e2e/scenarios/94-mlc01-source-poll.sh
    tests/e2e/scenarios/95-mlc02-source-trap.sh
    tests/e2e/scenarios/97-mlc04-source-synthetic.sh
  </files>
  <action>
    1. Update `tests/e2e/lib/report.sh`: Add `"Label Correctness|96|103"` to the `_REPORT_CATEGORIES` array (after `"Business Metric Value Correctness|88|95"`).

    2. Create `94-mlc01-source-poll.sh` (MLC-01: source="poll"):
       - SCENARIO_NAME="MLC-01: source=poll label on snmp_gauge (e2e_gauge_test)"
       - Query: `snmp_gauge{device_name="E2E-SIM",resolved_name="e2e_gauge_test",source="poll"}`
       - If result count > 0: read `.data.result[0].metric.source`, assert equals "poll"
       - Single `record_pass` or `record_fail`. No loops.
       - Follow scenario 11 pattern (static query, read label, assert).

    3. Create `95-mlc02-source-trap.sh` (MLC-02: source="trap"):
       - SCENARIO_NAME="MLC-02: source=trap label on snmp_gauge (e2e_gauge_test)"
       - Use a poll loop with 15s deadline, 3s interval (trap data should already be present from scenario 16, but safety net in case it's not).
       - Query: `snmp_gauge{device_name="E2E-SIM",resolved_name="e2e_gauge_test",source="trap"}`
       - Once found: read `.data.result[0].metric.source`, assert equals "trap"
       - Single `record_pass` or `record_fail`.

    4. Create `97-mlc04-source-synthetic.sh` (MLC-04: source="synthetic"):
       - SCENARIO_NAME="MLC-04: source=synthetic label on snmp_gauge (e2e_total_util)"
       - Query: `snmp_gauge{device_name="E2E-SIM",resolved_name="e2e_total_util",source="synthetic"}`
       - If result count > 0: read `.data.result[0].metric.source`, assert equals "synthetic"
       - Single `record_pass` or `record_fail`.
       - Note: synthetic metrics always have `oid="0.0"` (sentinel). Do NOT filter by a real OID.

    All scenario files must: have `# Scenario NN:` header comment, produce exactly 1 SCENARIO_RESULTS entry, use `record_pass`/`record_fail` from common.sh.
  </action>
  <verify>
    - `grep -c "Label Correctness|96|103" tests/e2e/lib/report.sh` returns 1
    - `bash -n tests/e2e/scenarios/94-mlc01-source-poll.sh` passes (valid bash)
    - `bash -n tests/e2e/scenarios/95-mlc02-source-trap.sh` passes
    - `bash -n tests/e2e/scenarios/97-mlc04-source-synthetic.sh` passes
    - Each file has exactly 1 `record_pass` and 1 `record_fail` call (no loops)
  </verify>
  <done>report.sh has Label Correctness category at 96-103; scenarios 94, 95, 97 each produce 1 SCENARIO_RESULTS entry asserting the exact source label value</done>
</task>

<task type="auto">
  <name>Task 2: Create snmp_type and resolved_name and device_name scenarios (98-101)</name>
  <files>
    tests/e2e/scenarios/98-mlc05-snmptype-gauge.sh
    tests/e2e/scenarios/99-mlc06-snmptype-info.sh
    tests/e2e/scenarios/100-mlc07-resolved-name.sh
    tests/e2e/scenarios/101-mlc08-device-name.sh
  </files>
  <action>
    1. Create `98-mlc05-snmptype-gauge.sh` (MLC-05: snmp_type for 5 numeric types):
       - SCENARIO_NAME="MLC-05: snmp_type labels correct for all 5 snmp_gauge types"
       - Check ALL 5 types in sequence (NOT a loop with separate record calls):
         - query `snmp_gauge{device_name="E2E-SIM",resolved_name="e2e_gauge_test"}` -> assert snmp_type="gauge32"
         - query `snmp_gauge{device_name="E2E-SIM",resolved_name="e2e_integer_test"}` -> assert snmp_type="integer32"
         - query `snmp_gauge{device_name="E2E-SIM",resolved_name="e2e_counter32_test"}` -> assert snmp_type="counter32"
         - query `snmp_gauge{device_name="E2E-SIM",resolved_name="e2e_counter64_test"}` -> assert snmp_type="counter64"
         - query `snmp_gauge{device_name="E2E-SIM",resolved_name="e2e_timeticks_test"}` -> assert snmp_type="timeticks"
       - Track PASS_COUNT. If all 5 match: single `record_pass` with evidence listing all 5 actual values.
       - If any mismatch: single `record_fail` listing which types failed.
       - Exactly 1 SCENARIO_RESULTS entry total.

    2. Create `99-mlc06-snmptype-info.sh` (MLC-06: snmp_type for 2 string types):
       - SCENARIO_NAME="MLC-06: snmp_type labels correct for snmp_info types"
       - Check both:
         - query `snmp_info{device_name="E2E-SIM",resolved_name="e2e_info_test"}` -> assert snmp_type="octetstring"
         - query `snmp_info{device_name="E2E-SIM",resolved_name="e2e_ip_test"}` -> assert snmp_type="ipaddress"
       - Single `record_pass` or `record_fail`. 1 SCENARIO_RESULTS entry.

    3. Create `100-mlc07-resolved-name.sh` (MLC-07: resolved_name matches OID mapping):
       - SCENARIO_NAME="MLC-07: resolved_name matches oidmaps.json for e2e_gauge_test"
       - Query by OID label (NOT by resolved_name) to cross-reference the mapping:
         `snmp_gauge{device_name="E2E-SIM",oid="1.3.6.1.4.1.47477.999.1.1.0"}`
       - Assert `.data.result[0].metric.resolved_name` equals "e2e_gauge_test"
       - Single `record_pass` or `record_fail`. 1 SCENARIO_RESULTS entry.
       - This verifies the OID-to-name mapping is correct in the live system.

    4. Create `101-mlc08-device-name.sh` (MLC-08: device_name from community string):
       - SCENARIO_NAME="MLC-08: device_name=E2E-SIM from community Simetra.E2E-SIM"
       - Query: `snmp_gauge{device_name="E2E-SIM",resolved_name="e2e_gauge_test"}`
       - Assert `.data.result[0].metric.device_name` equals "E2E-SIM"
       - Single `record_pass` or `record_fail`. 1 SCENARIO_RESULTS entry.

    All files must have `# Scenario NN:` header, use `record_pass`/`record_fail`, produce exactly 1 entry.
  </action>
  <verify>
    - `bash -n tests/e2e/scenarios/98-mlc05-snmptype-gauge.sh` passes
    - `bash -n tests/e2e/scenarios/99-mlc06-snmptype-info.sh` passes
    - `bash -n tests/e2e/scenarios/100-mlc07-resolved-name.sh` passes
    - `bash -n tests/e2e/scenarios/101-mlc08-device-name.sh` passes
    - Each file has exactly 1 `record_pass` call and 1 `record_fail` call (no loops producing multiple entries)
    - `grep -c "record_pass\|record_fail" tests/e2e/scenarios/98-mlc05-snmptype-gauge.sh` returns 2 (one pass, one fail)
  </verify>
  <done>Scenarios 98-101 each produce exactly 1 SCENARIO_RESULTS entry; MLC-05 checks all 5 gauge types as a unit, MLC-06 checks both info types as a unit, MLC-07 cross-references OID to resolved_name, MLC-08 asserts device_name</done>
</task>

</tasks>

<verification>
- Total scenario files: 7 (files 94, 95, 97, 98, 99, 100, 101) -- file 96 is in plan 02
- Total SCENARIO_RESULTS entries from this plan: 7 (one per file, no loops)
- `bash -n` passes for all 7 files
- report.sh has "Label Correctness|96|103" category
- No file overlaps with plan 02 (which creates scenario 96 only)
</verification>

<success_criteria>
All 7 scenario files exist, are valid bash, and each produces exactly 1 SCENARIO_RESULTS entry. report.sh category covers indices 96-103 for the full 8-entry Label Correctness suite (7 from this plan + 1 from plan 02).
</success_criteria>

<output>
After completion, create `.planning/phases/70-label-correctness/70-01-SUMMARY.md`
</output>
