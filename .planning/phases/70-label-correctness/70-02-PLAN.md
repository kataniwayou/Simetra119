---
phase: 70-label-correctness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/scenarios/96-mlc03-source-command.sh
autonomous: true

must_haves:
  truths:
    - "snmp_gauge for e2e_command_response carries source=command after tier=4 dispatch"
  artifacts:
    - path: "tests/e2e/scenarios/96-mlc03-source-command.sh"
      provides: "MLC-03 source=command assertion with tenant fixture setup/teardown"
  key_links:
    - from: "tests/e2e/scenarios/96-mlc03-source-command.sh"
      to: "tests/e2e/fixtures/tenant-cfg06-pss-single.yaml"
      via: "kubectl apply for tier=4 dispatch"
      pattern: "tenant-cfg06-pss-single"
    - from: "tests/e2e/scenarios/96-mlc03-source-command.sh"
      to: "snmp_gauge{source=command}"
      via: "query_prometheus after dispatch"
      pattern: "source.*command"
---

<objective>
Create E2E scenario 96 (MLC-03) that verifies `source="command"` label on snmp_gauge for SET response OIDs. This requires tier=4 command dispatch via tenant fixture setup, OID priming, dispatch wait, label assertion, and cleanup.

Purpose: Prove that SET command response varbinds carry the correct source="command" label in Prometheus.
Output: Scenario file 96-mlc03-source-command.sh producing 1 SCENARIO_RESULTS entry.
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-label-correctness/70-RESEARCH.md
@tests/e2e/scenarios/83-ccv01-command-dispatched.sh
@tests/e2e/lib/common.sh
@tests/e2e/lib/prometheus.sh
@tests/e2e/lib/kubectl.sh
@tests/e2e/lib/sim.sh
@tests/e2e/fixtures/tenant-cfg06-pss-single.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scenario 96 (MLC-03 source=command with fixture setup/teardown)</name>
  <files>tests/e2e/scenarios/96-mlc03-source-command.sh</files>
  <action>
    Create `96-mlc03-source-command.sh` following the exact pattern from scenario 83 (CCV-01). The scenario must:

    1. Header comment: `# Scenario 96: MLC-03 source=command label on snmp_gauge for SET response`

    2. Setup phase (identical to scenario 83):
       - Set FIXTURES_DIR to `$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)/fixtures`
       - `save_configmap "simetra-tenants" "simetra" "$FIXTURES_DIR/.original-tenants-configmap.yaml"` (|| true)
       - `kubectl apply -f "$FIXTURES_DIR/tenant-cfg06-pss-single.yaml"` (suppress output)
       - `poll_until_log 60 5 "Tenant vector reload complete\|TenantVectorWatcher initial load complete" 30`

    3. Prime T2 OIDs for readiness grace (same as scenario 83):
       - `sim_set_oid "5.1" "10"` (T2 eval in-range >= Min:10)
       - `sim_set_oid "5.2" "1"` (T2 res1 in-range >= Min:1)
       - `sim_set_oid "5.3" "1"` (T2 res2 in-range >= Min:1)
       - `sleep 8` (6s grace + 2s margin)

    4. Trigger tier=4 dispatch:
       - `sim_set_oid "5.1" "0"` (T2 eval violated < Min:10)
       - Wait for dispatch confirmation: poll `snmp_command_dispatched_total` with `device_name="e2e-pss-tenant"` for 30s (use `poll_until` if available, or manual poll loop)

    5. Assert source="command" label:
       - SCENARIO_NAME="MLC-03: source=command label on snmp_gauge (e2e_command_response)"
       - After dispatch confirmed, poll for `snmp_gauge{device_name="E2E-SIM",resolved_name="e2e_command_response",source="command"}` with a 30s deadline, 3s interval
       - Once found: read `.data.result[0].metric.source`, assert equals "command"
       - If not found after deadline: `record_fail`
       - Single `record_pass` or `record_fail`. Exactly 1 SCENARIO_RESULTS entry.

    6. Cleanup (mandatory):
       - `reset_oid_overrides`
       - Restore original tenant ConfigMap: `restore_configmap "$FIXTURES_DIR/.original-tenants-configmap.yaml"`
       - Cleanup must run regardless of pass/fail.

    IMPORTANT: Do NOT use `assert_delta_ge` -- this scenario asserts a label value, not a counter delta. Use `record_pass`/`record_fail` directly.

    IMPORTANT: The command response OID is `.999.4.4.0` which maps to `resolved_name="e2e_command_response"` and has `snmp_type="integer32"`, `source="command"`. Query by `resolved_name` and `source` to find it.

    IMPORTANT: `device_name` in the dispatch counter query is `"e2e-pss-tenant"` (the tenant name), but `device_name` on the snmp_gauge series is `"E2E-SIM"` (the SNMP device name). These are different label dimensions.
  </action>
  <verify>
    - `bash -n tests/e2e/scenarios/96-mlc03-source-command.sh` passes (valid bash)
    - File has exactly 1 `record_pass` call and 1 `record_fail` call
    - File includes `save_configmap` and `restore_configmap` for cleanup
    - File includes `reset_oid_overrides` for cleanup
    - `grep -c "source.*command" tests/e2e/scenarios/96-mlc03-source-command.sh` returns >= 1
  </verify>
  <done>Scenario 96 triggers tier=4 dispatch, waits for command response to appear in Prometheus, asserts source="command" label, cleans up fixtures, produces exactly 1 SCENARIO_RESULTS entry</done>
</task>

</tasks>

<verification>
- Scenario file 96-mlc03-source-command.sh exists and passes `bash -n`
- Exactly 1 SCENARIO_RESULTS entry (no loops)
- Fixture setup and cleanup are both present (save/restore configmap, reset OID overrides)
- Combined with plan 01's 7 entries, total is 8 entries at indices 96-103 matching report category
</verification>

<success_criteria>
Scenario 96 exists, is valid bash, produces exactly 1 SCENARIO_RESULTS entry, includes full fixture setup/teardown, and correctly asserts source="command" on the SET response metric.
</success_criteria>

<output>
After completion, create `.planning/phases/70-label-correctness/70-02-SUMMARY.md`
</output>
