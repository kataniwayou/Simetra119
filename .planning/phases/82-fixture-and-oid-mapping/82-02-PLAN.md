---
phase: 82-fixture-and-oid-mapping
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/fixtures/tenant-cfg12-v26-four-tenant.yaml
  - tests/e2e/lib/oid_map.sh
autonomous: true

must_haves:
  truths:
    - "Applying the fixture ConfigMap produces 4 tenants (T1_P1, T2_P1, T1_P2, T2_P2) with correct metric counts"
    - "Each tenant uses OIDs from its exclusive subtree (8, 9, 10, 11) with no collision"
    - "T1_P1 and T2_P1 share Priority=1; T1_P2 and T2_P2 share Priority=2"
    - "The mapping file provides O(1) lookup of any tenant/role/slot OID suffix and values"
    - "Adding a new tenant or metric to the mapping requires adding one line per OID"
  artifacts:
    - path: "tests/e2e/fixtures/tenant-cfg12-v26-four-tenant.yaml"
      provides: "K8s ConfigMap with 4 v2.6 tenants"
      contains: "T1_P1"
    - path: "tests/e2e/lib/oid_map.sh"
      provides: "Bash associative array mapping file for Phase 83 interpreter"
      contains: "declare -A OID_MAP"
  key_links:
    - from: "tests/e2e/fixtures/tenant-cfg12-v26-four-tenant.yaml"
      to: "deploy/k8s/snmp-collector/simetra-oid-metric-map.yaml"
      via: "MetricName fields must match OID map entries"
      pattern: "e2e_T1P1_eval1"
    - from: "tests/e2e/lib/oid_map.sh"
      to: "tests/e2e/fixtures/tenant-cfg12-v26-four-tenant.yaml"
      via: "OID suffixes and tenant names must match fixture"
      pattern: "T1_P1\\.E\\.1\\.oid.*8\\.1"
---

<objective>
Create the 4-tenant v2.6 fixture ConfigMap and the hardcoded OID mapping file that the Phase 83 command interpreter will source.

Purpose: The fixture defines the tenants that will be driven by the interactive interpreter. The mapping file provides the per-tenant per-role OID lookup that translates command patterns into simulator HTTP calls.
Output: K8s ConfigMap YAML at tests/e2e/fixtures/tenant-cfg12-v26-four-tenant.yaml, Bash mapping file at tests/e2e/lib/oid_map.sh.
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/82-fixture-and-oid-mapping/82-RESEARCH.md
@tests/e2e/fixtures/tenant-cfg08-pss-four-tenant.yaml
@tests/e2e/fixtures/tenant-cfg06-pss-single.yaml
@tests/e2e/lib/sim.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 4-tenant v2.6 fixture ConfigMap</name>
  <files>tests/e2e/fixtures/tenant-cfg12-v26-four-tenant.yaml</files>
  <action>
Create a K8s ConfigMap YAML file following the exact pattern of existing tenant fixture files (e.g. tenant-cfg08-pss-four-tenant.yaml). The ConfigMap should:

- Use `metadata.name: simetra-tenants` and `metadata.namespace: simetra`
- Have a `data.tenants.json` key containing a JSON array of 4 tenant objects

**Tenant definitions:**

1. **T1_P1** (Priority 1, 2E/2R/1C):
   - Name: "T1_P1", Priority: 1, SuppressionWindowSeconds: 10
   - Metrics (4 total):
     - e2e_T1P1_eval1: Role=Evaluate, Threshold={"Min":10.0}, TimeSeriesSize=3, GraceMultiplier=2.0
     - e2e_T1P1_eval2: Role=Evaluate, Threshold={"Min":10.0}, TimeSeriesSize=3, GraceMultiplier=2.0
     - e2e_T1P1_res1: Role=Resolved, Threshold={"Min":1.0} (no TimeSeriesSize)
     - e2e_T1P1_res2: Role=Resolved, Threshold={"Min":1.0} (no TimeSeriesSize)
   - Commands: 1 command using e2e_set_bypass, Value="0", ValueType="Integer32"
   - All metrics: Ip="e2e-simulator.simetra.svc.cluster.local", Port=161

2. **T2_P1** (Priority 1, 4E/4R/1C):
   - Name: "T2_P1", Priority: 1, SuppressionWindowSeconds: 10
   - Metrics (8 total):
     - e2e_T2P1_eval1 through e2e_T2P1_eval4: Role=Evaluate, same threshold/TSS/grace as above
     - e2e_T2P1_res1 through e2e_T2P1_res4: Role=Resolved, same threshold as above
   - Commands: 1 command using e2e_set_bypass

3. **T1_P2** (Priority 2, 2E/2R/1C):
   - Name: "T1_P2", Priority: 2, SuppressionWindowSeconds: 10
   - Metrics (4 total): same pattern as T1_P1 but using e2e_T1P2_* metric names
   - Commands: 1 command using e2e_set_bypass

4. **T2_P2** (Priority 2, 4E/4R/1C):
   - Name: "T2_P2", Priority: 2, SuppressionWindowSeconds: 10
   - Metrics (8 total): same pattern as T2_P1 but using e2e_T2P2_* metric names
   - Commands: 1 command using e2e_set_bypass

IMPORTANT: Evaluate metrics MUST include TimeSeriesSize=3 and GraceMultiplier=2.0 (enables readiness window). Resolved metrics must NOT include TimeSeriesSize (uses device default). All metric Ip fields use the full K8s service DNS name.
  </action>
  <verify>
Parse the YAML to confirm it's valid: `python -c "import yaml; yaml.safe_load(open('tests/e2e/fixtures/tenant-cfg12-v26-four-tenant.yaml')); print('yaml ok')"`.
Parse the embedded JSON: extract the `tenants.json` value and validate it's a JSON array with 4 elements.
Grep for all 4 tenant names: T1_P1, T2_P1, T1_P2, T2_P2.
Count total Metric entries — should be 24 (4+8+4+8).
Verify Evaluate metrics have TimeSeriesSize=3.
Verify all Priority 1 tenants have Priority: 1 and Priority 2 tenants have Priority: 2.
  </verify>
  <done>
ConfigMap YAML is valid. Contains 4 tenants with correct priority groupings (P1=1, P2=2). T1_P1 has 4 metrics, T2_P1 has 8, T1_P2 has 4, T2_P2 has 8 (24 total). All Evaluate metrics have TimeSeriesSize=3 and GraceMultiplier=2.0. All use e2e_set_bypass command.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create OID mapping file for Phase 83 interpreter</name>
  <files>tests/e2e/lib/oid_map.sh</files>
  <action>
Create `tests/e2e/lib/oid_map.sh` as a Bash source file (not directly executable — it will be sourced by the Phase 83 interpreter). Contents:

1. **Shebang + header comment** explaining the file purpose, key format, and how to add new tenants/metrics.

2. **`declare -A OID_MAP`** associative array with entries for all 24 OIDs. Key format: `TENANT.ROLE.SLOT.FIELD` where:
   - TENANT: T1_P1, T2_P1, T1_P2, T2_P2
   - ROLE: E (Evaluate), R (Resolved)
   - SLOT: 1-based index within that role for the tenant
   - FIELD: oid (suffix for sim_set_oid), healthy (value), violated (value)

   Each OID gets 3 keys on one line:
   ```
   OID_MAP[T1_P1.E.1.oid]="8.1";  OID_MAP[T1_P1.E.1.healthy]="10"; OID_MAP[T1_P1.E.1.violated]="0"
   ```

   Evaluate healthy=10, violated=0. Resolved healthy=1, violated=0. OID suffixes per research:
   - T1_P1: 8.1-8.4 (2E then 2R)
   - T2_P1: 9.1-9.8 (4E then 4R)
   - T1_P2: 10.1-10.4 (2E then 2R)
   - T2_P2: 11.1-11.8 (4E then 4R)

3. **Tenant metadata arrays** for Phase 83 validation:
   ```bash
   declare -A TENANT_EVAL_COUNT TENANT_RES_COUNT
   TENANT_EVAL_COUNT[T1_P1]=2; TENANT_RES_COUNT[T1_P1]=2
   TENANT_EVAL_COUNT[T2_P1]=4; TENANT_RES_COUNT[T2_P1]=4
   TENANT_EVAL_COUNT[T1_P2]=2; TENANT_RES_COUNT[T1_P2]=2
   TENANT_EVAL_COUNT[T2_P2]=4; TENANT_RES_COUNT[T2_P2]=4
   VALID_TENANTS="T1_P1 T2_P1 T1_P2 T2_P2"
   ```

Group entries by tenant with comment separator lines for readability. The complete mapping file content is specified in the research at 82-RESEARCH.md — use it as the reference.
  </action>
  <verify>
Source the file in bash and verify arrays are populated:
```bash
bash -c 'source tests/e2e/lib/oid_map.sh && echo "OID_MAP entries: ${#OID_MAP[@]}" && echo "T1_P1 E1 oid: ${OID_MAP[T1_P1.E.1.oid]}" && echo "T2_P2 R4 oid: ${OID_MAP[T2_P2.R.4.oid]}" && echo "Valid tenants: $VALID_TENANTS"'
```
Expected: OID_MAP entries: 72 (24 OIDs x 3 fields each), T1_P1 E1 oid: 8.1, T2_P2 R4 oid: 11.8, Valid tenants lists all 4.
Verify TENANT_EVAL_COUNT and TENANT_RES_COUNT are populated for all 4 tenants.
  </verify>
  <done>
oid_map.sh sources without errors. OID_MAP has 72 entries (24 OIDs x 3 fields). All tenant/role/slot lookups return correct OID suffixes and values. TENANT_EVAL_COUNT and TENANT_RES_COUNT correct for all 4 tenants. VALID_TENANTS lists all 4 names.
  </done>
</task>

</tasks>

<verification>
1. Fixture YAML parses and contains 4 tenants with correct metrics/priorities
2. oid_map.sh sources in bash and all lookups return expected values
3. Tenant names consistent between fixture and mapping file (T1_P1, T2_P1, T1_P2, T2_P2)
4. OID suffixes in mapping file match the subtree assignments (8.x, 9.x, 10.x, 11.x)
5. Metric names in fixture match the format used in Plan 01's OID map and devices config
</verification>

<success_criteria>
- tenant-cfg12-v26-four-tenant.yaml is valid YAML with 4 tenants, 24 metrics total, correct priority groups
- oid_map.sh is sourceable bash with 72 OID_MAP entries, correct tenant metadata, and VALID_TENANTS
- Names and OID suffixes are consistent between both files and with the research specification
</success_criteria>

<output>
After completion, create `.planning/phases/82-fixture-and-oid-mapping/82-02-SUMMARY.md`
</output>
