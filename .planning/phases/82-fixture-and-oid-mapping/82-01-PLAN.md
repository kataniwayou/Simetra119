---
phase: 82-fixture-and-oid-mapping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - simulators/e2e-sim/e2e_simulator.py
  - deploy/k8s/snmp-collector/simetra-oid-metric-map.yaml
  - deploy/k8s/snmp-collector/simetra-devices.yaml
  - src/SnmpCollector/config/oid_metric_map.json
  - src/SnmpCollector/config/devices.json
autonomous: true

must_haves:
  truths:
    - "All 24 new v2.6 OIDs are registered in the simulator SNMP engine"
    - "All 24 new OIDs return value 0 (not NoSuchInstance) by default"
    - "All 24 new OID strings resolve to named metrics in the OID metric map"
    - "All 24 new metrics are polled by the E2E-SIM device at 1s interval"
    - "Local dev config matches K8s config for all new OIDs"
  artifacts:
    - path: "simulators/e2e-sim/e2e_simulator.py"
      provides: "TENANT_OIDS_V26 list with 24 OID registrations + baseline dict entries"
      contains: "TENANT_OIDS_V26"
    - path: "deploy/k8s/snmp-collector/simetra-oid-metric-map.yaml"
      provides: "24 new OID-to-metric-name mappings"
      contains: "e2e_T1P1_eval1"
    - path: "deploy/k8s/snmp-collector/simetra-devices.yaml"
      provides: "Poll group for 24 new v2.6 metrics"
      contains: "e2e_T1P1_eval1"
    - path: "src/SnmpCollector/config/oid_metric_map.json"
      provides: "Local dev OID map with 24 new entries"
      contains: "e2e_T1P1_eval1"
    - path: "src/SnmpCollector/config/devices.json"
      provides: "Local dev device config with 24 new poll entries"
      contains: "e2e_T1P1_eval1"
  key_links:
    - from: "simulators/e2e-sim/e2e_simulator.py"
      to: "_make_scenario baseline dict"
      via: "baseline dict entries for all 24 OIDs"
      pattern: "E2E_PREFIX\\.8\\.1.*:.*0"
    - from: "simulators/e2e-sim/e2e_simulator.py"
      to: "OID registration loop"
      via: "TENANT_OIDS_V26 included in for loop"
      pattern: "TENANT_OIDS_V26"
    - from: "deploy/k8s/snmp-collector/simetra-oid-metric-map.yaml"
      to: "simulators/e2e-sim/e2e_simulator.py"
      via: "OID strings match exactly (with .0 suffix)"
      pattern: "999\\.8\\.1\\.0"
---

<objective>
Register 24 new v2.6 OIDs in the simulator, OID metric map, and device poll configuration so the SNMP collector can poll, resolve, and export all new tenant metrics.

Purpose: Without these infrastructure registrations, no v2.6 tenant can function — OIDs would return NoSuchInstance, resolve as "Unknown", or never get polled.
Output: Updated simulator with TENANT_OIDS_V26, extended OID metric map (K8s + local), extended device poll group (K8s + local).
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/82-fixture-and-oid-mapping/82-RESEARCH.md
@simulators/e2e-sim/e2e_simulator.py
@deploy/k8s/snmp-collector/simetra-oid-metric-map.yaml
@deploy/k8s/snmp-collector/simetra-devices.yaml
@src/SnmpCollector/config/oid_metric_map.json
@src/SnmpCollector/config/devices.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 24 v2.6 OIDs to the E2E simulator</name>
  <files>simulators/e2e-sim/e2e_simulator.py</files>
  <action>
Add a `TENANT_OIDS_V26` list after the existing `TENANT_OIDS` list, containing 24 entries for subtrees 8-11. Each entry is a tuple `(oid_str, label, syntax_cls, writable)` with `v2c.Gauge32` syntax and `False` for writable. The exact OID assignments:

```
T1_P1 (subtree 8): .8.1 eval1, .8.2 eval2, .8.3 res1, .8.4 res2
T2_P1 (subtree 9): .9.1 eval1, .9.2 eval2, .9.3 eval3, .9.4 eval4, .9.5 res1, .9.6 res2, .9.7 res3, .9.8 res4
T1_P2 (subtree 10): .10.1 eval1, .10.2 eval2, .10.3 res1, .10.4 res2
T2_P2 (subtree 11): .11.1 eval1, .11.2 eval2, .11.3 eval3, .11.4 eval4, .11.5 res1, .11.6 res2, .11.7 res3, .11.8 res4
```

Label format: `e2e_T1P1_eval1`, `e2e_T2P1_res3`, etc. (no underscore between T and digit, underscore between P and digit removed — e.g. T1P1 not T1_P1 in the metric name).

Include `TENANT_OIDS_V26` in the registration loop by changing the `for` statement from `TEST_OIDS + TENANT_OIDS` to `TEST_OIDS + TENANT_OIDS + TENANT_OIDS_V26`.

Add all 24 new OID keys with default value `0` to the `baseline` dict inside `_make_scenario()`. This is CRITICAL — without it, OIDs return NoSuchInstance (STALE sentinel) and tenants enter Stale state immediately.

Update the module docstring OID count from 24 to 48 (or whatever the current stated count is) to reflect the 24 new OIDs.
  </action>
  <verify>
Run `python -c "import ast; ast.parse(open('simulators/e2e-sim/e2e_simulator.py').read()); print('syntax ok')"` to verify no syntax errors.
Grep for `TENANT_OIDS_V26` in the file — should appear 3 times (definition, registration loop, and possibly a comment).
Grep for `e2e_T2P2_res4` in the file — last OID in the list, confirms all 24 were added.
Count baseline dict entries containing `.8.` through `.11.` — should be 24.
  </verify>
  <done>
TENANT_OIDS_V26 list has exactly 24 entries. Registration loop includes TENANT_OIDS_V26. Baseline dict has 24 new entries with value 0. File parses without syntax errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend OID metric map and device poll config (K8s + local dev)</name>
  <files>
    deploy/k8s/snmp-collector/simetra-oid-metric-map.yaml
    deploy/k8s/snmp-collector/simetra-devices.yaml
    src/SnmpCollector/config/oid_metric_map.json
    src/SnmpCollector/config/devices.json
  </files>
  <action>
**OID metric map (K8s):** Add 24 new entries to `simetra-oid-metric-map.yaml` in the E2E section at the bottom. Each entry follows the existing JSON-in-YAML format:
```
{ "Oid": "1.3.6.1.4.1.47477.999.8.1.0",  "MetricName": "e2e_T1P1_eval1" },
```
Note the `.0` suffix on every OID string (SNMP scalar convention). Group by tenant with a comment header for each (T1_P1, T2_P1, T1_P2, T2_P2). All 24 OIDs from subtrees 8-11.

**OID metric map (local dev):** Add the same 24 entries to `src/SnmpCollector/config/oid_metric_map.json`. Match the JSON format used in that file (it's a raw JSON array, not YAML-wrapped).

**Devices config (K8s):** Add a new poll group to the E2E-SIM device entry in `simetra-devices.yaml` with `IntervalSeconds: 1` and `GraceMultiplier: 2.0`, listing all 24 new metric names. Use the same format as the existing `e2e_eval_T2/T3/T4` poll group.

**Devices config (local dev):** Add the same poll group to `src/SnmpCollector/config/devices.json` in the E2E-SIM device entry.

IMPORTANT: OID strings in the metric map must include the `.0` suffix. Metric names must match exactly between OID map, devices, and the simulator labels (e.g. `e2e_T1P1_eval1` everywhere).
  </action>
  <verify>
Grep for `e2e_T1P1_eval1` across all 4 files — each should contain it.
Grep for `e2e_T2P2_res4` across all 4 files — each should contain it (last metric, confirms completeness).
Count occurrences of `999.8` through `999.11` in the OID metric map YAML — should total 24.
Verify `oid_metric_map.json` is valid JSON: `python -c "import json; json.load(open('src/SnmpCollector/config/oid_metric_map.json')); print('ok')"`.
Verify `devices.json` is valid JSON: `python -c "import json; json.load(open('src/SnmpCollector/config/devices.json')); print('ok')"`.
  </verify>
  <done>
All 4 config files contain all 24 new metric entries. OID map entries use .0 suffix. Device poll group uses IntervalSeconds=1. Local dev files match K8s files for the new entries. All JSON files parse without errors.
  </done>
</task>

</tasks>

<verification>
1. Simulator Python file has no syntax errors
2. All 24 v2.6 OIDs appear in: simulator TENANT_OIDS_V26, simulator baseline dict, OID metric map (K8s), OID metric map (local), devices (K8s), devices (local)
3. OID metric map entries use `.0` suffix on all OID strings
4. Device poll group uses `IntervalSeconds: 1` for fast readiness
5. No OID suffix collision with existing subtrees 1-7
</verification>

<success_criteria>
- 24 new OIDs registered in simulator with baseline value 0
- 24 new OID-to-metric mappings in K8s and local OID metric map
- 24 new metrics in K8s and local device poll config at 1s interval
- All files parse without errors (Python, JSON, YAML)
- Metric names consistent across all files
</success_criteria>

<output>
After completion, create `.planning/phases/82-fixture-and-oid-mapping/82-01-SUMMARY.md`
</output>
