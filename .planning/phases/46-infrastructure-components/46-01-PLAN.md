---
phase: 46-infrastructure-components
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/SnmpCollector/Configuration/TenantOptions.cs
  - src/SnmpCollector/Pipeline/Tenant.cs
  - src/SnmpCollector/Pipeline/TenantVectorRegistry.cs
  - src/SnmpCollector/Services/TenantVectorWatcherService.cs
  - src/SnmpCollector/Pipeline/ISuppressionCache.cs
  - src/SnmpCollector/Pipeline/SuppressionCache.cs
  - src/SnmpCollector/Extensions/ServiceCollectionExtensions.cs
  - tests/SnmpCollector.Tests/Pipeline/SuppressionCacheTests.cs
  - tests/SnmpCollector.Tests/Services/TenantVectorWatcherValidationTests.cs
autonomous: true

must_haves:
  truths:
    - "ISuppressionCache.TrySuppress returns false on first call and true on second call within window"
    - "SuppressionCache entries expire correctly after window elapses (lazy TTL)"
    - "Suppressed calls do NOT re-stamp the timestamp (window does not extend)"
    - "TenantOptions.SuppressionWindowSeconds exists with default 60"
    - "Tenant.SuppressionWindowSeconds is populated from TenantOptions at reload time"
    - "Value+ValueType parse validation at config load time prevents runtime FormatException"
  artifacts:
    - path: "src/SnmpCollector/Pipeline/ISuppressionCache.cs"
      provides: "ISuppressionCache interface"
      exports: ["TrySuppress", "Count"]
    - path: "src/SnmpCollector/Pipeline/SuppressionCache.cs"
      provides: "ConcurrentDictionary-based suppression cache"
      contains: "ConcurrentDictionary<string, DateTimeOffset>"
    - path: "tests/SnmpCollector.Tests/Pipeline/SuppressionCacheTests.cs"
      provides: "Full behavioral test coverage for suppression cache"
      min_lines: 40
  key_links:
    - from: "src/SnmpCollector/Extensions/ServiceCollectionExtensions.cs"
      to: "SuppressionCache"
      via: "AddSingleton<ISuppressionCache, SuppressionCache>()"
      pattern: "AddSingleton.*ISuppressionCache.*SuppressionCache"
    - from: "src/SnmpCollector/Pipeline/TenantVectorRegistry.cs"
      to: "Tenant constructor"
      via: "tenantOpts.SuppressionWindowSeconds passed to new Tenant()"
      pattern: "SuppressionWindowSeconds"
---

<objective>
Build ISuppressionCache + SuppressionCache singleton, add TenantOptions.SuppressionWindowSeconds (propagated to Tenant), and add Value+ValueType parse-time validation in TenantVectorWatcherService.

Purpose: SnapshotJob (Phase 48) needs the suppression cache to prevent duplicate SET commands, the suppression window from Tenant config, and pre-validated command values to avoid runtime FormatException.
Output: ISuppressionCache interface + implementation + DI registration, Tenant.SuppressionWindowSeconds property, Value+ValueType parse validation, and full test coverage.
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-infrastructure-components/46-CONTEXT.md
@.planning/phases/46-infrastructure-components/46-RESEARCH.md
@src/SnmpCollector/Pipeline/Tenant.cs
@src/SnmpCollector/Configuration/TenantOptions.cs
@src/SnmpCollector/Pipeline/TenantVectorRegistry.cs
@src/SnmpCollector/Services/TenantVectorWatcherService.cs
@src/SnmpCollector/Extensions/ServiceCollectionExtensions.cs
@src/SnmpCollector/Pipeline/LivenessVectorService.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: ISuppressionCache + SuppressionCache + TenantOptions.SuppressionWindowSeconds + Tenant propagation + Value+ValueType validation</name>
  <files>
    src/SnmpCollector/Pipeline/ISuppressionCache.cs
    src/SnmpCollector/Pipeline/SuppressionCache.cs
    src/SnmpCollector/Configuration/TenantOptions.cs
    src/SnmpCollector/Pipeline/Tenant.cs
    src/SnmpCollector/Pipeline/TenantVectorRegistry.cs
    src/SnmpCollector/Services/TenantVectorWatcherService.cs
    src/SnmpCollector/Extensions/ServiceCollectionExtensions.cs
  </files>
  <action>
    **1. Create `ISuppressionCache` interface** at `src/SnmpCollector/Pipeline/ISuppressionCache.cs`:
    - `bool TrySuppress(string key, int windowSeconds)` — returns true=suppressed (skip), false=not suppressed (proceed)
    - `int Count { get; }` — diagnostic property
    - XML doc comments on both members

    **2. Create `SuppressionCache` class** at `src/SnmpCollector/Pipeline/SuppressionCache.cs`:
    - `sealed class` implementing `ISuppressionCache`
    - Private `ConcurrentDictionary<string, DateTimeOffset> _stamps`
    - `TrySuppress`: if key exists AND `now - lastStamp < TimeSpan.FromSeconds(windowSeconds)` return true (do NOT update stamp). Otherwise, set `_stamps[key] = now` and return false.
    - CRITICAL: suppressed calls must NOT re-stamp — only the `return false` path stamps.
    - `Count => _stamps.Count`
    - Follow LivenessVectorService pattern exactly.

    **3. Add `SuppressionWindowSeconds` to `TenantOptions`** (`src/SnmpCollector/Configuration/TenantOptions.cs`):
    - `public int SuppressionWindowSeconds { get; set; } = 60;`
    - XML doc comment: "Duration of the command suppression window in seconds. Default: 60."

    **4. Add `SuppressionWindowSeconds` to `Tenant`** (`src/SnmpCollector/Pipeline/Tenant.cs`):
    - Add `public int SuppressionWindowSeconds { get; }` immutable property
    - Add `int suppressionWindowSeconds` as 5th constructor parameter
    - Assign in constructor body

    **5. Update `TenantVectorRegistry.Reload`** (`src/SnmpCollector/Pipeline/TenantVectorRegistry.cs`):
    - Pass `tenantOpts.SuppressionWindowSeconds` as the 5th argument to `new Tenant(...)` at line 113

    **6. Add Value+ValueType parse validation** in `TenantVectorWatcherService.ValidateAndBuildTenants` (`src/SnmpCollector/Services/TenantVectorWatcherService.cs`):
    - After the empty-Value check (step 5, around line 277), before the IP+Port check (step 6):
    - If `cmd.ValueType == "Integer32"` and `!int.TryParse(cmd.Value, out _)` — log Error and `continue`
    - If `cmd.ValueType == "IpAddress"` and `!System.Net.IPAddress.TryParse(cmd.Value, out _)` — log Error and `continue`
    - OctetString: any non-empty string is valid (already checked)

    **7. Register ISuppressionCache** in `ServiceCollectionExtensions.AddSnmpPipeline` (`src/SnmpCollector/Extensions/ServiceCollectionExtensions.cs`):
    - Add `services.AddSingleton<ISuppressionCache, SuppressionCache>();` after the HeartbeatLivenessService registration block
  </action>
  <verify>
    `dotnet build src/SnmpCollector/` compiles without errors.
    `dotnet build tests/SnmpCollector.Tests/` compiles without errors (check for any `new Tenant(` call sites in tests that need the 5th parameter — grep broadly).
  </verify>
  <done>
    ISuppressionCache and SuppressionCache exist with TrySuppress + Count. TenantOptions.SuppressionWindowSeconds defaults to 60. Tenant.SuppressionWindowSeconds is immutable and populated from TenantOptions in TenantVectorRegistry.Reload. Value+ValueType parse validation prevents invalid Integer32/IpAddress values at config load time. ISuppressionCache registered as singleton in DI. All code compiles.
  </done>
</task>

<task type="auto">
  <name>Task 2: SuppressionCache tests + Value+ValueType validation tests</name>
  <files>
    tests/SnmpCollector.Tests/Pipeline/SuppressionCacheTests.cs
    tests/SnmpCollector.Tests/Services/TenantVectorWatcherValidationTests.cs
  </files>
  <action>
    **1. Create `SuppressionCacheTests.cs`** at `tests/SnmpCollector.Tests/Pipeline/SuppressionCacheTests.cs`:
    - `FirstCall_ReturnsFalse_AndStamps` — first TrySuppress returns false, Count is 1
    - `SecondCallWithinWindow_ReturnsTrue` — immediate second call returns true (suppressed)
    - `AfterWindowExpires_ReturnsFalse` — use windowSeconds=0 so window expires immediately, second call returns false
    - `DifferentKeys_Independent` — two different keys are independent (first call for each returns false)
    - `WindowPassedAtCheckTime_NotStored` — first call with 60s window, second call with 0s window returns false (proves window is not stored)
    - `SuppressedCall_DoesNotUpdateTimestamp` — call with 60s window, second call is suppressed (true), third call still suppressed (true, proving stamp was NOT updated by second call)
    - `Count_ReflectsEntries` — add 3 different keys, Count is 3

    **2. Add Value+ValueType parse validation tests** to existing `TenantVectorWatcherValidationTests.cs`:
    - `Command_Integer32_InvalidValue_Skipped` — ValueType=Integer32, Value="not-a-number" is skipped with Error log
    - `Command_IpAddress_InvalidValue_Skipped` — ValueType=IpAddress, Value="not-an-ip" is skipped with Error log
    - `Command_OctetString_AnyValue_Accepted` — ValueType=OctetString, Value="anything" passes validation
    - `Command_Integer32_ValidValue_Accepted` — ValueType=Integer32, Value="42" passes validation
    - Follow existing test patterns in the file (use the same test helper methods for tenant config construction).
  </action>
  <verify>
    `dotnet test tests/SnmpCollector.Tests/ --filter "SuppressionCache|WatcherValidation"` — all new tests pass.
    `dotnet test tests/SnmpCollector.Tests/` — all existing tests still pass.
  </verify>
  <done>
    7+ SuppressionCache behavioral tests pass confirming first-call/suppress/expiry/independence/no-re-stamp semantics. 4+ Value+ValueType validation tests pass confirming invalid Integer32/IpAddress values are skipped at config load time. All existing tests remain green.
  </done>
</task>

</tasks>

<verification>
- `dotnet build src/SnmpCollector/` succeeds
- `dotnet test tests/SnmpCollector.Tests/` — all tests pass (new + existing)
- `grep -rn "ISuppressionCache" src/SnmpCollector/` shows interface, implementation, and DI registration
- `grep -rn "SuppressionWindowSeconds" src/SnmpCollector/` shows TenantOptions, Tenant, and TenantVectorRegistry
- `grep -rn "TryParse" src/SnmpCollector/Services/TenantVectorWatcherService.cs` shows Value+ValueType parse validation
</verification>

<success_criteria>
- ISuppressionCache.TrySuppress returns false on first call, true within window, false after expiry
- Suppressed calls do NOT re-stamp (verified by test)
- TenantOptions.SuppressionWindowSeconds defaults to 60
- Tenant.SuppressionWindowSeconds populated from TenantOptions in Reload
- Invalid Integer32/IpAddress values are skipped at config load time with Error log
- All tests pass (new + existing)
</success_criteria>

<output>
After completion, create `.planning/phases/46-infrastructure-components/46-01-SUMMARY.md`
</output>
