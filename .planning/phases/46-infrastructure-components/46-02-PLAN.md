---
phase: 46-infrastructure-components
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/SnmpCollector/Configuration/SnapshotJobOptions.cs
  - src/SnmpCollector/Pipeline/ISnmpClient.cs
  - src/SnmpCollector/Pipeline/SharpSnmpClient.cs
  - src/SnmpCollector/Extensions/ServiceCollectionExtensions.cs
  - tests/SnmpCollector.Tests/Pipeline/SharpSnmpClientSetTests.cs
autonomous: true

must_haves:
  truths:
    - "SnapshotJobOptions loads from 'SnapshotJob' config section with ValidateDataAnnotations + ValidateOnStart"
    - "SnapshotJobOptions.IntervalSeconds defaults to 15 with Range(1,300)"
    - "SnapshotJobOptions.TimeoutMultiplier defaults to 0.8 with Range(0.1,0.9)"
    - "ISnmpClient.SetAsync exists and SharpSnmpClient delegates to Messenger.SetAsync"
    - "ParseSnmpData dispatches Integer32, OctetString, and IpAddress correctly"
    - "ParseSnmpData uses Lextm.SharpSnmpLib.IP (not IpAddress) for IpAddress type"
  artifacts:
    - path: "src/SnmpCollector/Configuration/SnapshotJobOptions.cs"
      provides: "SnapshotJob config POCO"
      contains: "SectionName"
    - path: "src/SnmpCollector/Pipeline/ISnmpClient.cs"
      provides: "SetAsync method on interface"
      contains: "SetAsync"
    - path: "src/SnmpCollector/Pipeline/SharpSnmpClient.cs"
      provides: "SetAsync delegation + ParseSnmpData static helper"
      contains: "ParseSnmpData"
    - path: "tests/SnmpCollector.Tests/Pipeline/SharpSnmpClientSetTests.cs"
      provides: "ParseSnmpData dispatch tests"
      min_lines: 30
  key_links:
    - from: "src/SnmpCollector/Extensions/ServiceCollectionExtensions.cs"
      to: "SnapshotJobOptions"
      via: "AddOptions<SnapshotJobOptions>().Bind().ValidateDataAnnotations().ValidateOnStart()"
      pattern: "SnapshotJobOptions"
    - from: "src/SnmpCollector/Pipeline/SharpSnmpClient.cs"
      to: "Messenger.SetAsync"
      via: "delegation with single Variable wrapped in List"
      pattern: "Messenger.SetAsync"
---

<objective>
Create SnapshotJobOptions configuration POCO and add ISnmpClient.SetAsync with ParseSnmpData static helper on SharpSnmpClient.

Purpose: SnapshotJob (Phase 48) needs job options for interval and timeout configuration. CommandWorkerService (Phase 47) needs SetAsync to execute SNMP SET commands and ParseSnmpData to convert config values to SharpSnmpLib types.
Output: SnapshotJobOptions with DI registration, SetAsync on ISnmpClient/SharpSnmpClient, ParseSnmpData helper, and tests.
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-infrastructure-components/46-CONTEXT.md
@.planning/phases/46-infrastructure-components/46-RESEARCH.md
@src/SnmpCollector/Pipeline/ISnmpClient.cs
@src/SnmpCollector/Pipeline/SharpSnmpClient.cs
@src/SnmpCollector/Configuration/HeartbeatJobOptions.cs
@src/SnmpCollector/Extensions/ServiceCollectionExtensions.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: SnapshotJobOptions + ISnmpClient.SetAsync + SharpSnmpClient.SetAsync + ParseSnmpData</name>
  <files>
    src/SnmpCollector/Configuration/SnapshotJobOptions.cs
    src/SnmpCollector/Pipeline/ISnmpClient.cs
    src/SnmpCollector/Pipeline/SharpSnmpClient.cs
    src/SnmpCollector/Extensions/ServiceCollectionExtensions.cs
  </files>
  <action>
    **1. Create `SnapshotJobOptions.cs`** at `src/SnmpCollector/Configuration/SnapshotJobOptions.cs`:
    - Follow HeartbeatJobOptions pattern exactly
    - `public sealed class SnapshotJobOptions`
    - `public const string SectionName = "SnapshotJob";`
    - `[Range(1, 300)] public int IntervalSeconds { get; set; } = 15;`
    - `[Range(0.1, 0.9)] public double TimeoutMultiplier { get; set; } = 0.8;`
    - Using `System.ComponentModel.DataAnnotations`
    - NOTE: SuppressionWindowSeconds is NOT here — it is per-tenant on TenantOptions (plan 46-01 handles this)

    **2. Register SnapshotJobOptions** in `ServiceCollectionExtensions.AddSnmpConfiguration`:
    - Add after the HeartbeatJobOptions block (around line 209):
    ```csharp
    services.AddOptions<SnapshotJobOptions>()
        .Bind(configuration.GetSection(SnapshotJobOptions.SectionName))
        .ValidateDataAnnotations()
        .ValidateOnStart();
    ```

    **3. Add `SetAsync` to `ISnmpClient`** (`src/SnmpCollector/Pipeline/ISnmpClient.cs`):
    - Add method signature:
    ```csharp
    Task<IList<Variable>> SetAsync(
        VersionCode version,
        IPEndPoint endpoint,
        OctetString community,
        Variable variable,
        CancellationToken ct);
    ```
    - Note: single `Variable` parameter (not `IList<Variable>`) — one OID per call per CONTEXT.md
    - Add XML doc comment: "Performs an SNMP SET request and returns the response varbinds."

    **4. Implement `SetAsync` + `ParseSnmpData` on `SharpSnmpClient`** (`src/SnmpCollector/Pipeline/SharpSnmpClient.cs`):
    - Add using alias at top: `using SnmpTimeout = Lextm.SharpSnmpLib.Messaging.TimeoutException;` (prevents CS0104)
    - `SetAsync` delegates to `Messenger.SetAsync(version, endpoint, community, new List<Variable> { variable }, ct)`
    - Add `public static ISnmpData ParseSnmpData(string value, string valueType)` with switch expression:
      - `"Integer32" => new Integer32(int.Parse(value))`
      - `"OctetString" => new OctetString(value)`
      - `"IpAddress" => new IP(value)` — CRITICAL: use `IP` class, NOT `IpAddress` (CS0246)
      - `_ => throw new ArgumentException($"Unsupported ValueType: {valueType}", nameof(valueType))`
  </action>
  <verify>
    `dotnet build src/SnmpCollector/` compiles without errors.
    Verify `SetAsync` is on both `ISnmpClient` and `SharpSnmpClient`.
    Verify `ParseSnmpData` uses `IP` (not `IpAddress`).
  </verify>
  <done>
    SnapshotJobOptions exists with SectionName, IntervalSeconds (default 15), TimeoutMultiplier (default 0.8), registered with ValidateDataAnnotations + ValidateOnStart. ISnmpClient.SetAsync takes a single Variable. SharpSnmpClient.SetAsync delegates to Messenger.SetAsync. ParseSnmpData dispatches Integer32/OctetString/IpAddress correctly using IP class.
  </done>
</task>

<task type="auto">
  <name>Task 2: ParseSnmpData tests</name>
  <files>
    tests/SnmpCollector.Tests/Pipeline/SharpSnmpClientSetTests.cs
  </files>
  <action>
    **Create `SharpSnmpClientSetTests.cs`** at `tests/SnmpCollector.Tests/Pipeline/SharpSnmpClientSetTests.cs`:

    Tests for `SharpSnmpClient.ParseSnmpData`:
    - `ParseSnmpData_Integer32_ReturnsInteger32Instance` — `ParseSnmpData("42", "Integer32")` returns `Integer32` type
    - `ParseSnmpData_OctetString_ReturnsOctetStringInstance` — `ParseSnmpData("hello", "OctetString")` returns `OctetString` type
    - `ParseSnmpData_IpAddress_ReturnsIPInstance` — `ParseSnmpData("10.0.0.1", "IpAddress")` returns `IP` type (Lextm.SharpSnmpLib.IP)
    - `ParseSnmpData_Integer32_CorrectValue` — parse "42", cast result to Integer32, assert `.ToInt32() == 42`
    - `ParseSnmpData_UnknownType_ThrowsArgumentException` — `ParseSnmpData("value", "Unknown")` throws ArgumentException
    - `ParseSnmpData_NegativeInteger_Works` — `ParseSnmpData("-1", "Integer32")` returns Integer32 with value -1
    - Optional: `[Theory]` with `[InlineData]` for the three valid types confirming `NotNull`

    Use `using Lextm.SharpSnmpLib;` for type assertions. Reference SharpSnmpLib types directly.
  </action>
  <verify>
    `dotnet test tests/SnmpCollector.Tests/ --filter "SharpSnmpClientSet"` — all tests pass.
    `dotnet test tests/SnmpCollector.Tests/` — all existing tests still pass.
  </verify>
  <done>
    6+ ParseSnmpData tests pass confirming Integer32/OctetString/IpAddress dispatch, correct values, and ArgumentException for unknown types. All existing tests remain green.
  </done>
</task>

</tasks>

<verification>
- `dotnet build src/SnmpCollector/` succeeds
- `dotnet test tests/SnmpCollector.Tests/` — all tests pass
- `grep -rn "SnapshotJobOptions" src/SnmpCollector/` shows options class and DI registration
- `grep -rn "SetAsync" src/SnmpCollector/Pipeline/` shows interface and implementation
- `grep -rn "ParseSnmpData" src/SnmpCollector/` shows static helper
- No CS0246 or CS0104 compile errors
</verification>

<success_criteria>
- SnapshotJobOptions loads from "SnapshotJob" section with validation
- IntervalSeconds defaults to 15 (Range 1-300), TimeoutMultiplier defaults to 0.8 (Range 0.1-0.9)
- ISnmpClient.SetAsync accepts single Variable, returns IList<Variable>
- SharpSnmpClient.SetAsync delegates to Messenger.SetAsync
- ParseSnmpData maps "Integer32"->Integer32, "OctetString"->OctetString, "IpAddress"->IP
- All tests pass (new + existing)
</success_criteria>

<output>
After completion, create `.planning/phases/46-infrastructure-components/46-02-SUMMARY.md`
</output>
