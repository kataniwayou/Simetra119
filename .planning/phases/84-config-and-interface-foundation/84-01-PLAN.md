---
phase: 84-config-and-interface-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/SnmpCollector/Configuration/LeaseOptions.cs
  - src/SnmpCollector/Telemetry/IPreferredStampReader.cs
  - src/SnmpCollector/Telemetry/NullPreferredStampReader.cs
  - src/SnmpCollector/Telemetry/PreferredLeaderService.cs
  - src/SnmpCollector/Extensions/ServiceCollectionExtensions.cs
  - tests/SnmpCollector.Tests/Telemetry/PreferredLeaderServiceTests.cs
autonomous: true

must_haves:
  truths:
    - "App starts and behaves identically when PreferredNode is absent or empty in config"
    - "Pod reads PHYSICAL_HOSTNAME env var at startup and determines _isPreferredPod"
    - "Startup log line confirms preferred vs non-preferred identity when PreferredNode is configured"
    - "IPreferredStampReader.IsPreferredStampFresh returns false (stub) in this phase"
    - "Local dev (non-K8s) uses NullPreferredStampReader — feature silently disabled"
  artifacts:
    - path: "src/SnmpCollector/Configuration/LeaseOptions.cs"
      provides: "PreferredNode nullable string property"
      contains: "public string? PreferredNode"
    - path: "src/SnmpCollector/Telemetry/IPreferredStampReader.cs"
      provides: "Single-bool interface contract"
      exports: ["IPreferredStampReader"]
    - path: "src/SnmpCollector/Telemetry/NullPreferredStampReader.cs"
      provides: "Null-object implementation (always false)"
      exports: ["NullPreferredStampReader"]
    - path: "src/SnmpCollector/Telemetry/PreferredLeaderService.cs"
      provides: "Startup identity resolution and stub IPreferredStampReader"
      exports: ["PreferredLeaderService"]
    - path: "src/SnmpCollector/Extensions/ServiceCollectionExtensions.cs"
      provides: "DI registration for PreferredLeaderService (K8s) and NullPreferredStampReader (local)"
      contains: "AddSingleton<PreferredLeaderService>"
    - path: "tests/SnmpCollector.Tests/Telemetry/PreferredLeaderServiceTests.cs"
      provides: "Unit tests for identity resolution and DI singleton pattern"
      min_lines: 60
  key_links:
    - from: "src/SnmpCollector/Telemetry/PreferredLeaderService.cs"
      to: "src/SnmpCollector/Configuration/LeaseOptions.cs"
      via: "IOptions<LeaseOptions> constructor injection"
      pattern: "IOptions<LeaseOptions>"
    - from: "src/SnmpCollector/Extensions/ServiceCollectionExtensions.cs"
      to: "src/SnmpCollector/Telemetry/PreferredLeaderService.cs"
      via: "Concrete-first DI registration (same pattern as K8sLeaseElection)"
      pattern: "AddSingleton<PreferredLeaderService>"
    - from: "src/SnmpCollector/Extensions/ServiceCollectionExtensions.cs"
      to: "src/SnmpCollector/Telemetry/NullPreferredStampReader.cs"
      via: "Local dev else branch"
      pattern: "NullPreferredStampReader"
---

<objective>
Create the config field, interface, and stub implementation for preferred leader election so
that the system knows which pod is preferred and the two-lease design is locked in code before
any behavioral changes exist.

Purpose: Foundation layer for v3.0 preferred leader election. After this plan, downstream
phases (85-89) can build real heartbeat behavior against a stable interface contract.

Output: LeaseOptions.PreferredNode field, IPreferredStampReader interface,
NullPreferredStampReader (local dev), PreferredLeaderService (K8s stub with identity
resolution), DI wiring, and unit tests.
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/84-config-and-interface-foundation/84-RESEARCH.md
@src/SnmpCollector/Configuration/LeaseOptions.cs
@src/SnmpCollector/Telemetry/ILeaderElection.cs
@src/SnmpCollector/Telemetry/AlwaysLeaderElection.cs
@src/SnmpCollector/Extensions/ServiceCollectionExtensions.cs
@tests/SnmpCollector.Tests/Telemetry/LeaderElectionTests.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create interface, implementations, and extend LeaseOptions</name>
  <files>
    src/SnmpCollector/Configuration/LeaseOptions.cs
    src/SnmpCollector/Telemetry/IPreferredStampReader.cs
    src/SnmpCollector/Telemetry/NullPreferredStampReader.cs
    src/SnmpCollector/Telemetry/PreferredLeaderService.cs
  </files>
  <action>
    1. **LeaseOptions.cs** — Add a single nullable property after the existing DurationSeconds property:
       ```csharp
       /// <summary>
       /// Optional. When set, the pod whose PHYSICAL_HOSTNAME matches this value
       /// is treated as the preferred leader candidate.
       /// Feature is disabled when absent or empty — backward compatible.
       /// Derived heartbeat lease name: "{Name}-preferred".
       /// </summary>
       public string? PreferredNode { get; set; }
       ```
       No `[Required]`, no `[Range]`, no DataAnnotations. Do NOT modify LeaseOptionsValidator.

    2. **IPreferredStampReader.cs** — New file in `SnmpCollector.Telemetry` namespace.
       Single-member interface with XML doc:
       ```csharp
       bool IsPreferredStampFresh { get; }
       ```
       Include thread-safety note in XML doc (read on leader-election hot path).
       Follow the exact style of ILeaderElection.cs (file-scoped namespace, XML summary).

    3. **NullPreferredStampReader.cs** — New file in `SnmpCollector.Telemetry` namespace.
       Sealed class implementing IPreferredStampReader, always returns false.
       Follow the exact style of AlwaysLeaderElection.cs (sealed, single expression-bodied property).

    4. **PreferredLeaderService.cs** — New file in `SnmpCollector.Telemetry` namespace.
       Sealed class implementing IPreferredStampReader with:
       - Constructor takes `IOptions<LeaseOptions>` and `ILogger<PreferredLeaderService>`
       - Reads `LeaseOptions.PreferredNode` — if null/empty, sets `_isPreferredPod = false`, returns (no log)
       - Reads `Environment.GetEnvironmentVariable("PHYSICAL_HOSTNAME")` — if null/empty when PreferredNode IS configured, logs Warning and sets `_isPreferredPod = false` (no crash)
       - Otherwise: exact case-sensitive string match `physicalHostname == preferredNode`, logs Information with PHYSICAL_HOSTNAME, PreferredNode, and IsPreferredPod values
       - `_isPreferredPod` is a `readonly bool` set once in constructor
       - `IsPreferredStampFresh` property returns `false` (stub — real behavior in Phase 85)
       - `IsPreferredPod` public read-only property exposes `_isPreferredPod` for downstream phases
       - Add `using Microsoft.Extensions.Options;` and `using SnmpCollector.Configuration;`

    IMPORTANT: Do NOT use NODE_NAME — the env var in this codebase is PHYSICAL_HOSTNAME.
    Do NOT register PreferredLeaderService as IHostedService — no background loop until Phase 85.
    Do NOT add any new validators to LeaseOptionsValidator.
  </action>
  <verify>
    `dotnet build src/SnmpCollector/SnmpCollector.csproj` succeeds with no errors or warnings.
    Confirm: `grep -r "PreferredNode" src/SnmpCollector/Configuration/LeaseOptions.cs` shows the new property.
    Confirm: `grep -r "IsPreferredStampFresh" src/SnmpCollector/Telemetry/IPreferredStampReader.cs` shows the interface.
    Confirm: `grep -r "PHYSICAL_HOSTNAME" src/SnmpCollector/Telemetry/PreferredLeaderService.cs` shows correct env var.
  </verify>
  <done>
    Four files exist: LeaseOptions has PreferredNode, IPreferredStampReader defines the contract,
    NullPreferredStampReader is the null-object, PreferredLeaderService resolves identity at construction.
    Project compiles cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: DI registration and unit tests</name>
  <files>
    src/SnmpCollector/Extensions/ServiceCollectionExtensions.cs
    tests/SnmpCollector.Tests/Telemetry/PreferredLeaderServiceTests.cs
  </files>
  <action>
    1. **ServiceCollectionExtensions.cs** — In the `AddSnmpConfiguration` method:

       **K8s branch** (inside `if (k8s.KubernetesClientConfiguration.IsInCluster())`):
       After the K8sLeaseElection block (after line 242: `services.AddHostedService(sp => ...K8sLeaseElection...)`),
       and BEFORE the ConfigMap watcher registrations (before line 244: OidMapWatcherService), add:
       ```csharp
       // Phase 84: Preferred leader identity resolution (stub — no background loop until Phase 85).
       // Concrete-first pattern: same singleton serves both PreferredLeaderService and IPreferredStampReader.
       services.AddSingleton<PreferredLeaderService>();
       services.AddSingleton<IPreferredStampReader>(
           sp => sp.GetRequiredService<PreferredLeaderService>());
       ```

       **Local dev branch** (inside `else` block, after the AlwaysLeaderElection line):
       ```csharp
       // Phase 84: Preferred leader feature disabled in local dev.
       services.AddSingleton<IPreferredStampReader, NullPreferredStampReader>();
       ```

    2. **PreferredLeaderServiceTests.cs** — New test file following the LeaderElectionTests.cs pattern.
       Namespace: `SnmpCollector.Tests.Telemetry`. Use `Microsoft.Extensions.Options.Options.Create()`.

       Write these tests (minimum):

       a) `IsPreferredPod_WhenHostnameMatches_ReturnsTrue` — Set PHYSICAL_HOSTNAME="node-1",
          PreferredNode="node-1", assert IsPreferredPod is true.

       b) `IsPreferredPod_WhenHostnameDiffers_ReturnsFalse` — Set PHYSICAL_HOSTNAME="node-2",
          PreferredNode="node-1", assert IsPreferredPod is false.

       c) `IsPreferredPod_WhenPreferredNodeEmpty_ReturnsFalse` — PreferredNode=null,
          assert IsPreferredPod is false (feature disabled).

       d) `IsPreferredPod_WhenPhysicalHostnameEmpty_ReturnsFalse` — Set PHYSICAL_HOSTNAME to null,
          PreferredNode="node-1", assert IsPreferredPod is false (graceful degradation).

       e) `IsPreferredStampFresh_AlwaysReturnsFalse` — Stub behavior in Phase 84.

       f) `DiSingleton_K8sPath_ResolvesToSameInstance` — Register concrete-first pattern,
          assert IPreferredStampReader and PreferredLeaderService resolve to same instance (SC#5 pattern).

       g) `DiSingleton_LocalDev_RegistersNullStampReader` — Register NullPreferredStampReader,
          assert IPreferredStampReader resolves to NullPreferredStampReader.

       h) `NullPreferredStampReader_IsPreferredStampFresh_ReturnsFalse` — Direct construction test.

       IMPORTANT: Tests that set PHYSICAL_HOSTNAME MUST restore the original value in a try/finally
       or IDisposable pattern to avoid cross-test contamination. Use `[Collection("EnvVarTests")]`
       or equivalent to prevent parallel execution of env-var-mutating tests.

       Use `NullLogger<PreferredLeaderService>.Instance` for tests that don't assert on log output.
       For test (d), consider using a real ILogger or verify the warning is logged.

       LeaseOptions in tests needs `Name` and `Namespace` set (they are `required`):
       ```csharp
       new LeaseOptions { Name = "test-leader", Namespace = "test-ns", PreferredNode = "node-1" }
       ```
  </action>
  <verify>
    `dotnet build` succeeds for both projects.
    `dotnet test tests/SnmpCollector.Tests/SnmpCollector.Tests.csproj --filter "FullyQualifiedName~PreferredLeaderService"` — all tests pass.
    `dotnet test tests/SnmpCollector.Tests/SnmpCollector.Tests.csproj` — full test suite passes (no regressions).
  </verify>
  <done>
    DI registration wires PreferredLeaderService in K8s path and NullPreferredStampReader in local dev path.
    8 unit tests pass covering identity resolution, stub behavior, DI singleton pattern, and null-object.
    Full test suite passes with no regressions. Existing app behavior is unchanged.
  </done>
</task>

</tasks>

<verification>
1. `dotnet build src/SnmpCollector/SnmpCollector.csproj` — compiles with zero errors/warnings
2. `dotnet test tests/SnmpCollector.Tests/SnmpCollector.Tests.csproj` — all tests pass (existing + new)
3. `grep -r "NODE_NAME" src/SnmpCollector/Telemetry/PreferredLeaderService.cs` — returns nothing (correct env var is PHYSICAL_HOSTNAME)
4. `grep -r "AddHostedService.*PreferredLeader" src/SnmpCollector/` — returns nothing (not registered as hosted service in Phase 84)
5. `grep -r "PreferredNode" src/SnmpCollector/Configuration/LeaseOptions.cs` — shows nullable string property
6. `grep -r "IPreferredStampReader" src/SnmpCollector/Extensions/ServiceCollectionExtensions.cs` — shows registration in both K8s and local dev branches
</verification>

<success_criteria>
- LeaseOptions.PreferredNode is a nullable string with no validation attributes
- IPreferredStampReader has exactly one member: bool IsPreferredStampFresh
- NullPreferredStampReader.IsPreferredStampFresh always returns false
- PreferredLeaderService resolves _isPreferredPod once at construction from PHYSICAL_HOSTNAME env var
- PreferredLeaderService.IsPreferredStampFresh returns false (stub)
- PreferredLeaderService logs identity at Information level when PreferredNode is configured
- PreferredLeaderService logs Warning when PreferredNode configured but PHYSICAL_HOSTNAME empty
- DI: K8s path registers concrete-first (PreferredLeaderService -> IPreferredStampReader forwarding)
- DI: Local dev path registers NullPreferredStampReader
- All existing tests pass (no regressions)
- 8 new unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/84-config-and-interface-foundation/84-01-SUMMARY.md`
</output>
