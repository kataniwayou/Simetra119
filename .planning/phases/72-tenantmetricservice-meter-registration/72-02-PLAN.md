---
phase: 72-tenantmetricservice-meter-registration
plan: 02
type: execute
wave: 2
depends_on: ["72-01"]
files_modified:
  - src/SnmpCollector/Jobs/SnapshotJob.cs
  - tests/SnmpCollector.Tests/Jobs/SnapshotJobTests.cs
autonomous: true

must_haves:
  truths:
    - "SnapshotJob uses TenantState enum instead of internal TierResult"
    - "SnapshotJob accepts ITenantMetricService via constructor injection"
    - "Pre-tier path returns TenantState.NotReady (not Unresolved)"
    - "Advance gate blocks on both NotReady and Unresolved"
    - "All existing SnapshotJob tests pass with TenantState references"
  artifacts:
    - path: "src/SnmpCollector/Jobs/SnapshotJob.cs"
      provides: "SnapshotJob using TenantState enum and ITenantMetricService injection"
      contains: "ITenantMetricService"
    - path: "tests/SnmpCollector.Tests/Jobs/SnapshotJobTests.cs"
      provides: "Updated tests using TenantState instead of SnapshotJob.TierResult"
      contains: "TenantState"
  key_links:
    - from: "src/SnmpCollector/Jobs/SnapshotJob.cs"
      to: "src/SnmpCollector/Pipeline/TenantState.cs"
      via: "using SnmpCollector.Pipeline and TenantState return type"
      pattern: "TenantState"
    - from: "src/SnmpCollector/Jobs/SnapshotJob.cs"
      to: "ITenantMetricService"
      via: "constructor injection"
      pattern: "ITenantMetricService"
---

<objective>
Migrate SnapshotJob from internal TierResult enum to the shared TenantState enum, add ITenantMetricService constructor injection, and update the pre-tier path to return NotReady with advance-gate blocking on both NotReady and Unresolved.

Purpose: Prepares SnapshotJob for Phase 73 instrumentation by wiring in the metric service and establishing correct enum semantics. The pre-tier NotReady distinction (vs Unresolved) is essential for accurate metric recording.

Output: SnapshotJob.cs refactored (TierResult removed, TenantState used, ITenantMetricService injected), SnapshotJobTests.cs updated (30+ TierResult references changed to TenantState). All tests pass.
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-tenantmetricservice-meter-registration/72-CONTEXT.md
@.planning/phases/72-tenantmetricservice-meter-registration/72-RESEARCH.md
@.planning/phases/72-tenantmetricservice-meter-registration/72-01-SUMMARY.md
@src/SnmpCollector/Jobs/SnapshotJob.cs
@tests/SnmpCollector.Tests/Jobs/SnapshotJobTests.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate SnapshotJob from TierResult to TenantState and add ITenantMetricService injection</name>
  <files>
    src/SnmpCollector/Jobs/SnapshotJob.cs
  </files>
  <action>
    1. Remove the internal enum definition at line 33:
       `internal enum TierResult { Resolved, Healthy, Unresolved }`
       This is replaced by the public TenantState enum in src/SnmpCollector/Pipeline/TenantState.cs (created in Plan 01).

    2. Add `using SnmpCollector.Pipeline;` if not already present (TenantState lives there). The file already has `using SnmpCollector.Pipeline;` on line 6 — verify it's there.

    3. Replace ALL TierResult references with TenantState throughout the file. There are ~10 occurrences:
       - Line 68: `var results = new TierResult[...]` -> `var results = new TenantState[...]`
       - Line 85: `results[i] == TierResult.Unresolved` -> `results[i] == TenantState.Unresolved`
       - Line 92: `results[i] == TierResult.Unresolved` -> update to `results[i] == TenantState.Unresolved || results[i] == TenantState.NotReady` (advance gate blocks on BOTH)
       - Line 134: `internal TierResult EvaluateTenant(...)` -> `internal TenantState EvaluateTenant(...)`
       - Line 142: `return TierResult.Unresolved;` (pre-tier path) -> `return TenantState.NotReady;` (this is the semantic fix — pre-tier is NotReady, not Unresolved)
       - Line 161: `return TierResult.Resolved;` -> `return TenantState.Resolved;`
       - Line 175: `return TierResult.Healthy;` -> `return TenantState.Healthy;`
       - Line 218: `return TierResult.Unresolved;` -> `return TenantState.Unresolved;`

    4. IMPORTANT advance gate fix: The line that checks `results[i] == TierResult.Unresolved` for incrementing `totalUnresolved` (line 85) should also count NotReady. Update to:
       `if (results[i] == TenantState.Unresolved || results[i] == TenantState.NotReady) totalUnresolved++;`
       And the advance-gate decision line (line 92) similarly:
       `if (results[i] == TenantState.Unresolved || results[i] == TenantState.NotReady)`

       This is behavior-preserving: before, NotReady returned Unresolved which triggered both checks. Now NotReady is its own value that must be explicitly checked.

    5. Add ITenantMetricService to constructor:
       - Add field: `private readonly ITenantMetricService _tenantMetrics;`
       - Add constructor parameter: `ITenantMetricService tenantMetrics` after `PipelineMetricService pipelineMetrics`
       - Assign in constructor body: `_tenantMetrics = tenantMetrics;`
       - The field is not CALLED in this phase (Phase 73 adds the calls). It is wired and stored only.
  </action>
  <verify>
    Run `dotnet build src/SnmpCollector/SnmpCollector.csproj` — must compile with zero errors.
    Grep for "TierResult" in SnapshotJob.cs — must return zero results.
    Grep for "TenantState.NotReady" in SnapshotJob.cs — must appear exactly once (the pre-tier return).
    Grep for "ITenantMetricService" in SnapshotJob.cs — must appear (field + constructor parameter).
  </verify>
  <done>
    SnapshotJob no longer contains TierResult. All returns use TenantState. Pre-tier path returns NotReady. Advance gate blocks on both NotReady and Unresolved. ITenantMetricService is injected and stored.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update SnapshotJobTests to use TenantState instead of SnapshotJob.TierResult</name>
  <files>
    tests/SnmpCollector.Tests/Jobs/SnapshotJobTests.cs
  </files>
  <action>
    1. Add `using SnmpCollector.Pipeline;` to the test file (for TenantState).

    2. Replace ALL occurrences of `SnapshotJob.TierResult.Healthy` with `TenantState.Healthy` (~30 occurrences across the file).
       Replace ALL occurrences of `SnapshotJob.TierResult.Resolved` with `TenantState.Resolved`.
       Replace ALL occurrences of `SnapshotJob.TierResult.Unresolved` with `TenantState.Unresolved`.

    3. The test helper that constructs SnapshotJob now needs an ITenantMetricService parameter. Create a no-op mock:
       - Use `NSubstitute` if already in the test project, OR
       - Create a minimal `NullTenantMetricService` implementing ITenantMetricService with empty method bodies
       - Check the existing test file to see how PipelineMetricService is provided — it likely constructs a real one via IMeterFactory. Do the same for TenantMetricService if feasible, or use a substitute.
       - Pass the mock/null implementation when constructing SnapshotJob in the test setup.

    4. For assertions that check `Assert.Equal(SnapshotJob.TierResult.Unresolved, result)` on what is now the pre-tier (NotReady) path: update to `Assert.Equal(TenantState.NotReady, result)` — but ONLY for tests that exercise the pre-tier path (no holders present / staleness check). Tests that exercise the actual Tier 3 unresolved path keep `TenantState.Unresolved`.

       Key distinction: Read each test carefully. If the test sets up a scenario where the tenant has NO holders or all holders are stale (pre-tier exit), the expected result changes from `Unresolved` to `NotReady`. If the test goes through full evaluation and returns Unresolved at line 218, it stays `TenantState.Unresolved`.

       Specifically look at test names and setup: tests about "no holders" or "all stale before evaluation" should expect NotReady. Tests about "threshold violated but no command" or "command dispatch path ending unresolved" should expect Unresolved.

    5. Verify the entire test file compiles and all tests pass.
  </action>
  <verify>
    Run `dotnet test tests/SnmpCollector.Tests/SnmpCollector.Tests.csproj --filter "FullyQualifiedName~SnapshotJobTests"` — all SnapshotJob tests must pass.
    Run `dotnet test tests/SnmpCollector.Tests/SnmpCollector.Tests.csproj` — full test suite must pass (zero regressions).
    Grep for "TierResult" in SnapshotJobTests.cs — must return zero results.
    Grep for "TenantState" in SnapshotJobTests.cs — must appear in all assertions.
  </verify>
  <done>
    All SnapshotJob.TierResult references replaced with TenantState. Pre-tier tests expect NotReady. ITenantMetricService is provided to SnapshotJob in test setup. Full test suite passes with zero regressions.
  </done>
</task>

</tasks>

<verification>
1. `dotnet build src/SnmpCollector/SnmpCollector.csproj` compiles with zero errors
2. `dotnet test tests/SnmpCollector.Tests/SnmpCollector.Tests.csproj` — full suite passes
3. Grep confirms zero occurrences of "TierResult" in both SnapshotJob.cs and SnapshotJobTests.cs
4. Grep confirms "TenantState.NotReady" appears in SnapshotJob.cs pre-tier return
5. Grep confirms advance gate checks both NotReady and Unresolved
6. Grep confirms ITenantMetricService field and constructor parameter in SnapshotJob.cs
</verification>

<success_criteria>
- Zero occurrences of TierResult anywhere in SnapshotJob.cs or SnapshotJobTests.cs
- Pre-tier path returns TenantState.NotReady (not Unresolved)
- Advance gate blocks on both NotReady and Unresolved (behavior-preserving)
- ITenantMetricService injected into SnapshotJob constructor (stored, not called yet — Phase 73)
- All SnapshotJob tests pass with TenantState assertions
- Full test suite passes with zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/72-tenantmetricservice-meter-registration/72-02-SUMMARY.md`
</output>
