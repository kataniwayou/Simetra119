---
phase: 72-tenantmetricservice-meter-registration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/SnmpCollector/Pipeline/TenantState.cs
  - src/SnmpCollector/Telemetry/TelemetryConstants.cs
  - src/SnmpCollector/Telemetry/ITenantMetricService.cs
  - src/SnmpCollector/Telemetry/TenantMetricService.cs
  - src/SnmpCollector/Extensions/ServiceCollectionExtensions.cs
  - tests/SnmpCollector.Tests/Telemetry/TenantMetricServiceTests.cs
autonomous: true

must_haves:
  truths:
    - "TenantMetricService constructs without error via IMeterFactory"
    - "All 8 instruments (6 counters, 1 gauge, 1 histogram) are created with correct names"
    - "Every public method records with tenant_id and priority tags"
    - "SnmpCollector.Tenant meter is registered via AddMeter in ServiceCollectionExtensions"
    - "TelemetryConstants.TenantMeterName exists and is used — no magic strings"
    - "Unit tests confirm all 8 instruments emit measurements with correct names and tags"
  artifacts:
    - path: "src/SnmpCollector/Pipeline/TenantState.cs"
      provides: "TenantState enum with NotReady=0, Healthy=1, Resolved=2, Unresolved=3"
      contains: "public enum TenantState"
    - path: "src/SnmpCollector/Telemetry/ITenantMetricService.cs"
      provides: "Interface with 8 methods (6 increment, 1 state record, 1 duration record)"
      exports: ["ITenantMetricService"]
    - path: "src/SnmpCollector/Telemetry/TenantMetricService.cs"
      provides: "Singleton implementing ITenantMetricService with 8 OTel instruments"
      exports: ["TenantMetricService"]
    - path: "tests/SnmpCollector.Tests/Telemetry/TenantMetricServiceTests.cs"
      provides: "MeterListener-based tests for all 8 instruments"
      min_lines: 80
  key_links:
    - from: "src/SnmpCollector/Telemetry/TenantMetricService.cs"
      to: "TelemetryConstants.TenantMeterName"
      via: "meterFactory.Create(TelemetryConstants.TenantMeterName)"
      pattern: "meterFactory\\.Create\\(TelemetryConstants\\.TenantMeterName\\)"
    - from: "src/SnmpCollector/Extensions/ServiceCollectionExtensions.cs"
      to: "TelemetryConstants.TenantMeterName"
      via: "metrics.AddMeter"
      pattern: "AddMeter\\(TelemetryConstants\\.TenantMeterName\\)"
    - from: "src/SnmpCollector/Extensions/ServiceCollectionExtensions.cs"
      to: "TenantMetricService"
      via: "AddSingleton<ITenantMetricService, TenantMetricService>"
      pattern: "AddSingleton<ITenantMetricService, TenantMetricService>"
---

<objective>
Create the TenantMetricService singleton with all 8 OTel instruments, the TenantState enum, the ITenantMetricService interface, DI registration, and unit tests confirming all instruments emit correctly.

Purpose: Establishes the complete tenant metric infrastructure that Phase 73 (SnapshotJob instrumentation) will call into. The SnmpCollector.Tenant meter exports on all instances (not leader-gated), which is the key architectural distinction from pipeline/business meters.

Output: 4 new files (TenantState.cs, ITenantMetricService.cs, TenantMetricService.cs, TenantMetricServiceTests.cs), 2 modified files (TelemetryConstants.cs, ServiceCollectionExtensions.cs). All tests pass.
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-tenantmetricservice-meter-registration/72-CONTEXT.md
@.planning/phases/72-tenantmetricservice-meter-registration/72-RESEARCH.md
@src/SnmpCollector/Telemetry/PipelineMetricService.cs
@src/SnmpCollector/Telemetry/TelemetryConstants.cs
@src/SnmpCollector/Extensions/ServiceCollectionExtensions.cs
@tests/SnmpCollector.Tests/Telemetry/PipelineMetricServiceTests.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TenantState enum, TelemetryConstants, ITenantMetricService, and TenantMetricService</name>
  <files>
    src/SnmpCollector/Pipeline/TenantState.cs
    src/SnmpCollector/Telemetry/TelemetryConstants.cs
    src/SnmpCollector/Telemetry/ITenantMetricService.cs
    src/SnmpCollector/Telemetry/TenantMetricService.cs
  </files>
  <action>
    1. Create `src/SnmpCollector/Pipeline/TenantState.cs`:
       - Namespace: `SnmpCollector.Pipeline`
       - Public enum with explicit values: `NotReady = 0, Healthy = 1, Resolved = 2, Unresolved = 3`
       - XML doc explaining this replaces the former internal SnapshotJob.TierResult enum and that values are used as gauge integers

    2. Add `TenantMeterName` constant to `src/SnmpCollector/Telemetry/TelemetryConstants.cs`:
       - `public const string TenantMeterName = "SnmpCollector.Tenant";`
       - XML doc: "Tenant metrics meter -- exported by ALL instances (no leader gate). Used by TenantMetricService for tenant.* instruments."
       - Place after the existing `LeaderMeterName` constant

    3. Create `src/SnmpCollector/Telemetry/ITenantMetricService.cs`:
       - Namespace: `SnmpCollector.Telemetry`
       - 8 methods, one per instrument. All counter methods take `(string tenantId, int priority)`. State method takes `(string tenantId, int priority, TenantState state)`. Duration method takes `(string tenantId, int priority, double durationMs)`.
       - Method names: `IncrementTier1Stale`, `IncrementTier2Resolved`, `IncrementTier3Evaluate`, `IncrementCommandDispatched`, `IncrementCommandFailed`, `IncrementCommandSuppressed`, `RecordTenantState`, `RecordEvaluationDuration`
       - Add `using SnmpCollector.Pipeline;` for TenantState

    4. Create `src/SnmpCollector/Telemetry/TenantMetricService.cs`:
       - Mirror PipelineMetricService exactly: `sealed class TenantMetricService : ITenantMetricService, IDisposable`
       - Constructor takes `IMeterFactory meterFactory`, creates meter via `meterFactory.Create(TelemetryConstants.TenantMeterName)`
       - 6 counters (Counter<long>): tenant.tier1.stale, tenant.tier2.resolved, tenant.tier3.evaluate, tenant.command.dispatched, tenant.command.failed, tenant.command.suppressed
       - 1 gauge (Gauge<double>): tenant.state
       - 1 histogram (Histogram<double>): tenant.evaluation.duration.milliseconds (with description: "Duration of one tenant evaluation cycle in milliseconds")
       - All Add/Record calls use `new TagList { { "tenant_id", tenantId }, { "priority", priority } }`
       - State gauge records `(double)(int)state` (cast enum to int to double)
       - `Dispose()` disposes the meter: `_meter.Dispose()`
       - Do NOT use description parameters on counters (PipelineMetricService does not use them on counters)
  </action>
  <verify>
    Run `dotnet build src/SnmpCollector/SnmpCollector.csproj` — must compile with zero errors.
    Grep for "SnmpCollector.Tenant" in TenantMetricService.cs — must NOT appear (must use TelemetryConstants.TenantMeterName instead).
  </verify>
  <done>
    TenantState enum exists with 4 values. TelemetryConstants.TenantMeterName = "SnmpCollector.Tenant". ITenantMetricService has 8 methods. TenantMetricService creates all 8 instruments using the constant. Project compiles.
  </done>
</task>

<task type="auto">
  <name>Task 2: Register TenantMetricService in DI and AddMeter</name>
  <files>
    src/SnmpCollector/Extensions/ServiceCollectionExtensions.cs
  </files>
  <action>
    1. In `AddSnmpTelemetry` method, inside the `WithMetrics` lambda, add after line 87 (the LeaderMeterName AddMeter):
       `metrics.AddMeter(TelemetryConstants.TenantMeterName);`
       Add a comment: `// Tenant metrics (always exported — not leader-gated)`

    2. In `AddSnmpPipeline` method, after the `services.AddSingleton<PipelineMetricService>();` line (line 407), add:
       `services.AddSingleton<ITenantMetricService, TenantMetricService>();`
       Note: Register via interface (not concrete type) — this is a locked decision from CONTEXT.md for testability.

    3. Add `using SnmpCollector.Telemetry;` if not already present (it should already be there since PipelineMetricService is referenced).
  </action>
  <verify>
    Run `dotnet build src/SnmpCollector/SnmpCollector.csproj` — must compile.
    Grep ServiceCollectionExtensions.cs for "TenantMeterName" — must appear exactly once in AddMeter call.
    Grep ServiceCollectionExtensions.cs for "ITenantMetricService" — must appear exactly once in AddSingleton call.
  </verify>
  <done>
    AddMeter registers SnmpCollector.Tenant meter alongside the two existing meters. AddSingleton registers ITenantMetricService with TenantMetricService implementation. The meter will pass through MetricRoleGatedExporter ungated (no changes needed to exporter).
  </done>
</task>

<task type="auto">
  <name>Task 3: Unit tests for all 8 TenantMetricService instruments</name>
  <files>
    tests/SnmpCollector.Tests/Telemetry/TenantMetricServiceTests.cs
  </files>
  <action>
    Create `tests/SnmpCollector.Tests/Telemetry/TenantMetricServiceTests.cs` mirroring PipelineMetricServiceTests pattern exactly:

    1. Class setup:
       - `[Collection(NonParallelCollection.Name)]` (same as PipelineMetricServiceTests — MeterListener is global)
       - `public sealed class TenantMetricServiceTests : IDisposable`
       - ServiceCollection with `AddMetrics()`, build ServiceProvider
       - Construct `TenantMetricService` with `IMeterFactory` from the provider
       - Create `MeterListener` filtering on `TelemetryConstants.TenantMeterName`
       - Two measurement lists: `_measurements` for `long` (counters) and `_doubleMeasurements` for `double` (gauge, histogram)
       - Call `_listener.Start()` at end of constructor
       - `Dispose()` disposes listener, service, provider (in that order)

    2. Counter tests (6 tests, one per counter):
       - `IncrementTier1Stale_RecordsWithTenantIdAndPriorityTags` — call `IncrementTier1Stale("tenant-a", 1)`, assert instrument name is "tenant.tier1.stale", value is 1L, tags contain tenant_id="tenant-a" and priority=1
       - `IncrementTier2Resolved_RecordsWithTenantIdAndPriorityTags` — same pattern for "tenant.tier2.resolved"
       - `IncrementTier3Evaluate_RecordsWithTenantIdAndPriorityTags` — same for "tenant.tier3.evaluate"
       - `IncrementCommandDispatched_RecordsWithTenantIdAndPriorityTags` — same for "tenant.command.dispatched"
       - `IncrementCommandFailed_RecordsWithTenantIdAndPriorityTags` — same for "tenant.command.failed"
       - `IncrementCommandSuppressed_RecordsWithTenantIdAndPriorityTags` — same for "tenant.command.suppressed"

    3. Gauge test:
       - `RecordTenantState_RecordsEnumValueWithTags` — call `RecordTenantState("tenant-a", 1, TenantState.Unresolved)`, assert instrument name "tenant.state", value is 3.0 (Unresolved=3), tags correct

    4. Histogram test:
       - `RecordEvaluationDuration_RecordsMillisecondsWithTags` — call `RecordEvaluationDuration("tenant-a", 1, 42.5)`, assert instrument name "tenant.evaluation.duration.milliseconds", value is 42.5, tags correct

    5. Tag assertion pattern for each test: extract tags to Dictionary, assert tenant_id and priority values, assert NO device_name/host_name/pod_name tags (these are pipeline-only tags).

    6. Add required usings: `System.Diagnostics.Metrics`, `Microsoft.Extensions.DependencyInjection`, `SnmpCollector.Pipeline` (for TenantState), `SnmpCollector.Telemetry`, `SnmpCollector.Tests.Helpers`, `Xunit`
  </action>
  <verify>
    Run `dotnet test tests/SnmpCollector.Tests/SnmpCollector.Tests.csproj --filter "FullyQualifiedName~TenantMetricServiceTests"` — all 8 tests must pass.
    Run `dotnet test tests/SnmpCollector.Tests/SnmpCollector.Tests.csproj` — full test suite must pass (no regressions).
  </verify>
  <done>
    8 unit tests pass, confirming all 6 counters record with correct names/values/tags, the state gauge records enum values as doubles, and the histogram records duration values. Full test suite passes with zero regressions.
  </done>
</task>

</tasks>

<verification>
1. `dotnet build src/SnmpCollector/SnmpCollector.csproj` compiles with zero errors
2. `dotnet test tests/SnmpCollector.Tests/SnmpCollector.Tests.csproj` — all tests pass including 8 new TenantMetricServiceTests
3. Grep confirms no magic string "SnmpCollector.Tenant" in TenantMetricService.cs (uses constant)
4. Grep confirms AddMeter and AddSingleton registrations in ServiceCollectionExtensions.cs
</verification>

<success_criteria>
- TenantMetricService singleton with 8 instruments compiles and is registered in DI
- TelemetryConstants.TenantMeterName constant used everywhere (no magic strings)
- TenantState enum with explicit values NotReady=0, Healthy=1, Resolved=2, Unresolved=3
- 8 unit tests pass confirming correct instrument names, values, and tenant_id/priority tags
- Full test suite passes with zero regressions
- MetricRoleGatedExporter untouched (tenant meter passes ungated by design)
</success_criteria>

<output>
After completion, create `.planning/phases/72-tenantmetricservice-meter-registration/72-01-SUMMARY.md`
</output>
