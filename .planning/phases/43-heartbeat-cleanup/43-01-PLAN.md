---
phase: 43-heartbeat-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/SnmpCollector/Pipeline/TenantVectorRegistry.cs
  - src/SnmpCollector/Pipeline/Behaviors/TenantVectorFanOutBehavior.cs
  - tests/SnmpCollector.Tests/Pipeline/TenantVectorRegistryTests.cs
autonomous: true

must_haves:
  truths:
    - "TenantVectorRegistry.Reload contains no heartbeat holder, heartbeat tenant, int.MinValue priority bucket, or heartbeat carry-over logic -- the registry reflects only config-driven tenants"
    - "TenantCount returns survivingTenantCount (not survivingTenantCount + 1) -- no synthetic heartbeat inflation"
    - "TenantVectorFanOutBehavior contains no if (DeviceName == HeartbeatDeviceName) block -- the device-registry TryGetDeviceByName path is the only routing branch"
    - "Section 9 heartbeat tests are deleted from TenantVectorRegistryTests -- no test references heartbeat holder, heartbeat tenant, or int.MinValue priority"
    - "All remaining tests pass with adjusted count/index assertions (Groups.Count, TenantCount, SlotCount, and group indices each drop by 1)"
    - "HeartbeatJobOptions constants (HeartbeatDeviceName, HeartbeatOid, DefaultIntervalSeconds) are NOT deleted -- still used by HeartbeatJob"
  artifacts:
    - path: "src/SnmpCollector/Pipeline/TenantVectorRegistry.cs"
      provides: "Registry with heartbeat injection removed"
      contains: "TenantCount = survivingTenantCount;"
    - path: "src/SnmpCollector/Pipeline/Behaviors/TenantVectorFanOutBehavior.cs"
      provides: "Fan-out with heartbeat bypass removed"
      contains: "if (_deviceRegistry.TryGetDeviceByName(msg.DeviceName!, out var device))"
    - path: "tests/SnmpCollector.Tests/Pipeline/TenantVectorRegistryTests.cs"
      provides: "Tests with section 9 deleted and count assertions adjusted"
      contains: "No int.MinValue, no heartbeat holder/tenant references"
  key_links:
    - from: "TenantVectorRegistry.Reload"
      to: "TenantCount"
      via: "TenantCount = survivingTenantCount (no +1)"
      pattern: "Only config-driven tenants counted"
    - from: "TenantVectorFanOutBehavior.Handle"
      to: "_deviceRegistry.TryGetDeviceByName"
      via: "Single if branch (no preceding heartbeat bypass)"
      pattern: "Heartbeat device 'Simetra' not in DeviceRegistry -> TryGetDeviceByName returns false -> fan-out naturally skipped"
---

<objective>
Remove all hardcoded heartbeat special-case plumbing from TenantVectorRegistry and TenantVectorFanOutBehavior, then delete heartbeat-specific tests and fix count/index assertions in remaining tests.

Purpose: Eliminates synthetic heartbeat tenant so the pipeline treats heartbeat messages naturally (skipped by fan-out because "Simetra" is not a registered device). Satisfies HB-01, HB-02, HB-03.
Output: 2 modified production files, 1 modified test file with section 9 deleted and assertions adjusted.
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/43-heartbeat-cleanup/43-RESEARCH.md

@src/SnmpCollector/Pipeline/TenantVectorRegistry.cs
@src/SnmpCollector/Pipeline/Behaviors/TenantVectorFanOutBehavior.cs
@tests/SnmpCollector.Tests/Pipeline/TenantVectorRegistryTests.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove hardcoded heartbeat from TenantVectorRegistry and TenantVectorFanOutBehavior</name>
  <files>
    src/SnmpCollector/Pipeline/TenantVectorRegistry.cs
    src/SnmpCollector/Pipeline/Behaviors/TenantVectorFanOutBehavior.cs
  </files>
  <action>
**TenantVectorRegistry.cs — 3 deletions:**

1. Delete lines 75-89 entirely — the block that creates `heartbeatHolder`, builds `heartbeatKey`, does carry-over from `oldSlotLookup`, creates `heartbeatTenant`, assigns `priorityBuckets[int.MinValue]`, and increments `totalSlots++`. This is the entire heartbeat injection block between the `priorityBuckets` declaration and the tenant for-loop.

2. Delete the `totalSlots++` that follows the `priorityBuckets[int.MinValue]` assignment (line ~89). This is part of the block above — ensure it is included in the deletion.

3. Change `TenantCount = survivingTenantCount + 1;` to `TenantCount = survivingTenantCount;` (line ~173). The `+ 1` was compensating for the synthetic heartbeat tenant.

After deletion, verify that `HeartbeatJobOptions` is no longer referenced anywhere in TenantVectorRegistry.cs. The `using SnmpCollector.Configuration;` directive is likely shared with other types (TenantVectorOptions, TenantOptions) so do NOT remove the using directive — only confirm HeartbeatJobOptions itself is no longer referenced in this file.

**TenantVectorFanOutBehavior.cs — 1 deletion + 1 keyword fix:**

1. Delete lines 47-59 — the entire `if (string.Equals(msg.DeviceName, HeartbeatJobOptions.HeartbeatDeviceName, StringComparison.Ordinal))` block that routes heartbeat messages directly via `_registry.TryRoute("127.0.0.1", 0, metricName, ...)`.

2. Change the remaining `else if (_deviceRegistry.TryGetDeviceByName(msg.DeviceName!, out var device))` to `if (_deviceRegistry.TryGetDeviceByName(msg.DeviceName!, out var device))` — remove the `else` keyword since the preceding `if` block no longer exists. Forgetting this causes a compile error.

After deletion, check whether `HeartbeatJobOptions` is referenced anywhere else in TenantVectorFanOutBehavior.cs. If the only reference was in the deleted block, also remove the `using SnmpCollector.Configuration;` directive if no other types from that namespace are used in the file.

DO NOT touch `HeartbeatJobOptions.cs` itself, `HeartbeatJob.cs`, or `ILivenessVectorService` — these are unchanged.
  </action>
  <verify>
Run `dotnet build src/SnmpCollector/SnmpCollector.csproj` -- must compile with zero errors and zero warnings related to the changes.
  </verify>
  <done>
TenantVectorRegistry.Reload has no heartbeat holder, heartbeat tenant, int.MinValue bucket, or totalSlots++ for heartbeat. TenantCount = survivingTenantCount (no +1). TenantVectorFanOutBehavior has no heartbeat bypass block — only the device-registry if branch remains. Build succeeds.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete heartbeat tests and fix count/index assertions in TenantVectorRegistryTests</name>
  <files>
    tests/SnmpCollector.Tests/Pipeline/TenantVectorRegistryTests.cs
  </files>
  <action>
**Delete section 9 (heartbeat tests) — lines ~443-517:**

Delete all `[Fact]` methods in section 9. Delete by method name (not by count — the section header may say 5 but only 4 methods may exist):
- `Reload_EmptyConfig_HeartbeatTenantExists`
- `Reload_WithConfigTenants_HeartbeatIsFirstGroup`
- `Reload_HeartbeatRouting_TryRouteFindsHeartbeat`
- `Reload_HeartbeatValueCarriedOver`

Also delete the section 9 comment header (`// Section 9: ...`). If there is a fifth method, delete it too.

**Fix count/index assertions in remaining tests** — each assertion that previously included the heartbeat drops by 1:

| Test Method | Old Assertion | New Assertion |
|---|---|---|
| `Reload_SingleTenant_GroupsContainOneTenantWithCorrectSlots` | `registry.Groups.Count == 2` | `registry.Groups.Count == 1` |
| `Reload_SingleTenant_GroupsContainOneTenantWithCorrectSlots` | `registry.Groups[0].Priority == int.MinValue` (heartbeat group check) | DELETE this assertion entirely |
| `Reload_SingleTenant_GroupsContainOneTenantWithCorrectSlots` | `var group = registry.Groups[1]` | `var group = registry.Groups[0]` |
| `Reload_SingleTenant_CountsAreCorrect` | `TenantCount == 2` | `TenantCount == 1` |
| `Reload_SingleTenant_CountsAreCorrect` | `SlotCount == 4` | `SlotCount == 3` |
| `Reload_MultiplePriorities_GroupsSortedAscending` | `Groups.Count == 4` | `Groups.Count == 3` |
| `Reload_MultiplePriorities_GroupsSortedAscending` | `Groups[0].Priority == int.MinValue` | DELETE this assertion |
| `Reload_MultiplePriorities_GroupsSortedAscending` | Priorities at indices 1/2/3 (1, 5, 10) | Shift to indices 0/1/2 |
| `Reload_SamePriority_TenantsInSameGroup` | `Groups.Count == 2` | `Groups.Count == 1` |
| `Reload_SamePriority_TenantsInSameGroup` | `Groups[0].Priority == int.MinValue` | DELETE this assertion |
| `Reload_SamePriority_TenantsInSameGroup` | Group at index 1 | Shift to index 0 |
| `Reload_TenantWithName_UsesNameAsId` | `registry.Groups[1]` | `registry.Groups[0]` |
| `Reload_TenantWithoutName_UsesAutoGeneratedId` | `registry.Groups[1]` | `registry.Groups[0]` |

Read the test file carefully before editing to confirm each assertion location. The research provides line-level guidance but line numbers may have shifted.

DO NOT modify any tests in TenantVectorFanOutBehaviorTests.cs — confirmed no heartbeat-specific tests exist there.
  </action>
  <verify>
Run `dotnet test tests/SnmpCollector.Tests/SnmpCollector.Tests.csproj --filter "FullyQualifiedName~TenantVectorRegistryTests"` -- all remaining registry tests pass.
Run `dotnet test tests/SnmpCollector.Tests/SnmpCollector.Tests.csproj` -- ALL tests pass with zero failures.
  </verify>
  <done>
Section 9 heartbeat tests are deleted. Count assertions (Groups.Count, TenantCount, SlotCount) each drop by 1. Group index references shift down by 1 (heartbeat was always index 0). int.MinValue assertions are deleted. All remaining tests pass with zero regressions.
  </done>
</task>

</tasks>

<verification>
1. `dotnet build src/SnmpCollector/SnmpCollector.csproj` compiles cleanly
2. `dotnet build tests/SnmpCollector.Tests/SnmpCollector.Tests.csproj` compiles cleanly
3. `dotnet test tests/SnmpCollector.Tests/SnmpCollector.Tests.csproj` -- ALL tests pass (no regressions)
4. TenantVectorRegistry.cs contains no reference to HeartbeatJobOptions, heartbeatHolder, heartbeatTenant, heartbeatKey, or int.MinValue
5. TenantVectorRegistry.cs has `TenantCount = survivingTenantCount;` (no `+ 1`)
6. TenantVectorFanOutBehavior.cs contains no `HeartbeatDeviceName` reference and no heartbeat bypass block
7. TenantVectorFanOutBehavior.cs uses `if (_deviceRegistry.TryGetDeviceByName(...)` (not `else if`)
8. TenantVectorRegistryTests.cs has no section 9 heartbeat tests
9. TenantVectorRegistryTests.cs has no `int.MinValue` assertions
10. HeartbeatJobOptions.cs is unchanged (constants preserved for HeartbeatJob)
</verification>

<success_criteria>
- No hardcoded heartbeat plumbing in TenantVectorRegistry.Reload
- No heartbeat bypass in TenantVectorFanOutBehavior.Handle
- TenantCount reflects only config-driven tenants
- Section 9 heartbeat tests deleted
- All count/index assertions adjusted (drop by 1)
- All tests pass with zero regressions
- HeartbeatJobOptions constants untouched
</success_criteria>

<output>
After completion, create `.planning/phases/43-heartbeat-cleanup/43-01-SUMMARY.md`
</output>
