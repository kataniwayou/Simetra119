---
phase: 73-snapshotjob-instrumentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/SnmpCollector/Pipeline/CommandRequest.cs
  - src/SnmpCollector/Services/CommandWorkerService.cs
  - src/SnmpCollector/Jobs/SnapshotJob.cs
  - tests/SnmpCollector.Tests/Jobs/SnapshotJobTests.cs
  - tests/SnmpCollector.Tests/Services/CommandWorkerServiceTests.cs
autonomous: true

must_haves:
  truths:
    - "CommandRequest carries TenantId and Priority so downstream consumers can tag per-tenant metrics"
    - "CommandWorkerService increments tenant.command.failed on all 4 SET failure paths"
    - "All existing tests compile and pass after the structural changes"
  artifacts:
    - path: "src/SnmpCollector/Pipeline/CommandRequest.cs"
      provides: "Extended record with TenantId and Priority"
      contains: "string TenantId"
    - path: "src/SnmpCollector/Services/CommandWorkerService.cs"
      provides: "ITenantMetricService injection and IncrementCommandFailed calls"
      contains: "ITenantMetricService"
    - path: "tests/SnmpCollector.Tests/Services/CommandWorkerServiceTests.cs"
      provides: "Updated CreateService and MakeRequest helpers"
      contains: "ITenantMetricService"
  key_links:
    - from: "src/SnmpCollector/Jobs/SnapshotJob.cs"
      to: "CommandRequest"
      via: "new CommandRequest(..., tenant.Id, tenant.Priority)"
      pattern: "new CommandRequest.*tenant\\.Id.*tenant\\.Priority"
    - from: "src/SnmpCollector/Services/CommandWorkerService.cs"
      to: "ITenantMetricService"
      via: "constructor injection"
      pattern: "_tenantMetrics\\.IncrementCommandFailed"
---

<objective>
Extend CommandRequest with TenantId/Priority and wire ITenantMetricService into CommandWorkerService for SET failure tracking.

Purpose: Structural plumbing that enables per-tenant metric recording. CommandRequest must carry tenant context so CommandWorkerService can tag failed-SET counters. All construction sites and test fixtures must be updated in one pass to maintain build health.

Output: CommandRequest extended, CommandWorkerService instrumented for failures, all tests green.
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-snapshotjob-instrumentation/73-CONTEXT.md
@.planning/phases/73-snapshotjob-instrumentation/73-RESEARCH.md
@src/SnmpCollector/Pipeline/CommandRequest.cs
@src/SnmpCollector/Services/CommandWorkerService.cs
@src/SnmpCollector/Jobs/SnapshotJob.cs
@src/SnmpCollector/Telemetry/ITenantMetricService.cs
@tests/SnmpCollector.Tests/Jobs/SnapshotJobTests.cs
@tests/SnmpCollector.Tests/Services/CommandWorkerServiceTests.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend CommandRequest and update all construction sites</name>
  <files>
    src/SnmpCollector/Pipeline/CommandRequest.cs
    src/SnmpCollector/Jobs/SnapshotJob.cs
    tests/SnmpCollector.Tests/Jobs/SnapshotJobTests.cs
    tests/SnmpCollector.Tests/Services/CommandWorkerServiceTests.cs
  </files>
  <action>
    1. In `CommandRequest.cs`, add `string TenantId` and `int Priority` as the 6th and 7th positional parameters of the sealed record:
       ```
       public sealed record CommandRequest(
           string Ip, int Port, string CommandName, string Value, string ValueType,
           string TenantId, int Priority);
       ```
       Update the XML doc to mention the new fields carry tenant context for per-tenant metric tagging in CommandWorkerService.

    2. In `SnapshotJob.cs` line 193-194, update the CommandRequest construction to pass tenant context:
       ```
       var request = new CommandRequest(
           cmd.Ip, cmd.Port, cmd.CommandName, cmd.Value, cmd.ValueType,
           tenant.Id, tenant.Priority);
       ```

    3. In `SnapshotJobTests.cs` line 829, update the CommandRequest construction to include TenantId and Priority:
       ```
       new CommandRequest("x", 1, "fill", "0", "Integer32", "test-tenant", 1)
       ```
       Search the entire test file for any other `new CommandRequest(` calls and update them all.

    4. In `CommandWorkerServiceTests.cs` line 75-81, update `MakeRequest` helper to accept and pass TenantId and Priority:
       ```
       private static CommandRequest MakeRequest(
           string commandName = TestCommandName,
           string ip = TestIp,
           int port = TestPort,
           string value = TestValue,
           string valueType = TestValueType,
           string tenantId = "test-tenant",
           int priority = 1)
           => new(ip, port, commandName, value, valueType, tenantId, priority);
       ```

    5. Run `dotnet build` to confirm zero compilation errors across entire solution.
  </action>
  <verify>
    `dotnet build` succeeds with 0 errors. `dotnet test` passes all existing tests.
  </verify>
  <done>
    CommandRequest record has TenantId and Priority fields. All construction sites (SnapshotJob, SnapshotJobTests, CommandWorkerServiceTests) updated and compiling.
  </done>
</task>

<task type="auto">
  <name>Task 2: Inject ITenantMetricService into CommandWorkerService and call IncrementCommandFailed on SET failures</name>
  <files>
    src/SnmpCollector/Services/CommandWorkerService.cs
    tests/SnmpCollector.Tests/Services/CommandWorkerServiceTests.cs
  </files>
  <action>
    1. In `CommandWorkerService.cs`, add `ITenantMetricService _tenantMetrics` field and constructor parameter (insert after `PipelineMetricService pipelineMetrics`, before `IOptions<SnapshotJobOptions>`):
       ```
       private readonly ITenantMetricService _tenantMetrics;
       ```
       In constructor:
       ```
       public CommandWorkerService(
           ...,
           PipelineMetricService pipelineMetrics,
           ITenantMetricService tenantMetrics,       // NEW
           IOptions<SnapshotJobOptions> snapshotJobOptions,
           ILogger<CommandWorkerService> logger)
       ```
       Assign: `_tenantMetrics = tenantMetrics;`

    2. Add `_tenantMetrics.IncrementCommandFailed(req.TenantId, req.Priority)` at all 4 SET failure sites. Keep existing `_pipelineMetrics.IncrementCommandFailed(...)` calls — the tenant calls are additive, not replacements.

       Site 1 — General exception catch (line 87):
       ```
       _pipelineMetrics.IncrementCommandFailed($"{req.Ip}:{req.Port}");
       _tenantMetrics.IncrementCommandFailed(req.TenantId, req.Priority);
       ```

       Site 2 — OID not found (line 107):
       ```
       _pipelineMetrics.IncrementCommandFailed($"{req.Ip}:{req.Port}");
       _tenantMetrics.IncrementCommandFailed(req.TenantId, req.Priority);
       ```

       Site 3 — Device not found (line 117):
       ```
       _pipelineMetrics.IncrementCommandFailed($"{req.Ip}:{req.Port}");
       _tenantMetrics.IncrementCommandFailed(req.TenantId, req.Priority);
       ```

       Site 4 — Timeout (line 159):
       ```
       _pipelineMetrics.IncrementCommandFailed(device.Name);
       _tenantMetrics.IncrementCommandFailed(req.TenantId, req.Priority);
       ```

    3. In `CommandWorkerServiceTests.cs`, update `CreateService` helper to accept and pass `ITenantMetricService`:
       - Add a class-level field: `private readonly ITenantMetricService _tenantMetrics = Substitute.For<ITenantMetricService>();`
         (using NSubstitute — add `using NSubstitute;` import if not present)
       - Add optional parameter to CreateService: `ITenantMetricService? tenantMetrics = null`
       - Pass `tenantMetrics ?? _tenantMetrics` in the constructor call, between `_metrics` and `Options.Create(...)`.

    4. Run `dotnet build` and `dotnet test` to confirm everything compiles and passes.
  </action>
  <verify>
    `dotnet build` succeeds. `dotnet test` passes all existing tests. Grep for `_tenantMetrics.IncrementCommandFailed` in CommandWorkerService.cs shows exactly 4 call sites.
  </verify>
  <done>
    CommandWorkerService has ITenantMetricService injected. All 4 SET failure paths call IncrementCommandFailed with tenant context. Test fixture updated with NSubstitute mock. All tests green.
  </done>
</task>

</tasks>

<verification>
1. `dotnet build` — zero errors, zero warnings related to CommandRequest or CommandWorkerService
2. `dotnet test` — all existing tests pass (no regressions from structural changes)
3. `grep -rn "new CommandRequest(" src/ tests/` — every construction site includes TenantId and Priority
4. `grep -n "_tenantMetrics.IncrementCommandFailed" src/SnmpCollector/Services/CommandWorkerService.cs` — exactly 4 lines
5. `grep -n "ITenantMetricService" src/SnmpCollector/Services/CommandWorkerService.cs` — field + constructor parameter present
</verification>

<success_criteria>
- CommandRequest has TenantId (string) and Priority (int) as positional record parameters
- All construction sites (SnapshotJob, SnapshotJobTests, CommandWorkerServiceTests) updated
- CommandWorkerService has ITenantMetricService field and uses it at all 4 SET failure paths
- Existing _pipelineMetrics calls remain alongside new _tenantMetrics calls
- `dotnet test` passes all existing tests with zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/73-snapshotjob-instrumentation/73-01-SUMMARY.md`
</output>
