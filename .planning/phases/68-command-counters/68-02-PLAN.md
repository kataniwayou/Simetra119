---
phase: 68-command-counters
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/fixtures/tenant-cfg09-ccv-failed.yaml
  - tests/e2e/scenarios/85-ccv04-command-failed.sh
autonomous: true

must_haves:
  truths:
    - "snmp.command.failed increments when CommandWorkerService cannot resolve the OID for an unmapped CommandName"
    - "snmp.command.dispatched also increments for CCV-04 (dispatch happens before failure)"
  artifacts:
    - path: "tests/e2e/fixtures/tenant-cfg09-ccv-failed.yaml"
      provides: "Tenant fixture with unmapped CommandName e2e_set_unknown"
      contains: "e2e_set_unknown"
    - path: "tests/e2e/scenarios/85-ccv04-command-failed.sh"
      provides: "CCV-04 command.failed verification"
      contains: "snmp_command_failed_total"
  key_links:
    - from: "tests/e2e/scenarios/85-ccv04-command-failed.sh"
      to: "tests/e2e/fixtures/tenant-cfg09-ccv-failed.yaml"
      via: "kubectl apply for unmapped command trigger"
      pattern: "tenant-cfg09-ccv-failed"
    - from: "CommandWorkerService"
      to: "snmp_command_failed_total"
      via: "ResolveCommandOid returns null for e2e_set_unknown"
      pattern: "snmp_command_failed_total"
---

<objective>
Create the CCV-04 fixture (tenant with unmapped CommandName) and E2E scenario 85 verifying that snmp.command.failed increments when CommandWorkerService cannot resolve the OID from the command map.

Purpose: This scenario covers the command failure path -- a CommandName that does not exist in the oid_command_map causes ResolveCommandOid to return null, triggering IncrementCommandFailed. Both dispatched AND failed increment (dispatch happens in SnapshotJob, failure happens in CommandWorkerService).
Output: One fixture YAML and one scenario shell script.
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-command-counters/68-RESEARCH.md
@tests/e2e/scenarios/56-pss-04-unresolved.sh
@tests/e2e/lib/common.sh
@tests/e2e/lib/prometheus.sh
@tests/e2e/fixtures/tenant-cfg06-pss-single.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tenant-cfg09-ccv-failed.yaml fixture and scenario 85 (CCV-04 command.failed)</name>
  <files>
    tests/e2e/fixtures/tenant-cfg09-ccv-failed.yaml
    tests/e2e/scenarios/85-ccv04-command-failed.sh
  </files>
  <action>
**tenant-cfg09-ccv-failed.yaml** -- New fixture with unmapped CommandName:
- ConfigMap: `simetra-tenants` in namespace `simetra`
- Tenant name: `"e2e-ccv-failed"`
- Priority: 1
- SuppressionWindowSeconds: 5 (short, to avoid suppression interfering with repeated test runs)
- Metrics: same T2 OID structure as tenant-cfg06-pss-single.yaml:
  - e2e_eval_T2: Role=Evaluate, Threshold Min:10.0, TimeSeriesSize:3, GraceMultiplier:2.0
  - e2e_res1_T2: Role=Resolved, Threshold Min:1.0
  - e2e_res2_T2: Role=Resolved, Threshold Min:1.0
  All pointing to Ip: "e2e-simulator.simetra.svc.cluster.local", Port: 161
- Commands: ONE command entry:
  - Ip: "e2e-simulator.simetra.svc.cluster.local"
  - Port: 161
  - CommandName: "e2e_set_unknown" (NOT in simetra-oid-command-map -- this triggers the failure)
  - Value: "0"
  - ValueType: "Integer32"
- Use the exact JSON structure from the research RESEARCH.md code example

**85-ccv04-command-failed.sh** -- CCV-04 scenario:
- Header comment: Scenario 85, CCV-04 reference, explanation that unmapped CommandName triggers command.failed
- No shebang line (sourced by run-all.sh)
- FIXTURES_DIR setup: `FIXTURES_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)/fixtures"`

Pattern (follows PSS-04 with two-counter tracking):
1. Save original tenants ConfigMap: `save_configmap "simetra-tenants" "simetra" "$FIXTURES_DIR/.original-tenants-configmap.yaml" || true`
2. Apply fixture: `kubectl apply -f "$FIXTURES_DIR/tenant-cfg09-ccv-failed.yaml" > /dev/null 2>&1 || true`
3. Wait for tenant vector reload: `poll_until_log 60 5 "Tenant vector reload complete\|TenantVectorWatcher initial load complete" 30`
4. Prime T2 OIDs in-range: sim_set_oid "5.1" "10", sim_set_oid "5.2" "1", sim_set_oid "5.3" "1"
5. Wait 8s for readiness grace
6. Snapshot both baselines:
   - `BEFORE_SENT=$(snapshot_counter "snmp_command_dispatched_total" 'device_name="e2e-ccv-failed"')`
   - `BEFORE_FAILED=$(snapshot_counter "snmp_command_failed_total" '')` -- empty filter with sum() because device_name for failed is "e2e-simulator.simetra.svc.cluster.local:161" (IP:port), not tenant name. Using empty filter is simpler and avoids label brittleness.
7. Set eval violated: `sim_set_oid "5.1" "0"`
8. Poll for dispatched increment (confirms SnapshotJob reached tier=4 and enqueued):
   `poll_until 30 2 "snmp_command_dispatched_total" 'device_name="e2e-ccv-failed"' "$BEFORE_SENT"`
9. Wait additional 10s for CommandWorkerService to process the queued command and increment failed counter (async drain)
10. Snapshot AFTER values for both counters
11. Sub-assertion 85a: "CCV-04A: command.dispatched increments for unmapped command (dispatch precedes failure)"
    - Compute DELTA_SENT, assert_delta_ge DELTA_SENT 1
12. Sub-assertion 85b: "CCV-04B: command.failed increments for unmapped CommandName (OID not found)"
    - Compute DELTA_FAILED, assert_delta_ge DELTA_FAILED 1
    - Evidence: include both deltas, note "(CommandName e2e_set_unknown not in oid_command_map)"
13. Cleanup: reset_oid_overrides, restore original ConfigMap

CRITICAL label note: `snmp_command_dispatched_total` uses `device_name="e2e-ccv-failed"` (tenant name). `snmp_command_failed_total` uses `device_name="e2e-simulator.simetra.svc.cluster.local:161"` (IP:port from CommandWorkerService). Use EMPTY filter `''` for failed to avoid label brittleness -- `snapshot_counter` with empty filter uses `sum()` which captures all instances.

This scenario produces exactly 2 SCENARIO_RESULTS entries (indices 86-87 in the report).
  </action>
  <verify>
```bash
ls -la tests/e2e/fixtures/tenant-cfg09-ccv-failed.yaml tests/e2e/scenarios/85-ccv04-command-failed.sh
head -1 tests/e2e/scenarios/85-ccv04-command-failed.sh  # comment, not shebang
grep 'e2e_set_unknown' tests/e2e/fixtures/tenant-cfg09-ccv-failed.yaml  # unmapped CommandName
grep 'e2e-ccv-failed' tests/e2e/fixtures/tenant-cfg09-ccv-failed.yaml  # tenant name
grep 'snmp_command_failed_total' tests/e2e/scenarios/85-ccv04-command-failed.sh
grep 'snmp_command_dispatched_total' tests/e2e/scenarios/85-ccv04-command-failed.sh  # both counters tracked
grep 'tenant-cfg09-ccv-failed' tests/e2e/scenarios/85-ccv04-command-failed.sh  # uses new fixture
grep 'CCV-04' tests/e2e/scenarios/85-ccv04-command-failed.sh  # has CCV-04 label
```
  </verify>
  <done>Fixture tenant-cfg09-ccv-failed.yaml exists with CommandName "e2e_set_unknown" (not in command map). Scenario 85 asserts both dispatched delta >= 1 (dispatch happens before failure) and failed delta >= 1 (OID not found in command map). Empty filter used for failed counter to avoid IP:port label brittleness.</done>
</task>

</tasks>

<verification>
- tenant-cfg09-ccv-failed.yaml has CommandName "e2e_set_unknown" which is NOT in simetra-oid-command-map
- tenant-cfg09-ccv-failed.yaml has tenant name "e2e-ccv-failed" with SuppressionWindowSeconds=5
- Scenario 85 tracks BOTH dispatched and failed counters
- Dispatched uses device_name="e2e-ccv-failed" filter (tenant name)
- Failed uses empty filter (IP:port label avoidance)
- Scenario follows save/apply/prime/grace/snapshot/trigger/assert/cleanup pattern
- No shebang line
</verification>

<success_criteria>
- CCV-04: Scenario 85 proves command.failed increments when CommandWorkerService cannot resolve OID for unmapped CommandName
- Both dispatched and failed increment for CCV-04 (dispatch precedes failure)
- New fixture uses unmapped CommandName "e2e_set_unknown" to trigger the OID-not-found path
</success_criteria>

<output>
After completion, create `.planning/phases/68-command-counters/68-02-SUMMARY.md`
</output>
