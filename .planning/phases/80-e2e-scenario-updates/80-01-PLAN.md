---
phase: 80-e2e-scenario-updates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/scenarios/107-tvm01-smoke.sh
  - tests/e2e/scenarios/108-tvm02-notready.sh
  - tests/e2e/scenarios/112-tvm06-all-instances.sh
autonomous: true

must_haves:
  truths:
    - "Smoke test confirms 6 percentage gauge names present in Prometheus"
    - "Smoke test confirms 6 old counter names are ABSENT"
    - "Smoke test confirms tenant_evaluation_state (renamed from tenant_state)"
    - "NotReady test verifies state=0 + duration only, no percentage gauge assertions"
    - "All-instances test queries tenant_evaluation_state on all pods"
  artifacts:
    - path: "tests/e2e/scenarios/107-tvm01-smoke.sh"
      provides: "Percentage gauge presence + old counter absence checks"
      contains: "tenant_metric_stale_percent"
    - path: "tests/e2e/scenarios/108-tvm02-notready.sh"
      provides: "NotReady path: state + duration only"
      contains: "tenant_evaluation_state"
    - path: "tests/e2e/scenarios/112-tvm06-all-instances.sh"
      provides: "All-pod tenant_evaluation_state export check"
      contains: "tenant_evaluation_state"
  key_links:
    - from: "tests/e2e/scenarios/107-tvm01-smoke.sh"
      to: "Prometheus gauge endpoints"
      via: "query_prometheus with gauge names"
      pattern: "tenant_metric_stale_percent|tenant_metric_resolved_percent|tenant_metric_evaluate_percent"
    - from: "tests/e2e/scenarios/107-tvm01-smoke.sh"
      to: "Prometheus absence check"
      via: "query_prometheus returns 0 results for old names"
      pattern: "tenant_tier1_stale_total|tenant_tier2_resolved_total|tenant_tier3_evaluate_total"
---

<objective>
Rewrite E2E scenarios 107 (smoke), 108 (NotReady), and 112 (all-instances) for v2.5 percentage gauges.

Purpose: These three scenarios need simpler changes -- presence/absence checks, rename tenant_state to tenant_evaluation_state, and remove counter-based assertions that no longer apply.

Output: Three updated scenario scripts that pass against the v2.5 metric surface.
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/77-gather-then-decide-evaluation-flow/77-CONTEXT.md
@tests/e2e/lib/prometheus.sh
@tests/e2e/lib/common.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite 107-tvm01-smoke.sh for v2.5 gauge presence and counter absence</name>
  <files>tests/e2e/scenarios/107-tvm01-smoke.sh</files>
  <action>
Rewrite the smoke test to verify the v2.5 metric surface. The structure stays the same (apply fixture, prime OIDs, wait for evaluation, assert, cleanup) but the assertion targets change completely.

**Header comments update:**
- Replace 8 counter instrument names with 8 v2.5 instrument names in sub-assertion comments
- Sub-assertions TVM-01A through TVM-01F: check 6 percentage gauge names (not counters)
- TVM-01G: tenant_evaluation_state (renamed from tenant_state)
- TVM-01H: tenant_evaluation_duration_milliseconds_count (unchanged)
- TVM-01I: priority label on tenant_evaluation_state (not tenant_state)
- TVM-01J: Replace stale counter increment test with stale_percent gauge value check
- NEW TVM-01K through TVM-01P: Assert 6 OLD counter names are ABSENT

**Assertion changes for TVM-01A through TVM-01F:**
Replace the 6 counter metric names with the 6 percentage gauge names:
- `tenant_tier1_stale_total` -> `tenant_metric_stale_percent`
- `tenant_tier2_resolved_total` -> `tenant_metric_resolved_percent`
- `tenant_tier3_evaluate_total` -> `tenant_metric_evaluate_percent`
- `tenant_command_dispatched_total` -> `tenant_command_dispatched_percent`
- `tenant_command_failed_total` -> `tenant_command_failed_percent` (remove the special poll_until_exists fallback -- gauges are always present after first recording)
- `tenant_command_suppressed_total` -> `tenant_command_suppressed_percent`

Use the existing `_tvm01_assert_metric` helper pattern (query_prometheus, check series count > 0).

**TVM-01G:** Change `tenant_state` to `tenant_evaluation_state` in the assertion query.

**TVM-01I:** Change priority label check from `tenant_state{...}` to `tenant_evaluation_state{...}`.

**TVM-01J (stale path verification):** Replace counter-based stale test with gauge assertion:
- After making OID 5.1 stale, poll_until_log for tier=1 as before
- Then query `tenant_metric_stale_percent{tenant_id="e2e-pss-tenant"}` directly
- Assert the value is > 0 (stale percentage should be non-zero when one OID is stale)
- Use: `VALUE=$(query_prometheus 'tenant_metric_stale_percent{tenant_id="e2e-pss-tenant"}' | jq -r '.data.result[0].value[1] // "0"')`
- Compare as float > 0 using awk: `echo "$VALUE" | awk '{exit ($1 > 0) ? 0 : 1}'`
- Remove all snapshot_counter / poll_until / delta arithmetic for counters

**NEW TVM-01K through TVM-01P: Absence assertions for old v2.4 counter names.**
After all presence checks, add 6 absence checks confirming old counters are gone:
```bash
_tvm01_assert_absent() {
    local metric="$1"
    local sub_id="$2"
    local count
    count=$(query_prometheus "{__name__=\"${metric}\",tenant_id=\"e2e-pss-tenant\"}" | jq -r '.data.result | length')
    if [ "$count" -eq 0 ]; then
        record_pass "TVM-01${sub_id}: ${metric} absent (v2.4 counter removed)" "series_count=0"
    else
        record_fail "TVM-01${sub_id}: ${metric} absent (v2.4 counter removed)" "metric=${metric} series_count=${count} expected=0"
    fi
}

_tvm01_assert_absent "tenant_tier1_stale_total"        "K"
_tvm01_assert_absent "tenant_tier2_resolved_total"     "L"
_tvm01_assert_absent "tenant_tier3_evaluate_total"     "M"
_tvm01_assert_absent "tenant_command_dispatched_total"  "N"
_tvm01_assert_absent "tenant_command_failed_total"      "O"
_tvm01_assert_absent "tenant_command_suppressed_total"  "P"
```
  </action>
  <verify>
Run `bash -n tests/e2e/scenarios/107-tvm01-smoke.sh` to verify syntax.
Grep for old counter names used in non-absence-check contexts: `grep -n 'tenant_tier[123]' tests/e2e/scenarios/107-tvm01-smoke.sh` should only appear in absence assertions and comments.
Grep for new gauge names: `grep -c 'tenant_metric_stale_percent\|tenant_metric_resolved_percent\|tenant_metric_evaluate_percent\|tenant_command_dispatched_percent\|tenant_command_failed_percent\|tenant_command_suppressed_percent' tests/e2e/scenarios/107-tvm01-smoke.sh` should be >= 6.
Grep for tenant_evaluation_state: `grep -c 'tenant_evaluation_state' tests/e2e/scenarios/107-tvm01-smoke.sh` should be >= 2.
  </verify>
  <done>
107-tvm01-smoke.sh asserts 6 new gauge names present (A-F), tenant_evaluation_state present (G), duration histogram present (H), priority label correct (I), stale_percent > 0 after stale OID (J), and 6 old counter names absent (K-P). No snapshot_counter or assert_delta calls remain.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite 108-tvm02-notready.sh for v2.5 (state + duration only)</name>
  <files>tests/e2e/scenarios/108-tvm02-notready.sh</files>
  <action>
Rewrite the NotReady scenario for v2.5. The NotReady path records ONLY state gauge + duration -- no percentage gauges are recorded at all.

**Header comments update:**
- Replace counter references with v2.5 gauge names
- TVM-02A: tenant_evaluation_state valid value 0-3 (rename from tenant_state)
- TVM-02B: tenant_evaluation_duration_milliseconds_count > 0 (unchanged logic)
- TVM-02C/D/E: REMOVE entirely. Old tier counter delta assertions are meaningless -- percentage gauges don't exist for NotReady. Replace with a single informational assertion that percentage gauges are NOT present for a fresh tenant in NotReady.

**Changes:**

1. Remove all `snapshot_counter` calls for tier1/tier2/tier3 baselines (lines ~42-46).

2. TVM-02A: Change `tenant_state{...}` to `tenant_evaluation_state{...}` in both `poll_until_exists` and `query_prometheus` calls.

3. TVM-02B: Change the query from `tenant_evaluation_duration_milliseconds_count{tenant_id="e2e-pss-tenant"}` -- this is already correct, no change needed. Just verify the query still works.

4. Remove the "Waiting 10s to observe tier counter activity" sleep and all TIER*_AFTER / TIER*_DELTA code.

5. Remove TVM-02C, TVM-02D, TVM-02E assertions entirely (the old informational tier counter passes).

6. Optionally add a single new assertion TVM-02C: query `tenant_metric_stale_percent{tenant_id="e2e-pss-tenant"}` and verify series_count. In a truly fresh NotReady tenant, this should be 0 (gauge never recorded). But per the same CopyFrom caveat from v2.4, record it as informational:
```bash
STALE_COUNT=$(query_prometheus 'tenant_metric_stale_percent{tenant_id="e2e-pss-tenant"}' | jq -r '.data.result | length')
record_pass "TVM-02C: percentage gauges observation during NotReady (fresh tenant = absent)" "stale_percent_series=${STALE_COUNT} (0 = correct NotReady; >0 = prior series carried over)"
```
  </action>
  <verify>
Run `bash -n tests/e2e/scenarios/108-tvm02-notready.sh` to verify syntax.
Grep for old names: `grep -n 'tenant_state[^_]' tests/e2e/scenarios/108-tvm02-notready.sh` should return 0 matches (all should be tenant_evaluation_state now, or in comments describing old behavior).
Grep for snapshot_counter: `grep -c 'snapshot_counter' tests/e2e/scenarios/108-tvm02-notready.sh` should be 0.
Grep for tenant_evaluation_state: `grep -c 'tenant_evaluation_state' tests/e2e/scenarios/108-tvm02-notready.sh` should be >= 2.
  </verify>
  <done>
108-tvm02-notready.sh verifies tenant_evaluation_state is valid (0-3), duration histogram count > 0, and observes percentage gauge absence. No snapshot_counter, assert_delta, or tier counter code remains.
  </done>
</task>

<task type="auto">
  <name>Task 3: Rewrite 112-tvm06-all-instances.sh for tenant_evaluation_state rename</name>
  <files>tests/e2e/scenarios/112-tvm06-all-instances.sh</files>
  <action>
This scenario is the simplest change -- pure rename of `tenant_state` to `tenant_evaluation_state` in all PromQL queries.

**Changes (find-and-replace):**
1. All `tenant_state{...}` queries -> `tenant_evaluation_state{...}` (there are ~6 occurrences in query_prometheus calls)
2. Header comments: replace "tenant_state" with "tenant_evaluation_state" where it refers to the metric name
3. Leave `snmp_gauge` references unchanged (that's a different metric for leader-gated check)
4. Update `poll_until_exists` call from `'tenant_state'` to `'tenant_evaluation_state'` if present
5. Keep the k8s_pod_name verification, pod iteration, and follower absence logic unchanged

Do NOT change the overall scenario logic or structure. The assertions (TVM-06A: present on every pod, TVM-06B: follower has tenant_evaluation_state but no snmp_gauge) remain the same, just with the renamed metric.
  </action>
  <verify>
Run `bash -n tests/e2e/scenarios/112-tvm06-all-instances.sh` to verify syntax.
Grep for old name in queries: `grep -n '"tenant_state{' tests/e2e/scenarios/112-tvm06-all-instances.sh` should return 0 matches.
Grep for new name: `grep -c 'tenant_evaluation_state' tests/e2e/scenarios/112-tvm06-all-instances.sh` should be >= 6.
  </verify>
  <done>
112-tvm06-all-instances.sh queries tenant_evaluation_state on all pods (TVM-06A) and verifies follower absence of snmp_gauge (TVM-06B). No references to old `tenant_state` metric name remain in PromQL queries.
  </done>
</task>

</tasks>

<verification>
- `bash -n` passes for all 3 modified scripts (no syntax errors)
- No `snapshot_counter` calls remain in 107 or 108 (gauges are queried directly)
- No `assert_delta_gt` or `assert_delta_eq` calls remain in 107 or 108
- All PromQL queries reference v2.5 names: `tenant_evaluation_state`, `tenant_metric_*_percent`, `tenant_command_*_percent`
- Old v2.4 counter names only appear in absence-assertion contexts (107) or comments
</verification>

<success_criteria>
Scripts 107, 108, 112 are syntactically valid bash, reference only v2.5 gauge names in PromQL queries, and the smoke test (107) explicitly asserts old counter names are absent.
</success_criteria>

<output>
After completion, create `.planning/phases/80-e2e-scenario-updates/80-01-SUMMARY.md`
</output>
