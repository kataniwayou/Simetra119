---
phase: 71-negative-proofs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/scenarios/102-mnp01-heartbeat-not-in-snmp-info.sh
  - tests/e2e/scenarios/103-mnp02-unmapped-oid-absent.sh
  - tests/e2e/scenarios/104-mnp03-bad-community-no-business-metric.sh
  - tests/e2e/scenarios/105-mnp04-trap-dropped-stays-zero.sh
  - tests/e2e/scenarios/106-mnp05-follower-no-snmp-gauge.sh
  - tests/e2e/lib/report.sh
autonomous: true

must_haves:
  truths:
    - "Heartbeat OID never appears in snmp_info (it is Counter32, numeric only)"
    - "Heartbeat OID never appears with resolved_name=Unknown in snmp_gauge"
    - "Unmapped OIDs .999.2.x produce no snmp_gauge or snmp_info series"
    - "Bad-community traps produce no snmp_gauge or snmp_info series"
    - "snmp.trap.dropped stays 0 during normal E2E operation"
    - "Follower pods export no snmp_gauge or snmp_info series to Prometheus"
    - "Report includes Negative Proofs category with 5 scenarios"
  artifacts:
    - path: "tests/e2e/scenarios/102-mnp01-heartbeat-not-in-snmp-info.sh"
      provides: "MNP-01 heartbeat negative proof"
    - path: "tests/e2e/scenarios/103-mnp02-unmapped-oid-absent.sh"
      provides: "MNP-02 unmapped OID absence proof"
    - path: "tests/e2e/scenarios/104-mnp03-bad-community-no-business-metric.sh"
      provides: "MNP-03 bad-community business metric absence proof"
    - path: "tests/e2e/scenarios/105-mnp04-trap-dropped-stays-zero.sh"
      provides: "MNP-04 trap.dropped stays zero proof"
    - path: "tests/e2e/scenarios/106-mnp05-follower-no-snmp-gauge.sh"
      provides: "MNP-05 follower pod metric gating proof"
    - path: "tests/e2e/lib/report.sh"
      provides: "Negative Proofs report category"
  key_links:
    - from: "tests/e2e/scenarios/102-mnp01-heartbeat-not-in-snmp-info.sh"
      to: "Prometheus"
      via: "query_prometheus snmp_info{device_name=Simetra}"
      pattern: "query_prometheus.*snmp_info.*Simetra"
    - from: "tests/e2e/scenarios/106-mnp05-follower-no-snmp-gauge.sh"
      to: "kubectl + Prometheus"
      via: "kubectl get pods then query by k8s_pod_name label"
      pattern: "k8s_pod_name"
    - from: "tests/e2e/lib/report.sh"
      to: "SCENARIO_RESULTS"
      via: "category index range 104-108"
      pattern: "Negative Proofs|104|108"
---

<objective>
Create five E2E negative-proof scenarios (MNP-01 through MNP-05) proving the system suppresses,
rejects, or withholds metrics in every defined negative-path. Add the "Negative Proofs" report
category covering SCENARIO_RESULTS indices 104-108.

Purpose: Complete v2.3 Metric Validity & Correctness milestone by proving negative paths work.
Output: 5 scenario scripts (102-106), updated report.sh with Negative Proofs category.
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-negative-proofs/71-RESEARCH.md
@tests/e2e/scenarios/78-mcv09b-trap-received-negative.sh
@tests/e2e/scenarios/75-mcv07-errors-stays-zero.sh
@tests/e2e/scenarios/100-mlc07-resolved-name.sh
@tests/e2e/lib/report.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MNP-01 through MNP-05 scenario scripts</name>
  <files>
    tests/e2e/scenarios/102-mnp01-heartbeat-not-in-snmp-info.sh
    tests/e2e/scenarios/103-mnp02-unmapped-oid-absent.sh
    tests/e2e/scenarios/104-mnp03-bad-community-no-business-metric.sh
    tests/e2e/scenarios/105-mnp04-trap-dropped-stays-zero.sh
    tests/e2e/scenarios/106-mnp05-follower-no-snmp-gauge.sh
  </files>
  <action>
Create five scenario scripts. Each produces exactly 1 SCENARIO_RESULTS entry via record_pass/record_fail. No loops. All scripts use existing helpers from lib/prometheus.sh (query_prometheus, snapshot_counter, poll_until, assert_delta_eq, query_counter) and lib/report.sh (record_pass, record_fail).

**102-mnp01-heartbeat-not-in-snmp-info.sh** (MNP-01 — heartbeat negative proof):
CRITICAL: The heartbeat OID DOES produce snmp_gauge{resolved_name="Heartbeat", device_name="Simetra"} because OidMapService.MergeWithHeartbeatSeed injects it. Cannot assert snmp_gauge absence. Instead assert TWO things:
1. snmp_info{device_name="Simetra"} has count == 0 (heartbeat is Counter32, never goes to snmp_info)
2. snmp_gauge{device_name="Simetra",resolved_name="Unknown"} has count == 0 (heartbeat is correctly mapped, not leaked as Unknown)
Both must be true for record_pass. If snmp_info has Simetra series or snmp_gauge has Unknown+Simetra, record_fail.
SCENARIO_NAME="MNP-01: heartbeat never appears in snmp_info and never has resolved_name=Unknown"
Include EVIDENCE with info_count and unknown_gauge_count values.

**103-mnp02-unmapped-oid-absent.sh** (MNP-02 — unmapped OID absence):
Query for OIDs that are in the simulator but NOT in poll config and NOT in oidmaps.json:
- snmp_gauge{oid="1.3.6.1.4.1.47477.999.2.1.0"} — count must == 0
- snmp_info{oid="1.3.6.1.4.1.47477.999.2.2.0"} — count must == 0
No ConfigMap mutation needed. These OIDs are baseline-absent (never polled, never trapped).
SCENARIO_NAME="MNP-02: unmapped OIDs produce no snmp_gauge or snmp_info"
Both counts must be 0 for record_pass.

**104-mnp03-bad-community-no-business-metric.sh** (MNP-03 — extends MCV-09b):
Wait for snmp_trap_auth_failed_total to increment (confirming a bad-community trap arrived).
Then assert:
- snmp_gauge{device_name="unknown"} count == 0
- snmp_info{device_name="unknown"} count == 0
Use same pattern as scenario 78: snapshot auth_failed before, poll_until increment, sleep 15, check.
If auth_failed didn't increment, record_fail "No bad-community trap in window".
SCENARIO_NAME="MNP-03: bad-community traps produce no snmp_gauge or snmp_info"

**105-mnp04-trap-dropped-stays-zero.sh** (MNP-04 — trap.dropped safety net):
Same pattern as scenario 75 (MCV-07). Use snmp_trap_received_total with device_name="E2E-SIM" as activity guard.
Snapshot snmp_trap_dropped_total before and after. Assert delta == 0 only if activity_delta > 0.
If pipeline inactive, record_fail.
SCENARIO_NAME="MNP-04: trap.dropped stays 0 during normal E2E run"
Use assert_delta_eq for the dropped_delta == 0 check.

**106-mnp05-follower-no-snmp-gauge.sh** (MNP-05 — follower pod metric gating):
Step 1 (preflight): Query snmp_gauge (unfiltered) and check first result for k8s_pod_name label. If label absent, record_fail with diagnostic "k8s_pod_name label not present -- resource_to_telemetry_conversion may not be working" and exit early (do NOT skip record_fail).
Step 2: kubectl get pods -n simetra -l app=snmp-collector -o jsonpath to get all pod names.
Step 3: For each pod, query snmp_gauge{k8s_pod_name="<podname>"} count. If count == 0, that's a follower. Save first follower found.
Step 4: Also verify the follower has no snmp_info: query snmp_info{k8s_pod_name="<follower>"} count == 0.
Step 5: If follower found and both counts == 0, record_pass with follower pod name. If no follower found (all pods have snmp_gauge), record_fail.
SCENARIO_NAME="MNP-05: follower pod exports no snmp_gauge or snmp_info"
  </action>
  <verify>
All 5 files exist: `ls tests/e2e/scenarios/10[2-6]-mnp*.sh | wc -l` returns 5.
Each file contains exactly one record_pass and at least one record_fail call.
No file contains a `for` loop over scenarios (each is a single scenario).
  </verify>
  <done>
Five scenario scripts created, each producing exactly 1 SCENARIO_RESULTS entry.
MNP-01 correctly asserts heartbeat absence from snmp_info (not snmp_gauge).
MNP-05 includes k8s_pod_name preflight check.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Negative Proofs report category</name>
  <files>tests/e2e/lib/report.sh</files>
  <action>
Add `"Negative Proofs|104|108"` to the `_REPORT_CATEGORIES` array in report.sh, after the existing last entry `"Label Correctness|96|103"`. This covers SCENARIO_RESULTS indices 104-108 (scenarios 102-106, which are 1-indexed as 105-109 in report output).

Verify the array now has 12 entries total (was 11). Do NOT modify any other part of report.sh.
  </action>
  <verify>`grep -c 'Negative Proofs' tests/e2e/lib/report.sh` returns 1. `grep -c '|' tests/e2e/lib/report.sh` shows the new entry exists alongside all existing categories.</verify>
  <done>report.sh _REPORT_CATEGORIES array includes "Negative Proofs|104|108" as the final entry.</done>
</task>

</tasks>

<verification>
1. `ls tests/e2e/scenarios/10[2-6]-mnp*.sh` shows exactly 5 files
2. `grep -l record_pass tests/e2e/scenarios/10[2-6]-mnp*.sh | wc -l` returns 5
3. `grep -l record_fail tests/e2e/scenarios/10[2-6]-mnp*.sh | wc -l` returns 5
4. `grep 'Negative Proofs|104|108' tests/e2e/lib/report.sh` matches
5. No scenario contains a for loop over record_pass/record_fail (each produces exactly 1 result)
6. `grep -c 'SCENARIO_NAME=' tests/e2e/scenarios/102-mnp01-heartbeat-not-in-snmp-info.sh` returns 1
7. MNP-01 does NOT assert snmp_gauge{device_name="Simetra"} count == 0 (that would fail -- heartbeat IS in snmp_gauge)
</verification>

<success_criteria>
- 5 scenario scripts exist and follow established patterns
- MNP-01 correctly reframed: asserts snmp_info absence + no Unknown resolved_name (not snmp_gauge absence)
- MNP-02 queries by specific OID labels for unmapped OIDs
- MNP-03 extends MCV-09b with business metric dimension check
- MNP-04 uses activity guard pattern (not vacuous)
- MNP-05 includes k8s_pod_name preflight and identifies follower via Prometheus labels
- Report category "Negative Proofs|104|108" added to report.sh
- Each scenario produces exactly 1 SCENARIO_RESULTS entry
</success_criteria>

<output>
After completion, create `.planning/phases/71-negative-proofs/71-01-SUMMARY.md`
</output>
