---
phase: 33-config-model-additions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/SnmpCollector/Configuration/DeviceOptions.cs
  - src/SnmpCollector/Pipeline/DeviceInfo.cs
  - src/SnmpCollector/Pipeline/DeviceRegistry.cs
  - src/SnmpCollector/Configuration/Validators/DevicesOptionsValidator.cs
  - src/SnmpCollector/Jobs/MetricPollJob.cs
  - tests/SnmpCollector.Tests/Pipeline/DeviceRegistryTests.cs
  - tests/SnmpCollector.Tests/Jobs/MetricPollJobTests.cs
  - tests/SnmpCollector.Tests/Pipeline/TenantVectorRegistryTests.cs
  - src/SnmpCollector/config/devices.json
  - src/SnmpCollector/appsettings.Development.json
  - deploy/k8s/snmp-collector/simetra-devices.yaml
  - deploy/k8s/production/configmap.yaml
  - tests/e2e/fixtures/device-added-configmap.yaml
  - tests/e2e/fixtures/device-removed-configmap.yaml
  - tests/e2e/fixtures/device-modified-interval-configmap.yaml
  - tests/e2e/fixtures/fake-device-configmap.yaml
  - tests/e2e/fixtures/.original-devices-configmap.yaml
  - tests/e2e/scenarios/06-poll-unreachable.sh
autonomous: true

must_haves:
  truths:
    - "DeviceOptions has a CommunityString property (not Name) as the primary device identifier field"
    - "DeviceInfo.Name is derived from CommunityString via CommunityStringHelper.TryExtractDeviceName() at load time in DeviceRegistry"
    - "DeviceInfo.CommunityString is non-nullable and always populated from DeviceOptions.CommunityString"
    - "MetricPollJob uses device.CommunityString directly with no fallback derivation"
    - "DevicesOptionsValidator validates CommunityString is non-empty and follows Simetra.{name} convention"
    - "All config JSON files (local dev, K8s, e2e fixtures) use CommunityString instead of Name"
    - "All existing tests pass with the renamed field"
  artifacts:
    - path: "src/SnmpCollector/Configuration/DeviceOptions.cs"
      provides: "DeviceOptions with CommunityString replacing Name"
      contains: "CommunityString"
    - path: "src/SnmpCollector/Pipeline/DeviceInfo.cs"
      provides: "DeviceInfo record with non-nullable CommunityString"
      contains: "string CommunityString)"
    - path: "src/SnmpCollector/Pipeline/DeviceRegistry.cs"
      provides: "DeviceRegistry extracting Name from CommunityString via CommunityStringHelper"
      contains: "TryExtractDeviceName"
    - path: "src/SnmpCollector/Jobs/MetricPollJob.cs"
      provides: "MetricPollJob using device.CommunityString directly"
      contains: "device.CommunityString"
  key_links:
    - from: "DeviceRegistry constructor + ReloadAsync"
      to: "CommunityStringHelper.TryExtractDeviceName"
      via: "Derives short name from CommunityString at load time"
      pattern: "CommunityStringHelper.TryExtractDeviceName"
    - from: "DeviceInfo.Name"
      to: "_byName dictionary key"
      via: "Extracted short name used for trap routing and Prometheus labels"
      pattern: "byNameBuilder\\[deviceName\\]"
---

<objective>
Rename DeviceOptions.Name to DeviceOptions.CommunityString, make DeviceInfo.CommunityString non-nullable, derive DeviceInfo.Name from CommunityString at load time, remove MetricPollJob's CommunityString derivation fallback, update DevicesOptionsValidator, and update all config JSON/YAML files atomically.

Purpose: CS-01 and CS-02 require CommunityString as the primary device identifier. This is a clean break -- all config files must change in the same commit. Downstream consumers continue using the extracted short name unchanged.
Output: Updated C# models, DeviceRegistry logic, validator, MetricPollJob, tests, and all config files.
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/33-config-model-additions/33-CONTEXT.md
@.planning/phases/33-config-model-additions/33-RESEARCH.md

@src/SnmpCollector/Configuration/DeviceOptions.cs
@src/SnmpCollector/Pipeline/DeviceInfo.cs
@src/SnmpCollector/Pipeline/DeviceRegistry.cs
@src/SnmpCollector/Pipeline/CommunityStringHelper.cs
@src/SnmpCollector/Configuration/Validators/DevicesOptionsValidator.cs
@src/SnmpCollector/Jobs/MetricPollJob.cs
@tests/SnmpCollector.Tests/Pipeline/DeviceRegistryTests.cs
@tests/SnmpCollector.Tests/Jobs/MetricPollJobTests.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename DeviceOptions.Name to CommunityString, update DeviceInfo, DeviceRegistry, validator, and MetricPollJob</name>
  <files>
    src/SnmpCollector/Configuration/DeviceOptions.cs
    src/SnmpCollector/Pipeline/DeviceInfo.cs
    src/SnmpCollector/Pipeline/DeviceRegistry.cs
    src/SnmpCollector/Configuration/Validators/DevicesOptionsValidator.cs
    src/SnmpCollector/Jobs/MetricPollJob.cs
    tests/SnmpCollector.Tests/Pipeline/DeviceRegistryTests.cs
    tests/SnmpCollector.Tests/Jobs/MetricPollJobTests.cs
  </files>
  <action>
1. **DeviceOptions.cs** -- Remove the `Name` property entirely. Change `CommunityString` from `string?` to `string` with default `string.Empty`. Update XML doc to say "Full SNMP community string (e.g. 'Simetra.NPB-01'). Device name is derived from this via CommunityStringHelper.TryExtractDeviceName()." Remove the old XML doc about "Optional explicit SNMP community string". The resulting class has: CommunityString (string, required), IpAddress, Port, Polls.

2. **DeviceInfo.cs** -- Change the positional record to make `CommunityString` non-nullable and required (last positional parameter, no default value):
   ```csharp
   public sealed record DeviceInfo(
       string Name,
       string ConfigAddress,
       string ResolvedIp,
       int Port,
       IReadOnlyList<MetricPollInfo> PollGroups,
       string CommunityString);
   ```
   Update `Name` XML doc to "Derived from CommunityString via CommunityStringHelper.TryExtractDeviceName(). Used as device_name Prometheus label."
   Update `CommunityString` XML doc to "Full SNMP community string from config. Used directly for SNMP GET calls."

3. **DeviceRegistry.cs** -- In BOTH the constructor and `ReloadAsync`:
   - Replace `var pollGroups = BuildPollGroups(d.Polls, d.Name);` with:
     ```csharp
     if (!CommunityStringHelper.TryExtractDeviceName(d.CommunityString, out var deviceName))
     {
         _logger.LogError(
             "Device at {IpAddress}:{Port} has invalid CommunityString '{CommunityString}' -- skipping",
             d.IpAddress, d.Port, d.CommunityString);
         continue;
     }
     var pollGroups = BuildPollGroups(d.Polls, deviceName);
     ```
   - Replace `var info = new DeviceInfo(d.Name, d.IpAddress, ip.ToString(), d.Port, pollGroups, d.CommunityString);` with:
     `var info = new DeviceInfo(deviceName, d.IpAddress, ip.ToString(), d.Port, pollGroups, d.CommunityString);`
   - Replace `byNameBuilder[info.Name] = info;` -- this stays the same (info.Name is now the derived short name).
   - In the duplicate IP+Port error message, both paths already use `existing.Name` and `info.Name` which are derived short names. No change needed there.

4. **DevicesOptionsValidator.cs** -- Replace the `ValidateDevice` method's Name check:
   - Remove: `if (string.IsNullOrWhiteSpace(device.Name)) failures.Add(...)`
   - Add:
     ```csharp
     if (string.IsNullOrWhiteSpace(device.CommunityString))
     {
         failures.Add($"Devices[{index}].CommunityString is required");
     }
     else if (!CommunityStringHelper.TryExtractDeviceName(device.CommunityString, out _))
     {
         failures.Add(
             $"Devices[{index}].CommunityString '{device.CommunityString}' " +
             "does not follow Simetra.{DeviceName} convention");
     }
     ```
   - Add `using SnmpCollector.Pipeline;` at the top for CommunityStringHelper access.

5. **MetricPollJob.cs** -- Replace the 3-line community string derivation:
   ```csharp
   var communityStr = !string.IsNullOrEmpty(device.CommunityString)
       ? device.CommunityString
       : CommunityStringHelper.DeriveFromDeviceName(device.Name);
   ```
   With:
   ```csharp
   var communityStr = device.CommunityString;
   ```
   The `using SnmpCollector.Pipeline;` is already present. The `CommunityStringHelper` import can be kept or removed (it's still used elsewhere in the codebase).

6. **DeviceRegistryTests.cs** -- Update ALL `DeviceOptions` constructions:
   - `TwoDeviceOptions()`: Change `Name = "npb-core-01"` to `CommunityString = "Simetra.npb-core-01"` and `Name = "obp-edge-01"` to `CommunityString = "Simetra.obp-edge-01"`.
   - `Constructor_DuplicateIpPort_ThrowsInvalidOperationException`: Change `Name = "device-a"` to `CommunityString = "Simetra.device-a"` and `Name = "device-b"` to `CommunityString = "Simetra.device-b"`.
   - `Constructor_DuplicateName_DifferentIpPort_Accepted`: Change `Name = "same-name"` to `CommunityString = "Simetra.same-name"` (both entries).
   - `Constructor_DnsHostname_ResolvesToIpAddress`: Change `Name = "dns-device"` to `CommunityString = "Simetra.dns-device"`.
   - `Constructor_CommunityString_PassedThroughToDeviceInfo`: The test currently sets BOTH `Name = "community-device"` and `CommunityString = "my-custom-community"`. After the rename, `Name` no longer exists. Change to `CommunityString = "Simetra.community-device"` and update the assertion to check `device!.CommunityString == "Simetra.community-device"`.
   - `Constructor_NoCommunityString_DeviceInfoHasNull`: This test no longer makes sense (CommunityString is now required). Replace with a test named `Constructor_CommunityString_StoredOnDeviceInfo` that verifies `device.CommunityString` equals the configured value from `TwoDeviceOptions()` (i.e., `"Simetra.npb-core-01"`).
   - `ReloadAsync_AddsNewDevice_FoundByName`: Change inline `DeviceOptions` constructions from `Name = "npb-core-01"` to `CommunityString = "Simetra.npb-core-01"` etc for all 3 entries.
   - `ReloadAsync_RemovesDevice_NotFoundByName`: Change `Name = "npb-core-01"` to `CommunityString = "Simetra.npb-core-01"`.
   - `ResolvedMetricNames_PopulateOids`: Change `Name = "obp-edge-01"` to `CommunityString = "Simetra.obp-edge-01"`.
   - `UnresolvableMetricName_LogsWarningAndSkipped`: Change `Name = "test-device"` to `CommunityString = "Simetra.test-device"`.
   - `AllNamesUnresolvable_DeviceStillRegistered`: Change `Name = "no-match-device"` to `CommunityString = "Simetra.no-match-device"`.
   - `ReloadAsync_ResolvesMetricNamesToOids`: Change `Name = "obp-edge-01"` to `CommunityString = "Simetra.obp-edge-01"`.
   - `ReloadAsync_UnresolvableMetricName_LogsWarning`: Change `Name = "test-device"` to `CommunityString = "Simetra.test-device"`.
   - In ALL assertion lines, `device.Name` assertions remain valid because Name is now the derived short name (e.g., `"npb-core-01"` derived from `"Simetra.npb-core-01"`).

7. **MetricPollJobTests.cs** -- Update DeviceInfo constructions:
   - Line 188 (`Execute_UsesDevicePortAndDerivesCommunityString`): Change:
     `new DeviceInfo("custom-device", "10.0.0.99", "10.0.0.99", 1161, [pollGroup])` to
     `new DeviceInfo("custom-device", "10.0.0.99", "10.0.0.99", 1161, [pollGroup], "Simetra.custom-device")`.
     The assertion `Assert.Equal("Simetra.custom-device", snmpClient.LastCommunity!.ToString())` stays the same.
     Rename the test method to `Execute_UsesDevicePortAndCommunityString`.
     Update the section comment from "CommunityString derived from device name" to "Device Port and CommunityString used directly".
   - Line 322 (`Execute_ExplicitCommunityString_UsedInsteadOfConvention`): The DeviceInfo already passes `"my-explicit-community"` as the last arg. This still works because CommunityString is now required (non-nullable). However, "my-explicit-community" does not follow the `Simetra.*` pattern -- but MetricPollJob doesn't validate, it just uses the value directly. The test remains valid as-is since DeviceInfo in test is constructed directly (not via DeviceRegistry). Keep the test but rename it to `Execute_CommunityString_UsedDirectly` and update the section comment to "CommunityString used directly from DeviceInfo".
   - Search for ALL other `new DeviceInfo(` constructions in the test file. There should be one in the default test device setup. Check the `CreateJob` helper or any field that constructs DeviceInfo. If the default device (DeviceName="test-router") is created without CommunityString, add it: `"Simetra.test-router"`.

IMPORTANT: After removing `DeviceOptions.Name`, if any C# file still references `device.Name` on a `DeviceOptions` instance (not `DeviceInfo.Name`), the compiler will catch it. Fix any remaining references.

IMPORTANT: Do a project-wide search for `new DeviceInfo(` to find ALL construction sites. Update each to include a non-null CommunityString as the last parameter. The `TenantVectorRegistryTests.cs` DNS test constructs a `DeviceInfo` at line 355 -- update it to include `CommunityString: "Simetra.test-device"`.
  </action>
  <verify>
Run `dotnet build src/SnmpCollector/SnmpCollector.csproj` -- must compile with zero errors. Then run `dotnet build tests/SnmpCollector.Tests/SnmpCollector.Tests.csproj` -- must compile with zero errors.
  </verify>
  <done>
DeviceOptions.Name removed and replaced by CommunityString (string, non-nullable). DeviceInfo.CommunityString is non-nullable. DeviceRegistry derives Name from CommunityString in both constructor and ReloadAsync. DevicesOptionsValidator validates CommunityString format. MetricPollJob uses device.CommunityString directly. All test DeviceOptions/DeviceInfo constructions updated. Both projects compile cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update all JSON/YAML config files and e2e scripts for CommunityString rename</name>
  <files>
    src/SnmpCollector/config/devices.json
    src/SnmpCollector/appsettings.Development.json
    deploy/k8s/snmp-collector/simetra-devices.yaml
    deploy/k8s/production/configmap.yaml
    tests/e2e/fixtures/device-added-configmap.yaml
    tests/e2e/fixtures/device-removed-configmap.yaml
    tests/e2e/fixtures/device-modified-interval-configmap.yaml
    tests/e2e/fixtures/fake-device-configmap.yaml
    tests/e2e/fixtures/.original-devices-configmap.yaml
    tests/e2e/scenarios/06-poll-unreachable.sh
  </files>
  <action>
Apply the following mechanical rename to every device entry in every file listed below. For each device object that has `"Name": "XXX"`, replace it with `"CommunityString": "Simetra.XXX"`. The device name is prepended with `"Simetra."`.

**Special case:** The `device-added-configmap.yaml` has a device `E2E-SIM-2` that ALREADY has an explicit `"CommunityString": "Simetra.E2E-SIM"` alongside `"Name": "E2E-SIM-2"`. For this entry: remove the `"Name": "E2E-SIM-2"` line and keep the existing `"CommunityString": "Simetra.E2E-SIM"` line.

Files and their device entries:

1. **src/SnmpCollector/config/devices.json**: `"Name": "OBP-01"` -> `"CommunityString": "Simetra.OBP-01"`, `"Name": "NPB-01"` -> `"CommunityString": "Simetra.NPB-01"`.

2. **src/SnmpCollector/appsettings.Development.json**: Find the Devices section. `"Name": "npb-core-01"` -> `"CommunityString": "Simetra.npb-core-01"`, `"Name": "obp-edge-01"` -> `"CommunityString": "Simetra.obp-edge-01"`.

3. **deploy/k8s/snmp-collector/simetra-devices.yaml**: `"Name": "OBP-01"` -> `"CommunityString": "Simetra.OBP-01"`, `"Name": "NPB-01"` -> `"CommunityString": "Simetra.NPB-01"`, `"Name": "E2E-SIM"` -> `"CommunityString": "Simetra.E2E-SIM"`.

4. **deploy/k8s/production/configmap.yaml**: Same 3 device entries as simetra-devices.yaml plus check for any additional devices. Note: this file also has `"Name": "snmp-collector-leader"` in the Lease section -- DO NOT change that one, only change entries inside `devices.json` data.

5. **tests/e2e/fixtures/device-added-configmap.yaml**: `"Name": "OBP-01"` -> `"CommunityString": "Simetra.OBP-01"`, `"Name": "NPB-01"` -> `"CommunityString": "Simetra.NPB-01"`, `"Name": "E2E-SIM"` -> `"CommunityString": "Simetra.E2E-SIM"`, `"Name": "FAKE-UNREACHABLE"` -> `"CommunityString": "Simetra.FAKE-UNREACHABLE"`, `"Name": "E2E-SIM-2"` line removed (keep existing `"CommunityString": "Simetra.E2E-SIM"` line).

6. **tests/e2e/fixtures/device-removed-configmap.yaml**: `"Name": "OBP-01"` -> `"CommunityString": "Simetra.OBP-01"`, `"Name": "NPB-01"` -> `"CommunityString": "Simetra.NPB-01"`, `"Name": "FAKE-UNREACHABLE"` -> `"CommunityString": "Simetra.FAKE-UNREACHABLE"`.

7. **tests/e2e/fixtures/device-modified-interval-configmap.yaml**: Same pattern for all device entries (OBP-01, NPB-01, E2E-SIM, FAKE-UNREACHABLE).

8. **tests/e2e/fixtures/fake-device-configmap.yaml**: Same pattern for all device entries (OBP-01, NPB-01, E2E-SIM, FAKE-UNREACHABLE).

9. **tests/e2e/fixtures/.original-devices-configmap.yaml**: This is a saved snapshot file. Apply the same rename to all device entries found in it.

10. **tests/e2e/scenarios/06-poll-unreachable.sh**: Line 23 has inline JSON `"Name": "FAKE-UNREACHABLE"`. Change to `"CommunityString": "Simetra.FAKE-UNREACHABLE"`.

IMPORTANT: Do NOT modify the `"Name": "snmp-collector-leader"` entry in `deploy/k8s/snmp-collector/snmp-collector-config.yaml` or `deploy/k8s/production/configmap.yaml` Lease sections -- those are LeaderElection config, not device config.

IMPORTANT: After all changes, do a project-wide grep for `"Name":` in device JSON contexts to verify no remaining old-format entries. Search pattern: `grep -r '"Name":' --include="*.json" --include="*.yaml" src/SnmpCollector/config/ deploy/k8s/ tests/e2e/` and verify any remaining `"Name"` occurrences are NOT inside device objects.

After config changes, run all tests to verify the full system works end-to-end:
  </action>
  <verify>
Run `dotnet test tests/SnmpCollector.Tests/SnmpCollector.Tests.csproj` -- all tests pass. Grep for remaining `"Name":` in device JSON contexts to confirm none remain.
  </verify>
  <done>
All config files (local dev JSON, appsettings, K8s standalone, K8s production, 5 e2e fixture YAMLs, 1 e2e scenario script) use `"CommunityString": "Simetra.XXX"` instead of `"Name": "XXX"` for device entries. All existing tests pass. No device config entry anywhere in the repository uses the old `"Name"` field.
  </done>
</task>

</tasks>

<verification>
1. `dotnet build src/SnmpCollector/SnmpCollector.csproj` compiles cleanly
2. `dotnet test tests/SnmpCollector.Tests/SnmpCollector.Tests.csproj` -- all tests pass
3. `DeviceOptions.cs` has `CommunityString` property, no `Name` property
4. `DeviceInfo.cs` has non-nullable `CommunityString` (no default value)
5. `DeviceRegistry.cs` calls `CommunityStringHelper.TryExtractDeviceName` in both constructor and ReloadAsync
6. `MetricPollJob.cs` has `var communityStr = device.CommunityString;` with no fallback
7. `DevicesOptionsValidator.cs` validates CommunityString with TryExtractDeviceName
8. Grep `"Name":` in device JSON/YAML contexts returns zero matches (excluding Lease config)
9. All JSON config files and YAML fixtures use `"CommunityString": "Simetra.XXX"` format
</verification>

<success_criteria>
- DeviceOptions.Name is gone; CommunityString is the primary identifier
- DeviceInfo.Name is derived from CommunityString at load time
- MetricPollJob has no community string derivation fallback
- All config files updated atomically
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/33-config-model-additions/33-01-SUMMARY.md`
</output>
