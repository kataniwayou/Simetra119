---
phase: 50-label-rename
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/SnmpCollector/Telemetry/SnmpMetricFactory.cs
  - src/SnmpCollector/Telemetry/ISnmpMetricFactory.cs
  - src/SnmpCollector/Pipeline/CardinalityAuditService.cs
  - src/SnmpCollector/Configuration/MetricSlotOptions.cs
  - src/SnmpCollector/Configuration/OidMapOptions.cs
  - src/SnmpCollector/Configuration/TenantOptions.cs
  - src/SnmpCollector/Configuration/Validators/TenantVectorOptionsValidator.cs
  - tests/SnmpCollector.Tests/Telemetry/SnmpMetricFactoryTests.cs
  - deploy/grafana/dashboards/simetra-business.json
  - tests/e2e/scenarios/11-gauge-labels-e2e-sim.sh
  - tests/e2e/scenarios/12-gauge-labels-obp.sh
  - tests/e2e/scenarios/13-gauge-labels-npb.sh
  - tests/e2e/scenarios/14-info-labels.sh
  - tests/e2e/scenarios/15-unknown-oid.sh
  - tests/e2e/scenarios/16-trap-originated.sh
  - tests/e2e/scenarios/17-snmp-type-labels.sh
  - tests/e2e/scenarios/18-oid-rename.sh
  - tests/e2e/scenarios/19-oid-remove.sh
  - tests/e2e/scenarios/20-oid-add.sh
autonomous: true

must_haves:
  truths:
    - "All snmp_gauge and snmp_info time series use resolved_name label instead of metric_name"
    - "Grafana business dashboard queries use resolved_name in all PromQL expressions"
    - "E2E test assertions query and validate using resolved_name label"
    - "All unit tests pass with the new label name"
    - "No residual functional references to metric_name as a tag key remain in C# source"
  artifacts:
    - path: "src/SnmpCollector/Telemetry/SnmpMetricFactory.cs"
      provides: "Tag key resolved_name on all 4 instruments (snmp_gauge, snmp_info, snmp_gauge_duration, snmp_info_duration)"
      contains: "resolved_name"
    - path: "tests/SnmpCollector.Tests/Telemetry/SnmpMetricFactoryTests.cs"
      provides: "Unit test assertions on resolved_name tag"
      contains: "resolved_name"
    - path: "deploy/grafana/dashboards/simetra-business.json"
      provides: "PromQL queries using resolved_name label"
      contains: "resolved_name"
  key_links:
    - from: "src/SnmpCollector/Telemetry/SnmpMetricFactory.cs"
      to: "Prometheus time series"
      via: "OTel tag key in TagList"
      pattern: "resolved_name"
    - from: "deploy/grafana/dashboards/simetra-business.json"
      to: "Prometheus queries"
      via: "PromQL label selectors and label_join/label_replace"
      pattern: "resolved_name"
---

<objective>
Rename the `metric_name` label to `resolved_name` across all SNMP instruments, Grafana dashboards, unit tests, and E2E test scripts.

Purpose: The label serves both metric names (from oid_metric_map) and command names (from oid_command_map). `metric_name` is misleading for SET response data — `resolved_name` accurately describes what the label contains regardless of source.

Output: All snmp_gauge/snmp_info time series emit `resolved_name` instead of `metric_name`. Dashboard queries and test assertions updated to match.
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/SnmpCollector/Telemetry/SnmpMetricFactory.cs
@src/SnmpCollector/Telemetry/ISnmpMetricFactory.cs
@tests/SnmpCollector.Tests/Telemetry/SnmpMetricFactoryTests.cs
@deploy/grafana/dashboards/simetra-business.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename metric_name to resolved_name in C# source and unit tests</name>
  <files>
    src/SnmpCollector/Telemetry/SnmpMetricFactory.cs
    src/SnmpCollector/Telemetry/ISnmpMetricFactory.cs
    src/SnmpCollector/Pipeline/CardinalityAuditService.cs
    src/SnmpCollector/Configuration/MetricSlotOptions.cs
    src/SnmpCollector/Configuration/OidMapOptions.cs
    src/SnmpCollector/Configuration/TenantOptions.cs
    src/SnmpCollector/Configuration/Validators/TenantVectorOptionsValidator.cs
    tests/SnmpCollector.Tests/Telemetry/SnmpMetricFactoryTests.cs
  </files>
  <action>
    In SnmpMetricFactory.cs, change all 4 TagList entries from `{ "metric_name", metricName }` to `{ "resolved_name", metricName }`. These are on lines 41, 60, 76, and 95 — inside RecordGauge, RecordInfo, RecordGaugeDuration, and RecordInfoDuration respectively.

    In ISnmpMetricFactory.cs, update XML doc comments that reference `metric_name` to say `resolved_name` instead (lines 23 and 29).

    In CardinalityAuditService.cs, update the doc comment on line 20 (`metric_name -- bounded by OID map`) and the log message string on line 96 to say `resolved_name` instead. These are cosmetic but keep documentation accurate.

    In MetricSlotOptions.cs, OidMapOptions.cs, TenantOptions.cs, and TenantVectorOptionsValidator.cs, update doc comments that reference `metric_name` to say `resolved_name`. These are all XML doc or inline comments — no functional code changes.

    In SnmpMetricFactoryTests.cs, change all 4 assertions from `Assert.Equal("...", tags["metric_name"])` to `Assert.Equal("...", tags["resolved_name"])` (lines 62, 81, 99, 116).

    Do NOT rename the C# parameter `metricName` — that is an internal variable name, not a Prometheus label. Only the string literal `"metric_name"` in TagList entries and doc comments changes.
  </action>
  <verify>
    Run `dotnet test` from the repo root. All tests must pass, including the 4 SnmpMetricFactoryTests that assert on the tag key.

    Run `grep -rn "metric_name" src/SnmpCollector/Telemetry/` to confirm zero functional references remain (only the parameter name `metricName` should appear, not the string `"metric_name"`).
  </verify>
  <done>
    All 4 SNMP instruments (snmp_gauge, snmp_info, snmp_gauge_duration, snmp_info_duration) emit `resolved_name` tag instead of `metric_name`. All unit tests pass with updated assertions. No string literal `"metric_name"` remains in Telemetry source files.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Grafana dashboard and E2E test scripts</name>
  <files>
    deploy/grafana/dashboards/simetra-business.json
    tests/e2e/scenarios/11-gauge-labels-e2e-sim.sh
    tests/e2e/scenarios/12-gauge-labels-obp.sh
    tests/e2e/scenarios/13-gauge-labels-npb.sh
    tests/e2e/scenarios/14-info-labels.sh
    tests/e2e/scenarios/15-unknown-oid.sh
    tests/e2e/scenarios/16-trap-originated.sh
    tests/e2e/scenarios/17-snmp-type-labels.sh
    tests/e2e/scenarios/18-oid-rename.sh
    tests/e2e/scenarios/19-oid-remove.sh
    tests/e2e/scenarios/20-oid-add.sh
  </files>
  <action>
    In simetra-business.json, replace all 10 occurrences of `metric_name` with `resolved_name`. These appear in:
    - Column sort options (2 occurrences: `"options": "metric_name"`)
    - PromQL label_join and label_replace expressions (2 complex expressions using metric_name as both a label selector and label_replace pattern)
    - histogram_quantile sum-by clauses (2 occurrences: `sum by (le, metric_name, ...)`)
    - Column override configurations (2 occurrences: `"metric_name": 5`)
    - Count queries for Unknown metrics (2 occurrences: `metric_name=\"Unknown\"` and `metric_name!=\"Unknown\"`)

    Be precise with the JSON — this is a large file. Use exact string replacement of `metric_name` with `resolved_name` throughout. Do NOT change `__name__` (that is a Prometheus built-in, not our label).

    The operations dashboard (simetra-operations.json) has zero `metric_name` references — do NOT modify it.

    In E2E test scripts (scenarios 11-20), replace all occurrences of `metric_name` with `resolved_name` in:
    - PromQL query strings (e.g., `snmp_gauge{device_name="E2E-SIM",metric_name="e2e_gauge_test"}` becomes `snmp_gauge{device_name="E2E-SIM",resolved_name="e2e_gauge_test"}`)
    - jq label extraction (e.g., `.metric.metric_name` becomes `.metric.resolved_name`)
    - Shell variable names (e.g., `METRIC_NAME=` extractions from jq — rename the variable to `RESOLVED_NAME` for clarity)
    - Evidence strings and comments referencing `metric_name` label
    - Scenario description comments at top of files

    In the E2E lib files (tests/e2e/lib/common.sh, tests/e2e/lib/prometheus.sh), the `metric_name` variable refers to Prometheus metric names like `snmp_gauge` (the __name__ label), NOT our custom label. Do NOT rename those — they are unrelated to the `metric_name` tag on instruments.
  </action>
  <verify>
    Run `grep -rn "metric_name" deploy/grafana/dashboards/simetra-business.json` — should return zero results.

    Run `grep -rn "metric_name" tests/e2e/scenarios/` — should return zero results (all renamed to resolved_name).

    Verify `grep -rn "metric_name" tests/e2e/lib/` still shows hits — those are the Prometheus __name__ variable references and must NOT be changed.

    Validate JSON: `python -m json.tool deploy/grafana/dashboards/simetra-business.json > /dev/null` (confirms valid JSON after edits).
  </verify>
  <done>
    Business dashboard uses `resolved_name` in all 10 PromQL positions. All 10 E2E test scripts (scenarios 11-20) query and assert using `resolved_name`. E2E lib files unchanged (those reference Prometheus __name__, not our label).
  </done>
</task>

</tasks>

<verification>
1. `dotnet test` — all unit tests pass (specifically SnmpMetricFactoryTests)
2. `grep -rn '"metric_name"' src/SnmpCollector/Telemetry/` — zero results (no string literal tag key references remain)
3. `grep -rn 'metric_name' deploy/grafana/dashboards/simetra-business.json` — zero results
4. `grep -rn 'metric_name' tests/e2e/scenarios/` — zero results
5. `python -m json.tool deploy/grafana/dashboards/simetra-business.json > /dev/null` — valid JSON
</verification>

<success_criteria>
- All snmp_gauge and snmp_info time series use `resolved_name` label instead of `metric_name`
- Business dashboard updated with `resolved_name` in all PromQL queries (10 occurrences)
- All E2E test scripts (scenarios 11-20) use `resolved_name` in queries, assertions, and evidence strings
- All 414+ existing unit tests remain green
- No functional references to `metric_name` as a tag key remain in C# source or test files
</success_criteria>

<output>
After completion, create `.planning/phases/50-label-rename/50-01-SUMMARY.md`
</output>
