---
phase: 78-counter-reference-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/SnmpCollector/Jobs/SnapshotJob.cs
autonomous: true

must_haves:
  truths:
    - "No orphaned counting helper methods exist in SnapshotJob (CountResolvedNonViolated removed)"
    - "Build compiles cleanly with no warnings related to removed members"
  artifacts:
    - path: "src/SnmpCollector/Jobs/SnapshotJob.cs"
      provides: "Clean SnapshotJob with no dead counter code"
---

<objective>
Remove the orphaned CountResolvedNonViolated method from SnapshotJob.cs — the only dead code remaining from the counter-to-gauge migration.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Remove CountResolvedNonViolated dead method</name>
  <files>src/SnmpCollector/Jobs/SnapshotJob.cs</files>
  <action>
  Remove the `CountResolvedNonViolated` private static method (defined at line 449, ~30 lines). It was superseded by `CountResolvedViolated` in Phase 77 and has zero callers.

  Run `dotnet build` and `dotnet test` to confirm clean compilation.
  </action>
  <verify>
  `grep -n "CountResolvedNonViolated" src/SnmpCollector/Jobs/SnapshotJob.cs` returns no results.
  `dotnet build` succeeds. `dotnet test` passes all tests.
  </verify>
  <done>
  CountResolvedNonViolated removed. Zero dead counter code remains. Build clean.
  </done>
</task>

</tasks>

<success_criteria>
- No orphaned counting helper methods in SnapshotJob
- Build compiles cleanly
- All tests pass
</success_criteria>
