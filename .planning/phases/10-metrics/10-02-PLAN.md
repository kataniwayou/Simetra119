---
phase: 10-metrics
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/SnmpCollector/Telemetry/ISnmpMetricFactory.cs
  - src/SnmpCollector/Telemetry/SnmpMetricFactory.cs
  - src/SnmpCollector/Telemetry/PipelineMetricService.cs
  - src/SnmpCollector/Pipeline/Handlers/OtelMetricHandler.cs
  - src/SnmpCollector/Telemetry/SnmpLogEnrichmentProcessor.cs
  - src/SnmpCollector/Telemetry/SnmpConsoleFormatter.cs
  - src/SnmpCollector/Pipeline/CardinalityAuditService.cs
autonomous: true

must_haves:
  truths:
    - "snmp_gauge and snmp_info labels are: host_name, device_name, ip, metric_name, oid, source, snmp_type"
    - "Pipeline counter labels use host_name instead of site_name"
    - "Console formatter shows [hostname|role|correlationId] instead of [site|role|correlationId]"
    - "Log enrichment processor adds host_name instead of site_name"
    - "CardinalityAuditService label taxonomy log reflects new label names"
  artifacts:
    - path: "src/SnmpCollector/Telemetry/ISnmpMetricFactory.cs"
      provides: "Updated RecordGauge/RecordInfo signatures with deviceName + ip"
      exports: ["ISnmpMetricFactory"]
    - path: "src/SnmpCollector/Telemetry/SnmpMetricFactory.cs"
      provides: "host_name from hostname resolution, device_name + ip labels"
      contains: "host_name"
    - path: "src/SnmpCollector/Telemetry/PipelineMetricService.cs"
      provides: "host_name label on all 11 pipeline counters"
      contains: "host_name"
  key_links:
    - from: "src/SnmpCollector/Pipeline/Handlers/OtelMetricHandler.cs"
      to: "ISnmpMetricFactory"
      via: "RecordGauge/RecordInfo calls with deviceName and ip"
      pattern: "RecordGauge.*deviceName"
---

<objective>
Metric label taxonomy change: site_name -> host_name, agent -> device_name + ip

Purpose: Replace all occurrences of `site_name` with `host_name` (resolved from HOSTNAME env var or Environment.MachineName) and split the `agent` label into separate `device_name` and `ip` labels across all metric instruments, pipeline counters, log enrichment, and console formatter. This aligns the labeling convention with the community string convention where device identity is explicit.

Output: Updated ISnmpMetricFactory interface, SnmpMetricFactory, PipelineMetricService, OtelMetricHandler, SnmpLogEnrichmentProcessor, SnmpConsoleFormatter, and CardinalityAuditService.
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-metrics/10-CONTEXT.md
@.planning/phases/10-metrics/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ISnmpMetricFactory, SnmpMetricFactory, and PipelineMetricService label changes</name>
  <files>
    src/SnmpCollector/Telemetry/ISnmpMetricFactory.cs
    src/SnmpCollector/Telemetry/SnmpMetricFactory.cs
    src/SnmpCollector/Telemetry/PipelineMetricService.cs
  </files>
  <action>
    1. **ISnmpMetricFactory.cs**: Change `RecordGauge` and `RecordInfo` signatures to replace `string agent` with `string deviceName, string ip`:
       - `void RecordGauge(string metricName, string oid, string deviceName, string ip, string source, string snmpType, double value);`
       - `void RecordInfo(string metricName, string oid, string deviceName, string ip, string source, string snmpType, string value);`

    2. **SnmpMetricFactory.cs**:
       - Replace `private readonly string _siteName;` with `private readonly string _hostName;`
       - Change constructor: remove `IOptions<SiteOptions> siteOptions` parameter. Replace with hostname resolution:
         ```csharp
         _hostName = Environment.GetEnvironmentVariable("HOSTNAME") ?? Environment.MachineName;
         ```
         Constructor signature becomes: `public SnmpMetricFactory(IMeterFactory meterFactory)`
       - Remove `using SnmpCollector.Configuration;` and `using Microsoft.Extensions.Options;` imports (no longer needed).
       - Update `RecordGauge`: change parameter `string agent` to `string deviceName, string ip`. Replace TagList:
         ```csharp
         { "host_name", _hostName },
         { "metric_name", metricName },
         { "oid", oid },
         { "device_name", deviceName },
         { "ip", ip },
         { "source", source },
         { "snmp_type", snmpType }
         ```
       - Update `RecordInfo`: same parameter and TagList changes as RecordGauge (plus keep existing `value` tag).

    3. **PipelineMetricService.cs**:
       - Replace `private readonly string _siteName;` with `private readonly string _hostName;`
       - Change constructor: remove `IOptions<SiteOptions> siteOptions` parameter. Replace with hostname resolution:
         ```csharp
         _hostName = Environment.GetEnvironmentVariable("HOSTNAME") ?? Environment.MachineName;
         ```
         Constructor signature becomes: `public PipelineMetricService(IMeterFactory meterFactory)`
       - Remove `using SnmpCollector.Configuration;` and `using Microsoft.Extensions.Options;` imports.
       - Replace ALL `{ "site_name", _siteName }` with `{ "host_name", _hostName }` across all 11 Increment* methods.
  </action>
  <verify>Verify files compile by reading them for correct syntax. Full build will not pass yet because OtelMetricHandler still passes old `agent` parameter.</verify>
  <done>ISnmpMetricFactory has deviceName+ip params, SnmpMetricFactory uses host_name label with hostname resolution, PipelineMetricService uses host_name label with hostname resolution. Neither depends on SiteOptions.</done>
</task>

<task type="auto">
  <name>Task 2: OtelMetricHandler, log enrichment, console formatter, and cardinality audit</name>
  <files>
    src/SnmpCollector/Pipeline/Handlers/OtelMetricHandler.cs
    src/SnmpCollector/Telemetry/SnmpLogEnrichmentProcessor.cs
    src/SnmpCollector/Telemetry/SnmpConsoleFormatter.cs
    src/SnmpCollector/Pipeline/CardinalityAuditService.cs
  </files>
  <action>
    1. **OtelMetricHandler.cs**: Update the `Handle` method:
       - Replace `var agent = notification.DeviceName ?? notification.AgentIp.ToString();` with two separate variables:
         ```csharp
         var deviceName = notification.DeviceName ?? "unknown";
         var ip = notification.AgentIp.ToString();
         ```
       - Update ALL `_metricFactory.RecordGauge(...)` calls: replace the single `agent` argument with `deviceName, ip` (two arguments). There are 5 RecordGauge calls (Integer32, Gauge32, TimeTicks, Counter32, Counter64).
       - Update ALL `_metricFactory.RecordInfo(...)` calls: same change. There are 3 RecordInfo calls (OctetString, IPAddress, ObjectIdentifier).

    2. **SnmpLogEnrichmentProcessor.cs**: Change the constructor parameter name from `string siteName` to `string hostName`. Update `_siteName` field to `_hostName`. Update `OnEnd`: change `"site_name"` attribute key to `"host_name"` and use `_hostName`. Update XML doc comment accordingly.

    3. **SnmpConsoleFormatter.cs**: In the `Write` method:
       - Replace `var site = _siteOptions?.Value.Name ?? "unknown";` with hostname resolution:
         ```csharp
         var hostname = Environment.GetEnvironmentVariable("HOSTNAME") ?? Environment.MachineName;
         ```
       - Replace `textWriter.Write(site);` with `textWriter.Write(hostname);`
       - Remove `_siteOptions` field entirely. Remove `private IOptions<SiteOptions>? _siteOptions;` field.
       - In `EnsureServicesResolved()`: remove `_siteOptions = sp.GetService<IOptions<SiteOptions>>();` line.
       - Remove `using SnmpCollector.Configuration;` from imports if no other usages remain.
       - Update the XML doc comment format string: `[hostname|role|correlationId]` instead of `[site|role|correlationId]`.

    4. **CardinalityAuditService.cs**: Update label taxonomy log message:
       - Replace `"site_name (1 per deployment)"` with `"host_name (1 per deployment, from HOSTNAME env var)"`.
       - Replace `"agent (bounded by device count: {DeviceCount})"` with `"device_name (bounded by device count: {DeviceCount}), ip (bounded by device count)"`.
  </action>
  <verify>`dotnet build src/SnmpCollector/SnmpCollector.csproj` -- expect remaining errors only from files modified in Plan 03 (trap path) and Plan 04 (DI wiring), NOT from files in this plan.</verify>
  <done>OtelMetricHandler passes deviceName+ip to metric factory. Console formatter shows hostname. Log enrichment uses host_name. Cardinality audit reflects new taxonomy.</done>
</task>

</tasks>

<verification>
- grep for "site_name" in all modified files returns zero matches
- grep for "host_name" in SnmpMetricFactory, PipelineMetricService returns matches on all Increment/Record methods
- OtelMetricHandler passes exactly 2 args (deviceName, ip) instead of 1 (agent) to RecordGauge/RecordInfo
- SnmpConsoleFormatter no longer references SiteOptions
</verification>

<success_criteria>
All metric labels use host_name instead of site_name, agent split into device_name + ip. Console formatter and log enrichment updated. No SiteOptions dependency in metric/telemetry code.
</success_criteria>

<output>
After completion, create `.planning/phases/10-metrics/10-02-SUMMARY.md`
</output>
