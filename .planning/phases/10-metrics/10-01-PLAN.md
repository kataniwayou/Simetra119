---
phase: 10-metrics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/SnmpCollector/Configuration/SiteOptions.cs
  - src/SnmpCollector/Configuration/SnmpListenerOptions.cs
  - src/SnmpCollector/Configuration/DeviceOptions.cs
  - src/SnmpCollector/Configuration/Validators/SiteOptionsValidator.cs
  - src/SnmpCollector/Configuration/Validators/SnmpListenerOptionsValidator.cs
  - src/SnmpCollector/Pipeline/DeviceInfo.cs
  - src/SnmpCollector/Pipeline/DeviceRegistry.cs
  - src/SnmpCollector/Pipeline/CommunityStringHelper.cs
  - src/SnmpCollector/appsettings.json
  - src/SnmpCollector/appsettings.Development.json
autonomous: true

must_haves:
  truths:
    - "SiteOptions.Name is no longer required at startup -- missing Site.Name does not crash the app"
    - "SnmpListenerOptions.CommunityString field does not exist"
    - "DeviceOptions.CommunityString field does not exist"
    - "DeviceInfo record no longer has a CommunityString parameter"
    - "DeviceRegistry derives community string as Simetra.{Name} for each device"
    - "CommunityStringHelper.TryExtractDeviceName correctly parses Simetra.{DeviceName} convention"
    - "appsettings.json no longer contains Site.Name or SnmpListener.CommunityString"
  artifacts:
    - path: "src/SnmpCollector/Pipeline/CommunityStringHelper.cs"
      provides: "Static helper for Simetra.{DeviceName} community string parsing"
      contains: "TryExtractDeviceName"
    - path: "src/SnmpCollector/Configuration/SiteOptions.cs"
      provides: "SiteOptions with optional Name"
    - path: "src/SnmpCollector/Pipeline/DeviceInfo.cs"
      provides: "DeviceInfo without CommunityString"
  key_links:
    - from: "src/SnmpCollector/Pipeline/DeviceRegistry.cs"
      to: "CommunityStringHelper"
      via: "derives community from Simetra. + Name"
      pattern: "Simetra\\."
---

<objective>
Config cleanup and community string convention foundation

Purpose: Remove legacy config fields (SiteOptions.Name required, SnmpListenerOptions.CommunityString, DeviceOptions.CommunityString), update DeviceInfo/DeviceRegistry for derived community strings, and create the CommunityStringHelper utility. This is the foundation that all other Phase 10 plans depend on.

Output: Modified config classes, validators, DeviceInfo, DeviceRegistry, CommunityStringHelper, and updated appsettings files.
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-metrics/10-CONTEXT.md
@.planning/phases/10-metrics/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Config class cleanup and CommunityStringHelper</name>
  <files>
    src/SnmpCollector/Configuration/SiteOptions.cs
    src/SnmpCollector/Configuration/SnmpListenerOptions.cs
    src/SnmpCollector/Configuration/DeviceOptions.cs
    src/SnmpCollector/Configuration/Validators/SiteOptionsValidator.cs
    src/SnmpCollector/Configuration/Validators/SnmpListenerOptionsValidator.cs
    src/SnmpCollector/Pipeline/CommunityStringHelper.cs
  </files>
  <action>
    1. **SiteOptions.cs**: Remove `[Required]` attribute and `required` keyword from `Name` property. Make it `public string? Name { get; set; }` (nullable, optional). Keep `PodIdentity` unchanged. Keep `SectionName` constant.

    2. **SiteOptionsValidator.cs**: Remove the Name validation check entirely. The validator body should return `ValidateOptionsResult.Success` (or remove the validator class entirely and deregister from DI -- but keeping an empty validator is safer for now; just remove the Name check).

    3. **SnmpListenerOptions.cs**: Remove the `CommunityString` property entirely (the `[Required] public required string CommunityString` field). Keep BindAddress, Port, and Version.

    4. **SnmpListenerOptionsValidator.cs**: Remove the `CommunityString` validation check (the `IsNullOrWhiteSpace(options.CommunityString)` block). Keep BindAddress, Version, and Port validations.

    5. **DeviceOptions.cs**: Remove the `CommunityString` property entirely (the `public string? CommunityString` field). Keep Name, IpAddress, and MetricPolls.

    6. **CommunityStringHelper.cs**: Create new file at `src/SnmpCollector/Pipeline/CommunityStringHelper.cs`:
       ```
       namespace SnmpCollector.Pipeline;

       internal static class CommunityStringHelper
       {
           private const string CommunityPrefix = "Simetra.";

           internal static bool TryExtractDeviceName(string community, out string deviceName)
           {
               if (community.StartsWith(CommunityPrefix, StringComparison.Ordinal)
                   && community.Length > CommunityPrefix.Length)
               {
                   deviceName = community[CommunityPrefix.Length..];
                   return true;
               }
               deviceName = string.Empty;
               return false;
           }

           internal static string DeriveFromDeviceName(string deviceName)
               => string.Concat(CommunityPrefix, deviceName);
       }
       ```
  </action>
  <verify>Project compiles with `dotnet build src/SnmpCollector/SnmpCollector.csproj` (expect build errors in downstream files -- DeviceRegistry, MetricPollJob, SnmpTrapListenerService will fail because they reference removed fields; that is expected and will be fixed in Task 2 and subsequent plans).</verify>
  <done>SiteOptions.Name is optional, CommunityString removed from SnmpListenerOptions and DeviceOptions, CommunityStringHelper exists with TryExtractDeviceName and DeriveFromDeviceName.</done>
</task>

<task type="auto">
  <name>Task 2: DeviceInfo, DeviceRegistry, and appsettings updates</name>
  <files>
    src/SnmpCollector/Pipeline/DeviceInfo.cs
    src/SnmpCollector/Pipeline/DeviceRegistry.cs
    src/SnmpCollector/appsettings.json
    src/SnmpCollector/appsettings.Development.json
  </files>
  <action>
    1. **DeviceInfo.cs**: Remove `CommunityString` parameter from the record. New signature:
       `public sealed record DeviceInfo(string Name, string IpAddress, IReadOnlyList<MetricPollInfo> PollGroups);`

    2. **DeviceRegistry.cs**: Update the constructor:
       - Remove `IOptions<SnmpListenerOptions> listenerOptions` parameter entirely (no more global community string fallback).
       - Remove `var globalCommunity = listenerOptions.Value.CommunityString;` line.
       - Remove the community string resolution logic (the `string.IsNullOrWhiteSpace(d.CommunityString) ? globalCommunity : d.CommunityString` ternary).
       - Update `DeviceInfo` construction: `new DeviceInfo(d.Name, d.IpAddress, pollGroups)` (no CommunityString param).

    3. **appsettings.json**: Remove `"Site": { "Name": "site-nyc-01" }` section entirely. Remove `"CommunityString": "public"` from SnmpListener section. Keep BindAddress, Port only in SnmpListener.

    4. **appsettings.Development.json**: Remove `"CommunityString": "obp-secret"` from the obp-edge-01 device entry. Remove `"CommunityString": "public"` from SnmpListener section. Keep BindAddress, Port, Version in SnmpListener.

    NOTE: The build will still fail after this task because callers of DeviceInfo.CommunityString and DeviceRegistry constructor (ServiceCollectionExtensions) still reference removed members. Those will be fixed in Plan 04 (DI wiring). This plan focuses on getting the foundation types correct.
  </action>
  <verify>Verify the files are written correctly by reading them. Full project build is not expected to pass yet -- downstream callers will be fixed in Plans 02, 03, and 04.</verify>
  <done>DeviceInfo has no CommunityString, DeviceRegistry has no SnmpListenerOptions dependency, appsettings files cleaned of removed config fields.</done>
</task>

</tasks>

<verification>
- `CommunityStringHelper.TryExtractDeviceName("Simetra.npb-core-01", out var name)` returns true, name = "npb-core-01"
- `CommunityStringHelper.TryExtractDeviceName("public", out _)` returns false
- `CommunityStringHelper.DeriveFromDeviceName("npb-core-01")` returns "Simetra.npb-core-01"
- SiteOptions no longer has [Required] on Name
- DeviceInfo record has 3 parameters (Name, IpAddress, PollGroups)
</verification>

<success_criteria>
All config classes updated, CommunityStringHelper created, DeviceInfo/DeviceRegistry refactored. Foundation ready for Plans 02-04.
</success_criteria>

<output>
After completion, create `.planning/phases/10-metrics/10-01-SUMMARY.md`
</output>
