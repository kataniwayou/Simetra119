---
phase: 10-metrics
plan: 05
type: execute
wave: 4
depends_on: ["10-04"]
files_modified:
  - tests/SnmpCollector.Tests/Helpers/TestSnmpMetricFactory.cs
  - tests/SnmpCollector.Tests/Pipeline/Handlers/OtelMetricHandlerTests.cs
  - tests/SnmpCollector.Tests/Pipeline/PipelineIntegrationTests.cs
  - tests/SnmpCollector.Tests/Pipeline/DeviceChannelManagerTests.cs
  - tests/SnmpCollector.Tests/Pipeline/DeviceRegistryTests.cs
  - tests/SnmpCollector.Tests/Pipeline/Behaviors/ValidationBehaviorTests.cs
  - tests/SnmpCollector.Tests/Jobs/MetricPollJobTests.cs
  - tests/SnmpCollector.Tests/Services/SnmpTrapListenerServiceTests.cs
  - tests/SnmpCollector.Tests/Services/ChannelConsumerServiceTests.cs
  - tests/SnmpCollector.Tests/Lifecycle/GracefulShutdownServiceTests.cs
  - tests/SnmpCollector.Tests/Telemetry/SnmpMetricFactoryTests.cs
  - tests/SnmpCollector.Tests/Telemetry/PipelineMetricServiceTests.cs
autonomous: true

must_haves:
  truths:
    - "All tests compile and pass"
    - "TestSnmpMetricFactory matches new ISnmpMetricFactory signature (deviceName + ip instead of agent)"
    - "OtelMetricHandlerTests verify device_name and ip labels instead of agent"
    - "DeviceChannelManagerTests deleted or rewritten for TrapChannel"
    - "SnmpTrapListenerServiceTests verify community string convention"
    - "No test references IDeviceChannelManager, DeviceChannelManager, or device.CommunityString"
  artifacts:
    - path: "tests/SnmpCollector.Tests/Helpers/TestSnmpMetricFactory.cs"
      provides: "Test double matching new ISnmpMetricFactory interface"
    - path: "tests/SnmpCollector.Tests/Services/SnmpTrapListenerServiceTests.cs"
      provides: "Tests for community string validation"
      contains: "Simetra."
  key_links:
    - from: "tests/SnmpCollector.Tests/Pipeline/Handlers/OtelMetricHandlerTests.cs"
      to: "TestSnmpMetricFactory"
      via: "Test assertions on deviceName + ip"
      pattern: "device_name|DeviceName"
---

<objective>
Update all test files for Phase 10 changes

Purpose: Fix all test compilation errors and update test assertions to match the new metric label taxonomy (host_name, device_name, ip), community string convention (Simetra.{DeviceName}), single shared channel (ITrapChannel/TrapChannel), and removed config fields. Delete obsolete tests, rewrite tests for rewritten components.

Output: All tests compile and pass. Full test coverage for Phase 10 behavior.
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-metrics/10-CONTEXT.md
@.planning/phases/10-metrics/10-RESEARCH.md
@.planning/phases/10-metrics/10-01-SUMMARY.md
@.planning/phases/10-metrics/10-02-SUMMARY.md
@.planning/phases/10-metrics/10-03-SUMMARY.md
@.planning/phases/10-metrics/10-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test helpers and handler/behavior/integration test updates</name>
  <files>
    tests/SnmpCollector.Tests/Helpers/TestSnmpMetricFactory.cs
    tests/SnmpCollector.Tests/Pipeline/Handlers/OtelMetricHandlerTests.cs
    tests/SnmpCollector.Tests/Pipeline/PipelineIntegrationTests.cs
    tests/SnmpCollector.Tests/Pipeline/Behaviors/ValidationBehaviorTests.cs
    tests/SnmpCollector.Tests/Pipeline/DeviceRegistryTests.cs
    tests/SnmpCollector.Tests/Telemetry/SnmpMetricFactoryTests.cs
    tests/SnmpCollector.Tests/Telemetry/PipelineMetricServiceTests.cs
  </files>
  <action>
    Read each test file first to understand its current structure, then apply changes:

    1. **TestSnmpMetricFactory.cs**: Update to match new ISnmpMetricFactory interface:
       - Change `RecordGauge(string metricName, string oid, string agent, string source, string snmpType, double value)` to `RecordGauge(string metricName, string oid, string deviceName, string ip, string source, string snmpType, double value)`.
       - Change `RecordInfo(string metricName, string oid, string agent, string source, string snmpType, string value)` to `RecordInfo(string metricName, string oid, string deviceName, string ip, string source, string snmpType, string value)`.
       - Update any internal recording structures to capture `deviceName` and `ip` instead of `agent`.

    2. **OtelMetricHandlerTests.cs**: Update test assertions:
       - Replace any assertions checking `agent` with assertions checking `deviceName` and `ip` separately.
       - Update SnmpOidReceived construction in tests to ensure DeviceName is set (it should already be).
       - Update expected parameter order in RecordGauge/RecordInfo call verification.

    3. **PipelineIntegrationTests.cs**: Update for removed SiteOptions.Name requirement:
       - If tests create SiteOptions with Name, keep it (optional now) or remove -- either works.
       - Update any assertions on metric labels from `site_name`/`agent` to `host_name`/`device_name`/`ip`.
       - Remove SiteOptions dependency from PipelineMetricService construction (no longer takes IOptions<SiteOptions>).
       - Remove SiteOptions dependency from SnmpMetricFactory construction (no longer takes IOptions<SiteOptions>).

    4. **ValidationBehaviorTests.cs**: Update for removed IDeviceRegistry dependency:
       - Remove IDeviceRegistry mock/stub from test setup.
       - Update constructor call for ValidationBehavior to not pass IDeviceRegistry.
       - If tests verify "unknown device" rejection via registry lookup, change them to verify "MissingDeviceName" rejection (when DeviceName is null).

    5. **DeviceRegistryTests.cs**: Update for removed CommunityString:
       - Remove CommunityString from DeviceOptions in test data.
       - Remove SnmpListenerOptions from DeviceRegistry constructor calls in tests.
       - Update DeviceInfo assertions to not check CommunityString.
       - If tests verify community string resolution (per-device vs global fallback), DELETE those test cases entirely.

    6. **SnmpMetricFactoryTests.cs**: Update for removed SiteOptions dependency:
       - Remove IOptions<SiteOptions> from SnmpMetricFactory constructor calls.
       - Update MeterListener tag assertions: replace `site_name` with `host_name`, replace `agent` with `device_name` + `ip`.
       - Update RecordGauge/RecordInfo calls to pass `deviceName, ip` instead of `agent`.

    7. **PipelineMetricServiceTests.cs**: Update for removed SiteOptions dependency:
       - Remove IOptions<SiteOptions> from PipelineMetricService constructor calls.
       - Update MeterListener tag assertions: replace `site_name` with `host_name`.
  </action>
  <verify>`dotnet build tests/SnmpCollector.Tests/SnmpCollector.Tests.csproj` compiles with zero errors for the files touched in this task.</verify>
  <done>All pipeline, handler, behavior, and telemetry test files compile. Test assertions match new label taxonomy and interface signatures.</done>
</task>

<task type="auto">
  <name>Task 2: Service tests and channel tests update</name>
  <files>
    tests/SnmpCollector.Tests/Pipeline/DeviceChannelManagerTests.cs
    tests/SnmpCollector.Tests/Services/SnmpTrapListenerServiceTests.cs
    tests/SnmpCollector.Tests/Services/ChannelConsumerServiceTests.cs
    tests/SnmpCollector.Tests/Jobs/MetricPollJobTests.cs
    tests/SnmpCollector.Tests/Lifecycle/GracefulShutdownServiceTests.cs
  </files>
  <action>
    Read each test file first to understand its current structure, then apply changes:

    1. **DeviceChannelManagerTests.cs**: DELETE this file entirely (DeviceChannelManager was deleted). If there are useful patterns, consider creating a new `TrapChannelTests.cs` with basic tests for the TrapChannel class:
       - Test that `Writer.TryWrite` succeeds when channel is not full.
       - Test that `Complete()` signals Reader completion.
       - Test that `WaitForDrainAsync` completes after all items are read and channel is completed.
       However, if the original tests were complex, just delete -- TrapChannel is simple enough that it gets covered by integration tests.

    2. **SnmpTrapListenerServiceTests.cs**: This is the most significant rewrite. Read the existing file carefully first.
       - Remove all `StubDeviceRegistry` / `IDeviceRegistry` mock references.
       - Remove all community string matching tests that relied on device registry lookup.
       - Replace `IDeviceChannelManager` stubs with `ITrapChannel` stubs (or a simple CapturingTrapChannel test helper that captures TryWrite calls).
       - **New test cases for community string convention**:
         a. Valid `Simetra.npb-core-01` community -> varbinds written to channel with DeviceName="npb-core-01".
         b. Invalid community `public` -> trap dropped, IncrementTrapAuthFailed called, Debug log.
         c. Empty community string -> dropped.
         d. `Simetra.` with no device name after dot -> dropped.
       - Update test helper for creating trap PDUs: use `Simetra.{deviceName}` as community string.
       - If tests used a `CapturingChannelManager`, replace with a `CapturingTrapChannel` that implements ITrapChannel with an in-memory list for TryWrite captures.

    3. **ChannelConsumerServiceTests.cs**: Update for ITrapChannel:
       - Replace `IDeviceChannelManager` references with `ITrapChannel`.
       - Replace per-device channel reader setup with single channel reader.
       - Remove device name iteration; use single reader from ITrapChannel.

    4. **MetricPollJobTests.cs**: Update for removed DeviceInfo.CommunityString:
       - Update DeviceInfo construction in test setup: remove CommunityString parameter.
       - If tests verify the community string passed to ISnmpClient.GetAsync, update expected value from `device.CommunityString` to `"Simetra." + device.Name`.

    5. **GracefulShutdownServiceTests.cs**: Update for ITrapChannel:
       - Replace `IDeviceChannelManager` mock/stub with `ITrapChannel` mock/stub.
       - Update `CompleteAll()` call expectations to `Complete()`.
       - Update `WaitForDrainAsync()` expectations to use ITrapChannel.

    After all changes, run: `dotnet test tests/SnmpCollector.Tests/SnmpCollector.Tests.csproj`

    Fix any remaining compilation or assertion failures iteratively until ALL tests pass.
  </action>
  <verify>`dotnet test tests/SnmpCollector.Tests/SnmpCollector.Tests.csproj` -- ALL tests must pass with zero failures.</verify>
  <done>All test files compile and all tests pass. No references to IDeviceChannelManager, DeviceChannelManager, device.CommunityString, or site_name in any test file.</done>
</task>

</tasks>

<verification>
- `dotnet test tests/SnmpCollector.Tests/SnmpCollector.Tests.csproj` returns 0 failures
- grep for "IDeviceChannelManager" in tests/ returns zero matches
- grep for "site_name" in tests/ returns zero matches
- grep for "CommunityString" in tests/ returns zero matches (except CommunityStringHelper test references)
- grep for "\"agent\"" (as label name) in tests/ returns zero matches
</verification>

<success_criteria>
All tests compile and pass. Zero references to removed types/fields. Test coverage confirms community string convention, new label taxonomy, and single channel architecture.
</success_criteria>

<output>
After completion, create `.planning/phases/10-metrics/10-05-SUMMARY.md`
</output>
