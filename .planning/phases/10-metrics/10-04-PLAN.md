---
phase: 10-metrics
plan: 04
type: execute
wave: 3
depends_on: ["10-01", "10-02", "10-03"]
files_modified:
  - src/SnmpCollector/Jobs/MetricPollJob.cs
  - src/SnmpCollector/Pipeline/Behaviors/ValidationBehavior.cs
  - src/SnmpCollector/HealthChecks/ReadinessHealthCheck.cs
  - src/SnmpCollector/Lifecycle/GracefulShutdownService.cs
  - src/SnmpCollector/Extensions/ServiceCollectionExtensions.cs
autonomous: true

must_haves:
  truths:
    - "MetricPollJob derives community string as Simetra.{device.Name}"
    - "ValidationBehavior no longer does device registry lookup for traps (DeviceName always set)"
    - "ReadinessHealthCheck passes without devices configured -- checks trap listener bound + Quartz running"
    - "GracefulShutdownService drains via ITrapChannel instead of IDeviceChannelManager"
    - "ServiceCollectionExtensions wires ITrapChannel, removes IDeviceChannelManager, removes SiteOptions.Name requirement from telemetry"
    - "Project builds and dotnet test passes"
  artifacts:
    - path: "src/SnmpCollector/Extensions/ServiceCollectionExtensions.cs"
      provides: "Updated DI wiring for all Phase 10 changes"
      contains: "ITrapChannel"
    - path: "src/SnmpCollector/HealthChecks/ReadinessHealthCheck.cs"
      provides: "Readiness check without device count requirement"
      contains: "IsBound"
  key_links:
    - from: "src/SnmpCollector/Extensions/ServiceCollectionExtensions.cs"
      to: "ITrapChannel"
      via: "AddSingleton<ITrapChannel, TrapChannel>"
      pattern: "ITrapChannel.*TrapChannel"
    - from: "src/SnmpCollector/HealthChecks/ReadinessHealthCheck.cs"
      to: "SnmpTrapListenerService"
      via: "IsBound flag for trap listener status"
      pattern: "IsBound"
---

<objective>
Poll path alignment, readiness check, DI wiring, and build fix

Purpose: Update MetricPollJob to derive community strings from the Simetra.{DeviceName} convention, simplify ValidationBehavior (traps always have DeviceName set), update ReadinessHealthCheck to not require devices, update GracefulShutdownService for ITrapChannel, and fix ServiceCollectionExtensions DI wiring so the entire project compiles and tests pass.

Output: All remaining compile errors fixed. Full project builds. Poll path uses convention-derived community strings.
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-metrics/10-CONTEXT.md
@.planning/phases/10-metrics/10-RESEARCH.md
@.planning/phases/10-metrics/10-01-SUMMARY.md
@.planning/phases/10-metrics/10-02-SUMMARY.md
@.planning/phases/10-metrics/10-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: MetricPollJob, ValidationBehavior, ReadinessHealthCheck, GracefulShutdownService</name>
  <files>
    src/SnmpCollector/Jobs/MetricPollJob.cs
    src/SnmpCollector/Pipeline/Behaviors/ValidationBehavior.cs
    src/SnmpCollector/HealthChecks/ReadinessHealthCheck.cs
    src/SnmpCollector/Lifecycle/GracefulShutdownService.cs
  </files>
  <action>
    1. **MetricPollJob.cs**:
       - Change the community string derivation: replace `var community = new OctetString(device.CommunityString);` with `var community = new OctetString(CommunityStringHelper.DeriveFromDeviceName(device.Name));`
       - Add `using SnmpCollector.Pipeline;` if not already present (for CommunityStringHelper).
       - Note: DeviceInfo no longer has CommunityString field, so this is the only change needed.

    2. **ValidationBehavior.cs**:
       - The "unknown device check" block (lines 62-79 in current code) does a device registry IP lookup when `msg.DeviceName is null`. With the new design, traps ALWAYS have DeviceName set (extracted from community string in SnmpTrapListenerService), and polls ALWAYS have DeviceName set (set by MetricPollJob). So this entire block can be simplified.
       - **Keep the DeviceName null check** but change it to: if DeviceName is null, reject the message (this is now a programming error, not a normal trap flow):
         ```csharp
         if (msg.DeviceName is null)
         {
             _logger.LogWarning(
                 "SnmpOidReceived rejected: Oid={Oid} AgentIp={AgentIp} Reason={Reason}",
                 msg.Oid, msg.AgentIp, "MissingDeviceName");
             _metrics.IncrementRejected();
             return default!;
         }
         ```
       - **Remove the IDeviceRegistry dependency**: Remove `_deviceRegistry` field, remove from constructor parameter. Remove `using` for IDeviceRegistry if no longer needed.

    3. **ReadinessHealthCheck.cs**:
       - **Remove IDeviceChannelManager dependency**. Replace with `SnmpTrapListenerService` to check `IsBound`.
       - **Update constructor**: Replace `IDeviceChannelManager channels` with obtaining the trap listener. Use IServiceProvider to resolve SnmpTrapListenerService from hosted services, OR inject SnmpTrapListenerService directly as a constructor parameter. The cleanest approach: register SnmpTrapListenerService as a named singleton (see Task 2 DI wiring). For now, use `IServiceProvider` resolution pattern:
         Actually, simpler: Add a new interface or use the existing service. The cleanest approach per RESEARCH.md discretion: SnmpTrapListenerService exposes `bool IsBound` (added in Plan 03). Register the concrete type as singleton and resolve both as IHostedService and directly.
       - **Replace DeviceNames.Count > 0 check** with: resolve SnmpTrapListenerService, check its `IsBound` property. If not bound, return Unhealthy("Trap listener is not bound").
       - **Keep Quartz scheduler check** (IsStarted && !IsShutdown).
       - New constructor takes `IServiceProvider serviceProvider` and `ISchedulerFactory schedulerFactory`.
       - In CheckHealthAsync: `var listener = serviceProvider.GetServices<IHostedService>().OfType<SnmpTrapListenerService>().FirstOrDefault();` then check `listener?.IsBound != true`.

    4. **GracefulShutdownService.cs**:
       - Replace `IDeviceChannelManager _channelManager` with `ITrapChannel _trapChannel` in constructor and field.
       - **Step 4 (Drain)**: Change `_channelManager.CompleteAll()` to `_trapChannel.Complete()`. Change `_channelManager.WaitForDrainAsync(...)` to `_trapChannel.WaitForDrainAsync(...)`. Update log messages ("Trap channel" instead of "Device channels").
  </action>
  <verify>Read all 4 files to confirm correct structure. Build errors may remain in ServiceCollectionExtensions (fixed in Task 2).</verify>
  <done>MetricPollJob uses derived community string, ValidationBehavior has no IDeviceRegistry dependency, ReadinessHealthCheck checks trap listener IsBound, GracefulShutdownService uses ITrapChannel.</done>
</task>

<task type="auto">
  <name>Task 2: ServiceCollectionExtensions DI wiring update</name>
  <files>
    src/SnmpCollector/Extensions/ServiceCollectionExtensions.cs
  </files>
  <action>
    1. **AddSnmpConfiguration**:
       - **SiteOptions**: Keep the binding but remove `ValidateDataAnnotations()` (since [Required] is removed from Name). Keep `ValidateOnStart()` with the validator if the validator still exists (it does, but does nothing -- keep it for forward compatibility). OR remove `ValidateDataAnnotations()` only. Actually, since Name is now `string?` without `[Required]`, `ValidateDataAnnotations()` won't fail. Keep it -- it's harmless.
       - **SiteOptionsValidator**: Keep registered (it validates nothing now, returns Success -- harmless).
       - **SnmpListenerOptions**: The binding stays, but CommunityString is gone from the class. `ValidateDataAnnotations()` and `ValidateOnStart()` still work on remaining fields (BindAddress, Port, Version). Keep as is.
       - **SnmpListenerOptionsValidator**: Keep registered -- it no longer validates CommunityString (that check was removed in Plan 01). Validates BindAddress, Version, Port.
       - **DeviceRegistry constructor change**: DeviceRegistry no longer takes `IOptions<SnmpListenerOptions>`. Since it's registered as `services.AddSingleton<IDeviceRegistry, DeviceRegistry>()`, DI resolves constructor params automatically. Since DeviceRegistry now only takes `IOptions<DevicesOptions>`, this should work automatically. No change needed in the registration line.
       - **Log enrichment processor in AddSnmpTelemetry**: The processor factory lambda currently does:
         ```csharp
         var siteOptions = sp.GetRequiredService<IOptions<SiteOptions>>().Value;
         ...
         new SnmpLogEnrichmentProcessor(correlationService, siteOptions.Name, ...)
         ```
         Change to hostname resolution:
         ```csharp
         var hostName = Environment.GetEnvironmentVariable("HOSTNAME") ?? Environment.MachineName;
         ...
         new SnmpLogEnrichmentProcessor(correlationService, hostName, ...)
         ```
         Remove the `var siteOptions = sp.GetRequiredService<IOptions<SiteOptions>>().Value;` line.

    2. **AddSnmpPipeline**:
       - **Replace IDeviceChannelManager registration** with ITrapChannel:
         Change `services.AddSingleton<IDeviceChannelManager, DeviceChannelManager>();` to `services.AddSingleton<ITrapChannel, TrapChannel>();`
       - **SnmpTrapListenerService**: Keep `services.AddHostedService<SnmpTrapListenerService>();` as is.
       - **ChannelConsumerService**: Keep `services.AddHostedService<ChannelConsumerService>();` as is.

    3. **Build verification**: After all changes, run `dotnet build src/SnmpCollector/SnmpCollector.csproj` to confirm zero errors. Fix any remaining compile errors.

    4. **Run tests**: Run `dotnet test tests/SnmpCollector.Tests/SnmpCollector.Tests.csproj` -- tests WILL FAIL because test files still reference old types/signatures. That is expected and will be fixed in Plan 05.
  </action>
  <verify>`dotnet build src/SnmpCollector/SnmpCollector.csproj` must compile with zero errors. Test compilation failures are expected (fixed in Plan 05).</verify>
  <done>ServiceCollectionExtensions fully updated for all Phase 10 changes. Main project compiles cleanly. DI wiring uses ITrapChannel, hostname resolution for log enrichment, DeviceRegistry without SnmpListenerOptions.</done>
</task>

</tasks>

<verification>
- `dotnet build src/SnmpCollector/SnmpCollector.csproj` returns 0 errors
- grep for "IDeviceChannelManager" returns zero matches in src/ directory
- grep for "device.CommunityString" returns zero matches in src/ directory
- grep for "siteOptions.Name" returns zero matches outside of SiteOptions.cs itself
- ReadinessHealthCheck does not reference DeviceNames.Count
</verification>

<success_criteria>
Main SnmpCollector project compiles with zero errors. All Phase 10 source changes complete. Poll path uses Simetra.{DeviceName} convention. ReadinessHealthCheck works without devices. DI wiring correct.
</success_criteria>

<output>
After completion, create `.planning/phases/10-metrics/10-04-SUMMARY.md`
</output>
