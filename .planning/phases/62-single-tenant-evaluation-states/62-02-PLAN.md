---
phase: 62-single-tenant-evaluation-states
plan: 02
type: execute
wave: 2
depends_on: ["62-01"]
files_modified:
  - tests/e2e/scenarios/56-pss-04-unresolved.sh
  - tests/e2e/scenarios/57-pss-05-healthy.sh
  - tests/e2e/scenarios/58-pss-06-suppression.sh
  - tests/e2e/fixtures/tenant-cfg06-pss-suppression.yaml
autonomous: true

must_haves:
  truths:
    - "All evaluate violated produces tier=4 with commands dispatched (sent counter increments)"
    - "All metrics in-range produces tier=3 Healthy with no commands dispatched"
    - "Second tier=4 within suppression window shows suppressed counter incrementing"
  artifacts:
    - path: "tests/e2e/scenarios/56-pss-04-unresolved.sh"
      provides: "PSS-04 Unresolved scenario with tier=4 command dispatch"
      contains: "record_pass.*PSS-04"
    - path: "tests/e2e/scenarios/57-pss-05-healthy.sh"
      provides: "PSS-05 Healthy scenario with tier=3 no commands"
      contains: "record_pass.*PSS-05"
    - path: "tests/e2e/scenarios/58-pss-06-suppression.sh"
      provides: "PSS-06 Suppression scenario with sent then suppressed windows"
      contains: "record_pass.*PSS-06"
    - path: "tests/e2e/fixtures/tenant-cfg06-pss-suppression.yaml"
      provides: "Suppression variant fixture with SuppressionWindowSeconds=30"
      contains: "e2e-pss-tenant-supp"
  key_links:
    - from: "tests/e2e/scenarios/56-pss-04-unresolved.sh"
      to: "tests/e2e/fixtures/tenant-cfg06-pss-single.yaml"
      via: "kubectl apply"
      pattern: "kubectl apply -f.*tenant-cfg06-pss-single"
    - from: "tests/e2e/scenarios/58-pss-06-suppression.sh"
      to: "tests/e2e/fixtures/tenant-cfg06-pss-suppression.yaml"
      via: "kubectl apply"
      pattern: "kubectl apply -f.*tenant-cfg06-pss-suppression"
    - from: "tests/e2e/scenarios/58-pss-06-suppression.sh"
      to: "prometheus.sh"
      via: "snmp_command_suppressed_total with device_name=e2e-pss-tenant-supp"
      pattern: "snmp_command_suppressed_total.*e2e-pss-tenant-supp"
---

<objective>
Create the remaining three PSS scenario scripts covering Unresolved (tier=4 commands), Healthy (tier=3 no commands), and Suppression (second tier=4 suppressed within window), plus the suppression variant fixture.

Purpose: Completes the PSS single-tenant evaluation state coverage by verifying tier=4 command dispatch, tier=3 healthy state, and the suppression window mechanism with a dedicated suppression fixture.

Output: 3 scenario scripts (56-58), 1 suppression fixture YAML
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-single-tenant-evaluation-states/62-RESEARCH.md
@.planning/phases/62-single-tenant-evaluation-states/62-01-SUMMARY.md

@tests/e2e/fixtures/tenant-cfg06-pss-single.yaml
@tests/e2e/fixtures/tenant-cfg01-suppression.yaml
@tests/e2e/scenarios/44-sns-04-unresolved.sh
@tests/e2e/scenarios/45-sns-05-healthy.sh
@tests/e2e/scenarios/32-sts-04-suppression-window.sh
@tests/e2e/lib/sim.sh
@tests/e2e/lib/prometheus.sh
@tests/e2e/lib/common.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PSS scenarios 56-57 (Unresolved, Healthy)</name>
  <files>
    tests/e2e/scenarios/56-pss-04-unresolved.sh
    tests/e2e/scenarios/57-pss-05-healthy.sh
  </files>
  <action>
Create two scenario scripts following the established PSS pattern from Plan 01. Use @44-sns-04-unresolved.sh and @45-sns-05-healthy.sh as structural templates but adapted for the single-tenant PSS fixture.

**56-pss-04-unresolved.sh** (maps to PSS-08, Success Criteria 4 -- evaluate violated):
- Header: Scenario 56, PSS-04 Unresolved (Commands), uses tenant-cfg06-pss-single.yaml
- Tier=4 fires when: resolved NOT all violated + evaluate IS violated
- Setup: save_configmap, kubectl apply cfg06, poll_until_log for reload
- Prime 3 T2 OIDs in-range (5.1=10, 5.2=1, 5.3=1), sleep 8s
- Baseline: BEFORE_SENT = snapshot_counter "snmp_command_sent_total" 'device_name="E2E-SIM"'
- Stimulus: sim_set_oid "5.1" "0" (eval violated < Min:10; res1/res2 stay at 1 from priming)
- Assert PSS-04A: poll_until_log 30 1 "e2e-pss-tenant.*tier=4 — commands enqueued\|e2e-pss-tenant.*tier=4 -- commands enqueued" 15
- Assert PSS-04B: poll_until 30 2 "snmp_command_sent_total" 'device_name="E2E-SIM"' "$BEFORE_SENT" (counter increment with delta logging)
- Cleanup: reset_oid_overrides, restore_configmap

**57-pss-05-healthy.sh** (maps to PSS-09, Success Criteria 4 -- all in-range):
- Header: Scenario 57, PSS-05 Healthy, uses tenant-cfg06-pss-single.yaml
- Tier=3 fires when: resolved not all violated + evaluate not violated = all in-range
- Setup: save_configmap, kubectl apply cfg06, poll_until_log for reload
- Prime 3 T2 OIDs in-range (5.1=10, 5.2=1, 5.3=1), sleep 8s
- Baseline: BEFORE_SENT AFTER priming sleep (priming-phase commands already counted)
- NO additional stimulus -- T1 is already in healthy state from priming
- Assert PSS-05A: poll_until_log 30 1 "e2e-pss-tenant.*tier=3" 15
- Assert PSS-05B: sleep 10, compare sent counter, delta must be 0 (negative assertion)
- Cleanup: reset_oid_overrides, restore_configmap

CRITICAL notes:
- Only prime 3 OIDs (5.1, 5.2, 5.3) -- NOT 12 like the SNS suite (single tenant)
- Tenant name in grep patterns: "e2e-pss-tenant"
- Use "PSS-04" and "PSS-05" prefixes for log_info/record_pass/fail
- Follow identical cleanup pattern: reset_oid_overrides then restore_configmap
  </action>
  <verify>
`bash -n tests/e2e/scenarios/56-pss-04-unresolved.sh && bash -n tests/e2e/scenarios/57-pss-05-healthy.sh` -- no syntax errors.
`grep -c "record_pass\|record_fail" tests/e2e/scenarios/56-pss-04-unresolved.sh` -- at least 2 assertions.
`grep -c "record_pass\|record_fail" tests/e2e/scenarios/57-pss-05-healthy.sh` -- at least 2 assertions.
  </verify>
  <done>
Scenarios 56 and 57 exist, pass syntax check, and follow PSS pattern. Scenario 56 verifies tier=4 command dispatch when evaluate is violated. Scenario 57 verifies tier=3 healthy state with no commands when all metrics are in-range.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create suppression fixture and PSS scenario 58</name>
  <files>
    tests/e2e/fixtures/tenant-cfg06-pss-suppression.yaml
    tests/e2e/scenarios/58-pss-06-suppression.sh
  </files>
  <action>
Create the suppression variant fixture and the suppression scenario script.

**tenant-cfg06-pss-suppression.yaml:**
Copy tenant-cfg06-pss-single.yaml but change:
- Tenant Name: "e2e-pss-tenant-supp" (different name so suppressed counter label is distinguishable)
- SuppressionWindowSeconds: 30 (long enough to observe suppression with 1s SnapshotJob cycle)

The suppression counter uses `device_name` label with value = tenant ID (from IncrementCommandSuppressed). So the query label will be `device_name="e2e-pss-tenant-supp"`. The sent counter still uses `device_name="E2E-SIM"`.

**58-pss-06-suppression.sh** (maps to PSS-10, Success Criteria 5):
- Header: Scenario 58, PSS-06 Suppression Window, uses tenant-cfg06-pss-suppression.yaml (NOT cfg06-pss-single)
- SuppressionWindowSeconds=30, SnapshotJob ~1s cycle
- Setup: save_configmap, kubectl apply cfg06-pss-suppression, poll_until_log for reload
- Prime 3 T2 OIDs in-range (5.1=10, 5.2=1, 5.3=1), sleep 8s

**Window 1 -- First command SENT:**
- Baseline: BEFORE_SENT_W1 = snapshot_counter "snmp_command_sent_total" 'device_name="E2E-SIM"'
- Stimulus: sim_set_oid "5.1" "0" (eval violated)
- Assert PSS-06A: poll_until_log 30 1 "e2e-pss-tenant-supp.*tier=4 — commands enqueued\|e2e-pss-tenant-supp.*tier=4 -- commands enqueued" 15
- Assert PSS-06B: poll_until 30 2 "snmp_command_sent_total" 'device_name="E2E-SIM"' "$BEFORE_SENT_W1" (sent counter increment)

**Window 2 -- Second cycle SUPPRESSED (within 30s window):**
- Baseline: BEFORE_SUPP_W2 = snapshot_counter "snmp_command_suppressed_total" 'device_name="e2e-pss-tenant-supp"'
- BEFORE_SENT_W2 = snapshot_counter "snmp_command_sent_total" 'device_name="E2E-SIM"'
- Poll until suppressed counter increments: poll_until 30 5 "snmp_command_suppressed_total" 'device_name="e2e-pss-tenant-supp"' "$BEFORE_SUPP_W2"
- Capture AFTER values for both counters
- Assert PSS-06C: suppressed delta > 0 (use assert_delta_gt if available, otherwise manual comparison with record_pass/fail)
- Assert PSS-06D: sent delta == 0 during window 2 (command was suppressed, not re-sent)

**NOTE on Window 3 (sent again after expiry):** The existing STS-04 (scenario 32) already covers the full 3-window suppression lifecycle with 20s sleep for window expiry. For PSS-06, only Windows 1 and 2 are needed to satisfy Success Criteria 5 ("snmp_command_suppressed_total incrementing on the second cycle"). Including Window 3 would add 20+ seconds of dead wait. Skip Window 3 to keep PSS scenarios fast.

- Cleanup: reset_oid_overrides, restore_configmap

CRITICAL notes:
- Use suppression fixture (cfg06-pss-suppression), NOT cfg06-pss-single
- Suppressed counter label: device_name="e2e-pss-tenant-supp" (tenant ID, not device name)
- Sent counter label: device_name="E2E-SIM" (actual device name)
- Use "PSS-06" prefix for all log_info and record_pass/fail calls
- Check if assert_delta_gt helper exists in common.sh; if not, use manual comparison pattern like SNS scenarios
  </action>
  <verify>
`bash -n tests/e2e/scenarios/58-pss-06-suppression.sh` -- no syntax errors.
`grep -c "record_pass\|record_fail" tests/e2e/scenarios/58-pss-06-suppression.sh` -- at least 4 assertions (W1 log, W1 sent, W2 suppressed, W2 sent-unchanged).
Verify suppression fixture has SuppressionWindowSeconds=30 and tenant name "e2e-pss-tenant-supp".
Verify suppressed counter query uses 'device_name="e2e-pss-tenant-supp"' (not "E2E-SIM").
  </verify>
  <done>
Suppression fixture exists with 30s window and distinct tenant name. Scenario 58 verifies Window 1 (first command sent) and Window 2 (second command suppressed within window). Both sent and suppressed counters are correctly labeled.
  </done>
</task>

</tasks>

<verification>
1. `ls tests/e2e/fixtures/tenant-cfg06-pss-suppression.yaml` -- suppression fixture exists
2. `ls tests/e2e/scenarios/5[678]-pss-*.sh` -- all three scripts exist
3. `bash -n tests/e2e/scenarios/56-pss-04-unresolved.sh` -- no syntax errors
4. `bash -n tests/e2e/scenarios/57-pss-05-healthy.sh` -- no syntax errors
5. `bash -n tests/e2e/scenarios/58-pss-06-suppression.sh` -- no syntax errors
6. `grep "SuppressionWindowSeconds.*30" tests/e2e/fixtures/tenant-cfg06-pss-suppression.yaml` -- 30s window
7. `grep "e2e-pss-tenant-supp" tests/e2e/fixtures/tenant-cfg06-pss-suppression.yaml` -- correct tenant name
8. `grep 'device_name="e2e-pss-tenant-supp"' tests/e2e/scenarios/58-pss-06-suppression.sh` -- correct suppression counter label
9. Total PSS sub-assertions across scenarios 53-58: at least 15 record_pass/record_fail calls
</verification>

<success_criteria>
- Scenario 56 verifies tier=4 command dispatch when evaluate metric is violated (sent counter increments)
- Scenario 57 verifies tier=3 healthy state with zero commands when all metrics in-range
- Scenario 58 verifies suppression: first tier=4 sends command, second tier=4 within 30s window shows suppressed counter incrementing and sent counter unchanged
- Suppression fixture uses distinct tenant name "e2e-pss-tenant-supp" for counter label disambiguation
- All scripts pass bash -n syntax check
</success_criteria>

<output>
After completion, create `.planning/phases/62-single-tenant-evaluation-states/62-02-SUMMARY.md`
</output>
