---
phase: 62-single-tenant-evaluation-states
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/fixtures/tenant-cfg06-pss-single.yaml
  - tests/e2e/lib/report.sh
  - tests/e2e/scenarios/53-pss-01-not-ready.sh
  - tests/e2e/scenarios/54-pss-02-stale-to-commands.sh
  - tests/e2e/scenarios/55-pss-03-resolved.sh
autonomous: true

must_haves:
  truths:
    - "Not Ready detected within grace window when tenant has no primed data"
    - "Stale poll OIDs cause tier=1 stale followed by tier=4 command dispatch"
    - "All resolved violated produces tier=2 with zero command dispatches"
    - "Partial resolved violation continues past tier=2 to tier=3"
  artifacts:
    - path: "tests/e2e/fixtures/tenant-cfg06-pss-single.yaml"
      provides: "Single-tenant PSS fixture with 1 Evaluate + 2 Resolved metrics from 1s poll group"
      contains: "e2e-pss-tenant"
    - path: "tests/e2e/lib/report.sh"
      provides: "Progressive Snapshot Suite report category"
      contains: "Progressive Snapshot Suite"
    - path: "tests/e2e/scenarios/53-pss-01-not-ready.sh"
      provides: "PSS-01 Not Ready scenario"
      contains: "record_pass.*PSS-01"
    - path: "tests/e2e/scenarios/54-pss-02-stale-to-commands.sh"
      provides: "PSS-02 Stale to Commands scenario"
      contains: "record_pass.*PSS-02"
    - path: "tests/e2e/scenarios/55-pss-03-resolved.sh"
      provides: "PSS-03 Resolved gate scenario with all-violated and partial-violated sub-assertions"
      contains: "record_pass.*PSS-03"
  key_links:
    - from: "tests/e2e/scenarios/53-pss-01-not-ready.sh"
      to: "tests/e2e/fixtures/tenant-cfg06-pss-single.yaml"
      via: "kubectl apply"
      pattern: "kubectl apply -f.*tenant-cfg06-pss-single"
    - from: "tests/e2e/scenarios/54-pss-02-stale-to-commands.sh"
      to: "sim.sh"
      via: "sim_set_oid_stale for T2 OIDs"
      pattern: "sim_set_oid_stale.*5\\."
    - from: "tests/e2e/lib/report.sh"
      to: "_REPORT_CATEGORIES array"
      via: "category range covering scenarios 53+"
      pattern: "Progressive Snapshot Suite"
---

<objective>
Create the PSS single-tenant fixture, update report categories, and implement the first three PSS scenario scripts covering Not Ready, Stale-to-Commands, and Resolved gate evaluation.

Purpose: Establishes the PSS fixture and infrastructure (report category), then verifies the first three SnapshotJob evaluation outcomes (pre-tier readiness, tier-1 staleness, tier-2 resolved gate) with a simple single-tenant setup using T2 OIDs from the 1s poll group.

Output: 1 fixture YAML, 1 updated report.sh, 3 scenario scripts (53-55)
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-single-tenant-evaluation-states/62-RESEARCH.md

@tests/e2e/fixtures/tenant-cfg05-four-tenant-snapshot.yaml
@tests/e2e/scenarios/41-sns-01-not-ready.sh
@tests/e2e/scenarios/42-sns-02-stale-to-commands.sh
@tests/e2e/scenarios/43-sns-03-resolved.sh
@tests/e2e/lib/report.sh
@tests/e2e/lib/sim.sh
@tests/e2e/lib/kubectl.sh
@tests/e2e/lib/prometheus.sh
@tests/e2e/lib/common.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PSS fixture and update report category</name>
  <files>
    tests/e2e/fixtures/tenant-cfg06-pss-single.yaml
    tests/e2e/lib/report.sh
  </files>
  <action>
Create `tests/e2e/fixtures/tenant-cfg06-pss-single.yaml` -- a single-tenant ConfigMap fixture:

```yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: simetra-tenants
  namespace: simetra
data:
  tenants.json: |
    [
      {
        "Name": "e2e-pss-tenant",
        "Priority": 1,
        "SuppressionWindowSeconds": 10,
        "Metrics": [
          {
            "Ip": "e2e-simulator.simetra.svc.cluster.local",
            "Port": 161,
            "MetricName": "e2e_eval_T2",
            "TimeSeriesSize": 3,
            "GraceMultiplier": 2.0,
            "Role": "Evaluate",
            "Threshold": { "Min": 10.0 }
          },
          {
            "Ip": "e2e-simulator.simetra.svc.cluster.local",
            "Port": 161,
            "MetricName": "e2e_res1_T2",
            "Role": "Resolved",
            "Threshold": { "Min": 1.0 }
          },
          {
            "Ip": "e2e-simulator.simetra.svc.cluster.local",
            "Port": 161,
            "MetricName": "e2e_res2_T2",
            "Role": "Resolved",
            "Threshold": { "Min": 1.0 }
          }
        ],
        "Commands": [
          {
            "Ip": "e2e-simulator.simetra.svc.cluster.local",
            "Port": 161,
            "CommandName": "e2e_set_bypass",
            "Value": "0",
            "ValueType": "Integer32"
          }
        ]
      }
    ]
```

Key design points:
- Uses T2 OIDs (.999.5.x) which are in the 1s poll group -- this gives 6s grace window (3 * 1 * 2.0)
- Single tenant "e2e-pss-tenant" at Priority=1, SuppressionWindowSeconds=10
- 1 Evaluate metric (e2e_eval_T2, Min:10), 2 Resolved metrics (e2e_res1_T2/e2e_res2_T2, Min:1)
- 1 Command (e2e_set_bypass) for tier=4 dispatch testing
- Resolved metrics do NOT specify TimeSeriesSize/GraceMultiplier (they inherit from holder defaults)

Update `tests/e2e/lib/report.sh` to add the PSS category. Add a new entry to the `_REPORT_CATEGORIES` array:

```bash
"Progressive Snapshot Suite|52|57"
```

This covers scenarios 53-58 (0-based indices 52-57). Insert it after the existing "Snapshot State Suite|40|51" entry.
  </action>
  <verify>
Verify fixture YAML is valid: `python -c "import yaml; yaml.safe_load(open('tests/e2e/fixtures/tenant-cfg06-pss-single.yaml'))"` (or manual inspection).
Verify report.sh has 7 categories and the new entry covers indices 52-57.
  </verify>
  <done>
Fixture file exists with single tenant "e2e-pss-tenant" using T2 OIDs from 1s poll group. Report.sh has "Progressive Snapshot Suite" category covering the correct index range.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create PSS scenarios 53-55 (Not Ready, Stale, Resolved)</name>
  <files>
    tests/e2e/scenarios/53-pss-01-not-ready.sh
    tests/e2e/scenarios/54-pss-02-stale-to-commands.sh
    tests/e2e/scenarios/55-pss-03-resolved.sh
  </files>
  <action>
Create three scenario scripts following the exact v2.1 SNS pattern (see @41-sns-01-not-ready.sh, @42-sns-02-stale-to-commands.sh, @43-sns-03-resolved.sh as templates). The PSS versions are simpler because they use a 1-tenant fixture (no need to prime 4 tenants).

**53-pss-01-not-ready.sh** (maps to PSS-01, Success Criteria 1):
- Header: Scenario 53, PSS-01 Not Ready, uses tenant-cfg06-pss-single.yaml
- Timing math in header: Grace = 3 * 1 * 2.0 = 6s, SnapshotJob at ~1s cycle
- Setup: save_configmap, kubectl apply cfg06, poll_until_log for reload
- DO NOT prime (no sim_set_oid calls -- holders must remain empty)
- Assert PSS-01A: poll_until_log 15 1 "e2e-pss-tenant.*not ready" 15
- Cleanup: reset_oid_overrides, restore_configmap
- Use "PSS-01" prefix for all log_info and record_pass/fail calls
- OID suffixes: NOT used (no priming in this scenario)

**54-pss-02-stale-to-commands.sh** (maps to PSS-02, Success Criteria 2):
- Header: Scenario 54, PSS-02 Stale to Commands, timing math for staleness age-out
- Setup: save_configmap, kubectl apply cfg06, poll_until_log for reload
- Prime ONLY the 3 T2 OIDs (not 12 like SNS-02):
  - sim_set_oid "5.1" "10" (eval in-range)
  - sim_set_oid "5.2" "1" (res1 in-range)
  - sim_set_oid "5.3" "1" (res2 in-range)
- Sleep 8s (grace=6s + 2s margin)
- Baseline: BEFORE_SENT = snapshot_counter "snmp_command_sent_total" 'device_name="E2E-SIM"'
- Switch to stale: sim_set_oid_stale "5.1", "5.2", "5.3"
- Sleep 5s for staleness age-out (newest sample must be >2s old; 5s gives margin)
- Assert PSS-02A: poll_until_log 30 1 "e2e-pss-tenant.*tier=1 stale" 15
- Assert PSS-02B: poll_until_log 30 1 "e2e-pss-tenant.*tier=4 — commands enqueued\|e2e-pss-tenant.*tier=4 -- commands enqueued" 15
- Assert PSS-02C: poll_until 30 2 "snmp_command_sent_total" 'device_name="E2E-SIM"' "$BEFORE_SENT" (counter increment)
- Cleanup: reset_oid_overrides, restore_configmap

**55-pss-03-resolved.sh** (maps to PSS-06/07, Success Criteria 3):
- Header: Scenario 55, PSS-03 Resolved Gate, with all-violated and partial-violated sub-assertions
- Setup: save_configmap, kubectl apply cfg06, poll_until_log for reload
- Prime 3 T2 OIDs in-range, sleep 8s
- Baseline: BEFORE_SENT for command counter
- **PSS-03A (all resolved violated):** sim_set_oid "5.2" "0" and "5.3" "0" (both < Min:1)
  - poll_until_log 30 1 "e2e-pss-tenant.*tier=2" 15
- **PSS-03B (no commands at tier=2):** sleep 10, compare counter, delta must be 0
- **PSS-03C (partial resolved):** reset_oid_overrides, re-prime all 3 OIDs, sleep 3s flush, then sim_set_oid "5.2" "0" (only res1 violated, res2 stays 1)
  - poll_until_log 30 1 "e2e-pss-tenant.*tier=3" 10
  - Negative check: grep for tier=2 in --since=10s window, must be absent
  - Use the same pod-loop pattern as SNS-03C (kubectl logs per pod, check for tier=2)
- Cleanup: reset_oid_overrides, restore_configmap

CRITICAL implementation notes:
- All scenarios use "PSS-XX" prefix (not "SNS-XX")
- All scenarios reference tenant-cfg06-pss-single.yaml (not cfg05)
- All priming uses OID suffixes 5.1, 5.2, 5.3 (T2 OIDs in 1s poll group)
- Tenant name in log grep patterns is "e2e-pss-tenant" (not "e2e-tenant-G1-T1")
- Every scenario starts with FIXTURES_DIR= line and ends with cleanup block
- Use em-dash/double-dash alternation in tier=4 grep patterns: "tier=4 — commands enqueued\|tier=4 -- commands enqueued"
- Each sub-assertion uses record_pass/record_fail with descriptive name and evidence string
  </action>
  <verify>
Run `bash -n tests/e2e/scenarios/53-pss-01-not-ready.sh && bash -n tests/e2e/scenarios/54-pss-02-stale-to-commands.sh && bash -n tests/e2e/scenarios/55-pss-03-resolved.sh` to verify no syntax errors.
Verify each script contains: FIXTURES_DIR, save_configmap, kubectl apply cfg06, record_pass/record_fail, reset_oid_overrides, restore_configmap.
  </verify>
  <done>
Three scenario scripts (53-55) exist, pass bash -n syntax check, follow the established SNS pattern but use the PSS single-tenant fixture and T2 OID suffixes. Each script has setup, stimulus, assert, and cleanup phases.
  </done>
</task>

</tasks>

<verification>
1. `ls tests/e2e/fixtures/tenant-cfg06-pss-single.yaml` -- fixture exists
2. `grep "Progressive Snapshot Suite" tests/e2e/lib/report.sh` -- category present
3. `ls tests/e2e/scenarios/5[345]-pss-*.sh` -- all three scripts exist
4. `bash -n tests/e2e/scenarios/53-pss-01-not-ready.sh` -- no syntax errors
5. `bash -n tests/e2e/scenarios/54-pss-02-stale-to-commands.sh` -- no syntax errors
6. `bash -n tests/e2e/scenarios/55-pss-03-resolved.sh` -- no syntax errors
7. `grep -c "record_pass\|record_fail" tests/e2e/scenarios/53-pss-01-not-ready.sh` -- at least 1 assertion
8. `grep -c "record_pass\|record_fail" tests/e2e/scenarios/54-pss-02-stale-to-commands.sh` -- at least 3 assertions
9. `grep -c "record_pass\|record_fail" tests/e2e/scenarios/55-pss-03-resolved.sh` -- at least 4 assertions
</verification>

<success_criteria>
- Fixture tenant-cfg06-pss-single.yaml creates single tenant "e2e-pss-tenant" with T2 OIDs from 1s poll group
- report.sh includes "Progressive Snapshot Suite" category covering scenarios 53+
- Scenario 53 asserts Not Ready log within grace window (no priming)
- Scenario 54 asserts tier=1 stale, tier=4 commands, and sent counter increment after stale switch
- Scenario 55 asserts tier=2 all-resolved-violated with zero commands, and partial violation reaching tier=3
- All scripts pass bash -n syntax check
</success_criteria>

<output>
After completion, create `.planning/phases/62-single-tenant-evaluation-states/62-01-SUMMARY.md`
</output>
