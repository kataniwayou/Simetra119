---
phase: 48-snapshotjob-4-tier-evaluation
plan: 03
type: execute
wave: 3
depends_on: ["48-02"]
files_modified:
  - src/SnmpCollector/Jobs/SnapshotJob.cs
  - tests/SnmpCollector.Tests/Jobs/SnapshotJobTests.cs
autonomous: true

must_haves:
  truths:
    - "Tier 3 checks ALL Evaluate holders — if ALL are violated, tenant proceeds to Tier 4"
    - "If ANY Evaluate holder is NOT violated, tenant stops at Tier 3 with no command"
    - "Evaluate holder with null ReadSlot is excluded from the all-violated check"
    - "At Tier 4, each command in tenant.Commands is checked against ISuppressionCache before enqueue"
    - "Suppressed commands increment snmp.command.suppressed counter"
    - "Non-suppressed commands are enqueued via ICommandChannel.Writer.TryWrite"
    - "TryWrite failure increments snmp.command.failed and logs Warning"
    - "CommandRequest.DeviceName is set to tenant.Id"
    - "Command-dispatched outcomes are logged at Information level"
  artifacts:
    - path: "src/SnmpCollector/Jobs/SnapshotJob.cs"
      provides: "Tier 3 Evaluate check + Tier 4 command enqueue with suppression"
      contains: "TrySuppress"
    - path: "tests/SnmpCollector.Tests/Jobs/SnapshotJobTests.cs"
      provides: "Unit tests for Tier 3 and Tier 4 logic"
  key_links:
    - from: "SnapshotJob.cs"
      to: "ISuppressionCache"
      via: "TrySuppress call before each command enqueue"
      pattern: "TrySuppress"
    - from: "SnapshotJob.cs"
      to: "ICommandChannel"
      via: "Writer.TryWrite(CommandRequest)"
      pattern: "TryWrite"
    - from: "SnapshotJob.cs"
      to: "PipelineMetricService"
      via: "IncrementCommandSuppressed, IncrementCommandFailed"
      pattern: "IncrementCommand"
---

<objective>
Implement Tier 3 (Evaluate metrics check) and Tier 4 (command enqueue with suppression) in SnapshotJob with unit tests.

Purpose: Complete the per-tenant evaluation path. After Tier 2 confirms Resolved metrics are violated, Tier 3 checks if all Evaluate metrics are also violated. If so, Tier 4 enqueues SET commands through the suppression cache and command channel. This closes the loop from metric evaluation to command dispatch.

Output: SnapshotJob per-tenant evaluation is complete through all 4 tiers. Unit tests cover Tier 3 gate logic and Tier 4 command enqueue, suppression, and channel-full scenarios.
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-snapshotjob-4-tier-evaluation/48-CONTEXT.md
@.planning/phases/48-snapshotjob-4-tier-evaluation/48-RESEARCH.md
@.planning/phases/48-snapshotjob-4-tier-evaluation/48-02-SUMMARY.md
@src/SnmpCollector/Jobs/SnapshotJob.cs
@src/SnmpCollector/Pipeline/ISuppressionCache.cs
@src/SnmpCollector/Pipeline/ICommandChannel.cs
@src/SnmpCollector/Pipeline/CommandRequest.cs
@src/SnmpCollector/Configuration/CommandSlotOptions.cs
@src/SnmpCollector/Telemetry/PipelineMetricService.cs
@tests/SnmpCollector.Tests/Jobs/SnapshotJobTests.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Tier 3 Evaluate gate and Tier 4 command dispatch</name>
  <files>src/SnmpCollector/Jobs/SnapshotJob.cs</files>
  <action>
Add Tier 3 and Tier 4 logic to the per-tenant evaluation method created in plan 48-02.

**Tier 3 — Evaluate metrics check (private method, e.g., `AreAllEvaluateViolated`):**
- Same structure as `AreAllResolvedViolated` but filters to `Role == "Evaluate"`
- For each Evaluate holder:
  - `ReadSlot()` null → skip (does not participate)
  - Check `IsViolated` (reuse the same helper from 48-02)
  - If NOT violated → return false (not all Evaluate violated → no command)
- After loop: if at least one Evaluate holder with data was checked and ALL violated → return true
- If no Evaluate holders have data → vacuous fail per CONTEXT.md (no Evaluate data = no command)

**Tier 4 — Command dispatch:**
- Iterate `tenant.Commands`
- For each command:
  - Build suppression key: `$"{tenant.Id}:{cmd.Ip}:{cmd.Port}:{cmd.CommandName}"` (include tenant.Id per RESEARCH.md pitfall 2 — isolate tenants)
  - Call `_suppressionCache.TrySuppress(suppressionKey, tenant.SuppressionWindowSeconds)`
  - If suppressed (returns true): `_pipelineMetrics.IncrementCommandSuppressed(tenant.Id)` and continue
  - Build `CommandRequest`: `new CommandRequest(cmd.Ip, cmd.Port, cmd.CommandName, cmd.Value, cmd.ValueType, tenant.Id)` — DeviceName = tenant.Id per locked decision
  - Call `_commandChannel.Writer.TryWrite(request)`
  - If TryWrite returns false: `_logger.LogWarning(...)` + `_pipelineMetrics.IncrementCommandFailed(tenant.Id)`
  - If TryWrite returns true: log at Information level per SNAP-15

**Wire into per-tenant evaluation:**
After Tier 2 passes (all Resolved violated), call Tier 3. If Tier 3 passes (all Evaluate violated), execute Tier 4.

**Logging updates (SNAP-15):**
- Debug for evaluate-healthy stop: `"Tenant {TenantId} priority={Priority} tier=3 — not all evaluate metrics violated, no action"`
- Information for command dispatch: `"Tenant {TenantId} priority={Priority} tier=4 — commands enqueued, count={CommandCount}"`
- Warning for channel full: `"Command channel full, dropping command {CommandName} for tenant {TenantId}"`
- Debug for suppressed: `"Command {CommandName} suppressed for tenant {TenantId}"`
  </action>
  <verify>`dotnet build src/SnmpCollector/` compiles without errors</verify>
  <done>SnapshotJob per-tenant evaluation covers all 4 tiers: staleness -> Resolved gate -> Evaluate gate -> command dispatch with suppression and channel-full handling</done>
</task>

<task type="auto">
  <name>Task 2: Unit tests for Tier 3 and Tier 4</name>
  <files>tests/SnmpCollector.Tests/Jobs/SnapshotJobTests.cs</files>
  <action>
Add tests to the existing SnapshotJobTests.cs covering Tier 3 and Tier 4 scenarios.

**Tier 3 tests:**
1. All Evaluate holders violated → tenant proceeds to Tier 4 (verify command channel receives TryWrite)
2. One Evaluate holder in range → tenant stops at Tier 3 (no TryWrite on channel)
3. Evaluate holder with null ReadSlot → excluded from check (does not prevent commanding)
4. Evaluate holder with no threshold → treated as violated (proceeds to Tier 4)
5. Evaluate holder at exact boundary (value == Max) → NOT violated (in range, stops at Tier 3)

**Tier 4 tests:**
6. Command not suppressed → TryWrite called with correct CommandRequest fields (Ip, Port, CommandName, Value, ValueType, DeviceName=tenant.Id)
7. Command suppressed → TryWrite NOT called, IncrementCommandSuppressed called
8. TryWrite returns false (channel full) → IncrementCommandFailed called, no exception
9. Multiple commands per tenant → each checked against suppression independently
10. Suppression key includes tenant ID → two tenants with same command target get independent suppression

**Reuse existing test doubles from plan 48-02.** Extend StubSuppressionCache and StubCommandChannel as needed to capture and assert on calls.

**Test naming:** `Execute_{Scenario}_{ExpectedOutcome}`
  </action>
  <verify>`dotnet test tests/SnmpCollector.Tests/ --filter "SnapshotJobTests"` — all tests pass (existing + new)</verify>
  <done>10+ new tests covering Tier 3 Evaluate gate edge cases and Tier 4 command dispatch, suppression, and channel-full scenarios</done>
</task>

</tasks>

<verification>
- `dotnet build src/SnmpCollector/` compiles without errors
- `dotnet test tests/SnmpCollector.Tests/` — all tests pass
- `dotnet test tests/SnmpCollector.Tests/ --filter "SnapshotJobTests"` — 20+ tests pass (10 from 48-02 + 10 from 48-03)
- Suppression key includes tenant ID for isolation
- CommandRequest.DeviceName = tenant.Id in all enqueue paths
</verification>

<success_criteria>
- Per-tenant evaluation completes all 4 tiers correctly
- Suppression cache consulted before every command enqueue
- Channel-full path increments failed counter and logs Warning (does not throw)
- Information-level log for command dispatch, Debug-level for non-command outcomes
- 20+ total tests in SnapshotJobTests covering all tier edge cases
</success_criteria>

<output>
After completion, create `.planning/phases/48-snapshotjob-4-tier-evaluation/48-03-SUMMARY.md`
</output>
