---
phase: 48-snapshotjob-4-tier-evaluation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/SnmpCollector/Jobs/SnapshotJob.cs
  - src/SnmpCollector/Extensions/ServiceCollectionExtensions.cs
autonomous: true

must_haves:
  truths:
    - "SnapshotJob exists as a Quartz IJob with [DisallowConcurrentExecution]"
    - "SnapshotJob stamps liveness in finally block with job key from context"
    - "SnapshotJob sets and clears OperationCorrelationId correctly"
    - "Quartz trigger fires SnapshotJob every IntervalSeconds from SnapshotJobOptions"
    - "JobIntervalRegistry has snapshot registered for LivenessHealthCheck staleness detection"
  artifacts:
    - path: "src/SnmpCollector/Jobs/SnapshotJob.cs"
      provides: "SnapshotJob Quartz job skeleton"
      contains: "DisallowConcurrentExecution"
    - path: "src/SnmpCollector/Extensions/ServiceCollectionExtensions.cs"
      provides: "Quartz registration for snapshot job"
      contains: "snapshot"
  key_links:
    - from: "ServiceCollectionExtensions.cs"
      to: "SnapshotJob.cs"
      via: "AddJob<SnapshotJob> in AddSnmpScheduling"
      pattern: "AddJob<SnapshotJob>"
    - from: "ServiceCollectionExtensions.cs"
      to: "JobIntervalRegistry"
      via: "intervalRegistry.Register"
      pattern: 'Register\("snapshot"'
---

<objective>
Create the SnapshotJob skeleton and its Quartz registration.

Purpose: Establish the job shell that all subsequent tier evaluation logic will be added to. The skeleton handles correlation ID scoping, liveness stamping, error handling, and Quartz scheduling — the infrastructure that every job in this codebase requires.

Output: A compilable SnapshotJob.cs with an empty evaluation loop body, registered in AddSnmpScheduling with the correct trigger interval and liveness registration.
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-snapshotjob-4-tier-evaluation/48-CONTEXT.md
@.planning/phases/48-snapshotjob-4-tier-evaluation/48-RESEARCH.md
@src/SnmpCollector/Jobs/HeartbeatJob.cs
@src/SnmpCollector/Extensions/ServiceCollectionExtensions.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SnapshotJob skeleton</name>
  <files>src/SnmpCollector/Jobs/SnapshotJob.cs</files>
  <action>
Create `SnapshotJob.cs` in `src/SnmpCollector/Jobs/` following the HeartbeatJob pattern exactly.

**Class structure:**
- `[DisallowConcurrentExecution]` attribute
- `public sealed class SnapshotJob : IJob`
- Constructor injects: `ITenantVectorRegistry registry`, `ISuppressionCache suppressionCache`, `ICommandChannel commandChannel`, `ICorrelationService correlation`, `ILivenessVectorService liveness`, `PipelineMetricService pipelineMetrics`, `IOptions<SnapshotJobOptions> options`, `ILogger<SnapshotJob> logger`
- Store all as `private readonly` fields

**Execute method:**
- Set `_correlation.OperationCorrelationId = _correlation.CurrentCorrelationId` at entry
- Capture `var jobKey = context.JobDetail.Key.Name`
- try block: iterate `_registry.Groups` with a foreach loop — for now, the loop body is a placeholder comment `// Tier evaluation logic added in plans 48-02 through 48-04`
- catch `OperationCanceledException` and re-throw (let Quartz handle shutdown)
- catch generic `Exception ex` and log `LogError(ex, "Snapshot job {JobKey} failed", jobKey)`
- finally block: `_liveness.Stamp(jobKey)` then `_correlation.OperationCorrelationId = null`

**Important:** Do NOT call `_correlation.SetCorrelationId`. SnapshotJob only reads CurrentCorrelationId and scopes OperationCorrelationId.
  </action>
  <verify>Project compiles: `dotnet build src/SnmpCollector/`</verify>
  <done>SnapshotJob.cs exists with [DisallowConcurrentExecution], correct constructor DI, correlation ID scoping, liveness stamp in finally, and placeholder loop over Groups</done>
</task>

<task type="auto">
  <name>Task 2: Register SnapshotJob in AddSnmpScheduling</name>
  <files>src/SnmpCollector/Extensions/ServiceCollectionExtensions.cs</files>
  <action>
In the `AddSnmpScheduling` method, add SnapshotJob registration following the HeartbeatJob pattern.

**Before the `services.AddQuartz(q => {` call**, add local options bind:
```csharp
var snapshotOptions = new SnapshotJobOptions();
configuration.GetSection(SnapshotJobOptions.SectionName).Bind(snapshotOptions);
```

**Inside the `q => { ... }` lambda**, after the HeartbeatJob registration block (after `intervalRegistry.Register("heartbeat", ...)`), add:
```csharp
// SnapshotJob: evaluates tenant priority groups on a fixed interval.
var snapshotKey = new JobKey("snapshot");
q.AddJob<SnapshotJob>(j => j.WithIdentity(snapshotKey));
q.AddTrigger(t => t
    .ForJob(snapshotKey)
    .WithIdentity("snapshot-trigger")
    .StartNow()
    .WithSimpleSchedule(s => s
        .WithIntervalInSeconds(snapshotOptions.IntervalSeconds)
        .RepeatForever()
        .WithMisfireHandlingInstructionNextWithRemainingCount()));

intervalRegistry.Register("snapshot", snapshotOptions.IntervalSeconds);
```

**Update the initialJobCount** to include SnapshotJob: change `var initialJobCount = 2;` to `var initialJobCount = 3;` and update the comment to `// CorrelationJob + HeartbeatJob + SnapshotJob`.

Add the `using SnmpCollector.Jobs;` if not already present (it should be, since HeartbeatJob is already there — verify).
  </action>
  <verify>Project compiles: `dotnet build src/SnmpCollector/` and all tests pass: `dotnet test tests/SnmpCollector.Tests/`</verify>
  <done>SnapshotJob registered in Quartz with correct trigger interval from SnapshotJobOptions, intervalRegistry has "snapshot" entry, initialJobCount updated to 3</done>
</task>

</tasks>

<verification>
- `dotnet build src/SnmpCollector/` compiles without errors
- `dotnet test tests/SnmpCollector.Tests/` — all existing tests pass (no regressions)
- `grep -n "DisallowConcurrentExecution" src/SnmpCollector/Jobs/SnapshotJob.cs` returns a match
- `grep -n 'Register("snapshot"' src/SnmpCollector/Extensions/ServiceCollectionExtensions.cs` returns a match
</verification>

<success_criteria>
- SnapshotJob.cs exists with all 8 constructor parameters, [DisallowConcurrentExecution], correlation ID lifecycle, and liveness stamp in finally
- AddSnmpScheduling registers SnapshotJob with Quartz trigger using SnapshotJobOptions.IntervalSeconds
- intervalRegistry.Register("snapshot", ...) called so LivenessHealthCheck can detect staleness
- All existing tests remain green
</success_criteria>

<output>
After completion, create `.planning/phases/48-snapshotjob-4-tier-evaluation/48-01-SUMMARY.md`
</output>
