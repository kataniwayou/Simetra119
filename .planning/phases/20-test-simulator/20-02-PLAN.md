---
phase: 20-test-simulator
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - deploy/k8s/simulators/e2e-sim-deployment.yaml
  - deploy/k8s/snmp-collector/simetra-oidmaps.yaml
  - deploy/k8s/snmp-collector/simetra-devices.yaml
autonomous: true

must_haves:
  truths:
    - "E2E simulator pod is running in namespace simetra and responds to SNMP GET requests"
    - "E2E-SIM device entry exists in devices.json ConfigMap with all 7 mapped OIDs polled at 10s interval"
    - "E2E-SIM OID map entries exist in oidmaps.json ConfigMap for all 7 mapped OIDs with e2e_ prefix names"
    - "Unmapped OIDs (.999.2.1.0 and .999.2.2.0) are NOT in oidmaps.json, so collector classifies them as Unknown"
  artifacts:
    - path: "deploy/k8s/simulators/e2e-sim-deployment.yaml"
      provides: "K8s Deployment + ClusterIP Service for e2e-simulator"
      contains: "e2e-simulator"
    - path: "deploy/k8s/snmp-collector/simetra-oidmaps.yaml"
      provides: "OID map entries for 7 E2E-SIM mapped OIDs"
      contains: "e2e_gauge_test"
    - path: "deploy/k8s/snmp-collector/simetra-devices.yaml"
      provides: "E2E-SIM device config with poll OIDs"
      contains: "E2E-SIM"
  key_links:
    - from: "deploy/k8s/simulators/e2e-sim-deployment.yaml"
      to: "e2e-simulator:local"
      via: "image reference"
      pattern: "image: e2e-simulator:local"
    - from: "deploy/k8s/snmp-collector/simetra-devices.yaml"
      to: "deploy/k8s/simulators/e2e-sim-deployment.yaml"
      via: "Service DNS name in IpAddress field"
      pattern: "e2e-simulator.simetra.svc.cluster.local"
    - from: "deploy/k8s/snmp-collector/simetra-oidmaps.yaml"
      to: "simulators/e2e-sim/e2e_simulator.py"
      via: "OID strings match registered OIDs"
      pattern: "47477\\.999\\.1\\.[1-7]\\.0"
---

<objective>
Deploy the E2E test simulator to K8s and configure the snmp-collector to poll it.

Purpose: Makes the E2E simulator a running, always-on device in the simetra namespace that the collector actively polls and receives traps from.
Output: K8s deployment manifest, updated ConfigMaps with E2E-SIM device and OID map entries.
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-test-simulator/20-CONTEXT.md
@.planning/phases/20-test-simulator/20-RESEARCH.md
@.planning/phases/20-test-simulator/20-01-SUMMARY.md
@deploy/k8s/simulators/obp-deployment.yaml
@deploy/k8s/snmp-collector/simetra-oidmaps.yaml
@deploy/k8s/snmp-collector/simetra-devices.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create K8s deployment manifest for e2e-simulator</name>
  <files>deploy/k8s/simulators/e2e-sim-deployment.yaml</files>
  <action>
Create `deploy/k8s/simulators/e2e-sim-deployment.yaml` by copy-adapting from `deploy/k8s/simulators/obp-deployment.yaml`.

**Deployment spec:**
- `metadata.name: e2e-simulator`, `namespace: simetra`, `labels.app: e2e-simulator`
- `replicas: 1`
- Container:
  - `name: e2e-simulator`
  - `image: e2e-simulator:local`
  - `imagePullPolicy: Never`
  - `ports: [{containerPort: 161, name: snmp, protocol: UDP}]`
  - Env vars:
    - `DEVICE_NAME: "E2E-SIM"`
    - `TRAP_TARGET: "simetra-pods.simetra.svc.cluster.local"` (headless service, proven pattern from OBP)
    - `TRAP_PORT: "10162"` (collector listens on 10162, NOT 162)
    - `TRAP_INTERVAL: "30"` (seconds between valid traps)
    - `BAD_TRAP_INTERVAL: "45"` (seconds between bad-community traps)
  - Resources: requests 50m/64Mi, limits 100m/128Mi (matches OBP)
  - **Liveness probe** -- pysnmp GET to localhost:161 for E2E-SIM OID (copy OBP probe, change community to `Simetra.E2E-SIM` and OID to `1.3.6.1.4.1.47477.999.1.1.0`):
    ```
    initialDelaySeconds: 20, periodSeconds: 30, timeoutSeconds: 10, failureThreshold: 3
    ```
  - **Readiness probe** -- same as liveness but with `initialDelaySeconds: 10, periodSeconds: 15, timeoutSeconds: 10, failureThreshold: 2`

**Service spec (in same YAML file, separated by `---`):**
- `metadata.name: e2e-simulator`, `namespace: simetra`, `labels.app: e2e-simulator`
- `selector.app: e2e-simulator`
- `ports: [{name: snmp, port: 161, targetPort: snmp, protocol: UDP}]`
- Type: ClusterIP (default)

**Important:** Follow OBP pattern exactly for probe structure -- inline Python script using pysnmp hlapi asyncio get_cmd.
  </action>
  <verify>
`kubectl apply --dry-run=client -f deploy/k8s/simulators/e2e-sim-deployment.yaml` succeeds without errors.
  </verify>
  <done>
e2e-sim-deployment.yaml contains Deployment (1 replica, e2e-simulator:local image, env vars for trap config, liveness+readiness probes) and ClusterIP Service (port 161 UDP) in namespace simetra.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add E2E-SIM entries to oidmaps and devices ConfigMaps</name>
  <files>deploy/k8s/snmp-collector/simetra-oidmaps.yaml, deploy/k8s/snmp-collector/simetra-devices.yaml</files>
  <action>
**Update simetra-oidmaps.yaml:**

Read the existing file first. Add 7 E2E-SIM mapped OID entries at the END of the existing JSON object (before the closing `}`). Add a blank line comment separator for readability. The 2 unmapped OIDs (.999.2.1.0, .999.2.2.0) must NOT be in this file -- that is the whole point of "unmapped" testing.

Add these entries (after the last npb entry, before closing brace):
```json
      "1.3.6.1.4.1.47477.999.1.1.0": "e2e_gauge_test",
      "1.3.6.1.4.1.47477.999.1.2.0": "e2e_integer_test",
      "1.3.6.1.4.1.47477.999.1.3.0": "e2e_counter32_test",
      "1.3.6.1.4.1.47477.999.1.4.0": "e2e_counter64_test",
      "1.3.6.1.4.1.47477.999.1.5.0": "e2e_timeticks_test",
      "1.3.6.1.4.1.47477.999.1.6.0": "e2e_info_test",
      "1.3.6.1.4.1.47477.999.1.7.0": "e2e_ip_test"
```

Remember to add a comma after the last existing entry (`"npb_port_rx_drops_P8"`) before adding these new entries. Validate the resulting JSON is syntactically correct.

**Update simetra-devices.yaml:**

Read the existing file first. Add an E2E-SIM device entry to the JSON array (after the NPB-01 object, before the closing `]`). Add a comma after the NPB-01 closing brace.

Add this device entry:
```json
      {
        "Name": "E2E-SIM",
        "IpAddress": "e2e-simulator.simetra.svc.cluster.local",
        "Port": 161,
        "MetricPolls": [
          {
            "IntervalSeconds": 10,
            "Oids": [
              "1.3.6.1.4.1.47477.999.1.1.0",
              "1.3.6.1.4.1.47477.999.1.2.0",
              "1.3.6.1.4.1.47477.999.1.3.0",
              "1.3.6.1.4.1.47477.999.1.4.0",
              "1.3.6.1.4.1.47477.999.1.5.0",
              "1.3.6.1.4.1.47477.999.1.6.0",
              "1.3.6.1.4.1.47477.999.1.7.0"
            ]
          }
        ]
      }
```

**Critical checks:**
- OIDs in devices.json must match EXACTLY the OIDs registered in e2e_simulator.py (with .0 suffix)
- OIDs in oidmaps.json must match EXACTLY the mapped OIDs (7 of them, NOT the 2 unmapped ones)
- JSON must be valid after edits -- no trailing commas, no missing commas
- Poll interval is 10s matching OBP-01 and NPB-01
- Community string convention: `Simetra.E2E-SIM` (not in devices.json, derived from device Name by collector)
  </action>
  <verify>
Validate JSON syntax of both ConfigMaps. Use `python -c "import json, yaml; [json.loads(d['data']['devices.json' if 'devices' in f else 'oidmaps.json']) for f in ['devices','oidmaps'] for d in [yaml.safe_load(open(f'deploy/k8s/snmp-collector/simetra-{f}.yaml'))]]"` or manually verify:
1. `kubectl apply --dry-run=client -f deploy/k8s/snmp-collector/simetra-oidmaps.yaml` succeeds
2. `kubectl apply --dry-run=client -f deploy/k8s/snmp-collector/simetra-devices.yaml` succeeds
3. Count: oidmaps.json has exactly 7 entries containing "e2e_" prefix
4. Count: devices.json has E2E-SIM entry with exactly 7 OIDs in Oids array
5. No entries for .999.2.1.0 or .999.2.2.0 in oidmaps.json (these are unmapped)
  </verify>
  <done>
simetra-oidmaps.yaml contains 7 new e2e_ entries for mapped OIDs. simetra-devices.yaml contains E2E-SIM device with 7 poll OIDs at 10s interval. Unmapped OIDs are NOT in oidmaps. JSON is valid in both files.
  </done>
</task>

</tasks>

<verification>
- `kubectl apply --dry-run=client -f deploy/k8s/simulators/e2e-sim-deployment.yaml` passes
- `kubectl apply --dry-run=client -f deploy/k8s/snmp-collector/simetra-oidmaps.yaml` passes
- `kubectl apply --dry-run=client -f deploy/k8s/snmp-collector/simetra-devices.yaml` passes
- OID strings in devices.json, oidmaps.json, and e2e_simulator.py are consistent
- Unmapped OIDs (.999.2.1.0, .999.2.2.0) appear only in e2e_simulator.py, NOT in oidmaps.json
- Trap target uses simetra-pods.simetra.svc.cluster.local:10162 (matches OBP proven pattern)
</verification>

<success_criteria>
- e2e-sim-deployment.yaml deploys a single-replica pod with health probes, matching OBP pattern
- E2E-SIM device entry in devices.json polls all 7 mapped OIDs at 10s
- 7 oidmap entries with e2e_ prefix names exist in oidmaps.json
- 2 unmapped OIDs are deliberately absent from oidmaps.json
- All ConfigMap YAML files pass dry-run validation
</success_criteria>

<output>
After completion, create `.planning/phases/20-test-simulator/20-02-SUMMARY.md`
</output>
