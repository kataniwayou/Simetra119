---
phase: 01-infrastructure-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/SnmpCollector/Pipeline/ICorrelationService.cs
  - src/SnmpCollector/Pipeline/RotatingCorrelationService.cs
  - src/SnmpCollector/Jobs/CorrelationJob.cs
  - src/SnmpCollector/Telemetry/TelemetryConstants.cs
  - src/SnmpCollector/Telemetry/SnmpConsoleFormatter.cs
  - src/SnmpCollector/Telemetry/SnmpLogEnrichmentProcessor.cs
autonomous: true

must_haves:
  truths:
    - "ICorrelationService interface defines CurrentCorrelationId, OperationCorrelationId, and SetCorrelationId with correct signatures"
    - "RotatingCorrelationService uses volatile string for lock-free reads and static AsyncLocal for operation scoping"
    - "SnmpConsoleFormatter outputs lines in format: {timestamp} [{level}] [{site}|{role}|{globalId}|{operationId}] {category} {message}"
    - "SnmpConsoleFormatter shows both global and operation correlation IDs (not either/or like Simetra)"
    - "SnmpLogEnrichmentProcessor adds site_name, role, and correlationId attributes to every LogRecord"
    - "CorrelationJob rotates correlation ID without ILivenessVectorService dependency"
  artifacts:
    - path: "src/SnmpCollector/Pipeline/ICorrelationService.cs"
      provides: "Correlation service contract"
      contains: "interface ICorrelationService"
    - path: "src/SnmpCollector/Pipeline/RotatingCorrelationService.cs"
      provides: "Lock-free correlation service implementation"
      contains: "volatile string _correlationId"
    - path: "src/SnmpCollector/Telemetry/SnmpConsoleFormatter.cs"
      provides: "Custom console formatter with dual correlation ID display"
      contains: "class SnmpConsoleFormatter"
    - path: "src/SnmpCollector/Telemetry/SnmpLogEnrichmentProcessor.cs"
      provides: "OTLP log enrichment with site/role/correlationId"
      contains: "class SnmpLogEnrichmentProcessor"
    - path: "src/SnmpCollector/Jobs/CorrelationJob.cs"
      provides: "Quartz job for correlation ID rotation"
      contains: "class CorrelationJob"
    - path: "src/SnmpCollector/Telemetry/TelemetryConstants.cs"
      provides: "Meter name constant"
      contains: "MeterName"
  key_links:
    - from: "src/SnmpCollector/Telemetry/SnmpConsoleFormatter.cs"
      to: "src/SnmpCollector/Pipeline/ICorrelationService.cs"
      via: "Lazy DI resolution through PostConfigureOptions ServiceProvider"
      pattern: "GetService<ICorrelationService>"
    - from: "src/SnmpCollector/Telemetry/SnmpLogEnrichmentProcessor.cs"
      to: "src/SnmpCollector/Pipeline/ICorrelationService.cs"
      via: "Constructor injection"
      pattern: "ICorrelationService correlationService"
    - from: "src/SnmpCollector/Jobs/CorrelationJob.cs"
      to: "src/SnmpCollector/Pipeline/ICorrelationService.cs"
      via: "Constructor injection, calls SetCorrelationId"
      pattern: "_correlation.SetCorrelationId"
---

<objective>
Create the correlation service, console formatter, log enrichment processor, and correlation job -- all the telemetry and pipeline classes that the DI extension methods (Plan 01-04) will wire together.

Purpose: These are the building blocks that enable LOG-01 through LOG-05. The formatter produces human-readable console output, the enrichment processor tags OTLP logs, and the correlation service provides the rotating ID that ties them together.
Output: Six source files implementing correlation management, console formatting, and OTLP log enrichment.
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-foundation/01-RESEARCH.md
@.planning/phases/01-infrastructure-foundation/01-CONTEXT.md
@.planning/phases/01-infrastructure-foundation/01-01-SUMMARY.md

Reference files (copy from Simetra, modify as described):
@src/Simetra/Pipeline/ICorrelationService.cs
@src/Simetra/Pipeline/RotatingCorrelationService.cs
@src/Simetra/Jobs/CorrelationJob.cs
@src/Simetra/Telemetry/TelemetryConstants.cs
@src/Simetra/Telemetry/SimetraConsoleFormatter.cs
@src/Simetra/Telemetry/SimetraLogEnrichmentProcessor.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create correlation service and CorrelationJob</name>
  <files>
    src/SnmpCollector/Pipeline/ICorrelationService.cs
    src/SnmpCollector/Pipeline/RotatingCorrelationService.cs
    src/SnmpCollector/Jobs/CorrelationJob.cs
    src/SnmpCollector/Telemetry/TelemetryConstants.cs
  </files>
  <action>
**ICorrelationService.cs** -- Copy verbatim from `src/Simetra/Pipeline/ICorrelationService.cs`. Only change: namespace to `SnmpCollector.Pipeline`. Keep all three members: `CurrentCorrelationId`, `OperationCorrelationId`, `SetCorrelationId`.

**RotatingCorrelationService.cs** -- Copy verbatim from `src/Simetra/Pipeline/RotatingCorrelationService.cs`. Only change: namespace to `SnmpCollector.Pipeline`. CRITICAL: Keep `_operationCorrelationId` as `static readonly AsyncLocal<string?>` -- it MUST be static for AsyncLocal to flow correctly through async execution context.

**CorrelationJob.cs** -- Copy from `src/Simetra/Jobs/CorrelationJob.cs` with these changes:
- Namespace: `SnmpCollector.Jobs`
- REMOVE the `ILivenessVectorService` dependency entirely (constructor parameter, field, and `_liveness.Stamp(jobKey)` call in finally block). Phase 1 has no liveness vector.
- Keep `[DisallowConcurrentExecution]` attribute
- Keep the try/catch structure with `_logger.LogError` on failure
- Keep `Guid.NewGuid().ToString("N")` for correlation ID format
- The `finally` block can be removed entirely since it only contained the liveness stamp

**TelemetryConstants.cs** -- Create new (simplified from Simetra):
```csharp
namespace SnmpCollector.Telemetry;

public static class TelemetryConstants
{
    /// <summary>
    /// Primary meter for all SNMP collector metrics.
    /// Phase 7 will split into leader-gated and instance meters if needed.
    /// </summary>
    public const string MeterName = "SnmpCollector";
}
```

This is simpler than Simetra's split (LeaderMeterName/InstanceMeterName) because Phase 1 has no leader election.
  </action>
  <verify>Run `dotnet build src/SnmpCollector/SnmpCollector.csproj` -- must compile with zero errors. Verify CorrelationJob has NO ILivenessVectorService dependency. Verify RotatingCorrelationService._operationCorrelationId is `static`.</verify>
  <done>ICorrelationService and RotatingCorrelationService exist with correct lock-free pattern. CorrelationJob exists without liveness dependency. TelemetryConstants has single MeterName.</done>
</task>

<task type="auto">
  <name>Task 2: Create console formatter and log enrichment processor</name>
  <files>
    src/SnmpCollector/Telemetry/SnmpConsoleFormatter.cs
    src/SnmpCollector/Telemetry/SnmpLogEnrichmentProcessor.cs
  </files>
  <action>
**SnmpConsoleFormatter.cs** -- Copy from `src/Simetra/Telemetry/SimetraConsoleFormatter.cs` with these changes:
- Namespace: `SnmpCollector.Telemetry`
- Class rename: `SimetraConsoleFormatter` -> `SnmpConsoleFormatter`
- Options class rename: `SimetraConsoleFormatterOptions` -> `SnmpConsoleFormatterOptions`
- PostConfigure class rename: `PostConfigureSimetraFormatterOptions` -> `PostConfigureSnmpFormatterOptions`
- FormatterName constant: change from `"simetra"` to `"snmp"`
- REMOVE `ILeaderElection` lazy resolution (field, resolution line in `EnsureServicesResolved`, and usage)
- REPLACE role resolution: Instead of `_leaderElection?.CurrentRole`, resolve `IOptions<SiteOptions>` and use `.Value.Role` for the role string. The SiteOptions is already lazily resolved; just read the Role property from it.
- **DUAL CORRELATION ID DISPLAY** (key difference from Simetra): Simetra shows EITHER operationId OR globalId. This project shows BOTH:
  ```csharp
  var globalId = _correlationService?.CurrentCorrelationId ?? "none";
  var operationId = _correlationService?.OperationCorrelationId;

  textWriter.Write(timestamp);
  textWriter.Write(" [");
  textWriter.Write(level);
  textWriter.Write("] [");
  textWriter.Write(site);
  textWriter.Write('|');
  textWriter.Write(role);    // from SiteOptions.Role, not ILeaderElection
  textWriter.Write('|');
  textWriter.Write(globalId);
  if (operationId is not null)
  {
      textWriter.Write('|');
      textWriter.Write(operationId);
  }
  textWriter.Write("] ");
  textWriter.Write(category);
  textWriter.Write(' ');
  textWriter.WriteLine(message);
  ```
- Keep Simetra's timestamp format: `yyyy-MM-ddTHH:mm:ssZ` (ISO 8601 without millis)
- Keep Simetra's 3-char level abbreviations: TRC/DBG/INF/WRN/ERR/CRT
- Keep the `EnsureServicesResolved` lazy resolution pattern with `_servicesResolved` flag
- Keep exception output after the log line
- The `SnmpConsoleFormatterOptions` class keeps `ServiceProvider` property
- The `PostConfigureSnmpFormatterOptions` class is identical to Simetra's (just rename)
- All three classes (Options, Formatter, PostConfigure) can live in the same file

**SnmpLogEnrichmentProcessor.cs** -- Copy from `src/Simetra/Telemetry/SimetraLogEnrichmentProcessor.cs` with these changes:
- Namespace: `SnmpCollector.Telemetry`
- Class rename: `SimetraLogEnrichmentProcessor` -> `SnmpLogEnrichmentProcessor`
- REPLACE `Func<string> roleProvider` constructor parameter with `string role` (static string in Phase 1 -- no dynamic leader election role). Change the attribute line from `_roleProvider()` to `_role`.
- Keep the null-check on `data.Attributes` (`?.ToList() ?? new List<...>(3)`)
- Keep all three attributes: `site_name`, `role`, `correlationId`
- Keep the `OperationCorrelationId ?? CurrentCorrelationId` fallback for correlationId

IMPORTANT: Both files require `using` statements for `Microsoft.Extensions.Logging.Console`, `Microsoft.Extensions.Options`, `OpenTelemetry.Logs`, and project-internal namespaces (`SnmpCollector.Pipeline`).
  </action>
  <verify>Run `dotnet build src/SnmpCollector/SnmpCollector.csproj` -- must compile with zero errors. Verify SnmpConsoleFormatter.Write method shows both globalId and operationId. Verify SnmpLogEnrichmentProcessor constructor takes `string role` (not `Func<string>`).</verify>
  <done>SnmpConsoleFormatter shows dual correlation IDs with SiteOptions.Role for role string. SnmpLogEnrichmentProcessor enriches with static role string. Both compile and follow Simetra patterns with Phase 1 modifications.</done>
</task>

</tasks>

<verification>
1. `dotnet build src/SnmpCollector/SnmpCollector.csproj` compiles with zero errors
2. ICorrelationService has three members matching Simetra's contract
3. RotatingCorrelationService._operationCorrelationId is static AsyncLocal
4. CorrelationJob has NO ILivenessVectorService dependency
5. Console formatter output format: `{timestamp} [{level}] [{site}|{role}|{globalId}|{operationId}] {category} {message}`
6. Formatter resolves role from SiteOptions.Role (not ILeaderElection)
7. Enrichment processor takes string role (not Func<string>)
8. TelemetryConstants has single MeterName = "SnmpCollector"
</verification>

<success_criteria>
All six source files exist and compile. The formatter shows dual correlation IDs. The enrichment processor uses a static role string. No ILeaderElection or ILivenessVectorService dependencies exist in any Phase 1 code.
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-foundation/01-03-SUMMARY.md`
</output>
