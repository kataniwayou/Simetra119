---
phase: 01-infrastructure-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/SnmpCollector/SnmpCollector.csproj
  - src/SnmpCollector/GlobalUsings.cs
  - src/SnmpCollector/Configuration/SiteOptions.cs
  - src/SnmpCollector/Configuration/OtlpOptions.cs
  - src/SnmpCollector/Configuration/LoggingOptions.cs
  - src/SnmpCollector/Configuration/CorrelationJobOptions.cs
  - src/SnmpCollector/appsettings.json
  - src/SnmpCollector/appsettings.Development.json
  - src/SnmpCollector/appsettings.Production.json
autonomous: true

must_haves:
  truths:
    - "The project builds with `dotnet build` targeting net9.0 with all four NuGet packages restored"
    - "appsettings.json contains all config sections (Site, Otlp, Logging, CorrelationJob, Devices, OidMap, SnmpListener) with valid placeholder values"
    - "appsettings.Development.json overrides EnableConsole to true and Otlp:Endpoint to localhost:4317"
    - "All four configuration options classes have correct SectionName constants, data annotations, and default values"
  artifacts:
    - path: "src/SnmpCollector/SnmpCollector.csproj"
      provides: "Project file targeting net9.0 with OTel 1.15.0 and Quartz 3.15.1 packages"
      contains: "net9.0"
    - path: "src/SnmpCollector/Configuration/SiteOptions.cs"
      provides: "Site configuration with required Name and default Role"
      contains: "class SiteOptions"
    - path: "src/SnmpCollector/Configuration/OtlpOptions.cs"
      provides: "OTLP endpoint and service name configuration"
      contains: "class OtlpOptions"
    - path: "src/SnmpCollector/Configuration/LoggingOptions.cs"
      provides: "Console toggle configuration"
      contains: "class LoggingOptions"
    - path: "src/SnmpCollector/Configuration/CorrelationJobOptions.cs"
      provides: "Correlation job interval configuration"
      contains: "class CorrelationJobOptions"
    - path: "src/SnmpCollector/appsettings.json"
      provides: "Full config skeleton"
      contains: "Site"
  key_links:
    - from: "src/SnmpCollector/Configuration/SiteOptions.cs"
      to: "appsettings.json Site section"
      via: "SectionName constant matches JSON key"
      pattern: "SectionName = \"Site\""
    - from: "src/SnmpCollector/Configuration/OtlpOptions.cs"
      to: "appsettings.json Otlp section"
      via: "SectionName constant matches JSON key"
      pattern: "SectionName = \"Otlp\""
---

<objective>
Create the .NET 9 Generic Host project scaffold with all NuGet package references, configuration options classes, appsettings files, and GlobalUsings. This establishes the compilable project shell and typed configuration model that every subsequent plan builds into.

Purpose: Every other Phase 1 plan depends on this project existing and compiling. The config options classes define the binding contract between appsettings JSON and C# code.
Output: A buildable .NET 9 project with four config classes and three appsettings files.
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-foundation/01-RESEARCH.md
@.planning/phases/01-infrastructure-foundation/01-CONTEXT.md

Reference files (copy patterns from Simetra, adjust namespace to SnmpCollector):
@src/Simetra/Simetra.csproj
@src/Simetra/GlobalUsings.cs
@src/Simetra/Configuration/SiteOptions.cs
@src/Simetra/Configuration/OtlpOptions.cs
@src/Simetra/Configuration/LoggingOptions.cs
@src/Simetra/Configuration/CorrelationJobOptions.cs
@src/Simetra/appsettings.json
@src/Simetra/appsettings.Development.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create project file and GlobalUsings</name>
  <files>
    src/SnmpCollector/SnmpCollector.csproj
    src/SnmpCollector/GlobalUsings.cs
  </files>
  <action>
Create `src/SnmpCollector/SnmpCollector.csproj` using `Microsoft.NET.Sdk` (NOT `Microsoft.NET.Sdk.Web` -- this is a Generic Host, not a Web Host). Target `net9.0`, enable nullable and implicit usings. Set `OutputType` to `Exe` and `RootNamespace` to `SnmpCollector`.

Add exactly these PackageReferences (all versions pinned -- do NOT allow NuGet to resolve different versions):
- `OpenTelemetry.Exporter.OpenTelemetryProtocol` Version="1.15.0"
- `OpenTelemetry.Extensions.Hosting` Version="1.15.0"
- `OpenTelemetry.Instrumentation.Runtime` Version="1.15.0"
- `Quartz.Extensions.Hosting` Version="3.15.1"

IMPORTANT: Use `Quartz.Extensions.Hosting`, NOT `Quartz.AspNetCore`. The project is a Generic Host.

Create `src/SnmpCollector/GlobalUsings.cs` with a single global using for the Configuration namespace:
```csharp
global using SnmpCollector.Configuration;
```

Reference: See `src/Simetra/Simetra.csproj` for structure but use `Microsoft.NET.Sdk` instead of `Microsoft.NET.Sdk.Web`.
  </action>
  <verify>Run `dotnet restore src/SnmpCollector/SnmpCollector.csproj` -- must succeed with all packages restored.</verify>
  <done>Project file exists with correct SDK, target framework, and all four packages pinned at exact versions. GlobalUsings.cs exists with SnmpCollector.Configuration global using.</done>
</task>

<task type="auto">
  <name>Task 2: Create configuration options classes</name>
  <files>
    src/SnmpCollector/Configuration/SiteOptions.cs
    src/SnmpCollector/Configuration/OtlpOptions.cs
    src/SnmpCollector/Configuration/LoggingOptions.cs
    src/SnmpCollector/Configuration/CorrelationJobOptions.cs
  </files>
  <action>
Create four configuration options classes in `src/SnmpCollector/Configuration/`. Use file-scoped namespaces (`namespace SnmpCollector.Configuration;`). All classes are `public sealed`.

**SiteOptions.cs** -- Copy from `src/Simetra/Configuration/SiteOptions.cs` with these changes:
- Namespace: `SnmpCollector.Configuration`
- ADD a `Role` property: `public string Role { get; set; } = "standalone";` -- this replaces ILeaderElection in Phase 1. The Phase 1 formatter and enrichment processor read this static string instead of a dynamic role.
- REMOVE `PodIdentity` property (Phase 1 does not need it; Phase 7 can add it back)
- Keep `[Required]` on `Name` and `required` keyword

**OtlpOptions.cs** -- Copy from `src/Simetra/Configuration/OtlpOptions.cs` with these changes:
- Namespace: `SnmpCollector.Configuration`
- Change default `ServiceName` from `"simetra-supervisor"` to `"snmp-collector"`

**LoggingOptions.cs** -- Copy verbatim from `src/Simetra/Configuration/LoggingOptions.cs`, change namespace only.

**CorrelationJobOptions.cs** -- Copy verbatim from `src/Simetra/Configuration/CorrelationJobOptions.cs`, change namespace only. Keep `[Range(1, int.MaxValue)]` on `IntervalSeconds` with default `30`.
  </action>
  <verify>Run `dotnet build src/SnmpCollector/SnmpCollector.csproj` -- must compile with zero errors and zero warnings.</verify>
  <done>Four config options classes exist in src/SnmpCollector/Configuration/ with correct annotations, SectionName constants, and namespace. SiteOptions has Role property with default "standalone".</done>
</task>

<task type="auto">
  <name>Task 3: Create appsettings files</name>
  <files>
    src/SnmpCollector/appsettings.json
    src/SnmpCollector/appsettings.Development.json
    src/SnmpCollector/appsettings.Production.json
  </files>
  <action>
Create three appsettings files in `src/SnmpCollector/`.

**appsettings.json** -- Full skeleton with ALL sections present (placeholder values for sections not active in Phase 1). This is the base configuration that matches the CONTEXT.md decision for progressive config validation:
```json
{
  "Site": {
    "Name": "site-nyc-01",
    "Role": "standalone"
  },
  "Otlp": {
    "Endpoint": "http://otel-collector:4317",
    "ServiceName": "snmp-collector"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information"
    },
    "EnableConsole": false
  },
  "CorrelationJob": {
    "IntervalSeconds": 30
  },
  "Devices": [],
  "OidMap": {},
  "SnmpListener": {
    "BindAddress": "0.0.0.0",
    "Port": 162,
    "CommunityString": "public"
  }
}
```

**appsettings.Development.json** -- Safe local dev defaults:
```json
{
  "Logging": {
    "LogLevel": {
      "Default": "Debug"
    },
    "EnableConsole": true
  },
  "Otlp": {
    "Endpoint": "http://localhost:4317"
  }
}
```

**appsettings.Production.json** -- Empty placeholder (K8s ConfigMap provides overrides):
```json
{
}
```

IMPORTANT: `EnableConsole` is `false` in base appsettings.json and `true` in Development.json. This satisfies LOG-03 (console toggleable via config) and ensures production defaults to no console output.
  </action>
  <verify>Verify all three JSON files are valid: `dotnet build src/SnmpCollector/SnmpCollector.csproj` succeeds (invalid JSON would cause build warnings). Open each file and confirm JSON parses correctly.</verify>
  <done>Three appsettings files exist: base with full skeleton, Development with EnableConsole:true and localhost endpoint, Production as empty placeholder.</done>
</task>

</tasks>

<verification>
1. `dotnet build src/SnmpCollector/SnmpCollector.csproj` compiles with zero errors
2. Project targets net9.0 and uses Microsoft.NET.Sdk (not .Web)
3. All four NuGet packages are at pinned versions (1.15.0, 1.15.0, 1.15.0, 3.15.1)
4. Four config classes in Configuration/ folder with correct SectionName constants
5. SiteOptions has Role property defaulting to "standalone"
6. appsettings.json has all sections including future placeholders (Devices, OidMap, SnmpListener)
7. appsettings.Development.json enables console and points to localhost:4317
</verification>

<success_criteria>
`dotnet build src/SnmpCollector/SnmpCollector.csproj` passes. The project is a valid .NET 9 console application with typed configuration ready for DI binding in subsequent plans.
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-foundation/01-01-SUMMARY.md`
</output>
