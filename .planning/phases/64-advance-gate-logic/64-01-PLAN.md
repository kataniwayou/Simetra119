---
phase: 64-advance-gate-logic
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/fixtures/tenant-cfg08-pss-four-tenant.yaml
  - tests/e2e/run-stage3.sh
  - tests/e2e/lib/report.sh
  - tests/e2e/run-all.sh
autonomous: true

must_haves:
  truths:
    - "A 4-tenant PSS fixture exists with G1 (T1+T2 priority=1) gating G2 (T3+T4 priority=2)"
    - "run-stage3.sh gates Stage 3 on Stage 2 FAIL_COUNT and runs scenarios 62-68"
    - "Report category covers all PSS scenarios (indices 52-67)"
    - "run-all.sh prints cross-stage summary (total pass/fail across Stage 1, 2, and 3) at the end"
  artifacts:
    - path: "tests/e2e/fixtures/tenant-cfg08-pss-four-tenant.yaml"
      provides: "4-tenant 2-group PSS fixture"
      contains: "e2e-pss-g1-t1"
    - path: "tests/e2e/run-stage3.sh"
      provides: "Stage 3 runner with 3-stage gating"
      contains: "Stage 3"
    - path: "tests/e2e/lib/report.sh"
      provides: "Updated PSS report category range"
      contains: "52|67"
    - path: "tests/e2e/run-all.sh"
      provides: "Cross-stage PSS summary at end"
      contains: "Stage 1"
  key_links:
    - from: "tests/e2e/run-stage3.sh"
      to: "tests/e2e/scenarios/62-*.sh through 68-*.sh"
      via: "explicit source list"
      pattern: "scenarios/6[2-8]"
    - from: "tests/e2e/run-stage3.sh"
      to: "tests/e2e/fixtures/tenant-cfg08-pss-four-tenant.yaml"
      via: "kubectl apply"
      pattern: "tenant-cfg08-pss-four-tenant"
    - from: "tests/e2e/run-all.sh"
      to: "PASS_COUNT/FAIL_COUNT globals"
      via: "cross-stage summary after print_summary"
      pattern: "Stage 1.*Stage 2.*Stage 3"
---

<objective>
Create the 4-tenant PSS fixture, Stage 3 runner script, update report category range, and add cross-stage PSS summary to run-all.sh.

Purpose: Infrastructure required before any Stage 3 advance gate scenarios can run.
Output: tenant-cfg08-pss-four-tenant.yaml, run-stage3.sh, updated report.sh, updated run-all.sh
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-advance-gate-logic/64-RESEARCH.md

Key references (read during execution):
@tests/e2e/fixtures/tenant-cfg05-four-tenant-snapshot.yaml (SNS 4-tenant fixture -- structural template)
@tests/e2e/run-stage2.sh (Stage 2 runner -- direct template for Stage 3)
@tests/e2e/lib/report.sh (report category array to update)
@tests/e2e/run-all.sh (needs cross-stage PSS summary added at the end)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 4-tenant PSS fixture and update report category</name>
  <files>
    tests/e2e/fixtures/tenant-cfg08-pss-four-tenant.yaml
    tests/e2e/lib/report.sh
  </files>
  <action>
Create tests/e2e/fixtures/tenant-cfg08-pss-four-tenant.yaml modeled on tenant-cfg05-four-tenant-snapshot.yaml but with PSS tenant naming convention:

Tenant layout:
- e2e-pss-g1-t1: Priority=1, OIDs .999.4.x (e2e_port_utilization=Evaluate Min:10 TSS:3 GM:2.0, e2e_channel_state=Resolved Min:1, e2e_bypass_status=Resolved Min:1), Command: e2e_set_bypass, SuppressionWindowSeconds=10
- e2e-pss-g1-t2: Priority=1, OIDs .999.5.x (e2e_eval_T2=Evaluate Min:10 TSS:3 GM:2.0, e2e_res1_T2=Resolved Min:1, e2e_res2_T2=Resolved Min:1), Command: e2e_set_bypass, SuppressionWindowSeconds=10
- e2e-pss-g2-t3: Priority=2, OIDs .999.6.x (e2e_eval_T3=Evaluate Min:10 TSS:3 GM:2.0, e2e_res1_T3=Resolved Min:1, e2e_res2_T3=Resolved Min:1), Command: e2e_set_bypass, SuppressionWindowSeconds=10
- e2e-pss-g2-t4: Priority=2, OIDs .999.7.x (e2e_eval_T4=Evaluate Min:10 TSS:3 GM:2.0, e2e_res1_T4=Resolved Min:1, e2e_res2_T4=Resolved Min:1), Command: e2e_set_bypass, SuppressionWindowSeconds=10

All IP addresses: e2e-simulator.simetra.svc.cluster.local port 161. All OIDs are already registered in simetra-oid-metric-map (no OID map changes needed).

Update tests/e2e/lib/report.sh: Change the Progressive Snapshot Suite category from "52|60" to "52|67" (scenarios 53-68 = 0-based indices 52-67).
  </action>
  <verify>
- `grep "e2e-pss-g1-t1" tests/e2e/fixtures/tenant-cfg08-pss-four-tenant.yaml` returns match
- `grep "e2e-pss-g2-t4" tests/e2e/fixtures/tenant-cfg08-pss-four-tenant.yaml` returns match
- `grep "Priority.*1" tests/e2e/fixtures/tenant-cfg08-pss-four-tenant.yaml` returns 2 matches (G1 tenants)
- `grep "Priority.*2" tests/e2e/fixtures/tenant-cfg08-pss-four-tenant.yaml` returns 2 matches (G2 tenants)
- `grep "52|67" tests/e2e/lib/report.sh` returns match
  </verify>
  <done>
4-tenant PSS fixture has G1 (T1+T2 priority=1) and G2 (T3+T4 priority=2) with correct OID mappings and thresholds. Report category covers all 16 PSS scenarios (indices 52-67).
  </done>
</task>

<task type="auto">
  <name>Task 2: Create run-stage3.sh with 3-stage gating and update run-all.sh with cross-stage summary</name>
  <files>
    tests/e2e/run-stage3.sh
    tests/e2e/run-all.sh
  </files>
  <action>
Create tests/e2e/run-stage3.sh modeled directly on run-stage2.sh with these changes:

1. Banner: "PSS Stage 3 Runner"
2. Stage 1 section: Source scenarios 53-58 (identical to run-stage2.sh Stage 1)
3. Stage 1 gate: Check FAIL_COUNT > 0, exit early with report if Stage 1 failed
4. Stage 2 section: Source scenarios 59-61 (identical to run-stage2.sh Stage 2)
5. Stage 2 gate: Check FAIL_COUNT > 0, exit early with report if Stage 2 failed. Log "Stage 2 had $FAIL_COUNT failure(s) -- skipping Stage 3 scenarios"
6. Stage 3 setup: Apply 4-tenant fixture (tenant-cfg08-pss-four-tenant.yaml), wait for tenant vector reload, prime all 4 tenants to healthy (all 12 OIDs set: 4.1=10, 4.2=1, 4.3=1, 5.1=10, 5.2=1, 5.3=1, 6.1=10, 6.2=1, 6.3=1, 7.1=10, 7.2=1, 7.3=1), sleep 8s for readiness grace
7. Stage 3 scenarios: Source scenarios 62-68 with explicit file list (not glob)
8. Cleanup: Reset OID overrides, restore original tenant ConfigMap (save at start of Stage 3 setup, restore in cleanup trap AND after scenarios)
9. Report: Generate to e2e-pss-stage3-report-{timestamp}.md
10. Cross-stage summary: After report generation, print a 3-line summary showing Stage 1 (53-58), Stage 2 (59-61), Stage 3 (62-68) pass/fail counts. Use SCENARIO_RESULTS array indices to compute per-stage counts (Stage 1: indices 0-5, Stage 2: indices 6-8, Stage 3: indices 9-15 within the sourced results).

IMPORTANT: The Stage 3 scenarios do NOT apply/restore fixture themselves -- the runner handles that. Scenarios only manipulate OID values and assert results. This is different from the SNS scenarios (46-52) which each apply/restore their own fixture. The runner saves the original configmap before Stage 3 setup and restores it after all Stage 3 scenarios complete.

Make the file executable (chmod +x).

For the cross-stage summary, keep it simple: After print_summary, add a section that counts results by stage. Use the PASS_COUNT and FAIL_COUNT globals for the total, and note which stages ran.

ALSO update tests/e2e/run-all.sh: After the existing `print_summary` call (line 107), add a cross-stage PSS summary section. The current run-all.sh runs ALL scenarios (1-68) via glob. Add a summary block that counts PSS results by stage:

```
echo ""
echo "============================================="
echo "  Cross-Stage PSS Summary"
echo "============================================="
echo "  Stage 1 (53-58): scenarios $((SCENARIO_RESULTS counted for indices 52-57))"
echo "  Stage 2 (59-61): scenarios $((SCENARIO_RESULTS counted for indices 58-60))"
echo "  Stage 3 (62-68): scenarios $((SCENARIO_RESULTS counted for indices 61-67))"
echo "============================================="
```

Use the SCENARIO_RESULTS array (0-based) to count pass/fail per stage. The pattern: iterate over the index range, count PASS vs FAIL entries. If SCENARIO_RESULTS has fewer entries than expected (e.g., run-all.sh ran fewer scenarios), only print stages that have results. Keep it simple -- just print counts, not individual results.
  </action>
  <verify>
- `bash -n tests/e2e/run-stage3.sh` exits 0 (valid syntax)
- `grep "Stage 1 had.*failure.*skipping Stage 2" tests/e2e/run-stage3.sh` returns match (Stage 1 gate)
- `grep "Stage 2 had.*failure.*skipping Stage 3" tests/e2e/run-stage3.sh` returns match (Stage 2 gate)
- `grep "tenant-cfg08-pss-four-tenant" tests/e2e/run-stage3.sh` returns match (fixture apply)
- `grep "scenarios/62-" tests/e2e/run-stage3.sh` returns match (Stage 3 scenario list)
- `grep "scenarios/68-" tests/e2e/run-stage3.sh` returns match
- `bash -n tests/e2e/run-all.sh` exits 0 (valid syntax after edit)
- `grep "Cross-Stage PSS Summary" tests/e2e/run-all.sh` returns match
- `grep "Stage 1" tests/e2e/run-all.sh` returns match
- `grep "Stage 3" tests/e2e/run-all.sh` returns match
  </verify>
  <done>
run-stage3.sh sources all 3 stages sequentially with FAIL_COUNT gating between each. Stage 3 applies 4-tenant fixture, primes all tenants to healthy, runs scenarios 62-68, restores original configmap. Cross-stage summary prints at end of both run-stage3.sh AND run-all.sh.
  </done>
</task>

</tasks>

<verification>
- Fixture YAML is valid (4 tenants, correct priorities, correct OID/metric mappings)
- run-stage3.sh has valid bash syntax
- Report category range covers scenarios 53-68
- Stage gating logic mirrors run-stage2.sh pattern
- run-all.sh has valid bash syntax and prints cross-stage PSS summary after print_summary
</verification>

<success_criteria>
- tenant-cfg08-pss-four-tenant.yaml exists with 4 tenants (G1 priority=1, G2 priority=2)
- run-stage3.sh exists, is executable, and has valid bash syntax
- report.sh PSS category covers indices 52-67
- Runner gates Stage 3 on Stage 2 FAIL_COUNT
- run-all.sh prints cross-stage PSS summary (Stage 1, 2, 3 pass/fail) at the end
</success_criteria>

<output>
After completion, create `.planning/phases/64-advance-gate-logic/64-01-SUMMARY.md`
</output>
