---
phase: 64-advance-gate-logic
plan: 03
type: execute
wave: 2
depends_on: ["64-01"]
files_modified:
  - tests/e2e/scenarios/65-pss-17-all-g1-unresolved.sh
  - tests/e2e/scenarios/66-pss-18-g1-resolved-unresolved.sh
  - tests/e2e/scenarios/67-pss-19-g1-healthy-unresolved.sh
  - tests/e2e/scenarios/68-pss-20-all-g1-not-ready.sh
autonomous: true

must_haves:
  truths:
    - "When all G1 tenants are Unresolved (tier=4), gate blocks and G2 tenants have no tier logs in 15s window"
    - "When G1 has mixed Resolved+Unresolved, gate blocks and G2 is absent"
    - "When G1 has mixed Healthy+Unresolved, gate blocks and G2 is absent"
    - "When all G1 tenants are Not Ready (before grace expires), gate blocks and G2 is absent"
    - "Gate-block scenarios have dual proof: G1 positive assertion THEN G2 absence check (log absence AND metric non-increment)"
  artifacts:
    - path: "tests/e2e/scenarios/65-pss-17-all-g1-unresolved.sh"
      provides: "PSS-17 gate-block scenario"
      contains: "PSS-17"
    - path: "tests/e2e/scenarios/66-pss-18-g1-resolved-unresolved.sh"
      provides: "PSS-18 gate-block scenario"
      contains: "PSS-18"
    - path: "tests/e2e/scenarios/67-pss-19-g1-healthy-unresolved.sh"
      provides: "PSS-19 gate-block scenario"
      contains: "PSS-19"
    - path: "tests/e2e/scenarios/68-pss-20-all-g1-not-ready.sh"
      provides: "PSS-20 gate-block scenario"
      contains: "PSS-20"
  key_links:
    - from: "gate-block scenarios 65-67"
      to: "poll_until_log + kubectl logs absence + snapshot_counter non-increment"
      via: "dual proof pattern (log absence + metric non-increment)"
      pattern: "G2_FOUND|snapshot_counter"
    - from: "tests/e2e/scenarios/68-pss-20-all-g1-not-ready.sh"
      to: "poll_until_log + snapshot_counter"
      via: "not ready assertion (short timeout) + metric non-increment"
      pattern: "not ready|snapshot_counter"
---

<objective>
Create 4 gate-block scenarios (PSS-17, PSS-18, PSS-19, PSS-20) that verify the advance gate blocks G2 evaluation when any G1 tenant is Unresolved or Not Ready.

Purpose: Prove the advance gate correctly prevents G2 evaluation when G1 has blocking states.
Output: 4 scenario scripts (65, 66, 67, 68) ready to run within run-stage3.sh
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/64-advance-gate-logic/64-RESEARCH.md
@.planning/phases/64-advance-gate-logic/64-01-SUMMARY.md

Key references (read during execution):
@tests/e2e/scenarios/49-sns-b1-both-unresolved.sh (gate-block template -- dual proof pattern)
@tests/e2e/scenarios/50-sns-b2-both-not-ready.sh (not-ready template -- short timeout, no priming G1)
@tests/e2e/scenarios/51-sns-b3-resolved-unresolved.sh (mixed block template)
@tests/e2e/scenarios/52-sns-b4-healthy-unresolved.sh (mixed block template)
@tests/e2e/lib/prometheus.sh (snapshot_counter function for metric non-increment assertions)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PSS-17 (all G1 Unresolved) and PSS-18 (G1 mixed Resolved+Unresolved) scenarios</name>
  <files>
    tests/e2e/scenarios/65-pss-17-all-g1-unresolved.sh
    tests/e2e/scenarios/66-pss-18-g1-resolved-unresolved.sh
  </files>
  <action>
IMPORTANT: These PSS scenarios do NOT apply/restore the fixture -- the runner handles that. Each scenario re-primes all 4 tenants to healthy at its start (all 12 OIDs, sleep 8s), then manipulates only the OIDs needed. Gate-block scenarios use DUAL PROOF: (1) G1 positive assertion first, (2) G2 log absence check with --since=15s, AND (3) G2 metric non-increment via snapshot_counter.

The metric non-increment pattern:
- BEFORE the observation window (right after G1 positive assertions confirmed), capture: BEFORE_T3=$(snapshot_counter "snmp_poll_executed_total" 'device_name="e2e-pss-g2-t3"') and BEFORE_T4=$(snapshot_counter "snmp_poll_executed_total" 'device_name="e2e-pss-g2-t4"')
- AFTER the observation window (after sleep 10 + log absence check), capture: AFTER_T3=$(snapshot_counter "snmp_poll_executed_total" 'device_name="e2e-pss-g2-t3"') and AFTER_T4=$(snapshot_counter "snmp_poll_executed_total" 'device_name="e2e-pss-g2-t4"')
- Assert AFTER equals BEFORE (delta == 0) for both T3 and T4. record_pass/record_fail with evidence string.

PSS-17 (65-pss-17-all-g1-unresolved.sh):
- Header comment: "Scenario 65: PSS-17 All G1 Unresolved -- gate blocks, G2 not evaluated"
- Re-prime all 4 tenants to healthy (12 OIDs, sleep 8s)
- Set both G1 eval violated: sim_set_oid "4.1" "0" (T1 eval < Min:10 -> Unresolved), sim_set_oid "5.1" "0" (T2 eval < Min:10 -> Unresolved). Resolved OIDs stay in-range from priming.
- PSS-17a: poll_until_log 30 1 for "e2e-pss-g1-t1.*tier=4 — commands enqueued\|e2e-pss-g1-t1.*tier=4 -- commands enqueued" with --since=15. record_pass/record_fail "PSS-17a: G1-T1 tier=4 Unresolved"
- PSS-17b: poll_until_log 30 1 for "e2e-pss-g1-t2.*tier=4 — commands enqueued\|e2e-pss-g1-t2.*tier=4 -- commands enqueued" with --since=15. record_pass/record_fail "PSS-17b: G1-T2 tier=4 Unresolved"
- Capture G2 BEFORE snapshots (snmp_poll_executed_total for T3 and T4)
- PSS-17c: G2 log absence check. sleep 10. Then for each pod in `kubectl get pods -n simetra -l app=snmp-collector`, check `kubectl logs $POD -n simetra --since=15s | grep "e2e-pss-g2.*tier="`. If ANY G2 tier log found, set G2_FOUND=1. record_pass "PSS-17c: G2 not evaluated -- log absence (gate blocked -- all G1 Unresolved)" if G2_FOUND=0, record_fail otherwise.
- PSS-17d: G2 metric non-increment. Capture AFTER snapshots. DELTA_T3=$((AFTER_T3 - BEFORE_T3)), DELTA_T4=$((AFTER_T4 - BEFORE_T4)). If both deltas == 0, record_pass "PSS-17d: G2 not evaluated -- metric non-increment (T3 delta=$DELTA_T3, T4 delta=$DELTA_T4)". Else record_fail with the delta values.

PSS-18 (66-pss-18-g1-resolved-unresolved.sh):
- Header comment: "Scenario 66: PSS-18 G1 mixed Resolved+Unresolved -- gate blocks, G2 not evaluated"
- Re-prime all 4 tenants to healthy (12 OIDs, sleep 8s)
- Set T1 to Resolved: sim_set_oid "4.2" "0", sim_set_oid "4.3" "0" (res violated, eval stays 10)
- Set T2 to Unresolved: sim_set_oid "5.1" "0" (eval violated, res stays in-range)
- PSS-18a: poll_until_log 30 1 for "e2e-pss-g1-t1.*tier=2" with --since=15. record_pass/record_fail "PSS-18a: G1-T1 tier=2 Resolved"
- PSS-18b: poll_until_log 30 1 for "e2e-pss-g1-t2.*tier=4 — commands enqueued\|e2e-pss-g1-t2.*tier=4 -- commands enqueued" with --since=15. record_pass/record_fail "PSS-18b: G1-T2 tier=4 Unresolved"
- Capture G2 BEFORE snapshots (same pattern as PSS-17)
- PSS-18c: G2 log absence check (same pattern as PSS-17c). sleep 10, check --since=15s, record_pass "PSS-18c: G2 not evaluated -- log absence (gate blocked -- G1 mixed Resolved+Unresolved)" if absent.
- PSS-18d: G2 metric non-increment (same pattern as PSS-17d). record_pass "PSS-18d: G2 not evaluated -- metric non-increment (T3 delta=$DELTA_T3, T4 delta=$DELTA_T4)" if both deltas == 0.
  </action>
  <verify>
- `bash -n tests/e2e/scenarios/65-pss-17-all-g1-unresolved.sh` exits 0
- `bash -n tests/e2e/scenarios/66-pss-18-g1-resolved-unresolved.sh` exits 0
- `grep "tier=4" tests/e2e/scenarios/65-pss-17-all-g1-unresolved.sh` returns matches
- `grep "G2_FOUND" tests/e2e/scenarios/65-pss-17-all-g1-unresolved.sh` returns matches (log absence proof)
- `grep "snapshot_counter" tests/e2e/scenarios/65-pss-17-all-g1-unresolved.sh` returns matches (metric non-increment proof)
- `grep "snapshot_counter" tests/e2e/scenarios/66-pss-18-g1-resolved-unresolved.sh` returns matches (metric non-increment proof)
- `grep "tier=2" tests/e2e/scenarios/66-pss-18-g1-resolved-unresolved.sh` returns match (T1 Resolved positive assertion)
- No "kubectl apply" or "save_configmap" in either file
  </verify>
  <done>
PSS-17 and PSS-18 verify gate blocks with dual proof (G1 positive assertion + G2 log absence + G2 metric non-increment) for all-Unresolved and mixed Resolved+Unresolved G1 states. Each has 4 sub-assertions (a: G1-T1, b: G1-T2, c: G2 log absence, d: G2 metric non-increment).
  </done>
</task>

<task type="auto">
  <name>Task 2: Create PSS-19 (G1 mixed Healthy+Unresolved) and PSS-20 (all G1 Not Ready) scenarios</name>
  <files>
    tests/e2e/scenarios/67-pss-19-g1-healthy-unresolved.sh
    tests/e2e/scenarios/68-pss-20-all-g1-not-ready.sh
  </files>
  <action>
Both scenarios use the same DUAL PROOF pattern: log absence AND metric non-increment for G2 tenants. See Task 1 for the snapshot_counter pattern.

PSS-19 (67-pss-19-g1-healthy-unresolved.sh):
- Header comment: "Scenario 67: PSS-19 G1 mixed Healthy+Unresolved -- gate blocks, G2 not evaluated"
- Re-prime all 4 tenants to healthy (12 OIDs, sleep 8s)
- T1 stays Healthy (all in-range from priming -- no changes)
- Set T2 to Unresolved: sim_set_oid "5.1" "0" (eval violated)
- PSS-19a: poll_until_log 30 1 for "e2e-pss-g1-t1.*tier=3" with --since=15. record_pass/record_fail "PSS-19a: G1-T1 tier=3 Healthy"
- PSS-19b: poll_until_log 30 1 for "e2e-pss-g1-t2.*tier=4 — commands enqueued\|e2e-pss-g1-t2.*tier=4 -- commands enqueued" with --since=15. record_pass/record_fail "PSS-19b: G1-T2 tier=4 Unresolved"
- Capture G2 BEFORE snapshots (snmp_poll_executed_total for T3 and T4)
- PSS-19c: G2 log absence check (same dual proof pattern: sleep 10, --since=15s, G2_FOUND check). record_pass "PSS-19c: G2 not evaluated -- log absence (gate blocked -- G1 mixed Healthy+Unresolved)" if absent.
- PSS-19d: G2 metric non-increment. Capture AFTER snapshots. record_pass "PSS-19d: G2 not evaluated -- metric non-increment (T3 delta=$DELTA_T3, T4 delta=$DELTA_T4)" if both deltas == 0.

PSS-20 (68-pss-20-all-g1-not-ready.sh):
- Header comment: "Scenario 68: PSS-20 All G1 Not Ready -- gate blocks, G2 not evaluated"
- CRITICAL: This scenario is DIFFERENT from PSS-17/18/19. It must NOT re-prime G1 to healthy. Instead:
  1. Reset all OID overrides first: reset_oid_overrides (clears any state from prior scenarios)
  2. Re-apply the 4-tenant fixture to force fresh tenant vector reload (new holders with empty series): kubectl apply -f "$FIXTURES_DIR/tenant-cfg08-pss-four-tenant.yaml" > /dev/null 2>&1 || true
  3. Wait for tenant vector reload: poll_until_log 60 5 "Tenant vector reload complete\|TenantVectorWatcher initial load complete" 30
  4. Prime ONLY G2 tenants (T3, T4) so they would be evaluated if gate passed: sim_set_oid "6.1" "10", sim_set_oid "6.2" "1", sim_set_oid "6.3" "1", sim_set_oid "7.1" "10", sim_set_oid "7.2" "1", sim_set_oid "7.3" "1"
  5. DO NOT prime G1 (T1, T2) -- their holders stay empty -> Not Ready
  6. DO NOT sleep 8s -- must assert "not ready" BEFORE the 6s grace window expires
- Capture G2 BEFORE snapshots (snmp_poll_executed_total for T3 and T4) immediately after priming G2
- PSS-20a: poll_until_log 5 1 for "e2e-pss-g1-t1.*not ready" with --since=5. record_pass/record_fail "PSS-20a: G1-T1 not ready (in grace window)"
- PSS-20b: poll_until_log 5 1 for "e2e-pss-g1-t2.*not ready" with --since=5. record_pass/record_fail "PSS-20b: G1-T2 not ready (in grace window)"
- PSS-20c: G2 log absence check with SHORTER window (sleep 5, --since=10s) since we must stay within grace window. record_pass "PSS-20c: G2 not evaluated -- log absence (gate blocked -- G1 not ready)" if absent.
- PSS-20d: G2 metric non-increment. Capture AFTER snapshots. record_pass "PSS-20d: G2 not evaluated -- metric non-increment (T3 delta=$DELTA_T3, T4 delta=$DELTA_T4)" if both deltas == 0.

NOTE: PSS-20 is the only scenario that re-applies the fixture because it needs fresh tenant holders with empty series. The FIXTURES_DIR variable must be set: FIXTURES_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)/fixtures"
  </action>
  <verify>
- `bash -n tests/e2e/scenarios/67-pss-19-g1-healthy-unresolved.sh` exits 0
- `bash -n tests/e2e/scenarios/68-pss-20-all-g1-not-ready.sh` exits 0
- `grep "tier=3" tests/e2e/scenarios/67-pss-19-g1-healthy-unresolved.sh` returns match (T1 Healthy assertion)
- `grep "not ready" tests/e2e/scenarios/68-pss-20-all-g1-not-ready.sh` returns matches
- `grep -c "sleep 8" tests/e2e/scenarios/68-pss-20-all-g1-not-ready.sh` returns 0 (must NOT sleep 8 in PSS-20)
- `grep "G2_FOUND" tests/e2e/scenarios/67-pss-19-g1-healthy-unresolved.sh` returns matches (log absence proof)
- `grep "snapshot_counter" tests/e2e/scenarios/67-pss-19-g1-healthy-unresolved.sh` returns matches (metric non-increment proof)
- `grep "snapshot_counter" tests/e2e/scenarios/68-pss-20-all-g1-not-ready.sh` returns matches (metric non-increment proof)
- PSS-20 does NOT prime G1 OIDs (4.1, 4.2, 4.3, 5.1, 5.2, 5.3)
  </verify>
  <done>
PSS-19 verifies gate blocks with mixed Healthy+Unresolved using dual proof (log absence + metric non-increment). PSS-20 verifies gate blocks when G1 is Not Ready by asserting "not ready" logs within grace window, G2 log absence with short observation window, and G2 metric non-increment. All 4 gate-block scenarios have dual proof complete.
  </done>
</task>

</tasks>

<verification>
- All 4 scripts have valid bash syntax
- PSS-17/18/19 use dual proof: G1 positive assertion + G2 log absence (sleep 10, --since=15s) + G2 metric non-increment (snapshot_counter delta == 0)
- PSS-20 uses short timeout (5s poll), no G1 priming, no sleep 8, short G2 absence window, plus metric non-increment
- No fixture apply/restore in PSS-17/18/19 (runner handles it)
- PSS-20 re-applies fixture (needs fresh holders) but does NOT restore
- Gate-block G2 log absence pattern: kubectl logs --since, grep for "e2e-pss-g2.*tier=", G2_FOUND variable
- Gate-block G2 metric pattern: snapshot_counter "snmp_poll_executed_total" before/after observation window, delta == 0
</verification>

<success_criteria>
- 4 gate-block scenario scripts exist (65, 66, 67, 68)
- PSS-17/18/19/20 each have BOTH G2 log absence AND G2 metric non-increment assertions
- PSS-17/18/19 each have G1 positive assertion before G2 dual proof
- PSS-20 asserts "not ready" with short timeout, does not prime G1 or sleep 8s
- Valid bash syntax on all scripts
</success_criteria>

<output>
After completion, create `.planning/phases/64-advance-gate-logic/64-03-SUMMARY.md`
</output>
