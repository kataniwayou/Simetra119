---
phase: 75-e2e-validation-scenarios
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/scenarios/109-tvm03-resolved.sh
  - tests/e2e/scenarios/110-tvm04-healthy.sh
autonomous: true

must_haves:
  truths:
    - "tenant_state gauge equals 2 (Resolved) when all resolved holders are violated"
    - "tenant_tier2_resolved counter increments during Resolved path"
    - "tenant_tier1_stale counter increments during Resolved path (stale check runs before resolved)"
    - "No command counters increment during Resolved path (evaluation stops at tier=2)"
    - "tenant_state gauge equals 1 (Healthy) when all metrics are in-range"
    - "tenant_tier3_evaluate counter increments during Healthy path"
    - "No command counters increment during Healthy path"
    - "tenant_evaluation_duration_milliseconds histogram P99 > 0 during Healthy path"
  artifacts:
    - path: "tests/e2e/scenarios/109-tvm03-resolved.sh"
      provides: "Resolved evaluation path verification with tier counter deltas"
      contains: "tenant_tier2_resolved_total"
    - path: "tests/e2e/scenarios/110-tvm04-healthy.sh"
      provides: "Healthy evaluation path with P99 histogram verification"
      contains: "tenant_evaluation_duration_milliseconds"
  key_links:
    - from: "tests/e2e/scenarios/109-tvm03-resolved.sh"
      to: "Prometheus"
      via: "snapshot_counter delta for tier2_resolved"
      pattern: "snapshot_counter.*tenant_tier2_resolved_total"
    - from: "tests/e2e/scenarios/110-tvm04-healthy.sh"
      to: "Prometheus"
      via: "query_prometheus for histogram P99"
      pattern: "tenant_evaluation_duration_milliseconds"
---

<objective>
Create the Resolved path scenario (109) and the Healthy path scenario (110) with counter delta verification and histogram P99 check.

Purpose: Resolved verifies that all-resolved-violated stops evaluation at tier=2 and increments the correct counters. Healthy verifies the happy path (all in-range) and checks the histogram P99 is present and > 0 — proving the full instrumentation pipeline from SnapshotJob through OTel to Prometheus.

Output: Two executable scenario scripts covering requirements TE2E-02, TE2E-04, and TE2E-05.
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-e2e-validation-scenarios/75-RESEARCH.md
@.planning/phases/75-e2e-validation-scenarios/75-CONTEXT.md

Key references:
@tests/e2e/scenarios/55-pss-03-resolved.sh — Resolved path OID setup (set 5.2 and 5.3 to 0)
@tests/e2e/scenarios/57-pss-05-healthy.sh — Healthy path (no OID changes after prime)
@tests/e2e/scenarios/53-pss-01-not-ready.sh — fixture-prime-assert pattern, cleanup
@tests/e2e/lib/common.sh — assert_delta_gt, assert_delta_eq, record_pass, record_fail
@tests/e2e/lib/prometheus.sh — snapshot_counter, query_prometheus, query_counter
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scenario 109-tvm03-resolved.sh</name>
  <files>tests/e2e/scenarios/109-tvm03-resolved.sh</files>
  <action>
Create `tests/e2e/scenarios/109-tvm03-resolved.sh` — verifies the Resolved evaluation path (tenant_state=2).

**How to trigger Resolved (from RESEARCH.md):**
Prime OIDs to healthy -> sleep 8s -> set `sim_set_oid "5.2" "0"` and `sim_set_oid "5.3" "0"` (both resolved holders violated, eval stays in-range at 10).

**Scenario structure:**
1. Standard setup: save ConfigMap, apply `tenant-cfg06-pss-single.yaml`, wait for reload.
2. Prime T2 OIDs: `sim_set_oid "5.1" "10"`, `"5.2" "1"`, `"5.3" "1"`.
3. Sleep 8s for grace.
4. Snapshot counter baselines for: `tenant_tier1_stale_total`, `tenant_tier2_resolved_total`, `tenant_command_dispatched_total` — all with filter `tenant_id="e2e-pss-tenant",priority="1"`.
5. Trigger Resolved: `sim_set_oid "5.2" "0"` and `sim_set_oid "5.3" "0"`.
6. Wait for state change — poll_until_log for `"e2e-pss-tenant.*tier=2"` with 30s timeout, --since=15s.
7. Sleep 10s to accumulate counter deltas.
8. Snapshot counter after values.

**Sub-assertions:**

TVM-03A: `tenant_state` gauge = 2 (Resolved)
```bash
STATE=$(query_prometheus 'tenant_state{tenant_id="e2e-pss-tenant"}' | jq -r '.data.result[0].value[1] // "-1"' | cut -d. -f1)
```
Assert STATE == "2".

TVM-03B: `tenant_tier2_resolved_total` delta > 0
Use `assert_delta_gt` with the before/after snapshots. Resolved counter increments per non-violated resolved holder count — but in the Resolved path all resolved ARE violated, so the count-then-loop increments by the NON-violated count. Research says tier2_resolved increments per "non-violated resolved count." When ALL are violated, the non-violated count is 0. HOWEVER: the counter counts how many resolved holders were checked, not non-violated ones specifically. Re-read the research carefully: "tier2_resolved increments per non-violated resolved count." This means when both are violated, the counter may be 0 for the Resolved path.

CORRECTION: Looking at research more carefully: "Resolved (tier=2): Prime -> sleep 8 -> sim_set_oid '5.2' '0' + '5.3' '0'. tenant_state = 2, tier2_resolved increments per non-violated resolved count." If ALL resolved are violated and non-violated count = 0, then tier2_resolved may NOT increment during Resolved path. This needs careful handling.

SAFER APPROACH: Instead of asserting tier2_resolved delta, assert that `tenant_state == 2` (the primary signal). For counter assertion, use `tenant_tier1_stale_total` which counts stale holders — during Resolved path, tier=1 runs first and counts stale holders (0 stale since OIDs are set, not stale). So tier1_stale may also be 0 delta.

FINAL APPROACH: For Resolved path, the key assertions are:
- TVM-03A: `tenant_state == 2` (Resolved confirmed)
- TVM-03B: `tenant_evaluation_duration_milliseconds_count` delta > 0 (evaluation ran)
- TVM-03C: `tenant_command_dispatched_total` delta == 0 (no commands at tier=2, negative assertion)

This is the correct set — Resolved stops evaluation before commands are dispatched.

**Cleanup:** `reset_oid_overrides`, then `restore_configmap`.
  </action>
  <verify>
- `bash -n tests/e2e/scenarios/109-tvm03-resolved.sh` passes
- Script contains `sim_set_oid "5.2" "0"` and `sim_set_oid "5.3" "0"` (Resolved trigger)
- Script asserts `tenant_state` == 2
- Script contains negative command counter assertion (delta == 0)
  </verify>
  <done>
Scenario 109 triggers Resolved path, verifies tenant_state=2, duration recorded, and no command dispatch. Follows fixture-prime-assert-cleanup pattern.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create scenario 110-tvm04-healthy.sh</name>
  <files>tests/e2e/scenarios/110-tvm04-healthy.sh</files>
  <action>
Create `tests/e2e/scenarios/110-tvm04-healthy.sh` — verifies the Healthy evaluation path (tenant_state=1) and histogram P99.

**How to trigger Healthy (from RESEARCH.md):**
Prime OIDs to in-range values -> sleep 8s -> no OID changes needed. All in-range = tier=3 Healthy.

**Scenario structure:**
1. Standard setup: save ConfigMap, apply `tenant-cfg06-pss-single.yaml`, wait for reload.
2. Prime T2 OIDs: `sim_set_oid "5.1" "10"`, `"5.2" "1"`, `"5.3" "1"`.
3. Sleep 8s for grace.
4. Snapshot counter baselines for: `tenant_tier3_evaluate_total`, `tenant_command_dispatched_total` — filter `tenant_id="e2e-pss-tenant",priority="1"`.
5. Wait for healthy evaluation — `poll_until_log 30 1 "e2e-pss-tenant.*tier=3" 15`.
6. Sleep 10s to accumulate deltas.
7. Snapshot counter after values.

**Sub-assertions:**

TVM-04A: `tenant_state` gauge = 1 (Healthy)
```bash
STATE=$(query_prometheus 'tenant_state{tenant_id="e2e-pss-tenant"}' | jq -r '.data.result[0].value[1] // "-1"' | cut -d. -f1)
```
Assert STATE == "1".

TVM-04B: `tenant_tier3_evaluate_total` delta > 0
Use `assert_delta_gt` — tier3 increments by evaluate-not-violated count (1 evaluate holder, not violated = increment by 1 per cycle).

TVM-04C: `tenant_command_dispatched_total` delta == 0 (Healthy means no commands)
Use `assert_delta_eq` with expected 0.

TVM-04D: Histogram P99 present and > 0
```bash
P99=$(query_prometheus 'histogram_quantile(0.99, rate(tenant_evaluation_duration_milliseconds_bucket{tenant_id="e2e-pss-tenant"}[5m]))' | jq -r '.data.result[0].value[1] // "0"')
# P99 is a float; check it's not "0", "NaN", or empty
if [ -n "$P99" ] && [ "$P99" != "0" ] && [ "$P99" != "NaN" ] && [ "$P99" != "+Inf" ]; then
    record_pass "TVM-04D: histogram P99 present and > 0" "p99=${P99}ms"
else
    record_fail "TVM-04D: histogram P99 present and > 0" "p99=${P99} expected > 0"
fi
```

**Cleanup:** `reset_oid_overrides`, then `restore_configmap`.
  </action>
  <verify>
- `bash -n tests/e2e/scenarios/110-tvm04-healthy.sh` passes
- Script does NOT call `sim_set_oid` after sleep 8 (Healthy = no OID changes)
- Script asserts `tenant_state == 1`
- Script contains `histogram_quantile(0.99` for P99 check
- Script contains tier3_evaluate delta > 0 assertion
  </verify>
  <done>
Scenario 110 verifies tenant_state=1 (Healthy), tier3_evaluate counter incrementing, no command dispatch, and histogram P99 > 0. Covers TE2E-04 (state=1) and TE2E-05 (P99).
  </done>
</task>

</tasks>

<verification>
- Both scenario files pass `bash -n` syntax check
- Scenario 109 triggers Resolved via both-resolved-violated OID setup
- Scenario 110 triggers Healthy via all-in-range (no OID mutation after prime)
- Both scenarios follow fixture-prime-assert-cleanup pattern with proper cleanup order
- P99 histogram check handles NaN/+Inf edge cases
</verification>

<success_criteria>
1. `109-tvm03-resolved.sh` triggers Resolved (state=2), verifies duration and no commands
2. `110-tvm04-healthy.sh` triggers Healthy (state=1), verifies tier3 counter, no commands, and P99 > 0
3. All scripts pass `bash -n` syntax validation
4. Counter delta assertions use `assert_delta_gt 0` (not `assert_delta_eq 1`) per research guidance
</success_criteria>

<output>
After completion, create `.planning/phases/75-e2e-validation-scenarios/75-02-SUMMARY.md`
</output>
