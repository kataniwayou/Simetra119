---
phase: 18-operations-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - deploy/grafana/dashboards/simetra-operations.json
  - deploy/grafana/dashboards/npb-device.json
  - deploy/grafana/dashboards/obp-device.json
  - deploy/grafana/provisioning/datasources/simetra-prometheus.yaml
autonomous: true

must_haves:
  truths:
    - "Operations dashboard JSON file exists at deploy/grafana/dashboards/simetra-operations.json ready for manual Grafana import"
    - "Stale reference files (npb-device.json, obp-device.json, simetra-prometheus.yaml) are deleted"
    - "Operator sees pod identity table with service_instance_id, pod name, and leader/follower role"
    - "Operator sees 11 time series panels for pipeline counters with per-pod breakdown"
    - "Operator sees 6 time series panels for .NET runtime metrics (GC, memory, thread pool)"
    - "Dashboard auto-refreshes every 5 seconds with 15-minute default time range"
    - "Pod filter variable dropdown filters all panels by service_instance_id"
  artifacts:
    - path: "deploy/grafana/dashboards/simetra-operations.json"
      provides: "Complete operations dashboard"
      contains: "__inputs"
  key_links:
    - from: "dashboard templating"
      to: "all panel targets"
      via: "service_instance_id=~\"$pod\" filter"
      pattern: "service_instance_id=~"
    - from: "dashboard __inputs"
      to: "all panel datasources"
      via: "${DS_PROMETHEUS} reference"
      pattern: "DS_PROMETHEUS"
---

<objective>
Create the Simetra Operations dashboard as a Grafana-importable JSON file, and delete stale reference project files under deploy/grafana/.

Purpose: Operators need a single dashboard to monitor pipeline health, pod roles, and .NET runtime metrics across all replicas.
Output: One dashboard JSON file at deploy/grafana/dashboards/simetra-operations.json, stale files removed.
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-operations-dashboard/18-CONTEXT.md
@.planning/phases/18-operations-dashboard/18-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete stale reference files and clean up empty directories</name>
  <files>
    deploy/grafana/dashboards/npb-device.json
    deploy/grafana/dashboards/obp-device.json
    deploy/grafana/provisioning/datasources/simetra-prometheus.yaml
  </files>
  <action>
    Delete these 3 stale reference project files:
    - deploy/grafana/dashboards/npb-device.json
    - deploy/grafana/dashboards/obp-device.json
    - deploy/grafana/provisioning/datasources/simetra-prometheus.yaml

    After deleting, remove the now-empty deploy/grafana/provisioning/ directory tree entirely.

    Do NOT delete deploy/grafana/dashboards/simetra-operations.json yet -- it will be overwritten in Task 2 with the new dashboard.
  </action>
  <verify>
    Run: ls -R deploy/grafana/
    Expected: Only deploy/grafana/dashboards/ directory exists, containing only simetra-operations.json (the stale one, to be replaced in Task 2).
  </verify>
  <done>
    npb-device.json, obp-device.json, and simetra-prometheus.yaml are deleted. provisioning/ directory tree is removed. Only deploy/grafana/dashboards/ remains.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create operations dashboard JSON file</name>
  <files>deploy/grafana/dashboards/simetra-operations.json</files>
  <action>
    Create (overwrite) deploy/grafana/dashboards/simetra-operations.json with a complete Grafana dashboard JSON.

    Follow ALL patterns from 18-RESEARCH.md. Key structural requirements:

    **Dashboard skeleton:**
    - __inputs: DS_PROMETHEUS datasource placeholder (pluginId: prometheus)
    - __requires: grafana 11.0.0, prometheus 1.0.0, timeseries panel, table panel
    - title: "Simetra Operations"
    - uid: "simetra-operations"
    - tags: ["simetra", "operations"]
    - schemaVersion: 39
    - refresh: "5s"
    - time: { from: "now-15m", to: "now" }
    - graphTooltip: 1 (shared crosshair)
    - editable: true, timezone: "browser"

    **Template variable (pod filter):**
    - name: "pod", label: "Pod", type: "query"
    - definition/query: label_values(snmp_event_published_total, service_instance_id)
    - multi: true, includeAll: true, allValue: ".*"
    - current: { selected: true, text: "All", value: "$__all" }
    - refresh: 2 (on time range change)
    - datasource: ${DS_PROMETHEUS}

    **Panel layout (24-column grid):**
    All panel IDs: null (Grafana auto-assigns on import).

    Row 1 - Pod Identity (y=0):
    - Row header panel: "Pod Identity" at y=0 (type: "row", collapsed: false)
    - Table panel at y=1, x=0, w=24, h=5
      - Query A: count by (service_instance_id, k8s_pod_name) (snmp_event_published_total{service_instance_id=~"$pod"}) -- instant, format: table
      - Query B: count by (service_instance_id) (snmp_gauge{service_instance_id=~"$pod"}) -- instant, format: table
      - Transformations: merge
      - Field overrides:
        - Hide "Time" and "Value #A" columns
        - Rename service_instance_id to "Service Instance"
        - Rename k8s_pod_name to "Pod Name"
        - Value #B column: displayName "Role", value mappings: 1="Leader" (green), null="Follower" (gray/text color)

    Row 2 - Pipeline Counters (y=6):
    - Row header panel: "Pipeline Counters" at y=6
    - 11 time series panels, 4 per row (w=6, h=8):
      - y=7:  (1) Events Published, (2) Events Handled, (3) Event Errors, (4) Events Rejected
      - y=15: (5) Polls Executed, (6) Traps Received, (7) Trap Auth Failed, (8) Trap Unknown Device
      - y=23: (9) Traps Dropped, (10) Poll Unreachable, (11) Poll Recovered
    - Each panel:
      - type: "timeseries"
      - datasource: ${DS_PROMETHEUS}
      - expr: sum by (service_instance_id) (rate(METRIC_NAME{service_instance_id=~"$pod"}[$__rate_interval]))
      - legendFormat: "{{service_instance_id}}"
      - unit: "ops" (operations per second)
      - drawStyle: "line", lineWidth: 2, fillOpacity: 10, showPoints: "never", spanNulls: false
      - tooltip: mode "multi", sort "desc"
      - legend: displayMode "list", placement "bottom"

    Pipeline counter metric names (in panel order):
    1. snmp_event_published_total
    2. snmp_event_handled_total
    3. snmp_event_errors_total
    4. snmp_event_rejected_total
    5. snmp_poll_executed_total
    6. snmp_trap_received_total
    7. snmp_trap_auth_failed_total
    8. snmp_trap_unknown_device_total
    9. snmp_trap_dropped_total
    10. snmp_poll_unreachable_total
    11. snmp_poll_recovered_total

    Panel titles (human-friendly, no metric names):
    1. "Events Published"
    2. "Events Handled"
    3. "Event Errors"
    4. "Events Rejected"
    5. "Polls Executed"
    6. "Traps Received"
    7. "Trap Auth Failed"
    8. "Trap Unknown Device"
    9. "Traps Dropped"
    10. "Poll Unreachable"
    11. "Poll Recovered"

    Row 3 - .NET Runtime (y=31):
    - Row header panel: ".NET Runtime" at y=31
    - 6 time series panels, 3 per row (w=8, h=8):
      - y=32: (1) GC Collections Rate, (2) GC Pause Time Rate, (3) Process Working Set
      - y=40: (4) GC Heap Size, (5) Thread Pool Threads, (6) Thread Pool Queue Length

    Runtime panel details:
    1. GC Collections Rate
       - expr: sum by (service_instance_id) (rate(dotnet_gc_collections_total{service_instance_id=~"$pod"}[$__rate_interval]))
       - unit: "ops"
    2. GC Pause Time Rate
       - expr: sum by (service_instance_id) (rate(dotnet_gc_pause_time_seconds_total{service_instance_id=~"$pod"}[$__rate_interval]))
       - unit: "s" (seconds)
    3. Process Working Set
       - expr: dotnet_process_memory_working_set_bytes{service_instance_id=~"$pod"}
       - unit: "bytes" (NOT rate -- this is a gauge)
    4. GC Heap Size
       - expr: dotnet_gc_last_collection_heap_size_bytes{service_instance_id=~"$pod"}
       - unit: "bytes" (NOT rate -- this is a gauge)
    5. Thread Pool Threads
       - expr: dotnet_thread_pool_thread_count{service_instance_id=~"$pod"}
       - unit: "short" (NOT rate -- this is a gauge)
    6. Thread Pool Queue Length
       - expr: dotnet_thread_pool_queue_length{service_instance_id=~"$pod"}
       - unit: "short" (NOT rate -- this is a gauge)

    Runtime panels use the same visual config as pipeline panels (timeseries, line, legend bottom) EXCEPT unit varies per panel as specified above.

    **Critical anti-patterns to avoid:**
    - Do NOT hardcode datasource UIDs -- always use ${DS_PROMETHEUS}
    - Do NOT use fixed rate intervals like [5m] -- always use $__rate_interval
    - Do NOT use rate() on gauge metrics (working set, heap size, thread count, queue length)
    - Do NOT assign panel IDs -- use null for all
    - Do NOT use dots in metric names -- use underscores (snmp_event_published_total not snmp.event.published)

    **JSON formatting:** Pretty-print with 2-space indentation for readability.
  </action>
  <verify>
    1. Run: python -m json.tool deploy/grafana/dashboards/simetra-operations.json > /dev/null && echo "Valid JSON"
       Expected: "Valid JSON" (no parse errors)

    2. Run: grep -c '"type": "timeseries"' deploy/grafana/dashboards/simetra-operations.json
       Expected: 17 (11 pipeline + 6 runtime)

    3. Run: grep -c '"type": "table"' deploy/grafana/dashboards/simetra-operations.json
       Expected: 1 (pod identity table)

    4. Run: grep -c '"type": "row"' deploy/grafana/dashboards/simetra-operations.json
       Expected: 3 (Pod Identity, Pipeline Counters, .NET Runtime)

    5. Run: grep -c 'DS_PROMETHEUS' deploy/grafana/dashboards/simetra-operations.json
       Expected: > 0 (used throughout, no hardcoded datasource UIDs)

    6. Run: grep -c 'service_instance_id=~' deploy/grafana/dashboards/simetra-operations.json
       Expected: >= 18 (every panel query uses the pod filter)

    7. Run: grep '"refresh"' deploy/grafana/dashboards/simetra-operations.json
       Expected: Contains "5s"

    8. Verify no stale files remain:
       ls deploy/grafana/provisioning/ 2>/dev/null && echo "FAIL: provisioning dir still exists" || echo "OK: provisioning removed"
       ls deploy/grafana/dashboards/npb-device.json 2>/dev/null && echo "FAIL: npb still exists" || echo "OK: npb removed"
       ls deploy/grafana/dashboards/obp-device.json 2>/dev/null && echo "FAIL: obp still exists" || echo "OK: obp removed"
  </verify>
  <done>
    - simetra-operations.json is valid JSON containing 18 panels (1 table + 17 timeseries) plus 3 row headers
    - All panels use ${DS_PROMETHEUS} datasource and respect the $pod template variable
    - Pipeline counters use rate() with $__rate_interval; gauges use instant queries
    - Dashboard defaults to 15-minute range with 5-second auto-refresh
    - Pod identity table shows service_instance_id, pod name, and leader/follower role
  </done>
</task>

</tasks>

<verification>
1. Valid JSON: `python -m json.tool deploy/grafana/dashboards/simetra-operations.json > /dev/null`
2. Panel count: 1 table + 17 timeseries + 3 row headers = 21 panel entries
3. All queries contain `service_instance_id=~"$pod"` filter
4. All datasources reference `${DS_PROMETHEUS}` (no hardcoded UIDs)
5. Stale files deleted: no npb-device.json, obp-device.json, or provisioning/ directory
6. Dashboard metadata: schemaVersion 39, refresh "5s", time range "now-15m"
</verification>

<success_criteria>
- deploy/grafana/dashboards/simetra-operations.json exists and is valid JSON
- deploy/grafana/dashboards/ contains ONLY simetra-operations.json (stale files removed)
- deploy/grafana/provisioning/ directory does not exist
- Dashboard has 18 data panels: 1 pod identity table, 11 pipeline counter timeseries, 6 runtime timeseries
- Pod filter variable populated from label_values(snmp_event_published_total, service_instance_id)
- All panels filtered by $pod variable
- Auto-refresh: 5s, default range: 15m
</success_criteria>

<output>
After completion, create `.planning/phases/18-operations-dashboard/18-01-SUMMARY.md`
</output>
