---
phase: 79-dashboard-percentage-update
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - deploy/grafana/dashboards/simetra-operations.json
autonomous: true

must_haves:
  truths:
    - "Tenant Status table State column queries tenant_evaluation_state (not tenant_state)"
    - "6 percentage columns query gauge instruments directly (no increase/rate wrapping)"
    - "Trend column queries delta of tenant_command_dispatched_percent (not _total)"
    - "P99 column query unchanged (tenant_evaluation_duration_milliseconds)"
    - "Column headers show (%) suffix on 6 percentage columns"
    - "All percentage columns display 0 decimal places"
  artifacts:
    - path: "deploy/grafana/dashboards/simetra-operations.json"
      provides: "Updated Tenant Status table panel"
      contains: "tenant_evaluation_state"
  key_links:
    - from: "deploy/grafana/dashboards/simetra-operations.json"
      to: "Prometheus gauge instruments"
      via: "PromQL expr fields in panel id=28 targets"
      pattern: "tenant_metric_stale_percent|tenant_metric_resolved_percent|tenant_metric_evaluate_percent|tenant_command_dispatched_percent|tenant_command_failed_percent|tenant_command_suppressed_percent"
---

<objective>
Update the Operations dashboard Tenant Status table (panel id=28) to query the new percentage gauge instruments instead of the old counter-based increase() queries, and rename the state metric from tenant_state to tenant_evaluation_state.

Purpose: Complete DSH-01 and DSH-02 requirements -- the dashboard reflects the v2.5 gauge-based metric approach.
Output: Updated simetra-operations.json with correct PromQL and column display names.
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/79-dashboard-percentage-update/79-CONTEXT.md
@deploy/grafana/dashboards/simetra-operations.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update PromQL queries in panel id=28 targets</name>
  <files>deploy/grafana/dashboards/simetra-operations.json</files>
  <action>
In the Tenant Status table panel (id=28), update all 9 target queries. The panel starts around line 1692 of the JSON. Make these exact changes:

**RefId A (State gauge) -- rename only:**
Old: `max by (tenant_id, priority, service_instance_id, k8s_pod_name) (tenant_state{service_instance_id=~"$host", k8s_pod_name=~"$pod"})`
New: `max by (tenant_id, priority, service_instance_id, k8s_pod_name) (tenant_evaluation_state{service_instance_id=~"$host", k8s_pod_name=~"$pod"})`

**RefId B (Trend -- delta on dispatched percent gauge):**
Old: `max by (tenant_id, priority, service_instance_id, k8s_pod_name) (delta(tenant_command_dispatched_total{service_instance_id=~"$host", k8s_pod_name=~"$pod"}[30s]))`
New: `max by (tenant_id, priority, service_instance_id, k8s_pod_name) (delta(tenant_command_dispatched_percent{service_instance_id=~"$host", k8s_pod_name=~"$pod"}[30s]))`

**RefId C (P99 duration) -- NO CHANGE:**
Keep existing histogram_quantile query as-is.

**RefId D (Dispatched %):**
Old: `sum by (...) (increase(tenant_command_dispatched_total{...}[30s])) or on(...) (...) * 0`
New: `max by (tenant_id, priority, service_instance_id, k8s_pod_name) (tenant_command_dispatched_percent{service_instance_id=~"$host", k8s_pod_name=~"$pod"})`

**RefId E (Failed %):**
Old: `sum by (...) (increase(tenant_command_failed_total{...}[30s])) or on(...) (...) * 0`
New: `max by (tenant_id, priority, service_instance_id, k8s_pod_name) (tenant_command_failed_percent{service_instance_id=~"$host", k8s_pod_name=~"$pod"})`

**RefId F (Suppressed %):**
Old: `sum by (...) (increase(tenant_command_suppressed_total{...}[30s])) or on(...) (...) * 0`
New: `max by (tenant_id, priority, service_instance_id, k8s_pod_name) (tenant_command_suppressed_percent{service_instance_id=~"$host", k8s_pod_name=~"$pod"})`

**RefId G (Stale %):**
Old: `sum by (...) (increase(tenant_tier1_stale_total{...}[30s])) or on(...) (...) * 0`
New: `max by (tenant_id, priority, service_instance_id, k8s_pod_name) (tenant_metric_stale_percent{service_instance_id=~"$host", k8s_pod_name=~"$pod"})`

**RefId H (Resolved %):**
Old: `sum by (...) (increase(tenant_tier2_resolved_total{...}[30s])) or on(...) (...) * 0`
New: `max by (tenant_id, priority, service_instance_id, k8s_pod_name) (tenant_metric_resolved_percent{service_instance_id=~"$host", k8s_pod_name=~"$pod"})`

**RefId I (Evaluate %):**
Old: `sum by (...) (increase(tenant_tier3_evaluate_total{...}[30s])) or on(...) (...) * 0`
New: `max by (tenant_id, priority, service_instance_id, k8s_pod_name) (tenant_metric_evaluate_percent{service_instance_id=~"$host", k8s_pod_name=~"$pod"})`

Note the metric name mapping (old counter -> new gauge):
- tenant_command_dispatched_total -> tenant_command_dispatched_percent
- tenant_command_failed_total -> tenant_command_failed_percent
- tenant_command_suppressed_total -> tenant_command_suppressed_percent
- tenant_tier1_stale_total -> tenant_metric_stale_percent
- tenant_tier2_resolved_total -> tenant_metric_resolved_percent
- tenant_tier3_evaluate_total -> tenant_metric_evaluate_percent

Also update the zero-fallback references inside the D-I queries. Since the zero-fallback `or on(...) (...) * 0` pattern referenced `tenant_state`, those entire fallback clauses are removed (the new queries are simple gauge reads with max by).
  </action>
  <verify>
Run these checks:
1. `grep -c "increase(" deploy/grafana/dashboards/simetra-operations.json` returns 0 (no increase() left in file)
2. `grep -c "tenant_state" deploy/grafana/dashboards/simetra-operations.json` returns 0 (old name fully replaced)
3. `grep -c "tenant_evaluation_state" deploy/grafana/dashboards/simetra-operations.json` returns at least 1
4. `grep -c "tenant_metric_stale_percent" deploy/grafana/dashboards/simetra-operations.json` returns at least 1
5. `grep -c "tenant_command_dispatched_percent" deploy/grafana/dashboards/simetra-operations.json` returns at least 2 (RefId B + RefId D)
6. JSON is valid: `python -m json.tool deploy/grafana/dashboards/simetra-operations.json > /dev/null`
  </verify>
  <done>All 9 RefId queries in panel id=28 use the correct v2.5 gauge instrument names. No increase() or rate() wrapping on percentage gauges. State query references tenant_evaluation_state.</done>
</task>

<task type="auto">
  <name>Task 2: Update column display names to include (%) suffix</name>
  <files>deploy/grafana/dashboards/simetra-operations.json</files>
  <action>
In the same panel id=28 overrides section, update the displayName values for the 6 percentage columns:

- Value #D: "Dispatched" -> "Dispatched(%)"
- Value #E: "Failed" -> "Failed(%)"
- Value #F: "Suppressed" -> "Suppressed(%)"
- Value #G: "Stale" -> "Stale(%)"
- Value #H: "Resolved" -> "Resolved(%)"
- Value #I: "Evaluate" -> "Evaluate(%)"

Keep all other override properties unchanged (custom.width, decimals=0).

Do NOT add a `unit` property to these columns -- the raw number display (e.g., "75" not "75%") is intentional per the context decisions. The (%) is in the column header only.

Leave unchanged:
- Value #A (State) -- no (%) suffix
- Value #B (Trend) -- no (%) suffix
- Value #C (P99 (ms)) -- no change
  </action>
  <verify>
1. `grep -c 'Stale(%)' deploy/grafana/dashboards/simetra-operations.json` returns 1
2. `grep -c 'Resolved(%)' deploy/grafana/dashboards/simetra-operations.json` returns 1
3. `grep -c 'Evaluate(%)' deploy/grafana/dashboards/simetra-operations.json` returns 1
4. `grep -c 'Dispatched(%)' deploy/grafana/dashboards/simetra-operations.json` returns 1
5. `grep -c 'Failed(%)' deploy/grafana/dashboards/simetra-operations.json` returns 1
6. `grep -c 'Suppressed(%)' deploy/grafana/dashboards/simetra-operations.json` returns 1
7. JSON is still valid: `python -m json.tool deploy/grafana/dashboards/simetra-operations.json > /dev/null`
  </verify>
  <done>All 6 percentage column headers display with (%) suffix. Raw number format preserved (decimals=0, no unit property).</done>
</task>

</tasks>

<verification>
1. `python -m json.tool deploy/grafana/dashboards/simetra-operations.json > /dev/null` -- valid JSON
2. No `increase(` in the file: `grep -c "increase(" deploy/grafana/dashboards/simetra-operations.json` returns 0
3. No `tenant_state` (old name): `grep -c "tenant_state[^_]" deploy/grafana/dashboards/simetra-operations.json` returns 0
4. `tenant_evaluation_state` present: `grep -c "tenant_evaluation_state" deploy/grafana/dashboards/simetra-operations.json` >= 1
5. All 6 gauge names present: tenant_metric_stale_percent, tenant_metric_resolved_percent, tenant_metric_evaluate_percent, tenant_command_dispatched_percent, tenant_command_failed_percent, tenant_command_suppressed_percent
6. All 6 column headers have (%) suffix
7. No `_total` counter metric names remain in panel id=28 queries
</verification>

<success_criteria>
- DSH-01: Tenant Status table columns show percentage values (0-100) via direct gauge queries
- DSH-02: All PromQL queries updated from increase() counter queries to direct gauge queries
- State column queries tenant_evaluation_state (renamed in Phase 76)
- Trend column queries delta of tenant_command_dispatched_percent
- P99 column unchanged
- Valid JSON, no regressions to other panels
</success_criteria>

<output>
After completion, create `.planning/phases/79-dashboard-percentage-update/79-01-SUMMARY.md`
</output>
