---
phase: 42-threshold-validation-and-config-files
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/SnmpCollector/config/tenants.json
  - deploy/k8s/snmp-collector/simetra-tenants.yaml
  - deploy/k8s/production/configmap.yaml
autonomous: true

must_haves:
  truths:
    - "At least one metric entry in local dev tenants.json carries a valid Threshold object with Min and Max"
    - "At least one metric entry in K8s standalone simetra-tenants.yaml carries a valid Threshold object with Min and Max"
    - "At least one metric entry in production configmap.yaml simetra-tenants section carries a valid Threshold object with Min and Max"
    - "All three config files remain valid JSON that deserializes without errors"
    - "Existing metrics without Threshold are unchanged -- backward compatible"
  artifacts:
    - path: "src/SnmpCollector/config/tenants.json"
      provides: "Example Threshold on at least one Evaluate metric per tenant (double-wrapped format)"
      contains: "Threshold"
    - path: "deploy/k8s/snmp-collector/simetra-tenants.yaml"
      provides: "Example Threshold on at least one Evaluate metric per tenant (single-wrapped format)"
      contains: "Threshold"
    - path: "deploy/k8s/production/configmap.yaml"
      provides: "Example Threshold on at least one Evaluate metric in simetra-tenants section"
      contains: "Threshold"
  key_links: []
---

<objective>
Add example Threshold objects to tenant config files across all three locations: local dev (double-wrapped), K8s standalone (single-wrapped), and production configmap. One valid Threshold per tenant on an Evaluate metric. No code changes -- config-only.

Purpose: Operators see real Threshold examples in every config template. Satisfies THR-07.
Output: 3 modified config files.
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/42-threshold-validation-and-config-files/42-RESEARCH.md

@src/SnmpCollector/config/tenants.json
@deploy/k8s/snmp-collector/simetra-tenants.yaml
@deploy/k8s/production/configmap.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Threshold examples to local dev tenants.json</name>
  <files>
    src/SnmpCollector/config/tenants.json
  </files>
  <action>
This file has **double-wrapped** structure: `{ "Tenants": { "Tenants": [ ... ] } }`.

Add `"Threshold"` to one Evaluate metric per tenant:

**Tenant 1 (Priority 1):** Add `"Threshold": { "Min": -10.0, "Max": 3.0 }` to the `obp_r1_power_L1` metric entry (Evaluate, optical dBm range). The entry becomes:
```json
{
  "Ip": "127.0.0.1",
  "Port": 10161,
  "MetricName": "obp_r1_power_L1",
  "Role": "Evaluate",
  "Threshold": { "Min": -10.0, "Max": 3.0 }
}
```

**Tenant 2 (Priority 2):** Add `"Threshold": { "Min": 0.0, "Max": 95.0 }` to the `npb_cpu_util` metric entry (Evaluate, percent range). The entry becomes:
```json
{
  "Ip": "127.0.0.1",
  "Port": 10162,
  "MetricName": "npb_cpu_util",
  "TimeSeriesSize": 5,
  "Role": "Evaluate",
  "Threshold": { "Min": 0.0, "Max": 95.0 }
}
```

Do NOT change any other metric entries. Preserve exact formatting and indentation of the file.
  </action>
  <verify>
Validate JSON syntax: run `python -m json.tool src/SnmpCollector/config/tenants.json` or equivalent -- must parse without errors.
Verify 2 occurrences of "Threshold" in the file.
  </verify>
  <done>
tenants.json has Threshold on obp_r1_power_L1 (Tenant 1) and npb_cpu_util (Tenant 2). JSON parses without errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Threshold examples to K8s standalone simetra-tenants.yaml and production configmap.yaml</name>
  <files>
    deploy/k8s/snmp-collector/simetra-tenants.yaml
    deploy/k8s/production/configmap.yaml
  </files>
  <action>
Both files contain tenant JSON inside YAML literal blocks (`tenants.json: |`). The format is **single-wrapped**: `{ "Tenants": [ ... ] }`.

**File 1: `deploy/k8s/snmp-collector/simetra-tenants.yaml`**

Add Threshold to one Evaluate metric per tenant (3 tenants):

- Tenant Priority 1: Add `"Threshold": { "Min": 0.0, "Max": 95.0 }` to `npb_cpu_util` entry (line 17). Change the entry from:
  `{ "Ip": "npb-simulator.simetra.svc.cluster.local", "Port": 161, "MetricName": "npb_cpu_util", "TimeSeriesSize": 10, "Role": "Evaluate" }`
  to:
  `{ "Ip": "npb-simulator.simetra.svc.cluster.local", "Port": 161, "MetricName": "npb_cpu_util", "TimeSeriesSize": 10, "Role": "Evaluate", "Threshold": { "Min": 0.0, "Max": 95.0 } }`

- Tenant Priority 2: Add `"Threshold": { "Min": 0.0, "Max": 95.0 }` to `npb_mem_util` entry (line 27). Change the entry from:
  `{ "Ip": "npb-simulator.simetra.svc.cluster.local", "Port": 161, "MetricName": "npb_mem_util", "TimeSeriesSize": 5, "Role": "Evaluate" }`
  to:
  `{ "Ip": "npb-simulator.simetra.svc.cluster.local", "Port": 161, "MetricName": "npb_mem_util", "TimeSeriesSize": 5, "Role": "Evaluate", "Threshold": { "Min": 0.0, "Max": 95.0 } }`

- Tenant Priority 3: Add `"Threshold": { "Min": -10.0, "Max": 3.0 }` to `obp_r1_power_L1` entry (line 42). Change the entry from:
  `{ "Ip": "obp-simulator.simetra.svc.cluster.local", "Port": 161, "MetricName": "obp_r1_power_L1", "TimeSeriesSize": 3, "Role": "Evaluate" }`
  to:
  `{ "Ip": "obp-simulator.simetra.svc.cluster.local", "Port": 161, "MetricName": "obp_r1_power_L1", "TimeSeriesSize": 3, "Role": "Evaluate", "Threshold": { "Min": -10.0, "Max": 3.0 } }`

**File 2: `deploy/k8s/production/configmap.yaml`**

This file has a `simetra-tenants` ConfigMap section (starting at line 453). Add Threshold to one Evaluate metric per tenant (3 tenants), using the same pattern as above:

- Tenant Priority 1: Add `"Threshold": { "Min": 0.0, "Max": 95.0 }` to `npb_cpu_util` entry (line 488). Change from:
  `{ "Ip": "npb-simulator.simetra.svc.cluster.local", "Port": 161, "MetricName": "npb_cpu_util", "Role": "Evaluate" }`
  to:
  `{ "Ip": "npb-simulator.simetra.svc.cluster.local", "Port": 161, "MetricName": "npb_cpu_util", "Role": "Evaluate", "Threshold": { "Min": 0.0, "Max": 95.0 } }`

- Tenant Priority 2: Add `"Threshold": { "Min": 0.0, "Max": 95.0 }` to `npb_mem_util` entry (line 497). Change from:
  `{ "Ip": "npb-simulator.simetra.svc.cluster.local", "Port": 161, "MetricName": "npb_mem_util", "Role": "Evaluate" }`
  to:
  `{ "Ip": "npb-simulator.simetra.svc.cluster.local", "Port": 161, "MetricName": "npb_mem_util", "Role": "Evaluate", "Threshold": { "Min": 0.0, "Max": 95.0 } }`

- Tenant Priority 3: Add `"Threshold": { "Min": -10.0, "Max": 3.0 }` to `obp_r1_power_L1` entry (line 511). Change from:
  `{ "Ip": "obp-simulator.simetra.svc.cluster.local", "Port": 161, "MetricName": "obp_r1_power_L1", "Role": "Evaluate" }`
  to:
  `{ "Ip": "obp-simulator.simetra.svc.cluster.local", "Port": 161, "MetricName": "obp_r1_power_L1", "Role": "Evaluate", "Threshold": { "Min": -10.0, "Max": 3.0 } }`

Preserve exact YAML indentation (the JSON is inside a YAML literal block `|`). Do NOT change any other entries.
  </action>
  <verify>
Verify YAML syntax of both files. The JSON inside the literal blocks must also be valid.
Verify 3 occurrences of "Threshold" in simetra-tenants.yaml and 3 in the production configmap.yaml simetra-tenants section.
  </verify>
  <done>
simetra-tenants.yaml has Threshold on npb_cpu_util (T1), npb_mem_util (T2), obp_r1_power_L1 (T3). Production configmap.yaml has Threshold on npb_cpu_util (T1), npb_mem_util (T2), obp_r1_power_L1 (T3). All JSON blocks parse correctly.
  </done>
</task>

</tasks>

<verification>
1. `src/SnmpCollector/config/tenants.json` parses as valid JSON
2. `deploy/k8s/snmp-collector/simetra-tenants.yaml` YAML and embedded JSON are valid
3. `deploy/k8s/production/configmap.yaml` YAML and embedded JSON are valid
4. tenants.json has 2 Threshold entries (one per tenant)
5. simetra-tenants.yaml has 3 Threshold entries (one per tenant)
6. production configmap.yaml simetra-tenants section has 3 Threshold entries (one per tenant)
7. All Threshold values use valid Min < Max ranges (no invalid examples)
8. No other metric entries are modified
9. `dotnet build src/SnmpCollector/SnmpCollector.csproj` still compiles (config changes don't break build)
</verification>

<success_criteria>
- Every tenant config location has at least one Threshold example per tenant
- All Threshold examples use valid Min < Max ranges
- JSON/YAML remains valid and parseable
- No existing metrics are altered beyond adding Threshold
- Config reload would succeed without threshold validation errors
</success_criteria>

<output>
After completion, create `.planning/phases/42-threshold-validation-and-config-files/42-02-SUMMARY.md`
</output>
