---
phase: 85-preferredheartbeatservice-reader-path
plan: 02
type: execute
wave: 2
depends_on: ["85-01"]
files_modified:
  - tests/SnmpCollector.Tests/Telemetry/PreferredLeaderServiceTests.cs
  - tests/SnmpCollector.Tests/Jobs/PreferredHeartbeatJobTests.cs
autonomous: true

must_haves:
  truths:
    - "UpdateStampFreshness flips the volatile bool and is verified by unit test"
    - "Fresh lease within threshold yields IsPreferredStampFresh = true"
    - "Stale lease beyond threshold yields IsPreferredStampFresh = false"
    - "404 response yields IsPreferredStampFresh = false without exception"
    - "Null renewTime + null acquireTime yields stale"
    - "Transient K8s error preserves last known freshness value"
    - "Liveness is always stamped (even on error)"
  artifacts:
    - path: "tests/SnmpCollector.Tests/Jobs/PreferredHeartbeatJobTests.cs"
      provides: "Unit tests for lease read, 404, transient error, freshness logic"
      min_lines: 100
    - path: "tests/SnmpCollector.Tests/Telemetry/PreferredLeaderServiceTests.cs"
      provides: "Extended tests for UpdateStampFreshness"
      contains: "UpdateStampFreshness"
  key_links:
    - from: "tests/SnmpCollector.Tests/Jobs/PreferredHeartbeatJobTests.cs"
      to: "src/SnmpCollector/Jobs/PreferredHeartbeatJob.cs"
      via: "instantiation with mocked IKubernetes"
      pattern: "Mock<.*IKubernetes>"
---

<objective>
Add unit tests for PreferredLeaderService.UpdateStampFreshness and PreferredHeartbeatJob covering all lease-read scenarios: fresh lease, stale lease, 404, null timestamps, transient errors, and liveness stamping.

Purpose: Satisfy SC-4 ("IPreferredStampReader.IsPreferredStampFresh returns a real derived value, not a stub -- verified by unit test with a mocked lease response") and verify all edge cases from the phase requirements.

Output: Extended PreferredLeaderServiceTests + new PreferredHeartbeatJobTests with mocked K8s client.
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/85-preferredheartbeatservice-reader-path/85-CONTEXT.md
@.planning/phases/85-preferredheartbeatservice-reader-path/85-RESEARCH.md
@.planning/phases/85-preferredheartbeatservice-reader-path/85-01-SUMMARY.md

Key source files (from Plan 01):
@src/SnmpCollector/Jobs/PreferredHeartbeatJob.cs
@src/SnmpCollector/Telemetry/PreferredLeaderService.cs
@src/SnmpCollector/Configuration/PreferredHeartbeatJobOptions.cs
@src/SnmpCollector/Configuration/LeaseOptions.cs

Test patterns:
@tests/SnmpCollector.Tests/Telemetry/PreferredLeaderServiceTests.cs
@tests/SnmpCollector.Tests/Jobs/HeartbeatJobTests.cs (if exists, for mock patterns)
@tests/SnmpCollector.Tests/Jobs/CorrelationJobTests.cs (if exists, for mock patterns)
</context>

<tasks>

<task type="auto">
  <name>Task 1: PreferredLeaderService UpdateStampFreshness tests</name>
  <files>
    tests/SnmpCollector.Tests/Telemetry/PreferredLeaderServiceTests.cs
  </files>
  <action>
Add tests to the existing `PreferredLeaderServiceTests.cs` class. These test the UpdateStampFreshness method and the volatile bool behavior. Follow the existing test style (numbered sections with separator comments).

New tests to add (continuing from existing test #8):

9. `UpdateStampFreshness_WhenCalledWithTrue_SetsIsPreferredStampFreshTrue`
   - Create service, confirm IsPreferredStampFresh starts false
   - Call UpdateStampFreshness(true)
   - Assert IsPreferredStampFresh == true

10. `UpdateStampFreshness_WhenCalledWithFalse_SetsIsPreferredStampFreshFalse`
    - Create service, call UpdateStampFreshness(true) then UpdateStampFreshness(false)
    - Assert IsPreferredStampFresh == false

11. `UpdateStampFreshness_InitialValueIsFalse`
    - Create service, assert IsPreferredStampFresh == false without calling UpdateStampFreshness

12. `UpdateStampFreshness_LogsOnTransition_FreshToStale`
    - Use a real or fake logger that captures log entries (check existing test patterns for how logging is tested in this project — if no pattern exists, use NullLogger and just verify the state change)
    - Call UpdateStampFreshness(true) then UpdateStampFreshness(false)
    - Verify state changed correctly

13. `UpdateStampFreshness_NoLogOnSameValue`
    - Call UpdateStampFreshness(false) when already false
    - Verify no exception, state remains false

Note: The CreateOptions helper already exists. PreferredLeaderService constructor requires IOptions<LeaseOptions> and ILogger<PreferredLeaderService>. For these tests, PreferredNode can be null (feature disabled is fine — we're testing UpdateStampFreshness, not identity).
  </action>
  <verify>
    `dotnet test tests/SnmpCollector.Tests/SnmpCollector.Tests.csproj --filter "FullyQualifiedName~PreferredLeaderServiceTests"` — all tests pass including new ones.
  </verify>
  <done>
    UpdateStampFreshness is covered: initial false, set true, set false, transitions, idempotent same-value calls.
  </done>
</task>

<task type="auto">
  <name>Task 2: PreferredHeartbeatJob unit tests with mocked K8s client</name>
  <files>
    tests/SnmpCollector.Tests/Jobs/PreferredHeartbeatJobTests.cs
  </files>
  <action>
Create `PreferredHeartbeatJobTests.cs` in `tests/SnmpCollector.Tests/Jobs/`. This file tests the Quartz job's lease-reading logic with a mocked `IKubernetes` client.

Check what mocking library the project uses (likely Moq based on existing tests — verify by checking `.csproj` or existing test files). Use the same library.

Setup helper:
- Create a helper method that builds a `PreferredHeartbeatJob` with mocked dependencies:
  - `Mock<IKubernetes>` (or equivalent) — mock `CoordinationV1.ReadNamespacedLeaseAsync`
  - Real `PreferredLeaderService` instance (created with null PreferredNode + NullLogger)
  - `LeaseOptions` with Name="snmp-collector-leader", Namespace="default", DurationSeconds=15
  - `Mock<ILivenessVectorService>` — verify Stamp is called
  - `NullLogger<PreferredHeartbeatJob>`
- Create a mock `IJobExecutionContext` with a `JobDetail.Key.Name` of "preferred-heartbeat" and a CancellationToken

Tests to write:

1. `Execute_WithFreshLease_SetsStampFreshTrue`
   - Mock ReadNamespacedLeaseAsync to return a V1Lease with RenewTime = DateTime.UtcNow (just renewed)
   - Execute the job
   - Assert preferredLeaderService.IsPreferredStampFresh == true
   - Verify liveness.Stamp was called

2. `Execute_WithStaleLease_SetsStampFreshFalse`
   - Mock ReadNamespacedLeaseAsync to return a V1Lease with RenewTime = DateTime.UtcNow.AddSeconds(-(15 + 5 + 1)) (beyond threshold)
   - Execute the job
   - Assert IsPreferredStampFresh == false

3. `Execute_WithLeaseAtExactThreshold_SetsStampFreshTrue`
   - Mock RenewTime = DateTime.UtcNow.AddSeconds(-(15 + 5)) (exactly at threshold — age <= threshold should be true)
   - Assert IsPreferredStampFresh == true

4. `Execute_With404_SetsStampFreshFalse`
   - Mock ReadNamespacedLeaseAsync to throw HttpOperationException with 404 status
   - Execute the job
   - Assert IsPreferredStampFresh == false
   - Verify liveness.Stamp was called (finally block runs)

5. `Execute_WithTransientError_KeepsLastValue`
   - First: set IsPreferredStampFresh to true by running with a fresh lease
   - Then: mock ReadNamespacedLeaseAsync to throw a generic Exception (e.g., HttpRequestException)
   - Execute the job again
   - Assert IsPreferredStampFresh is STILL true (kept last value)

6. `Execute_WithNullRenewTimeAndNullAcquireTime_SetsStampFreshFalse`
   - Mock lease with Spec.RenewTime = null and Spec.AcquireTime = null
   - Assert IsPreferredStampFresh == false

7. `Execute_WithNullRenewTime_FallsBackToAcquireTime`
   - Mock lease with RenewTime = null, AcquireTime = DateTime.UtcNow (fresh)
   - Assert IsPreferredStampFresh == true

8. `Execute_AlwaysStampsLiveness_EvenOnError`
   - Mock ReadNamespacedLeaseAsync to throw
   - Verify liveness.Stamp("preferred-heartbeat") is called exactly once

For constructing the mock HttpOperationException with 404:
```csharp
var response = new HttpResponseMessageWrapper(new HttpResponseMessage(HttpStatusCode.NotFound), "");
var ex = new HttpOperationException("Not Found") { Response = response };
```
Check the actual `k8s.Autorest.HttpOperationException` constructor — it may need `Microsoft.Rest.HttpOperationException` or `k8s.Autorest.HttpOperationException` depending on the KubernetesClient version. Inspect the project's package version and adapt.

For constructing `V1Lease` responses:
```csharp
new V1Lease
{
    Spec = new V1LeaseSpec
    {
        RenewTime = DateTime.UtcNow,
        AcquireTime = DateTime.UtcNow.AddMinutes(-5)
    }
}
```

IMPORTANT: When mocking `IKubernetes.CoordinationV1.ReadNamespacedLeaseAsync`, note that `CoordinationV1` is a property returning `ICoordinationV1Operations`. You need to mock the property AND the method. With Moq:
```csharp
var mockCoordV1 = new Mock<ICoordinationV1Operations>();
mockCoordV1.Setup(c => c.ReadNamespacedLeaseAsync(...)).ReturnsAsync(lease);
mockKubeClient.Setup(k => k.CoordinationV1).Returns(mockCoordV1.Object);
```

Verify the exact interface names by checking `using k8s;` namespace types.
  </action>
  <verify>
    `dotnet test tests/SnmpCollector.Tests/SnmpCollector.Tests.csproj --filter "FullyQualifiedName~PreferredHeartbeatJobTests"` — all 8 new tests pass.
    `dotnet test tests/SnmpCollector.Tests/SnmpCollector.Tests.csproj` — full suite passes (487 existing + new tests).
  </verify>
  <done>
    PreferredHeartbeatJob is tested for: fresh lease, stale lease, exact threshold, 404, transient error (keep-last-value), null timestamps, acquireTime fallback, and liveness stamping. SC-4 satisfied: "IPreferredStampReader.IsPreferredStampFresh returns a real derived value, not a stub — verified by unit test with a mocked lease response."
  </done>
</task>

</tasks>

<verification>
1. `dotnet test tests/SnmpCollector.Tests/SnmpCollector.Tests.csproj` — full suite passes with 495+ tests (487 existing + ~8 new)
2. At least one test verifies: mock fresh lease -> IsPreferredStampFresh == true (SC-4)
3. At least one test verifies: mock 404 -> IsPreferredStampFresh == false (SC-2)
4. At least one test verifies: stale lease beyond DurationSeconds+5s -> false (SC-3)
5. At least one test verifies: transient error keeps last value
</verification>

<success_criteria>
- UpdateStampFreshness unit tested (initial false, set true, set false, transitions)
- PreferredHeartbeatJob tested with mocked IKubernetes for all edge cases
- SC-4 explicitly satisfied: unit test proves IsPreferredStampFresh returns real derived value from mocked lease
- Full test suite passes with zero failures
</success_criteria>

<output>
After completion, create `.planning/phases/85-preferredheartbeatservice-reader-path/85-02-SUMMARY.md`
</output>
