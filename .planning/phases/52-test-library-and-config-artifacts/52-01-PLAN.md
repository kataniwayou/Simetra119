---
phase: 52-test-library-and-config-artifacts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - deploy/k8s/snmp-collector/simetra-oid-metric-map.yaml
  - deploy/k8s/snmp-collector/simetra-oid-command-map.yaml
  - deploy/k8s/snmp-collector/simetra-devices.yaml
  - simulators/e2e-sim/e2e_simulator.py
autonomous: true

must_haves:
  truths:
    - "OID metric map contains 6 new entries mapping .999.4.x OIDs to device-semantic metric names"
    - "OID command map contains e2e_set_bypass entry pointing to .999.4.4.0"
    - "Device config E2E-SIM block has a second poll group for the 6 new .4.x metrics plus an aggregate poll group"
    - "Simulator has a command_trigger scenario that clears resolved metrics and breaches evaluate"
  artifacts:
    - path: "deploy/k8s/snmp-collector/simetra-oid-metric-map.yaml"
      provides: "6 new OID-to-metric-name mappings for .999.4.x subtree"
      contains: "e2e_port_utilization"
    - path: "deploy/k8s/snmp-collector/simetra-oid-command-map.yaml"
      provides: "e2e_set_bypass command mapping"
      contains: "e2e_set_bypass"
    - path: "deploy/k8s/snmp-collector/simetra-devices.yaml"
      provides: "E2E-SIM poll group for .4.x OIDs and aggregate poll group"
      contains: "e2e_total_util"
    - path: "simulators/e2e-sim/e2e_simulator.py"
      provides: "command_trigger scenario"
      contains: "command_trigger"
  key_links:
    - from: "simetra-oid-metric-map.yaml"
      to: "simetra-devices.yaml"
      via: "MetricNames in device poll groups must match MetricName values in OID map"
      pattern: "e2e_port_utilization"
    - from: "simetra-oid-command-map.yaml"
      to: "tenant fixture CommandName references"
      via: "CommandName must match between command map and tenant config"
      pattern: "e2e_set_bypass"
---

<objective>
Add OID metric map, command map, and device config entries for the 6 new E2E test OIDs, plus a 6th simulator scenario (command_trigger) that enables Tier 3/4 testing.

Purpose: The collector must be able to resolve .999.4.x OIDs to metric names, route the e2e_set_bypass command, and poll all 6 new OIDs. The command_trigger scenario is needed because no existing scenario simultaneously breaches the evaluate metric AND clears the resolved metrics (required to reach Tier 4).

Output: Extended K8s ConfigMaps and a new simulator scenario entry.
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-test-library-and-config-artifacts/52-RESEARCH.md

Key source files:
@deploy/k8s/snmp-collector/simetra-oid-metric-map.yaml
@deploy/k8s/snmp-collector/simetra-oid-command-map.yaml
@deploy/k8s/snmp-collector/simetra-devices.yaml
@simulators/e2e-sim/e2e_simulator.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add OID metric map, command map, and device config entries</name>
  <files>
    deploy/k8s/snmp-collector/simetra-oid-metric-map.yaml
    deploy/k8s/snmp-collector/simetra-oid-command-map.yaml
    deploy/k8s/snmp-collector/simetra-devices.yaml
  </files>
  <action>
    **simetra-oid-metric-map.yaml** -- Append 6 entries to the existing JSON array (after the last E2E-SIM entry). All OIDs MUST include the `.0` scalar instance suffix (matching all 107 existing entries):
    ```json
    { "Oid": "1.3.6.1.4.1.47477.999.4.1.0", "MetricName": "e2e_port_utilization" },
    { "Oid": "1.3.6.1.4.1.47477.999.4.2.0", "MetricName": "e2e_channel_state" },
    { "Oid": "1.3.6.1.4.1.47477.999.4.3.0", "MetricName": "e2e_bypass_status" },
    { "Oid": "1.3.6.1.4.1.47477.999.4.4.0", "MetricName": "e2e_command_response" },
    { "Oid": "1.3.6.1.4.1.47477.999.4.5.0", "MetricName": "e2e_agg_source_a" },
    { "Oid": "1.3.6.1.4.1.47477.999.4.6.0", "MetricName": "e2e_agg_source_b" }
    ```
    Place these in a visually grouped block with a comment separator `// E2E test-purpose OIDs (.999.4.x)` (JSON does not support comments but this is inside a YAML heredoc string -- check the existing file format; if comments are not used, just place them after a blank line after the existing E2E-SIM entries with the 7 original .999.1.x OIDs).

    **simetra-oid-command-map.yaml** -- Append 1 entry to the existing JSON array:
    ```json
    { "Oid": "1.3.6.1.4.1.47477.999.4.4.0", "CommandName": "e2e_set_bypass" }
    ```

    **simetra-devices.yaml** -- In the E2E-SIM device block (CommunityString: "Simetra.E2E-SIM"), add TWO new poll groups after the existing 7-metric poll group:

    Poll group 1 (individual .4.x OIDs):
    ```json
    {
      "IntervalSeconds": 10,
      "GraceMultiplier": 2.0,
      "MetricNames": [
        "e2e_port_utilization",
        "e2e_channel_state",
        "e2e_bypass_status",
        "e2e_command_response",
        "e2e_agg_source_a",
        "e2e_agg_source_b"
      ]
    }
    ```

    Poll group 2 (aggregate):
    ```json
    {
      "IntervalSeconds": 10,
      "GraceMultiplier": 2.0,
      "MetricNames": ["e2e_agg_source_a", "e2e_agg_source_b"],
      "AggregatedMetricName": "e2e_total_util",
      "Aggregator": "sum"
    }
    ```

    IMPORTANT: The existing E2E-SIM device has exactly 1 poll group with 7 MetricNames. Do NOT modify the existing poll group. Add the two new ones as additional entries in the "Polls" array.
  </action>
  <verify>
    - Validate JSON syntax in each ConfigMap: extract the JSON from each YAML file and pipe through `python -m json.tool` or `jq .`
    - Count OID map entries: should be original count + 6
    - Count command map entries: should be original count (12) + 1 = 13
    - Verify E2E-SIM device has 3 poll groups total
    - Verify all 6 new OIDs end in `.0`
  </verify>
  <done>
    - simetra-oid-metric-map.yaml contains 6 new entries with .999.4.x.0 OIDs mapped to e2e_port_utilization, e2e_channel_state, e2e_bypass_status, e2e_command_response, e2e_agg_source_a, e2e_agg_source_b
    - simetra-oid-command-map.yaml contains e2e_set_bypass entry for .999.4.4.0
    - simetra-devices.yaml E2E-SIM block has 3 poll groups: original 7-metric group, new 6-metric group, new aggregate group producing e2e_total_util
    - All JSON is syntactically valid
  </done>
</task>

<task type="auto">
  <name>Task 2: Add command_trigger simulator scenario</name>
  <files>
    simulators/e2e-sim/e2e_simulator.py
  </files>
  <action>
    Add a 6th scenario to the SCENARIOS dict in e2e_simulator.py. This scenario must simultaneously:
    - Breach the evaluate metric: `.4.1 = 90` (above Max threshold of 80.0)
    - Clear both resolved metrics: `.4.2 = 2` and `.4.3 = 2` (above Min threshold of 1.0, so NOT violated)

    Add this entry after the "stale" scenario:
    ```python
    "command_trigger": _make_scenario({
                           f"{E2E_PREFIX}.4.1": 90,
                           f"{E2E_PREFIX}.4.2": 2,
                           f"{E2E_PREFIX}.4.3": 2,
                       }),
    ```

    This is the ONLY scenario that allows the 4-tier evaluation to reach Tier 4 (command dispatch). Without it, the default resolved values (0) are always violated (below Min 1.0), causing Tier 2 ConfirmedBad to block progression.

    Do NOT modify any existing scenarios or OID definitions.
  </action>
  <verify>
    - `python -c "exec(open('simulators/e2e-sim/e2e_simulator.py').read()); print(SCENARIOS.keys())"` shows 6 scenarios including command_trigger
    - `python -c "exec(open('simulators/e2e-sim/e2e_simulator.py').read()); s=SCENARIOS['command_trigger']; print(s['1.3.6.1.4.1.47477.999.4.1'], s['1.3.6.1.4.1.47477.999.4.2'], s['1.3.6.1.4.1.47477.999.4.3'])"` prints `90 2 2`
    - Existing 5 scenario names still present and unchanged
  </verify>
  <done>
    - e2e_simulator.py SCENARIOS dict has 6 entries: default, threshold_breach, threshold_clear, bypass_active, stale, command_trigger
    - command_trigger sets .4.1=90 (evaluate breached), .4.2=2 (resolved clear), .4.3=2 (resolved clear)
    - All other scenarios unchanged
  </done>
</task>

</tasks>

<verification>
- All 3 K8s ConfigMap YAML files have valid JSON payloads
- Simulator has 6 scenarios including command_trigger
- No existing entries modified or removed in any file
- OID .0 suffix present on all 6 new metric map entries and the 1 command map entry
</verification>

<success_criteria>
CFG-01 (OID metric map entries), CFG-02 (command map entry), CFG-03 (device config) requirements satisfied. Simulator has the command_trigger scenario needed for Tier 4 testing in Phase 53.
</success_criteria>

<output>
After completion, create `.planning/phases/52-test-library-and-config-artifacts/52-01-SUMMARY.md`
</output>
