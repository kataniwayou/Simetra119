---
phase: 52-test-library-and-config-artifacts
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/fixtures/tenant-cfg01-single.yaml
  - tests/e2e/fixtures/tenant-cfg02-two-same-prio.yaml
  - tests/e2e/fixtures/tenant-cfg03-two-diff-prio.yaml
  - tests/e2e/fixtures/tenant-cfg04-aggregate.yaml
autonomous: true

must_haves:
  truths:
    - "Each fixture file is a valid K8s ConfigMap YAML targeting simetra-tenants in the simetra namespace"
    - "Each fixture uses distinct tenant IDs (no ID reuse across fixtures)"
    - "All fixtures use GraceMultiplier >= 2.0 on time-series metrics"
    - "Resolved metrics have Min threshold 1.0 so default value 0 is violated (idle = ConfirmedBad)"
    - "Evaluate metric has Max threshold 80.0 so default value 0 is not violated"
    - "CFG-04 aggregate fixture uses e2e_total_util as the Evaluate metric"
  artifacts:
    - path: "tests/e2e/fixtures/tenant-cfg01-single.yaml"
      provides: "Single tenant at priority 1 with 1 evaluate, 2 resolved, 1 command"
      contains: "e2e_port_utilization"
    - path: "tests/e2e/fixtures/tenant-cfg02-two-same-prio.yaml"
      provides: "Two tenants at same priority (1) with distinct IDs"
      contains: "e2e-tenant-B"
    - path: "tests/e2e/fixtures/tenant-cfg03-two-diff-prio.yaml"
      provides: "Two tenants at different priorities (1 and 2)"
      contains: "Priority.*2"
    - path: "tests/e2e/fixtures/tenant-cfg04-aggregate.yaml"
      provides: "Single tenant using aggregate metric e2e_total_util as evaluate"
      contains: "e2e_total_util"
  key_links:
    - from: "tenant fixtures"
      to: "simetra-oid-metric-map.yaml"
      via: "MetricName values must exist in OID map for collector resolution"
      pattern: "e2e_port_utilization|e2e_channel_state|e2e_bypass_status"
    - from: "tenant fixtures"
      to: "simetra-oid-command-map.yaml"
      via: "CommandName must exist in command map for dispatch"
      pattern: "e2e_set_bypass"
---

<objective>
Create 4 tenant fixture YAML files for the E2E snapshot scenarios, covering single-tenant, two-tenant same-priority, two-tenant different-priority, and aggregate-metric topologies.

Purpose: Scenario scripts in Phase 53-55 apply these fixtures via `kubectl apply -f` to configure tenant evaluation before driving the simulator. Each fixture represents a distinct test topology that exercises different branches of the 4-tier evaluation.

Output: 4 fixture YAML files in tests/e2e/fixtures/.
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-test-library-and-config-artifacts/52-RESEARCH.md
@.planning/phases/52-test-library-and-config-artifacts/52-CONTEXT.md

Key source files:
@deploy/k8s/snmp-collector/simetra-tenants.yaml
@tests/e2e/scenarios/28-tenantvector-routing.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create single-tenant and aggregate fixture files</name>
  <files>
    tests/e2e/fixtures/tenant-cfg01-single.yaml
    tests/e2e/fixtures/tenant-cfg04-aggregate.yaml
  </files>
  <action>
    **tenant-cfg01-single.yaml** (CFG-04: single tenant, priority 1, 1 evaluate + 2 resolved + 1 command)

    Create a ConfigMap YAML with this structure:
    ```yaml
    apiVersion: v1
    kind: ConfigMap
    metadata:
      name: simetra-tenants
      namespace: simetra
    data:
      tenants.json: |
        [JSON array]
    ```

    Single tenant definition:
    - **Id**: "e2e-tenant-A" (explicit, distinct)
    - **Priority**: 1
    - **SuppressionWindowSeconds**: 10 (short for testability)
    - **Metrics** (3 entries):
      1. `e2e_port_utilization` -- Role: "Evaluate", Threshold: `{ "Max": 80.0 }`, TimeSeriesSize: 3
         - Ip: "e2e-simulator.simetra.svc.cluster.local", Port: 161
         - Default value 0 is NOT violated (0 < 80). Breached when simulator returns 90.
      2. `e2e_channel_state` -- Role: "Resolved", Threshold: `{ "Min": 1.0 }`
         - Ip: "e2e-simulator.simetra.svc.cluster.local", Port: 161
         - Default value 0 IS violated (0 < 1.0). Cleared when simulator returns >= 1.
      3. `e2e_bypass_status` -- Role: "Resolved", Threshold: `{ "Min": 1.0 }`
         - Ip: "e2e-simulator.simetra.svc.cluster.local", Port: 161
         - Default value 0 IS violated (0 < 1.0). Cleared when simulator returns >= 1.
    - **Commands** (1 entry):
      1. `e2e_set_bypass` -- Value: "0", ValueType: "Integer32"
         - Ip: "e2e-simulator.simetra.svc.cluster.local", Port: 161

    **Threshold design rationale (from CONTEXT.md locked decision):**
    - Resolved metrics violated by default (idle = Tier 2 ConfirmedBad)
    - Evaluate metric NOT violated by default (avoids false command triggers)
    - To reach Tier 4 (command dispatch), scenario must use `command_trigger` which sets .4.1=90, .4.2=2, .4.3=2

    ---

    **tenant-cfg04-aggregate.yaml** (CFG-07: aggregate metric as evaluate)

    Same ConfigMap structure. Single tenant:
    - **Id**: "e2e-tenant-agg" (distinct)
    - **Priority**: 1
    - **SuppressionWindowSeconds**: 10
    - **Metrics** (3 entries):
      1. `e2e_total_util` -- Role: "Evaluate", Threshold: `{ "Max": 80.0 }`, TimeSeriesSize: 3
         - Ip: "e2e-simulator.simetra.svc.cluster.local", Port: 161
         - This is the AGGREGATE metric (sum of e2e_agg_source_a + e2e_agg_source_b). Source: Synthetic pipeline. Default sum = 0, not violated.
      2. `e2e_channel_state` -- Role: "Resolved", Threshold: `{ "Min": 1.0 }`
         - Same resolved pattern as cfg01
      3. `e2e_bypass_status` -- Role: "Resolved", Threshold: `{ "Min": 1.0 }`
         - Same resolved pattern as cfg01
    - **Commands** (1 entry):
      1. `e2e_set_bypass` -- same as cfg01

    **Note:** The tenant config references e2e_total_util which is the AggregatedMetricName from the device config aggregate poll group (Plan 01). The collector resolves this via Synthetic source, not via direct OID poll.
  </action>
  <verify>
    - Both YAML files parse: `python -c "import yaml; yaml.safe_load(open('tests/e2e/fixtures/tenant-cfg01-single.yaml'))"` succeeds
    - Both contain `name: simetra-tenants` and `namespace: simetra`
    - cfg01 has exactly 1 tenant with Id "e2e-tenant-A", 3 metrics, 1 command
    - cfg04 has exactly 1 tenant with Id "e2e-tenant-agg", 3 metrics (including e2e_total_util), 1 command
    - All Resolved metrics have `"Min": 1.0` threshold
    - Evaluate metrics have `"Max": 80.0` threshold
    - SuppressionWindowSeconds = 10 in both
  </verify>
  <done>
    - tenant-cfg01-single.yaml: valid ConfigMap with 1 tenant (e2e-tenant-A), 1 evaluate (e2e_port_utilization, Max 80), 2 resolved (Min 1.0), 1 command (e2e_set_bypass)
    - tenant-cfg04-aggregate.yaml: valid ConfigMap with 1 tenant (e2e-tenant-agg), 1 aggregate evaluate (e2e_total_util, Max 80), 2 resolved (Min 1.0), 1 command
    - Both use SuppressionWindowSeconds: 10 and GraceMultiplier >= 2.0
  </done>
</task>

<task type="auto">
  <name>Task 2: Create multi-tenant fixture files</name>
  <files>
    tests/e2e/fixtures/tenant-cfg02-two-same-prio.yaml
    tests/e2e/fixtures/tenant-cfg03-two-diff-prio.yaml
  </files>
  <action>
    **tenant-cfg02-two-same-prio.yaml** (CFG-05: two tenants, same priority)

    ConfigMap with 2 tenants at the SAME priority level:
    - Tenant 1:
      - **Id**: "e2e-tenant-A"
      - **Priority**: 1
      - **SuppressionWindowSeconds**: 10
      - Metrics: same 3 as cfg01 (e2e_port_utilization Evaluate Max:80, e2e_channel_state Resolved Min:1.0, e2e_bypass_status Resolved Min:1.0)
      - Commands: same as cfg01 (e2e_set_bypass)
    - Tenant 2:
      - **Id**: "e2e-tenant-B" (DISTINCT from tenant-A)
      - **Priority**: 1 (SAME priority as tenant-A)
      - **SuppressionWindowSeconds**: 10
      - Metrics: same 3 OIDs but under tenant-B's identity (same Ip/Port/MetricName/Role/Threshold -- multi-tenant means same OIDs, different tenant IDs per CONTEXT.md decision)
      - Commands: same as tenant-A (e2e_set_bypass)

    Both tenants monitor the same OIDs. A scenario switch affects both equally. The test validates that SnapshotJob evaluates them independently (per-tenant tier results).

    ---

    **tenant-cfg03-two-diff-prio.yaml** (CFG-06: two tenants, different priorities)

    ConfigMap with 2 tenants at DIFFERENT priority levels:
    - Tenant 1:
      - **Id**: "e2e-tenant-P1"
      - **Priority**: 1
      - **SuppressionWindowSeconds**: 10
      - Metrics: same 3 OIDs (e2e_port_utilization Evaluate, e2e_channel_state Resolved, e2e_bypass_status Resolved)
      - Commands: e2e_set_bypass
    - Tenant 2:
      - **Id**: "e2e-tenant-P2"
      - **Priority**: 2 (DIFFERENT priority -- group 2 is gated by group 1's advance gate)
      - **SuppressionWindowSeconds**: 10
      - Metrics: same 3 OIDs
      - Commands: e2e_set_bypass

    The advance gate test: if any tenant in priority group 1 is not Healthy (i.e., is Commanded or ConfirmedBad), group 2 tenants should NOT be evaluated.

    **Important design notes:**
    - Use distinct tenant IDs per fixture to prevent suppression cache bleed (per STATE.md key fact)
    - GraceMultiplier on TimeSeriesSize metrics must be >= 2.0 (per success criteria)
    - TimeSeriesSize: 3 on evaluate metrics (same as cfg01 for consistency)
    - All Ip fields use the full DNS name: "e2e-simulator.simetra.svc.cluster.local"
  </action>
  <verify>
    - Both YAML files parse: `python -c "import yaml; yaml.safe_load(open('tests/e2e/fixtures/tenant-cfg02-two-same-prio.yaml'))"` succeeds for both
    - cfg02 has exactly 2 tenants with Ids "e2e-tenant-A" and "e2e-tenant-B", both Priority 1
    - cfg03 has exactly 2 tenants with Ids "e2e-tenant-P1" and "e2e-tenant-P2", Priority 1 and 2 respectively
    - All tenant IDs across all 4 fixture files are distinct within each file
    - All resolved metrics have Min:1.0, all evaluate metrics have Max:80.0
    - SuppressionWindowSeconds = 10 in all tenants
  </verify>
  <done>
    - tenant-cfg02-two-same-prio.yaml: 2 tenants (e2e-tenant-A, e2e-tenant-B) both Priority 1, same OIDs, distinct IDs
    - tenant-cfg03-two-diff-prio.yaml: 2 tenants (e2e-tenant-P1, e2e-tenant-P2) at Priority 1 and 2, same OIDs, distinct IDs
    - All fixtures use consistent threshold design: resolved Min:1.0 (violated by default), evaluate Max:80.0 (not violated by default)
    - All fixtures use SuppressionWindowSeconds: 10 and GraceMultiplier >= 2.0
  </done>
</task>

</tasks>

<verification>
- All 4 fixture YAML files parse as valid YAML
- JSON payloads inside each ConfigMap parse as valid JSON arrays
- Tenant IDs are distinct within each fixture
- Threshold design is consistent: resolved Min:1.0, evaluate Max:80.0
- All fixtures target simetra-tenants ConfigMap in simetra namespace
- SuppressionWindowSeconds = 10 in all tenants
- TimeSeriesSize: 3 on evaluate metrics, GraceMultiplier >= 2.0
</verification>

<success_criteria>
CFG-04 (single tenant), CFG-05 (two same priority), CFG-06 (two different priority), and CFG-07 (aggregate evaluate) requirements satisfied. All 4 tenant fixture YAML files exist with distinct tenant IDs, correct thresholds, and proper ConfigMap structure.
</success_criteria>

<output>
After completion, create `.planning/phases/52-test-library-and-config-artifacts/52-03-SUMMARY.md`
</output>
