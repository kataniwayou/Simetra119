---
phase: 49-observability-and-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/SnmpCollector/Services/CommandWorkerService.cs
  - tests/SnmpCollector.Tests/Services/CommandWorkerServiceTests.cs
  - deploy/grafana/dashboards/simetra-operations.json
autonomous: true

must_haves:
  truths:
    - "Pod logs for a successful SET contain an Information-level entry with device name, command name, and round-trip duration in milliseconds"
    - "Pod logs for a failed SET contain a Warning-level entry with device name, command name, error, and round-trip duration in milliseconds"
    - "Operations dashboard shows three new panels in the Pipeline Counters section — Command Sent, Command Failed, Command Suppressed — each w=8 at y=39"
    - ".NET Runtime row and its panels shift down by 8 units to accommodate the new command panels"
  artifacts:
    - path: "src/SnmpCollector/Services/CommandWorkerService.cs"
      provides: "Stopwatch-based duration logging on SET success and failure"
      contains: "Stopwatch"
    - path: "tests/SnmpCollector.Tests/Services/CommandWorkerServiceTests.cs"
      provides: "Tests verifying duration log output"
    - path: "deploy/grafana/dashboards/simetra-operations.json"
      provides: "3 command panels at y=39 + shifted .NET Runtime row"
      contains: "snmp_command_sent_total"
  key_links:
    - from: "CommandWorkerService.ExecuteCommandAsync"
      to: "ILogger"
      via: "Stopwatch around SetAsync"
      pattern: "Stopwatch\\.StartNew|sw\\.Stop|ElapsedMilliseconds"
    - from: "simetra-operations.json"
      to: "PipelineMetricService counters"
      via: "PromQL rate queries"
      pattern: "snmp_command_(sent|failed|suppressed)_total"
---

<objective>
Add command execution observability: Stopwatch-based round-trip duration logs in CommandWorkerService (Information for success, Warning for failure) and three snmp.command.* dashboard panels in the operations dashboard.

Purpose: SNAP-16 (command execution logs) and SNAP-17 (dashboard panels) — operators can see command activity in Grafana and diagnose SET latency/failures from pod logs.
Output: Modified CommandWorkerService with duration logging, updated tests, updated dashboard JSON with 3 new panels.
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-observability-and-dashboard/49-CONTEXT.md
@src/SnmpCollector/Services/CommandWorkerService.cs
@src/SnmpCollector/Jobs/MetricPollJob.cs
@deploy/grafana/dashboards/simetra-operations.json
@tests/SnmpCollector.Tests/Services/CommandWorkerServiceTests.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Stopwatch duration logging to CommandWorkerService</name>
  <files>
    src/SnmpCollector/Services/CommandWorkerService.cs
    tests/SnmpCollector.Tests/Services/CommandWorkerServiceTests.cs
  </files>
  <action>
In `CommandWorkerService.ExecuteCommandAsync`, add `System.Diagnostics` using and wrap the SetAsync call with a Stopwatch, following the same pattern as MetricPollJob (lines 95-102):

1. Before the `try` block around SetAsync (line 127), add `var sw = Stopwatch.StartNew();`
2. After the successful SetAsync call (line 130), add `sw.Stop();`
3. After the success counter increment (line 162, end of method), add an Information log:
   ```
   _logger.LogInformation(
       "Command {CommandName} completed for {DeviceName} in {DurationMs:F1}ms",
       req.CommandName, req.DeviceName, sw.Elapsed.TotalMilliseconds);
   ```
4. In the timeout catch block (line 133-139), stop the Stopwatch and change the existing Warning log to include duration:
   ```
   sw.Stop();
   _logger.LogWarning(
       "Command {CommandName} timed out for {DeviceName} after {DurationMs:F1}ms",
       req.CommandName, req.DeviceName, sw.Elapsed.TotalMilliseconds);
   ```
5. In the outer catch in ExecuteAsync (line 80-83), the Stopwatch is not in scope — this is fine. The outer catch handles unexpected exceptions (OID resolution failure, device lookup failure) that happen BEFORE SetAsync. No duration needed for those pre-SET failures.

For the test file, add 2 tests:
- `Successful_SET_logs_Information_with_duration`: Verify that after a successful SET, an Information log entry is produced containing the command name, device name, and a duration value. Use a test logger that captures log entries and assert on log level + message template parameters.
- `Failed_SET_logs_Warning_with_duration`: Mock SetAsync to throw, verify a Warning log entry with command name, device name, and duration.

Follow existing test patterns in CommandWorkerServiceTests (same helpers, same mock setup).
  </action>
  <verify>
Run `dotnet test` — all existing tests pass plus the 2 new duration-logging tests.
  </verify>
  <done>
CommandWorkerService logs Information with device name, command name, and round-trip ms on success. Logs Warning with device name, command name, error, and round-trip ms on failure/timeout. Two new tests confirm the behavior.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add 3 snmp.command.* panels to operations dashboard</name>
  <files>
    deploy/grafana/dashboards/simetra-operations.json
  </files>
  <action>
Add three time-series panels to the Pipeline Counters section of `deploy/grafana/dashboards/simetra-operations.json`. These panels visualize the counters that already exist in PipelineMetricService (Phase 46-03).

**Panel placement:**
- All three panels at y=39 (after Row 4: Tenant Vector Routed + Aggregated Computed at y=31, h=8)
- Each panel: h=8, w=8
- Command Sent: x=0, Command Failed: x=8, Command Suppressed: x=16

**Panel IDs:** Use 24, 25, 26 (highest existing is 23).

**For each panel, clone the structure from the Polls Executed panel (id=8) and update:**

1. **Command Sent** (id=24):
   - gridPos: {h:8, w:8, x:0, y:39}
   - expr: `sum by (k8s_pod_name) (rate(snmp_command_sent_total{k8s_pod_name=~\"$pod\"}[$__rate_interval]))`
   - title: "Command Sent"
   - description: "Rate of SNMP SET commands successfully executed by CommandWorkerService. Source: snmp_command_sent_total."

2. **Command Failed** (id=25):
   - gridPos: {h:8, w:8, x:8, y:39}
   - expr: `sum by (k8s_pod_name) (rate(snmp_command_failed_total{k8s_pod_name=~\"$pod\"}[$__rate_interval]))`
   - title: "Command Failed"
   - description: "Rate of SNMP SET commands that failed (timeout, SNMP error, or device not found). Source: snmp_command_failed_total."

3. **Command Suppressed** (id=26):
   - gridPos: {h:8, w:8, x:16, y:39}
   - expr: `sum by (k8s_pod_name) (rate(snmp_command_suppressed_total{k8s_pod_name=~\"$pod\"}[$__rate_interval]))`
   - title: "Command Suppressed"
   - description: "Rate of SNMP SET commands skipped by the suppression cache. Source: snmp_command_suppressed_total."

**Shift .NET Runtime row and panels down by 8:**
- .NET Runtime row: y=39 -> y=47
- GC Collections Rate (id=16): y=40 -> y=48
- CPU Time (id=17): y=40 -> y=48
- Process Working Set (id=18): y=40 -> y=48
- Exceptions (id=19): y=48 -> y=56
- Thread Pool Threads (id=20): y=48 -> y=56
- Thread Pool Queue Length (id=21): y=48 -> y=56

All new panels must use:
- datasource: `{"type": "prometheus", "uid": "${DS_PROMETHEUS}"}`
- legendFormat: `{{k8s_pod_name}}`
- unit: "ops"
- Same fieldConfig as Polls Executed panel (palette-classic, line, fillOpacity 10, lineWidth 2)
- refId: "A", range: true, instant: false

Validate the JSON is syntactically valid after editing by running `python -c "import json; json.load(open('deploy/grafana/dashboards/simetra-operations.json'))"`.
  </action>
  <verify>
JSON is valid (python json.load succeeds). Grep for `snmp_command_sent_total`, `snmp_command_failed_total`, `snmp_command_suppressed_total` — all three present. Grep for `"y": 47` to confirm .NET Runtime row shifted. Count total panels — should be 23 (was 20 panels + 3 new).
  </verify>
  <done>
Operations dashboard contains three new command panels at y=39 (sent | failed | suppressed, w=8 each), .NET Runtime row shifted to y=47, all JSON valid.
  </done>
</task>

</tasks>

<verification>
1. `dotnet test` — all tests pass (existing + 2 new duration-logging tests)
2. `python -c "import json; json.load(open('deploy/grafana/dashboards/simetra-operations.json'))"` — valid JSON
3. Grep dashboard JSON for `snmp_command_sent_total`, `snmp_command_failed_total`, `snmp_command_suppressed_total` — all present
4. Grep CommandWorkerService.cs for `Stopwatch` — present
5. Grep CommandWorkerService.cs for `DurationMs` — present in log templates
</verification>

<success_criteria>
- SNAP-16: CommandWorkerService logs Information on SET success with device, command, duration; logs Warning on SET failure/timeout with device, command, error, duration
- SNAP-17: Operations dashboard has 3 command panels (sent, failed, suppressed) at y=39, w=8 each, with rate() PromQL queries matching existing panel conventions
- All tests green
- Dashboard JSON valid
</success_criteria>

<output>
After completion, create `.planning/phases/49-observability-and-dashboard/49-01-SUMMARY.md`
</output>
