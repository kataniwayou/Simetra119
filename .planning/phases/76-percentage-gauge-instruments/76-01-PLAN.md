---
phase: 76-percentage-gauge-instruments
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/SnmpCollector/Telemetry/ITenantMetricService.cs
  - src/SnmpCollector/Telemetry/TenantMetricService.cs
  - tests/SnmpCollector.Tests/Telemetry/TenantMetricServiceTests.cs
autonomous: true

must_haves:
  truths:
    - "ITenantMetricService exposes 6 RecordXxxPercent methods accepting (tenantId, priority, double percent); no Increment methods remain"
    - "TenantMetricService registers 6 Gauge<double> instruments with correct OTel names; no Counter<long> instruments remain"
    - "tenant.state is renamed to tenant.evaluation.state in the service; RecordTenantState method signature unchanged"
    - "tenant.evaluation.duration.milliseconds histogram is unchanged"
    - "All unit tests pass asserting gauge values, instrument names, and tag correctness"
    - "Zero-percent edge case: RecordXxxPercent(tenantId, priority, 0.0) records 0.0 without error"
  artifacts:
    - path: "src/SnmpCollector/Telemetry/ITenantMetricService.cs"
      provides: "6 RecordXxxPercent method signatures + unchanged RecordTenantState + RecordEvaluationDuration"
      contains: "RecordMetricStalePercent"
    - path: "src/SnmpCollector/Telemetry/TenantMetricService.cs"
      provides: "6 Gauge<double> instruments + renamed tenant.evaluation.state gauge + unchanged histogram"
      contains: "tenant.metric.stale.percent"
    - path: "tests/SnmpCollector.Tests/Telemetry/TenantMetricServiceTests.cs"
      provides: "8 unit tests covering all instruments"
      contains: "tenant.metric.stale.percent"
  key_links:
    - from: "TenantMetricService.cs"
      to: "ITenantMetricService.cs"
      via: "implements interface"
      pattern: "class TenantMetricService : ITenantMetricService"
    - from: "TenantMetricServiceTests.cs"
      to: "TenantMetricService.cs"
      via: "direct instantiation in test constructor"
      pattern: "new TenantMetricService"
---

<objective>
Replace 6 counter instruments with 6 percentage gauge instruments in TenantMetricService, rename tenant.state to tenant.evaluation.state, update the interface, and rewrite unit tests to verify the new gauge API.

Purpose: Transition from per-holder counter increments to single-call percentage gauges, enabling the gather-then-decide evaluation flow in Phase 77. The resolved gauge measures violated holders (higher = worse), consistent with evaluate direction.

Output: Updated ITenantMetricService interface, TenantMetricService implementation, and TenantMetricServiceTests — all passing.
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/76-percentage-gauge-instruments/76-CONTEXT.md
@.planning/phases/76-percentage-gauge-instruments/76-RESEARCH.md
@src/SnmpCollector/Telemetry/ITenantMetricService.cs
@src/SnmpCollector/Telemetry/TenantMetricService.cs
@tests/SnmpCollector.Tests/Telemetry/TenantMetricServiceTests.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace interface and implementation — counters to percentage gauges</name>
  <files>
    src/SnmpCollector/Telemetry/ITenantMetricService.cs
    src/SnmpCollector/Telemetry/TenantMetricService.cs
  </files>
  <action>
**ITenantMetricService.cs — complete rewrite:**

Remove all 6 Increment methods. Add 6 RecordXxxPercent methods that accept pre-computed percentage values. Keep RecordTenantState and RecordEvaluationDuration unchanged.

New method signatures (all accept tenantId, priority, and a double percent value):
- `void RecordMetricStalePercent(string tenantId, int priority, double percent);`
- `void RecordMetricResolvedPercent(string tenantId, int priority, double percent);`
- `void RecordMetricEvaluatePercent(string tenantId, int priority, double percent);`
- `void RecordCommandDispatchedPercent(string tenantId, int priority, double percent);`
- `void RecordCommandFailedPercent(string tenantId, int priority, double percent);`
- `void RecordCommandSuppressedPercent(string tenantId, int priority, double percent);`

Update the interface XML doc comment to say "8 instruments" still (6 percentage gauges + state gauge + duration histogram). Update each method's XML doc to describe the gauge purpose (e.g., "Record the stale metric percentage for the given tenant").

**TenantMetricService.cs — complete rewrite of instrument fields and methods:**

1. Replace 6 `Counter<long>` fields with 6 `Gauge<double>` fields:
   - `_metricStalePercent` = `_meter.CreateGauge<double>("tenant.metric.stale.percent")`
   - `_metricResolvedPercent` = `_meter.CreateGauge<double>("tenant.metric.resolved.percent")`
   - `_metricEvaluatePercent` = `_meter.CreateGauge<double>("tenant.metric.evaluate.percent")`
   - `_commandDispatchedPercent` = `_meter.CreateGauge<double>("tenant.command.dispatched.percent")`
   - `_commandFailedPercent` = `_meter.CreateGauge<double>("tenant.command.failed.percent")`
   - `_commandSuppressedPercent` = `_meter.CreateGauge<double>("tenant.command.suppressed.percent")`

2. Rename `_tenantState` instrument from `"tenant.state"` to `"tenant.evaluation.state"`. Keep field name as `_tenantEvaluationState` (or `_tenantState` — your choice, but the OTel instrument name string MUST be `"tenant.evaluation.state"`).

3. Keep `_evaluationDuration` histogram completely unchanged.

4. Replace 6 Increment method bodies with 6 RecordXxxPercent method bodies. Each records the `percent` double value with tenant_id and priority tags via `gauge.Record(percent, new TagList { ... })`. Follow the exact same TagList pattern used by the existing RecordTenantState method.

5. Update the class XML doc comment: "8 instruments" stays, but describe them as "6 percentage gauges, 1 state gauge, 1 duration histogram" instead of "6 counters, 1 gauge, 1 histogram".

6. Update field comments to describe what each gauge measures (e.g., "Percentage of stale metric slots for the tenant (0.0-100.0)").

**Critical:** Do NOT change RecordTenantState method signature — it still accepts TenantState enum. Only the instrument name changes. Do NOT change RecordEvaluationDuration at all.
  </action>
  <verify>
Run `dotnet build src/SnmpCollector/SnmpCollector.csproj` — must compile cleanly. The build WILL produce errors in SnapshotJob.cs and CommandWorkerService.cs because they call the old Increment methods — that is EXPECTED and correct. Those callers are updated in Phase 77. Verify the build errors are ONLY in SnapshotJob.cs and CommandWorkerService.cs referencing removed Increment methods — no other errors.

Alternative: run `dotnet build tests/SnmpCollector.Tests/SnmpCollector.Tests.csproj` which will show the same caller errors. The test project itself should compile once the old test methods are updated (Task 2).
  </verify>
  <done>
- ITenantMetricService has exactly 8 methods: 6 RecordXxxPercent + RecordTenantState + RecordEvaluationDuration
- TenantMetricService has 6 Gauge&lt;double&gt; fields and zero Counter&lt;long&gt; fields
- Instrument name "tenant.evaluation.state" replaces "tenant.state"
- Instrument name "tenant.evaluation.duration.milliseconds" is unchanged
- Build errors exist ONLY in SnapshotJob.cs and CommandWorkerService.cs (expected — Phase 77 scope)
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite unit tests for percentage gauge API</name>
  <files>
    tests/SnmpCollector.Tests/Telemetry/TenantMetricServiceTests.cs
  </files>
  <action>
**Rewrite all 8 test methods** in TenantMetricServiceTests.cs to verify the new gauge instruments.

Keep the test class structure identical: same constructor (ServiceCollection + MeterListener), same Dispose, same `_doubleMeasurements` list. The `_measurements` list (for long values) can be removed entirely since no Counter&lt;long&gt; instruments remain — all 8 instruments now emit double values (6 gauges + 1 gauge + 1 histogram).

**Tests 1-6: Replace counter tests with gauge tests.**

For each of the 6 percentage gauges, write a test that:
1. Calls the corresponding RecordXxxPercent method with a known percentage value (e.g., 50.0, 75.0, 33.33 — use distinct values per test for clarity)
2. Asserts the measurement appears in `_doubleMeasurements` with the correct OTel instrument name
3. Asserts the recorded value matches the input percentage
4. Asserts tenant_id and priority tags are present and correct
5. Asserts device_name, host_name, pod_name tags are NOT present (same negative assertions as current tests)

Test name and instrument name mapping:
- `RecordMetricStalePercent_RecordsWithTenantIdAndPriorityTags` -> `"tenant.metric.stale.percent"`
- `RecordMetricResolvedPercent_RecordsWithTenantIdAndPriorityTags` -> `"tenant.metric.resolved.percent"`
- `RecordMetricEvaluatePercent_RecordsWithTenantIdAndPriorityTags` -> `"tenant.metric.evaluate.percent"`
- `RecordCommandDispatchedPercent_RecordsWithTenantIdAndPriorityTags` -> `"tenant.command.dispatched.percent"`
- `RecordCommandFailedPercent_RecordsWithTenantIdAndPriorityTags` -> `"tenant.command.failed.percent"`
- `RecordCommandSuppressedPercent_RecordsWithTenantIdAndPriorityTags` -> `"tenant.command.suppressed.percent"`

**Test 7: Update RecordTenantState test for renamed instrument.**

Change the assertion from `m.InstrumentName == "tenant.state"` to `m.InstrumentName == "tenant.evaluation.state"`. Everything else in this test stays the same (TenantState.Unresolved = 3.0, same tags).

**Test 8: RecordEvaluationDuration test — completely unchanged.**

Already asserts on `"tenant.evaluation.duration.milliseconds"` with correct value and tags. Do not modify.

**Additional test: Zero percent edge case.**

Add a 9th test: `RecordMetricStalePercent_ZeroPercent_RecordsZero` that calls `RecordMetricStalePercent("tenant-a", 1, 0.0)` and asserts value is 0.0. This covers the zero-denominator edge case from CONTEXT.md.

**Update class XML doc:** Change "8 instruments" to "8 instruments (6 percentage gauges, 1 state gauge, 1 duration histogram)" and update the summary to mention gauge API instead of counter API.
  </action>
  <verify>
Run `dotnet test tests/SnmpCollector.Tests/SnmpCollector.Tests.csproj --filter "FullyQualifiedName~TenantMetricServiceTests"` — all 9 tests must pass. If the full test project fails to compile due to SnapshotJob caller errors, use `--filter` to run only TenantMetricServiceTests.

Verify test count: `dotnet test ... --list-tests` should show exactly 9 test methods.
  </verify>
  <done>
- 9 unit tests exist and pass: 6 percentage gauge tests + 1 renamed state gauge test + 1 unchanged duration test + 1 zero-percent edge case test
- All 6 gauge tests assert on correct OTel instrument names (tenant.metric.stale.percent, etc.)
- State gauge test asserts on renamed "tenant.evaluation.state" instrument name
- No references to old counter instrument names (tenant.tier1.stale, etc.) remain in test file
- No `_measurements` (long) list usage remains — all assertions use `_doubleMeasurements`
  </done>
</task>

</tasks>

<verification>
1. `dotnet test tests/SnmpCollector.Tests/SnmpCollector.Tests.csproj --filter "FullyQualifiedName~TenantMetricServiceTests"` — 9/9 pass
2. Grep ITenantMetricService.cs for "Increment" — zero matches
3. Grep TenantMetricService.cs for "Counter" — zero matches
4. Grep TenantMetricService.cs for "tenant.evaluation.state" — one match (instrument creation)
5. Grep TenantMetricService.cs for "tenant.state" — zero matches (old name removed; "tenant.evaluation.state" contains "tenant." but NOT bare "tenant.state")
6. Grep TenantMetricServiceTests.cs for "tenant.evaluation.state" — one match (test assertion)
7. Grep TenantMetricServiceTests.cs for "tenant.metric.stale.percent" — at least one match
</verification>

<success_criteria>
- ITenantMetricService has 8 methods: 6 RecordXxxPercent + RecordTenantState + RecordEvaluationDuration
- TenantMetricService has 8 instruments: 6 Gauge<double> + 1 Gauge<double> (state) + 1 Histogram<double> (duration); zero Counter<long> instruments
- All 6 percentage gauge OTel names match CONTEXT.md: tenant.metric.stale.percent, tenant.metric.resolved.percent, tenant.metric.evaluate.percent, tenant.command.dispatched.percent, tenant.command.failed.percent, tenant.command.suppressed.percent
- tenant.state renamed to tenant.evaluation.state
- tenant.evaluation.duration.milliseconds unchanged
- 9 unit tests pass
- Requirements satisfied: PGA-01 through PGA-06, RMD-01 (resolved direction verifiable via naming — caller computes violated count), UCH-01, UCH-02, CLN-01, CLN-02, UTT-02
</success_criteria>

<output>
After completion, create `.planning/phases/76-percentage-gauge-instruments/76-01-SUMMARY.md`
</output>
