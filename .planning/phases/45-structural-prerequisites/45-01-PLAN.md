---
phase: 45-structural-prerequisites
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/SnmpCollector/Pipeline/SnmpSource.cs
  - src/SnmpCollector/Pipeline/Behaviors/OidResolutionBehavior.cs
  - tests/SnmpCollector.Tests/Pipeline/Behaviors/OidResolutionBehaviorTests.cs
autonomous: true

must_haves:
  truths:
    - "SnmpSource.Command exists as an enum value alongside Poll, Trap, Synthetic"
    - "OidResolutionBehavior bypasses OID resolution when MetricName is already set and valid (not null, not Unknown) — no Source-specific conditions remain"
    - "Poll and Trap messages without pre-set MetricName still go through full OID resolution (regression guard)"
    - "Synthetic messages with pre-set MetricName still bypass OID resolution (behavior unchanged)"
    - "A Command-source message with pre-set MetricName bypasses OID resolution via the same guard"
  artifacts:
    - path: "src/SnmpCollector/Pipeline/SnmpSource.cs"
      provides: "Command enum value"
      contains: "Command"
    - path: "src/SnmpCollector/Pipeline/Behaviors/OidResolutionBehavior.cs"
      provides: "MetricName-already-set guard replacing Source==Synthetic bypass"
      contains: "msg.MetricName is not null"
    - path: "tests/SnmpCollector.Tests/Pipeline/Behaviors/OidResolutionBehaviorTests.cs"
      provides: "Command source bypass tests"
      contains: "CommandSource_WithPresetMetricName"
  key_links:
    - from: "OidResolutionBehavior.cs"
      to: "OidMapService.Unknown"
      via: "guard excludes Unknown from bypass"
      pattern: "OidMapService\\.Unknown"
---

<objective>
Add `SnmpSource.Command` enum value and refactor `OidResolutionBehavior` to use a data-driven MetricName-already-set guard instead of `Source == Synthetic` bypass.

Purpose: SET response varbinds need `source=Command` through the MediatR pipeline (Phase 47+). The OidResolutionBehavior refactor removes Source coupling — the guard fires for any message with MetricName pre-set (Synthetic today, Command in the future), making the behavior extensible without adding new Source-specific conditions.

Output: Updated SnmpSource enum, refactored OidResolutionBehavior, updated + new tests.
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/45-structural-prerequisites/45-CONTEXT.md
@.planning/phases/45-structural-prerequisites/45-RESEARCH.md
@src/SnmpCollector/Pipeline/SnmpSource.cs
@src/SnmpCollector/Pipeline/Behaviors/OidResolutionBehavior.cs
@tests/SnmpCollector.Tests/Pipeline/Behaviors/OidResolutionBehaviorTests.cs
@src/SnmpCollector/Pipeline/OidMapService.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SnmpSource.Command enum value</name>
  <files>src/SnmpCollector/Pipeline/SnmpSource.cs</files>
  <action>
Add `Command` as the 4th enum value in `SnmpSource`:

```csharp
public enum SnmpSource
{
    Poll,
    Trap,
    Synthetic,
    Command
}
```

This is a pure addition — no existing code references `Command` yet, so no other files need updating.
  </action>
  <verify>Build compiles: `dotnet build src/SnmpCollector/SnmpCollector.csproj --no-restore`</verify>
  <done>SnmpSource.Command exists as an enum value; build succeeds with zero errors</done>
</task>

<task type="auto">
  <name>Task 2: Refactor OidResolutionBehavior to MetricName-already-set guard + add tests</name>
  <files>
    src/SnmpCollector/Pipeline/Behaviors/OidResolutionBehavior.cs
    tests/SnmpCollector.Tests/Pipeline/Behaviors/OidResolutionBehaviorTests.cs
  </files>
  <action>
**OidResolutionBehavior.cs** — Replace the `Source == Synthetic` bypass (line 35) with a data-driven MetricName guard:

Replace:
```csharp
if (msg.Source == SnmpSource.Synthetic) { return await next(); }
```

With:
```csharp
// Data-driven guard: MetricName already resolved (Synthetic pre-sets it; Command path will too).
// Replaces Source-specific bypass conditions — no Source checks in OidResolutionBehavior.
if (msg.MetricName is not null && msg.MetricName != OidMapService.Unknown)
{
    return await next();
}
```

This changes the bypass mechanism from Source-based to data-based. The behavior is identical for all existing callers:
- Poll/Trap: MetricName is null at construction, guard does NOT fire, OID resolution runs. No change.
- Synthetic: Callers pre-set MetricName before dispatching, guard fires, bypass. No change.
- Command (future): CommandWorker pre-sets MetricName from command map, guard fires, bypass. Correct.

**OidResolutionBehaviorTests.cs** — Add 2 new tests at the end (before the StubOidMapService class):

1. `CommandSource_WithPresetMetricName_BypassesOidResolution` — Create a `SnmpSource.Command` notification with `MetricName = "setSpeed"` pre-set. Assert MetricName remains "setSpeed" after Handle (OidMapService was NOT called to overwrite it).

2. `CommandSource_WithPresetMetricName_StillCallsNext` — Same setup as above, but verify `next()` is called (pipeline continues past the guard).

Use the existing `MakeNotification` helper with `SnmpSource.Command` and set `notification.MetricName = "setSpeed"` after construction. Follow the exact pattern of the existing `SyntheticMessage_BypassesOidResolution_MetricNamePreserved` and `SyntheticMessage_StillCallsNext` tests.

Do NOT rename the existing Synthetic tests — their names are accurate (Synthetic messages DO bypass, just via the MetricName guard now instead of Source check).
  </action>
  <verify>
Run all OidResolutionBehavior tests: `dotnet test tests/SnmpCollector.Tests/SnmpCollector.Tests.csproj --filter "FullyQualifiedName~OidResolutionBehavior" --no-restore`

All 10 tests pass (8 existing + 2 new). Specifically verify:
- `SyntheticMessage_BypassesOidResolution_MetricNamePreserved` still passes (regression)
- `PollMessage_StillResolvesOid_NotAffectedByBypassGuard` still passes (regression)
- `CommandSource_WithPresetMetricName_BypassesOidResolution` passes (new)
- `CommandSource_WithPresetMetricName_StillCallsNext` passes (new)
  </verify>
  <done>OidResolutionBehavior has zero Source-specific conditions; bypass is purely data-driven via MetricName guard; 10/10 tests pass including 2 new Command-source tests</done>
</task>

</tasks>

<verification>
1. `dotnet build src/SnmpCollector/SnmpCollector.csproj --no-restore` — compiles with no errors
2. `dotnet test tests/SnmpCollector.Tests/SnmpCollector.Tests.csproj --filter "FullyQualifiedName~OidResolutionBehavior" --no-restore` — all 10 tests pass
3. `dotnet test tests/SnmpCollector.Tests/SnmpCollector.Tests.csproj --no-restore` — full test suite green (no regressions)
4. Grep `OidResolutionBehavior.cs` for `SnmpSource.Synthetic` — zero matches (Source-specific bypass fully removed)
</verification>

<success_criteria>
- SnmpSource enum has 4 values: Poll, Trap, Synthetic, Command
- OidResolutionBehavior contains NO `Source ==` conditions — bypass is `MetricName is not null && MetricName != Unknown`
- All existing tests pass unchanged (Synthetic bypass, Poll resolution, heartbeat, unknown OID)
- 2 new Command-source tests pass
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/45-structural-prerequisites/45-01-SUMMARY.md`
</output>
