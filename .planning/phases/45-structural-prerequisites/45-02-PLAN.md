---
phase: 45-structural-prerequisites
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/SnmpCollector/Pipeline/MetricSlotHolder.cs
  - src/SnmpCollector/Pipeline/Tenant.cs
  - src/SnmpCollector/Pipeline/TenantVectorRegistry.cs
  - tests/SnmpCollector.Tests/Pipeline/TenantVectorRegistryTests.cs
autonomous: true

must_haves:
  truths:
    - "MetricSlotHolder.Role is populated from MetricSlotOptions.Role during TenantVectorRegistry.Reload — a holder built from Role='Evaluate' reports Role='Evaluate' at runtime"
    - "Tenant.Commands returns the IReadOnlyList<CommandSlotOptions> populated from TenantOptions.Commands at reload time"
    - "Tenant.Commands is re-read from new config on reload (not carried over from old tenant)"
    - "All existing TenantVectorRegistry tests remain green after the constructor changes"
  artifacts:
    - path: "src/SnmpCollector/Pipeline/MetricSlotHolder.cs"
      provides: "Role read-only property"
      contains: "public string Role { get; }"
    - path: "src/SnmpCollector/Pipeline/Tenant.cs"
      provides: "Commands read-only property"
      contains: "IReadOnlyList<CommandSlotOptions> Commands"
    - path: "src/SnmpCollector/Pipeline/TenantVectorRegistry.cs"
      provides: "Role and Commands propagation in Reload"
      contains: "metric.Role"
    - path: "tests/SnmpCollector.Tests/Pipeline/TenantVectorRegistryTests.cs"
      provides: "Role and Commands propagation tests"
      contains: "Reload_RoleFromConfig_StoredInHolder"
  key_links:
    - from: "TenantVectorRegistry.cs"
      to: "MetricSlotHolder constructor"
      via: "metric.Role passed as constructor arg"
      pattern: "metric\\.Role"
    - from: "TenantVectorRegistry.cs"
      to: "Tenant constructor"
      via: "tenantOpts.Commands passed as constructor arg"
      pattern: "tenantOpts\\.Commands"
---

<objective>
Add `MetricSlotHolder.Role` property and `Tenant.Commands` property, propagated from config options during `TenantVectorRegistry.Reload`.

Purpose: SnapshotJob needs `holder.Role` to partition holders into Evaluate vs Resolved at runtime, and `tenant.Commands` to access command targets per tenant at Tier 4. These are pure data propagation additions closing the gap between config models and runtime models.

Output: Updated MetricSlotHolder, Tenant, TenantVectorRegistry, and 4 new tests.
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/45-structural-prerequisites/45-CONTEXT.md
@.planning/phases/45-structural-prerequisites/45-RESEARCH.md
@src/SnmpCollector/Pipeline/MetricSlotHolder.cs
@src/SnmpCollector/Pipeline/Tenant.cs
@src/SnmpCollector/Pipeline/TenantVectorRegistry.cs
@src/SnmpCollector/Configuration/MetricSlotOptions.cs
@src/SnmpCollector/Configuration/TenantOptions.cs
@src/SnmpCollector/Configuration/CommandSlotOptions.cs
@tests/SnmpCollector.Tests/Pipeline/TenantVectorRegistryTests.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Role to MetricSlotHolder and Commands to Tenant, update TenantVectorRegistry.Reload</name>
  <files>
    src/SnmpCollector/Pipeline/MetricSlotHolder.cs
    src/SnmpCollector/Pipeline/Tenant.cs
    src/SnmpCollector/Pipeline/TenantVectorRegistry.cs
  </files>
  <action>
**MetricSlotHolder.cs** — Add `string role` as a required constructor parameter after `intervalSeconds` and before the optional parameters. Add `public string Role { get; }` property. Set `Role = role;` in the constructor body.

New constructor signature:
```csharp
public MetricSlotHolder(string ip, int port, string metricName, int intervalSeconds,
    string role, int timeSeriesSize = 1, double graceMultiplier = 2.0,
    ThresholdOptions? threshold = null)
```

Do NOT add Role to `CopyFrom` — Role is immutable config-time data set by the constructor. `CopyFrom` only transfers mutable runtime state (TypeCode, Source, time series).

Do NOT add Role to `MetricSlot` record — Role is a holder concern, not a per-sample attribute.

**Tenant.cs** — Add `IReadOnlyList<CommandSlotOptions> commands` as a 4th constructor parameter. Add `public IReadOnlyList<CommandSlotOptions> Commands { get; }` property. Set `Commands = commands;` in the constructor body. Add `using SnmpCollector.Configuration;` at the top.

New constructor signature:
```csharp
public Tenant(string id, int priority, IReadOnlyList<MetricSlotHolder> holders,
    IReadOnlyList<CommandSlotOptions> commands)
```

Do NOT wrap commands in ImmutableList — `List<CommandSlotOptions>` already satisfies `IReadOnlyList<CommandSlotOptions>`.

**TenantVectorRegistry.cs** — Update both call sites in `Reload`:

1. MetricSlotHolder construction (~line 88): Add `metric.Role` after `metric.IntervalSeconds`:
```csharp
var newHolder = new MetricSlotHolder(
    metric.Ip,
    metric.Port,
    metric.MetricName,
    metric.IntervalSeconds,
    metric.Role,
    metric.TimeSeriesSize,
    metric.GraceMultiplier,
    metric.Threshold);
```

2. Tenant construction (~line 112): Add `tenantOpts.Commands` as 4th argument:
```csharp
var tenant = new Tenant(tenantId, tenantOpts.Priority, holders, tenantOpts.Commands);
```
  </action>
  <verify>Build compiles: `dotnet build src/SnmpCollector/SnmpCollector.csproj --no-restore` — check for compile errors at MetricSlotHolder and Tenant constructor call sites</verify>
  <done>MetricSlotHolder has Role property; Tenant has Commands property; TenantVectorRegistry.Reload passes both from config options</done>
</task>

<task type="auto">
  <name>Task 2: Fix test call sites and add Role/Commands propagation tests</name>
  <files>tests/SnmpCollector.Tests/Pipeline/TenantVectorRegistryTests.cs</files>
  <action>
**Fix compile errors** — Search all test files for `new MetricSlotHolder(` and `new Tenant(` and update call sites:

1. Any direct `new MetricSlotHolder(...)` calls need the `role` parameter added after `intervalSeconds`. Use `"Evaluate"` as default role for existing tests that don't specifically test Role.

2. Any direct `new Tenant(...)` calls need `commands` parameter added. Use `Array.Empty<CommandSlotOptions>()` or `[]` for existing tests that don't specifically test Commands.

Search broadly: `grep -rn "new MetricSlotHolder\|new Tenant(" tests/` to find all call sites.

**Add 4 new tests** as sections 13 and 14 in TenantVectorRegistryTests, following the existing `//──` section pattern:

Section 13 — Role propagation (2 tests):

1. `Reload_RoleFromConfig_StoredInHolder` — Build TenantVectorOptions with two metrics: one `Role = "Evaluate"`, one `Role = "Resolved"`. Reload. TryRoute to each holder and assert `holder.Role` matches. (Use the exact test from RESEARCH.md.)

2. `Reload_RolePreservedAcrossReload` — Reload once, write a value to a holder, reload again with same config. Assert the new holder still has the correct Role from config (not from CopyFrom).

Section 14 — Commands propagation (2 tests):

1. `Reload_CommandsFromConfig_StoredOnTenant` — Build TenantVectorOptions with one CommandSlotOptions entry. Reload. Assert `tenant.Commands` has 1 entry with correct CommandName and Ip. (Use the exact test from RESEARCH.md.)

2. `Reload_NoCommands_TenantCommandsIsEmpty` — Build TenantVectorOptions with `Commands = []`. Reload. Assert `tenant.Commands` is empty (not null). (Use the exact test from RESEARCH.md.)
  </action>
  <verify>
Run all TenantVectorRegistry tests: `dotnet test tests/SnmpCollector.Tests/SnmpCollector.Tests.csproj --filter "FullyQualifiedName~TenantVectorRegistry" --no-restore`

All tests pass — existing tests (sections 1-12) plus 4 new tests (sections 13-14). Specifically verify:
- `Reload_RoleFromConfig_StoredInHolder` passes
- `Reload_RolePreservedAcrossReload` passes
- `Reload_CommandsFromConfig_StoredOnTenant` passes
- `Reload_NoCommands_TenantCommandsIsEmpty` passes
- All carry-over tests (section 5) still pass (CopyFrom not broken)

Then run full test suite: `dotnet test tests/SnmpCollector.Tests/SnmpCollector.Tests.csproj --no-restore` — all green (no regressions from constructor signature changes)
  </verify>
  <done>All MetricSlotHolder and Tenant constructor call sites compile; 4 new tests pass confirming Role and Commands propagation; full test suite green</done>
</task>

</tasks>

<verification>
1. `dotnet build src/SnmpCollector/SnmpCollector.csproj --no-restore` — compiles with no errors
2. `dotnet test tests/SnmpCollector.Tests/SnmpCollector.Tests.csproj --filter "FullyQualifiedName~TenantVectorRegistry" --no-restore` — all tests pass including 4 new ones
3. `dotnet test tests/SnmpCollector.Tests/SnmpCollector.Tests.csproj --no-restore` — full test suite green
4. Grep `MetricSlotHolder.cs` for `public string Role { get; }` — one match
5. Grep `Tenant.cs` for `IReadOnlyList<CommandSlotOptions> Commands` — one match
6. Grep `TenantVectorRegistry.cs` for `metric.Role` — one match (in Reload)
7. Grep `TenantVectorRegistry.cs` for `tenantOpts.Commands` — one match (in Reload)
</verification>

<success_criteria>
- MetricSlotHolder.Role is an immutable string property set in constructor, NOT in CopyFrom
- Tenant.Commands is an IReadOnlyList<CommandSlotOptions> set in constructor
- TenantVectorRegistry.Reload passes metric.Role to MetricSlotHolder and tenantOpts.Commands to Tenant
- 4 new tests confirm Role and Commands propagation
- All existing tests remain green (no regressions from constructor changes)
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/45-structural-prerequisites/45-02-SUMMARY.md`
</output>
