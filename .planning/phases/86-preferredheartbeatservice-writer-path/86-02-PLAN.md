---
phase: 86-preferredheartbeatservice-writer-path
plan: 02
type: execute
wave: 2
depends_on: ["86-01"]
files_modified:
  - tests/SnmpCollector.Tests/Jobs/PreferredHeartbeatJobTests.cs
autonomous: true

must_haves:
  truths:
    - "Existing reader tests still pass after constructor signature change"
    - "Writer path creates heartbeat lease when preferred + ready"
    - "Writer path skips silently when not preferred"
    - "Writer path skips silently when not ready (ApplicationStarted has not fired)"
    - "409 Conflict on create triggers read-then-replace fallback"
    - "409 Conflict on replace invalidates cached resourceVersion"
    - "404 on replace invalidates cached resourceVersion"
    - "Transient write errors are caught and do not crash Execute"
    - "Liveness stamp fires regardless of writer errors"
  artifacts:
    - path: "tests/SnmpCollector.Tests/Jobs/PreferredHeartbeatJobTests.cs"
      provides: "Writer path unit tests covering readiness gate, create/replace, conflict handling, non-preferred skip"
      contains: "WriteHeartbeat"
  key_links:
    - from: "PreferredHeartbeatJobTests constructor"
      to: "PreferredHeartbeatJob new constructor"
      via: "IHostApplicationLifetime mock, IOptions<PodIdentityOptions>"
      pattern: "IHostApplicationLifetime|PodIdentityOptions"
    - from: "Writer test helpers"
      to: "ICoordinationV1Operations mock"
      via: "CreateNamespacedLeaseWithHttpMessagesAsync, ReplaceNamespacedLeaseWithHttpMessagesAsync"
      pattern: "CreateNamespacedLease|ReplaceNamespacedLease"
---

<objective>
Update existing unit tests to match the new PreferredHeartbeatJob constructor signature, then add comprehensive writer-path tests covering all scenarios: readiness gate, create/replace, conflict recovery, non-preferred skip, and transient error handling.

Purpose: Verify HB-01 (stamps lease with pod identity and renewTime), HB-02 (stamping gated by readiness), and HB-03 (TTL expiry, no explicit delete) through automated tests.

Output: Updated PreferredHeartbeatJobTests.cs with all existing reader tests passing and new writer-path tests.
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-preferredheartbeatservice-writer-path/86-RESEARCH.md
@.planning/phases/86-preferredheartbeatservice-writer-path/86-01-SUMMARY.md
@src/SnmpCollector/Jobs/PreferredHeartbeatJob.cs
@tests/SnmpCollector.Tests/Jobs/PreferredHeartbeatJobTests.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix existing test constructor and add writer-path unit tests</name>
  <files>tests/SnmpCollector.Tests/Jobs/PreferredHeartbeatJobTests.cs</files>
  <action>
**1. Fix test constructor to match new PreferredHeartbeatJob signature:**

The constructor now requires `IOptions<PodIdentityOptions>` and `IHostApplicationLifetime`. Update the test setup:

- Add a mock `IHostApplicationLifetime`:
  ```
  var mockLifetime = Substitute.For<IHostApplicationLifetime>();
  // For reader-only tests: set ApplicationStarted to a default CancellationToken (never fires)
  // For writer tests: use a CancellationTokenSource, cancel it to simulate ApplicationStarted
  mockLifetime.ApplicationStarted.Returns(CancellationToken.None);
  ```
- Add `IOptions<PodIdentityOptions>`:
  ```
  var podIdentityOptions = Options.Create(new PodIdentityOptions { PodIdentity = "test-pod-01" });
  ```
- Update `new PreferredHeartbeatJob(...)` call to pass both new parameters.
- All 8 existing reader tests should pass unchanged after this constructor fix.

**2. Create a helper to build a job with specific settings:**

Build a helper method or secondary constructor path that allows tests to control:
- `IsPreferredPod` — via `PreferredLeaderService` with `LeaseOptions.PreferredNode` set + `PHYSICAL_HOSTNAME` env var
- `_isSchedulerReady` — via a `CancellationTokenSource` that is cancelled before the test runs (simulates ApplicationStarted)

Pattern for controlling IsPreferredPod in tests:
- Set `Environment.SetEnvironmentVariable("PHYSICAL_HOSTNAME", "node-a")` + `LeaseOptions.PreferredNode = "node-a"` -> IsPreferredPod = true
- Clean up env var in Dispose/finally to avoid test pollution

Pattern for controlling _isSchedulerReady:
- Create a `CancellationTokenSource`, mock `lifetime.ApplicationStarted.Returns(cts.Token)`, then `cts.Cancel()` before executing the job -> _isSchedulerReady = true
- Not cancelling -> _isSchedulerReady stays false

**3. Add writer-path test cases (new section after existing reader tests):**

Tests to add:

a) `Execute_PreferredAndReady_CreatesHeartbeatLease` — preferred pod + scheduler ready + no existing lease -> calls CreateNamespacedLeaseWithHttpMessagesAsync. Verify HolderIdentity matches pod identity, RenewTime is set, LeaseDurationSeconds matches config.

b) `Execute_PreferredAndReady_RenewsWithCachedResourceVersion` — preferred pod + scheduler ready + second tick (first tick cached resourceVersion) -> calls ReplaceNamespacedLeaseWithHttpMessagesAsync with the cached resourceVersion. Verify RenewTime is updated.

c) `Execute_PreferredButNotReady_SkipsWrite` — preferred pod + scheduler NOT ready -> does NOT call Create or Replace. Reader path still runs.

d) `Execute_NotPreferred_SkipsWrite` — non-preferred pod -> does NOT call Create or Replace. Reader path still runs.

e) `Execute_CreateConflict409_FallsBackToReadThenReplace` — preferred + ready + CreateNamespacedLeaseWithHttpMessagesAsync throws 409 -> reads existing lease -> calls ReplaceNamespacedLeaseWithHttpMessagesAsync.

f) `Execute_ReplaceConflict409_InvalidatesCache` — preferred + ready + ReplaceNamespacedLeaseWithHttpMessagesAsync throws 409 -> _cachedResourceVersion is null on next tick (verifiable by checking Create is called again on the next Execute).

g) `Execute_Replace404_InvalidatesCache` — same pattern as 409 but with 404.

h) `Execute_WriteTransientError_LogsWarningAndContinuesRead` — preferred + ready + CreateNamespacedLeaseWithHttpMessagesAsync throws generic exception -> job does not throw, reader path still runs, liveness stamped.

i) `Execute_AcquireTimeSetOnlyOnCreate_NotOnReplace` — create succeeds (verify AcquireTime in body), then replace on next tick (verify AcquireTime is NOT set on the replace body). Use Arg.Do or capture the V1Lease argument.

**Mocking strategy for write operations:**
- Mock `CreateNamespacedLeaseWithHttpMessagesAsync` and `ReplaceNamespacedLeaseWithHttpMessagesAsync` (the underlying interface methods, same as reader path uses `ReadNamespacedLeaseWithHttpMessagesAsync`)
- Return `HttpOperationResponse<V1Lease>` with a body containing `Metadata.ResourceVersion = "rv-123"`
- For 409: throw `HttpOperationException` with `Response.StatusCode == HttpStatusCode.Conflict`
- For 404: throw `HttpOperationException` with `Response.StatusCode == HttpStatusCode.NotFound`

**What NOT to do:**
- Do NOT add integration tests (unit tests only this plan)
- Do NOT test shutdown behavior (TTL expiry is passive, nothing to test at unit level)
- Do NOT modify PreferredHeartbeatJob.cs (only test file)
  </action>
  <verify>
1. `dotnet test tests/SnmpCollector.Tests/ --filter "PreferredHeartbeatJobTests"` — ALL tests pass (existing reader + new writer)
2. `dotnet test tests/SnmpCollector.Tests/` — full test suite passes
3. Verify test count increased by at least 7 new writer-path tests
  </verify>
  <done>
All 8 existing reader tests pass with updated constructor. At least 7 new writer-path tests pass covering: create, replace with cached resourceVersion, readiness gate skip, non-preferred skip, 409 conflict fallback, cache invalidation on 409/404, transient error handling, AcquireTime only on create. Full test suite green.
  </done>
</task>

</tasks>

<verification>
- `dotnet test tests/SnmpCollector.Tests/ --filter "PreferredHeartbeatJobTests"` passes with 15+ tests
- `dotnet test tests/SnmpCollector.Tests/` full suite passes
- Writer-path tests verify HB-01 (lease stamped with identity and renewTime), HB-02 (readiness gate), HB-03 (no explicit delete in tests)
</verification>

<success_criteria>
- All 8 existing reader-path tests pass with updated constructor
- At least 7 new writer-path tests pass
- Tests cover: create, renew, readiness gate, non-preferred skip, 409 create conflict, 409/404 replace conflict, transient error, AcquireTime only on create
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/86-preferredheartbeatservice-writer-path/86-02-SUMMARY.md`
</output>
