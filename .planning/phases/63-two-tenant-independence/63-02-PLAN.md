---
phase: 63-two-tenant-independence
plan: 02
type: execute
wave: 2
depends_on: ["63-01"]
files_modified:
  - tests/e2e/scenarios/59-pss-11-t1-healthy-t2-unresolved.sh
  - tests/e2e/scenarios/60-pss-12-t1-resolved-t2-healthy.sh
  - tests/e2e/scenarios/61-pss-13-both-unresolved.sh
autonomous: true

must_haves:
  truths:
    - "T1=Healthy and T2=Unresolved produces independent tier logs -- T1 tier=3 and T2 tier=4 with commands"
    - "T1=Resolved and T2=Healthy produces T1 tier=2 and T2 tier=3 -- each tenant's tier from its own metrics"
    - "Both tenants Unresolved produces both T1 tier=4 and T2 tier=4 with command counter incrementing"
  artifacts:
    - path: "tests/e2e/scenarios/59-pss-11-t1-healthy-t2-unresolved.sh"
      provides: "PSS-11 independence scenario -- T1 healthy, T2 unresolved"
      contains: "record_pass.*PSS-11"
    - path: "tests/e2e/scenarios/60-pss-12-t1-resolved-t2-healthy.sh"
      provides: "PSS-12 independence scenario -- T1 resolved, T2 healthy"
      contains: "record_pass.*PSS-12"
    - path: "tests/e2e/scenarios/61-pss-13-both-unresolved.sh"
      provides: "PSS-13 independence scenario -- both unresolved"
      contains: "record_pass.*PSS-13"
  key_links:
    - from: "tests/e2e/scenarios/59-pss-11-t1-healthy-t2-unresolved.sh"
      to: "tests/e2e/fixtures/tenant-cfg07-pss-two-tenant.yaml"
      via: "kubectl apply"
      pattern: "kubectl apply -f.*tenant-cfg07-pss-two-tenant"
    - from: "tests/e2e/scenarios/61-pss-13-both-unresolved.sh"
      to: "prometheus.sh"
      via: "snmp_command_dispatched_total counter increment for both tenants"
      pattern: "snmp_command_dispatched_total"
---

<objective>
Create the three two-tenant independence scenario scripts (59-61) that prove T1 and T2 evaluate independently within the same priority group.

Purpose: Verifies the core independence property -- one tenant's metric values do not affect the other tenant's evaluation tier or command dispatch. This is the essential property for multi-tenant correctness.

Output: 3 scenario scripts (59-61) covering PSS-11, PSS-12, PSS-13
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-two-tenant-independence/63-RESEARCH.md
@.planning/phases/63-two-tenant-independence/63-01-SUMMARY.md

@tests/e2e/fixtures/tenant-cfg07-pss-two-tenant.yaml
@tests/e2e/scenarios/56-pss-04-unresolved.sh
@tests/e2e/scenarios/57-pss-05-healthy.sh
@tests/e2e/scenarios/55-pss-03-resolved.sh
@tests/e2e/lib/sim.sh
@tests/e2e/lib/prometheus.sh
@tests/e2e/lib/common.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PSS scenarios 59-60 (T1 Healthy/T2 Unresolved, T1 Resolved/T2 Healthy)</name>
  <files>
    tests/e2e/scenarios/59-pss-11-t1-healthy-t2-unresolved.sh
    tests/e2e/scenarios/60-pss-12-t1-resolved-t2-healthy.sh
  </files>
  <action>
Create two independence scenario scripts following the established PSS pattern from Phase 62 (use 56-pss-04-unresolved.sh and 57-pss-05-healthy.sh as structural templates). Both scenarios use the 2-tenant fixture (tenant-cfg07-pss-two-tenant.yaml).

**59-pss-11-t1-healthy-t2-unresolved.sh** (maps to PSS-11, Success Criteria 1):
- Header: Scenario 59, PSS-11 T1=Healthy T2=Unresolved, uses tenant-cfg07-pss-two-tenant.yaml
- T1 uses T2 OIDs (5.1-5.3), T2 uses T3 OIDs (6.1-6.3)
- Setup: save_configmap, kubectl apply cfg07, poll_until_log for reload
- Prime ALL 6 OIDs in-range:
  - sim_set_oid "5.1" "10" (T1 eval in-range)
  - sim_set_oid "5.2" "1"  (T1 res1 in-range)
  - sim_set_oid "5.3" "1"  (T1 res2 in-range)
  - sim_set_oid "6.1" "10" (T2 eval in-range)
  - sim_set_oid "6.2" "1"  (T2 res1 in-range)
  - sim_set_oid "6.3" "1"  (T2 res2 in-range)
- Sleep 8s for readiness grace (6s grace + 2s margin)
- Stimulus: sim_set_oid "6.1" "0" (T2 eval violated -- only T2 becomes unresolved)
  - T1 OIDs untouched -- T1 stays healthy (all in-range)
- Assert PSS-11A: poll_until_log 30 1 "e2e-pss-t1.*tier=3" 15 (T1 = Healthy)
- Assert PSS-11B: poll_until_log 30 1 "e2e-pss-t2.*tier=4 — commands enqueued\|e2e-pss-t2.*tier=4 -- commands enqueued" 15 (T2 = Unresolved with commands)
- NOTE: Do NOT assert counter-based "T1 has no commands dispatched" -- the snmp_command_dispatched_total counter with device_name="E2E-SIM" is shared across both tenants. T1 tier=3 log IS the proof that T1 had no commands (tier=3 by definition means no commands dispatched). The research explicitly warns against this anti-pattern.
- Cleanup: reset_oid_overrides, restore_configmap

**60-pss-12-t1-resolved-t2-healthy.sh** (maps to PSS-12, Success Criteria 2):
- Header: Scenario 60, PSS-12 T1=Resolved T2=Healthy, uses tenant-cfg07-pss-two-tenant.yaml
- Setup: save_configmap, kubectl apply cfg07, poll_until_log for reload
- Prime ALL 6 OIDs in-range (same as above), sleep 8s
- Stimulus: sim_set_oid "5.2" "0" and sim_set_oid "5.3" "0" (T1 resolved metrics violated, BOTH of them)
  - T1 eval stays at 10 (in-range), T1 res1+res2 = 0 (violated) -> tier=2 Resolved
  - T2 OIDs untouched -> stays healthy tier=3
- Assert PSS-12A: poll_until_log 30 1 "e2e-pss-t1.*tier=2" 15 (T1 = Resolved)
- Assert PSS-12B: poll_until_log 30 1 "e2e-pss-t2.*tier=3" 15 (T2 = Healthy)
- Cleanup: reset_oid_overrides, restore_configmap

CRITICAL implementation notes:
- ALL scenarios use "PSS-11" / "PSS-12" prefixes for log_info and record_pass/fail
- ALL scenarios reference tenant-cfg07-pss-two-tenant.yaml (NOT cfg06)
- ALL scenarios prime ALL 6 OIDs (5.1-5.3 for T1, 6.1-6.3 for T2) before any stimulus
- Tenant names in log grep: "e2e-pss-t1" and "e2e-pss-t2"
- Every scenario starts with FIXTURES_DIR= line and ends with cleanup block
- Use em-dash/double-dash alternation in tier=4 grep: "tier=4 — commands enqueued\|tier=4 -- commands enqueued"
  </action>
  <verify>
`bash -n tests/e2e/scenarios/59-pss-11-t1-healthy-t2-unresolved.sh && bash -n tests/e2e/scenarios/60-pss-12-t1-resolved-t2-healthy.sh` -- no syntax errors.
`grep -c "record_pass\|record_fail" tests/e2e/scenarios/59-pss-11-t1-healthy-t2-unresolved.sh` -- at least 2 assertions (T1 tier=3, T2 tier=4).
`grep -c "record_pass\|record_fail" tests/e2e/scenarios/60-pss-12-t1-resolved-t2-healthy.sh` -- at least 2 assertions (T1 tier=2, T2 tier=3).
`grep "tenant-cfg07-pss-two-tenant" tests/e2e/scenarios/59-pss-11-t1-healthy-t2-unresolved.sh` -- uses 2-tenant fixture.
`grep "6\\.1\|6\\.2\|6\\.3" tests/e2e/scenarios/59-pss-11-t1-healthy-t2-unresolved.sh` -- primes T3 OIDs for T2.
  </verify>
  <done>
Scenarios 59-60 exist, pass syntax check, use 2-tenant fixture, prime all 6 OIDs, and assert independent tier evaluations per tenant. PSS-11 proves T1 healthy while T2 unresolved. PSS-12 proves T1 resolved while T2 healthy.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create PSS scenario 61 (Both Unresolved with independent command dispatch)</name>
  <files>
    tests/e2e/scenarios/61-pss-13-both-unresolved.sh
  </files>
  <action>
Create the final independence scenario that proves both tenants can be Unresolved simultaneously with independent command dispatch.

**61-pss-13-both-unresolved.sh** (maps to PSS-13, Success Criteria 3):
- Header: Scenario 61, PSS-13 Both Unresolved, uses tenant-cfg07-pss-two-tenant.yaml
- Setup: save_configmap, kubectl apply cfg07, poll_until_log for reload
- Prime ALL 6 OIDs in-range (5.1=10, 5.2=1, 5.3=1, 6.1=10, 6.2=1, 6.3=1), sleep 8s
- Baseline: BEFORE_SENT = snapshot_counter "snmp_command_dispatched_total" 'device_name="E2E-SIM"'
- Stimulus: sim_set_oid "5.1" "0" AND sim_set_oid "6.1" "0" (BOTH tenants' evaluate violated)
  - T1 res1/res2 stay at 1 (not all resolved violated) -> tier=4
  - T2 res1/res2 stay at 1 (not all resolved violated) -> tier=4
- Assert PSS-13A: poll_until_log 30 1 "e2e-pss-t1.*tier=4" 15 (T1 = Unresolved)
- Assert PSS-13B: poll_until_log 30 1 "e2e-pss-t2.*tier=4" 15 (T2 = Unresolved)
- Assert PSS-13C: poll_until 30 2 "snmp_command_dispatched_total" 'device_name="E2E-SIM"' "$BEFORE_SENT" then capture AFTER_SENT, compute DELTA_SENT.
  - The counter increments for BOTH tenants dispatching to the same device E2E-SIM. We cannot distinguish per-tenant from this counter alone, but we CAN assert the delta is >= 2 (at least one command from each tenant). Use: `if [ "$DELTA_SENT" -ge 2 ]; then record_pass ... else record_fail ... fi`
  - This assertion proves commands were dispatched for both tenants (combined), not just one.
- Cleanup: reset_oid_overrides, restore_configmap

CRITICAL implementation notes:
- Use "PSS-13" prefix for all log_info and record_pass/fail
- Counter label: device_name="E2E-SIM" (the SNMP device name, shared across tenants)
- The delta >= 2 assertion is the correct way to verify both tenants dispatched -- the research confirms the counter is per-device, not per-tenant
- poll_until already confirms the counter incremented; then snapshot again to get exact delta for the >= 2 check
- Each sub-assertion uses record_pass/record_fail with descriptive name and evidence string
  </action>
  <verify>
`bash -n tests/e2e/scenarios/61-pss-13-both-unresolved.sh` -- no syntax errors.
`grep -c "record_pass\|record_fail" tests/e2e/scenarios/61-pss-13-both-unresolved.sh` -- at least 3 assertions (T1 tier=4, T2 tier=4, counter delta).
`grep "tenant-cfg07-pss-two-tenant" tests/e2e/scenarios/61-pss-13-both-unresolved.sh` -- uses 2-tenant fixture.
`grep "snmp_command_dispatched_total" tests/e2e/scenarios/61-pss-13-both-unresolved.sh` -- counter assertion present.
`grep "e2e-pss-t1.*tier=4\|e2e-pss-t2.*tier=4" tests/e2e/scenarios/61-pss-13-both-unresolved.sh` -- both tenant tier=4 assertions.
  </verify>
  <done>
Scenario 61 exists, passes syntax check, uses 2-tenant fixture, primes all 6 OIDs, sets both evaluate metrics to violated, and asserts both T1 and T2 reach tier=4 with combined command counter delta >= 2.
  </done>
</task>

</tasks>

<verification>
1. `ls tests/e2e/scenarios/5[9]-pss-*.sh tests/e2e/scenarios/6[01]-pss-*.sh` -- all three scripts exist
2. `bash -n tests/e2e/scenarios/59-pss-11-t1-healthy-t2-unresolved.sh` -- no syntax errors
3. `bash -n tests/e2e/scenarios/60-pss-12-t1-resolved-t2-healthy.sh` -- no syntax errors
4. `bash -n tests/e2e/scenarios/61-pss-13-both-unresolved.sh` -- no syntax errors
5. `grep -c "record_pass\|record_fail" tests/e2e/scenarios/59-pss-11-t1-healthy-t2-unresolved.sh` -- at least 2
6. `grep -c "record_pass\|record_fail" tests/e2e/scenarios/60-pss-12-t1-resolved-t2-healthy.sh` -- at least 2
7. `grep -c "record_pass\|record_fail" tests/e2e/scenarios/61-pss-13-both-unresolved.sh` -- at least 3
8. `grep "tenant-cfg07" tests/e2e/scenarios/59-pss-11-t1-healthy-t2-unresolved.sh tests/e2e/scenarios/60-pss-12-t1-resolved-t2-healthy.sh tests/e2e/scenarios/61-pss-13-both-unresolved.sh` -- all reference 2-tenant fixture
9. Total PSS sub-assertions across scenarios 59-61: at least 7 record_pass/record_fail calls
</verification>

<success_criteria>
- Scenario 59 proves T1=Healthy (tier=3) while T2=Unresolved (tier=4 with commands) -- no interference
- Scenario 60 proves T1=Resolved (tier=2) while T2=Healthy (tier=3) -- per-tenant tier determination
- Scenario 61 proves both T1 and T2 reach tier=4 with independent command dispatch (counter delta >= 2)
- All scripts prime all 6 OIDs (T1: 5.1-5.3, T2: 6.1-6.3) before stimulus
- All scripts use 2-tenant fixture (tenant-cfg07-pss-two-tenant.yaml)
- All scripts pass bash -n syntax check
</success_criteria>

<output>
After completion, create `.planning/phases/63-two-tenant-independence/63-02-SUMMARY.md`
</output>
